window.LINUX_COURSE_DATA = window.GRASSHOPPER_DATA = [{"course_id": "getting-started", "course_name": "Getting Started", "icon": "\ud83c\udf31", "description": "Learn what Linux is, distributions, and foundational history.", "lessons_count": 11, "quizzes_count": 47, "lessons": [{"lesson_id": "linux-history", "course_id": "getting-started", "title": "Linux History", "order_index": 1, "description": "Learn how UNIX, GNU, and the Linux kernel contributed to modern Linux systems.", "content": "Welcome to your **Linux Journey**! If you're ready to dive into the powerful world of Linux, you've come to the right place. My name is Penguin Pete, and I'll be your guide. To get started, let's explore a brief **history of Linux**.\n\n## The Predecessors of Linux\n\nTo understand how Linux was created, we must go back to 1969 when Ken Thompson and Dennis Ritchie of Bell Laboratories developed the UNIX operating system. It was later rewritten in the C programming language, which made it portable and led to its widespread adoption.\n\n![Timeline of Unix](https://file.labex.io/images/ed9c245d-e8be-4287-bf34-67750b042542.jpg)\n\nOver a decade later, Richard Stallman initiated the GNU project. GNU is a recursive acronym for \"GNU's Not UNIX,\" and its goal was to create a completely free and open-source UNIX-like operating system. The project produced many essential components and the GNU General Public License (GPL), but its own kernel, the GNU Hurd, was not ready for general use when Linux became available.\n\n## The Role of the Kernel\n\nThe kernel is the core component of an operating system. It acts as a bridge, allowing the hardware to communicate with the software. The kernel manages system resources, such as the CPU, memory, and peripheral devices. A complete operating system needs this resource-managing core in addition to the tools and applications people use.\n\n## The Birth of the Linux Kernel\n\nThis brings us to 1991, when a Finnish student named Linus Torvalds began developing a new kernel as a personal project. This kernel became known as the Linux kernel. After Linux was released as free software in 1992, it could be combined with the nearly complete GNU system to form a complete free operating system, commonly called GNU/Linux. This milestone was a pivotal moment in the **history of Linux**.\n\n![Linus Torvalds in 2018](https://file.labex.io/images/3e1311fd-b8ca-45e7-8d02-9aac6377bb36.jpg)\n\n_Linus Torvalds in 2018 (Source: [Wikipedia](https://en.wikipedia.org/wiki/Linus_Torvalds))_\n\nTo continue your **Linux journey**, try these hands-on labs to practice fundamental commands and build your confidence in the command-line environment.\n\n1. **[Getting Started with Linux](https://labex.io/labs/linux-getting-started-with-linux-446315)** - Begin your Linux journey by learning essential terminal commands like `echo`, `date`, and basic calculations. Perfect for complete beginners.\n2. **[Your First Linux Lab](https://labex.io/labs/linux-your-first-linux-lab-270253)** - This introductory lab guides you through the classic \"Hello, World!\" program in Linux and teaches you some fundamental commands.\n3. **[Create Personalized Terminal Greeting](https://labex.io/labs/linux-create-personalized-terminal-greeting-446322)** - A quick and fun challenge to use basic Linux terminal commands to create an engaging welcome message.\n\n## Summary\n\nYou can now explain how UNIX, GNU, and the Linux kernel contributed to modern Linux systems.\n\n1. Describe why UNIX portability mattered.\n2. Identify the kernel as GNU's major missing component.\n3. Explain the kernel's role in managing system resources.\n4. Identify Linus Torvalds as the creator of the Linux kernel.", "code_snippets": [], "quizzes": [{"id": "understand-unix-portability", "question": "What was an important result of rewriting UNIX in C?", "options": [{"text": "It became the free kernel created for the GNU system.", "correct": false, "explanation": "UNIX existed before the GNU project and was not GNU's kernel. GNU later began developing a separate kernel called the Hurd."}, {"text": "It became easier to move across different hardware systems.", "correct": true, "explanation": "Writing UNIX in C made it more portable. That portability helped it spread beyond its original hardware."}, {"text": "It became a command shell used only at Bell Labs.", "correct": false, "explanation": "UNIX is an operating system rather than only a shell. Rewriting it in C helped adoption beyond Bell Labs."}]}, {"id": "identify-gnu-missing-component", "question": "Which major GNU component was not ready when Linux became available?", "options": [{"text": "A production-ready kernel", "correct": true, "explanation": "GNU had produced many system components, but its own kernel, the GNU Hurd, was not ready for general use."}, {"text": "A free software license", "correct": false, "explanation": "The GNU project had already produced the GNU General Public License. The missing system component was a usable kernel."}, {"text": "Essential system tools", "correct": false, "explanation": "GNU had already produced many essential tools. Its kernel remained the major unfinished part of the system."}]}, {"id": "recognize-kernel-role", "question": "Which responsibility belongs to the operating system kernel?", "options": [{"text": "Writing every command entered in the shell", "correct": false, "explanation": "People or scripts provide shell commands. The kernel supplies the lower-level resources needed when programs run those commands."}, {"text": "Choosing the license for every installed application", "correct": false, "explanation": "Software authors and distributors choose application licenses. License selection is not a kernel resource-management task."}, {"text": "Managing the CPU, memory, and connected devices", "correct": true, "explanation": "The kernel manages hardware resources and makes them available to software. CPU time, memory, and devices are central examples."}]}, {"id": "identify-linux-kernel-creator", "question": "Who began developing the Linux kernel in 1991?", "options": [{"text": "Richard Stallman", "correct": false, "explanation": "Richard Stallman initiated the GNU project. GNU supplied many system components, but Linus Torvalds began the Linux kernel."}, {"text": "Dennis Ritchie", "correct": false, "explanation": "Dennis Ritchie helped develop UNIX and the C programming language. The Linux kernel project was started later by Linus Torvalds."}, {"text": "Linus Torvalds", "correct": true, "explanation": "Linus Torvalds began the kernel project in 1991. That project became the Linux kernel."}]}]}, {"lesson_id": "choosing-a-linux-distribution", "course_id": "getting-started", "title": "Choosing a Linux Distribution", "order_index": 2, "description": "Learn how to compare Linux distributions by goals, release style, support, and experience level.", "content": "In the previous lesson, we learned about the Linux kernel. While people often use \"Linux\" to describe the whole operating system, the kernel is only one part of the system. The complete operating systems built around the Linux kernel are called **Linux distributions**, or **Linux distros**.\n\nIf you are trying to find the **best Linux distro**, the first thing to know is that there is no single best choice for everyone. The right distro depends on whether you care most about ease of use, software freshness, stability, system control, or enterprise support.\n\nA Linux system is divided into three main parts:\n\n- **Hardware** - This includes the physical components of your computer, such as the CPU, memory, and storage devices.\n- **Linux Kernel** - As the core of the operating system, the kernel manages the hardware and facilitates communication between software and hardware.\n- **User Space** - This is the environment where you, the user, interact with the system through applications and command-line interfaces.\n\n## What Is a Linux Distro\n\nA Linux distribution bundles the Linux kernel with system utilities, libraries, applications, and usually a package manager. Many distros also include a desktop environment for graphical use. In practical terms, a Linux distro is a complete operating system built around the Linux kernel.\n\nDifferent Linux distributions make different choices about stability, software freshness, desktop experience, package management, support, and system philosophy. That is why there is no single best Linux distro for everyone.\n\n## How to Choose the Best Linux Distro\n\nChoosing a Linux distro becomes much easier when you start with your own needs. Think about your experience level, the kind of computer you are using, and what you want the system to do. A beginner setting up a laptop may want something very different from a developer building a workstation or an administrator deploying servers.\n\nThe best Linux distro is usually the one that matches your goals, not the one with the loudest reputation. For most users, the main factors are ease of use, package management, release style, documentation, and long-term support.\n\nRelease style describes how a distro delivers major software updates. Stable or point-release distros publish updates in planned batches and focus on predictability. Rolling-release distros deliver updates continuously, which usually means newer software but also more frequent change.\n\n## Linux Distros for Beginners\n\nIf you are new to Linux, start with distros that offer a smooth installation process, strong documentation, and a polished desktop experience. [Ubuntu](https://labex.io/lesson/ubuntu) and [Linux Mint](https://labex.io/lesson/linux-mint) are common starting points because they are easy to install and widely documented. openSUSE can also be approachable, especially for users who like graphical administration tools.\n\nBeginner-friendly does not always mean simplistic. It usually means the distro has sensible defaults, a large community, and fewer surprises during day-to-day use.\n\n## Linux Distros for Developers and Power Users\n\nSome users want more control over the system, newer software, or a more hands-on experience. [Fedora](https://labex.io/lesson/fedora) is popular with developers because it moves quickly while still aiming for a polished experience. [Arch Linux](https://labex.io/lesson/arch-linux) appeals to users who want a rolling release and more direct control over system setup. [Gentoo](https://labex.io/lesson/gentoo) is even more specialized, giving advanced users deep control through source-based package building.\n\nThese distros can be excellent, but they usually make more sense once you already know what kind of workflow you want.\n\n## Linux Distros for Servers and Stability\n\nIf you care most about predictability and long-term reliability, stable release models matter more than visual polish. [Debian](https://labex.io/lesson/debian) is well known for its conservative approach and strong reputation on servers. [Red Hat Enterprise Linux](https://labex.io/lesson/red-hat-enterprise-linux) is designed for enterprise environments where support, certifications, and long life cycles are important.\n\nUbuntu is also widely used on servers, especially when users want a large ecosystem and familiar tooling. The right choice depends on whether you value community-driven stability, commercial support, or a balance of both.\n\n## Best Linux Distro by Use Case\n\nIf you want a quick answer, these are common starting points:\n\n- **Best Linux distro for beginners**: [Ubuntu](https://labex.io/lesson/ubuntu) or [Linux Mint](https://labex.io/lesson/linux-mint)\n- **Best Linux distro for developers**: [Fedora](https://labex.io/lesson/fedora)\n- **Best Linux distro for stability**: [Debian](https://labex.io/lesson/debian)\n- **Best Linux distro for maximum control**: [Arch Linux](https://labex.io/lesson/arch-linux) or [Gentoo](https://labex.io/lesson/gentoo)\n- **Best Linux distro for enterprise environments**: [Red Hat Enterprise Linux](https://labex.io/lesson/red-hat-enterprise-linux)\n- **Best Linux distro for cybersecurity**: [Best Linux Distro for Cybersecurity](https://labex.io/lesson/best-linux-distro-for-cybersecurity)\n\nThese are not universal answers, but they are useful starting points when you are comparing Linux distros by goal rather than by popularity alone.\n\n## Popular Linux Distros\n\nSome Linux distros are widely recommended because they solve different problems well:\n\n- [Debian](https://labex.io/lesson/debian): stable, foundational, and widely respected\n- [Ubuntu](https://labex.io/lesson/ubuntu): beginner-friendly and broadly adopted on desktop and server systems\n- [Fedora](https://labex.io/lesson/fedora): modern, developer-friendly, and closely tied to the Red Hat ecosystem\n- [Linux Mint](https://labex.io/lesson/linux-mint): desktop-focused and especially comfortable for new users\n- [Arch Linux](https://labex.io/lesson/arch-linux): rolling release with a strong do-it-yourself culture\n- [openSUSE](https://labex.io/lesson/opensuse): flexible, polished, and known for YaST and multiple release options\n- [Gentoo](https://labex.io/lesson/gentoo): source-based and highly customizable\n- [Red Hat Enterprise Linux](https://labex.io/lesson/red-hat-enterprise-linux): enterprise-focused with commercial support\n\n## Debian, Ubuntu, Fedora, and Other Options\n\nMany popular Linux distros belong to larger families. Debian is the base for distributions such as Ubuntu, and Ubuntu in turn influences Linux Mint. Fedora sits in the Red Hat world and helps shape technologies that later appear in RHEL. Understanding these relationships makes it easier to compare Linux distributions because package management, release style, and system behavior often follow family lines.\n\nIf you are deciding between a few options, it helps to read the distro-specific pages rather than relying only on broad recommendations. A distro that is ideal for one kind of user may be a poor fit for another.\n\n## Start with One Distro\n\nIt is easy to spend too much time searching for the best Linux distro and never start using one. In practice, many popular distributions are good enough to begin learning Linux. Pick a distro that fits your goals, try it with a live system or virtual machine, and spend time learning the basics.\n\nOnce you understand one Linux distro, moving to another becomes much easier. The important step is to start.\n\n After identifying your goals, what is a practical next step?\n\n## Further Reading\n\n- [Debian](https://www.debian.org/intro/)\n- [Ubuntu](https://ubuntu.com/desktop)\n- [Fedora Workstation](https://fedoraproject.org/workstation/)\n- [openSUSE Desktop Distributions](https://get.opensuse.org/desktop/)\n\nTo continue learning after comparing Linux distros, we recommend these LabEx courses:\n\n1. **[Quick Start with Linux](https://labex.io/courses/quick-start-with-linux)** - Build a practical foundation in Linux basics before committing to one distro.\n2. **[Linux for Noobs](https://labex.io/courses/linux-for-noobs)** - Follow a beginner-friendly introduction to Linux concepts and workflows.\n3. **[Linux Commands Practice Online](https://labex.io/courses/linux-basic-commands-practice-online)** - Strengthen the command-line skills that transfer across most Linux distributions.\n\n## Summary\n\nYou can now compare Linux distributions according to your own goals instead of searching for one universal best choice.\n\n1. Explain what a Linux distribution contains.\n2. Identify the kernel as the hardware-managing core.\n3. Compare stable and rolling release styles.\n4. Recognize qualities that support new Linux users.\n5. Choose a practical way to try a suitable distribution.", "code_snippets": [], "quizzes": [{"id": "identify-hardware-manager", "question": "Which main part of a Linux system manages the hardware?", "options": [{"text": "User Space", "correct": false, "explanation": "User space is where applications and command-line interfaces run. Those programs rely on the kernel to work with hardware."}, {"text": "Linux Kernel", "correct": true, "explanation": "The Linux kernel manages hardware resources and communication between hardware and software. It is the core around which a distribution is built."}, {"text": "Physical Hardware", "correct": false, "explanation": "Hardware provides the CPU, memory, and storage. The kernel is the system component that manages those resources."}]}, {"id": "recognize-linux-distribution", "question": "Which description best matches a Linux distribution?", "options": [{"text": "A kernel distributed without system tools, applications, or software management", "correct": false, "explanation": "The kernel alone is only one part of an operating system. A distribution adds utilities, libraries, applications, and software management."}, {"text": "A kernel packaged with system tools, applications, and software management", "correct": true, "explanation": "A distribution combines the Linux kernel with the user-space software needed for a usable operating system. It commonly includes a package manager as well."}, {"text": "A desktop design shared by every operating system that uses Linux", "correct": false, "explanation": "Distributions can offer different desktop environments or no graphical desktop at all. A shared desktop design does not define a distribution."}]}, {"id": "choose-release-style", "question": "Which release style best fits someone who prioritizes planned updates and predictability?", "options": [{"text": "A continuously updated rolling release", "correct": false, "explanation": "A rolling release usually provides newer software through continuous updates. It also brings more frequent change than the stated goal calls for."}, {"text": "A stable or point-release model", "correct": true, "explanation": "Stable and point-release models deliver major changes in planned releases. This supports a more predictable environment."}, {"text": "A graphical desktop environment", "correct": false, "explanation": "A desktop environment controls the graphical experience, not the timing of distribution releases. It does not answer the release-style requirement."}]}, {"id": "prioritize-beginner-needs", "question": "Which qualities are the strongest starting point for a new Linux user?", "options": [{"text": "Newest packages, manual setup, and limited documentation", "correct": false, "explanation": "New software and manual setup may suit an experienced user, but limited guidance adds avoidable difficulty for a beginner."}, {"text": "Maximum control, complex maintenance, and frequent surprises", "correct": false, "explanation": "Deep control can be valuable after a user knows the workflow they want. It is not the most supportive default for a first distribution."}, {"text": "Smooth installation, strong documentation, and sensible defaults", "correct": true, "explanation": "These qualities reduce setup friction and make it easier to find help. They let a beginner focus on learning the system."}]}, {"id": "take-practical-next-step", "question": "After identifying your goals, what is a practical next step?", "options": [{"text": "Keep searching until one distro is best for everyone", "correct": false, "explanation": "The lesson establishes that different users need different things. Waiting for a universal best choice prevents you from gaining useful experience."}, {"text": "Switch repeatedly before learning the basics of any distro", "correct": false, "explanation": "Frequent switching makes it harder to build foundational skills. Learning one suitable distribution first makes later changes easier."}, {"text": "Choose a suitable distro and try it live or virtually", "correct": true, "explanation": "Trying a suitable option turns comparison into experience without requiring an immediate permanent commitment. You can begin learning and adjust later."}]}]}, {"lesson_id": "debian", "course_id": "getting-started", "title": "Debian", "order_index": 3, "description": "Learn how Debian organizes releases, packages, and community-maintained Linux systems.", "content": "## What Is Debian?\n\n**Debian** is one of the best-known and most influential Linux distributions. It is a free and open-source operating system developed by a global community rather than a single company.\n\nThe Debian Project has existed since the early days of Linux and has built a reputation for careful engineering, openness, and long-term reliability. In practice, the **Debian Linux distribution** is known for providing a solid base system, a huge software collection, and clear project principles.\n\n## Why Debian Is Popular\n\nDebian remains popular because it focuses on stability, consistency, and software freedom. Many users choose Debian when they want a system that changes carefully instead of rapidly. That approach has made Debian especially respected for servers, development environments, and any setup where reliability matters more than having the newest features immediately.\n\nAnother reason Debian is so widely known is its role in the larger Linux ecosystem. Debian has influenced countless users, administrators, and developers, and it has also served as the foundation for many other distributions. Its long history and large volunteer community give it a level of trust that few projects can match.\n\n## Debian Branches\n\nA major feature of Debian is its branch model. Instead of offering only one stream of packages, Debian maintains multiple branches so users can choose the balance between stability and newer software.\n\n- **Stable**: This is the official release. It prioritizes reliability and security over having the latest software versions, making it an excellent choice for servers and daily-use desktops where stability is critical.\n- **Testing**: This branch contains packages that are being prepared for the next Stable release. It usually offers newer software than Stable, but it may still receive important changes as packages move toward release quality.\n- **Unstable**: Also known as \"Sid,\" this is where active development happens. New package uploads enter Unstable first, so it changes frequently and may occasionally break.\n\nDuring most of Debian's development cycle, packages flow continuously through Unstable and into Testing. Testing later enters freeze stages while the next Stable release is prepared, so it is more accurate to understand these as development branches than to treat both as ordinary rolling-release products.\n\nThese branches help explain why Debian can serve very different users. Someone who wants a predictable system will usually prefer Stable, while developers and advanced users may explore Testing or Unstable for newer software.\n\n## Debian Releases\n\nDebian follows a release-based model. The project periodically publishes a new Stable release after packages have matured through development and testing. This is one reason Debian has a reputation for conservative, well-tested changes.\n\nFor beginners, the main idea is simple: Debian does not chase rapid change. New packages normally enter Unstable, qualifying packages move into Testing, and a prepared Testing branch later becomes the next Stable release. This model helps Debian stay reliable while still moving forward over time.\n\n## Package Management\n\nPackage management is one of Debian's biggest strengths. Debian uses the `.deb` package format and the **APT** toolset to install, update, remove, and manage software. This makes it easy to keep the system consistent and install software from official repositories.\n\nBecause Debian has a very large package collection, users can install everything from desktop applications to development tools through the same package system. For example, developers often install common build tools with packages such as `build-essential`. This mature package system is one reason Debian is so widely used and trusted.\n\n## Common Uses\n\nDebian is used in several common scenarios. It is especially popular for:\n\n- **Servers**, where stability and predictable updates are important\n- **Development environments**, where users want a clean and dependable base system\n- **Desktop systems**, especially for people who prefer a straightforward and stable Linux experience\n- **Learning Linux**, because Debian exposes many standard Linux tools and conventions without much unnecessary customization\n\nThis range of use cases helps explain Debian's long-lasting reputation. It is flexible enough for desktops and dependable enough for infrastructure.\n\n## Debian-Based Distributions\n\nDebian is also important because many other Linux distributions are built from its work. These are often called **Debian-based distributions**. Ubuntu is the most famous example, and other systems in the Debian family build on the same packaging and repository tradition.\n\nThis means Debian is not only a Linux distribution in its own right, but also a foundation for a large part of the Linux world. When you learn Debian concepts such as APT, `.deb` packages, or release branches, that knowledge often transfers to Debian-based systems as well. If you want a more beginner-focused Debian-based option, see [Ubuntu](https://labex.io/lesson/ubuntu).\n\n## Is Debian Beginner-Friendly?\n\nDebian can be beginner-friendly, but it depends on what kind of beginner you are. If you want a highly polished out-of-the-box desktop experience with many convenience defaults, another Debian-based system such as Ubuntu may feel easier at first. However, if you want to learn a classic, respected Linux distribution with strong documentation and a stable design, Debian is an excellent choice.\n\nIn other words, Debian is not only for experts. It is a strong option for learners who value reliability, clarity, and a deeper understanding of how Linux systems are put together. If you are still comparing options, [Choosing a Linux Distribution](https://labex.io/lesson/choosing-a-linux-distribution) gives a broader view of where Debian fits.\n\n## Further Reading\n\n- [Introduction to Debian](https://www.debian.org/intro/)\n- [About Debian](https://www.debian.org/intro/about)\n- [Debian Releases](https://www.debian.org/releases/)\n- [APT on the Debian Wiki](https://wiki.debian.org/Apt)\n\nTo build hands-on Linux skills after learning about Debian, we recommend these LabEx courses:\n\n1. **[Quick Start with Linux](https://labex.io/courses/quick-start-with-linux)** - Learn the Linux basics that apply cleanly to Debian and many other distributions.\n2. **[Software Package Management](https://labex.io/courses/software-package-management)** - Practice core package management concepts used across Linux environments.\n3. **[Become a Junior System Administrator](https://labex.io/courses/become-a-junior-system-administrator)** - Go deeper into practical Linux administration skills.\n\n## Summary\n\nYou can now explain how Debian balances stable releases with active package development.\n\n1. Describe Debian's community-driven project model.\n2. Compare the Stable, Testing, and Unstable branches.\n3. Trace the simplified package flow toward a Stable release.\n4. Explain how APT manages Debian software.\n5. Recognize knowledge that transfers to Debian-based systems.", "code_snippets": [], "quizzes": [{"id": "identify-debian-project-model", "question": "How is Debian primarily developed?", "options": [{"text": "By one commercial software company", "correct": false, "explanation": "Debian is not developed by a single company. Volunteers and contributors around the world maintain the project."}, {"text": "By one computer hardware manufacturer", "correct": false, "explanation": "Debian supports many kinds of hardware, but no hardware manufacturer owns its development. The project is community maintained."}, {"text": "By a global open-source community", "correct": true, "explanation": "Debian is maintained by a worldwide community rather than controlled by one company. Its project structure is a defining part of the distribution."}]}, {"id": "choose-debian-stable", "question": "Which Debian branch best fits a user who prioritizes reliability and predictable updates?", "options": [{"text": "Testing", "correct": false, "explanation": "Testing usually has newer packages that are being prepared for a future release. It can still change significantly during development."}, {"text": "Unstable", "correct": false, "explanation": "Unstable receives new package uploads first and changes frequently. That does not match a priority of predictable updates."}, {"text": "Stable", "correct": true, "explanation": "Stable is Debian's official production release and emphasizes reliability and security. It is the natural match for a predictable system."}]}, {"id": "trace-debian-package-flow", "question": "Which sequence best represents the simplified path of Debian packages toward a release?", "options": [{"text": "Unstable \u2192 Testing \u2192 Stable", "correct": true, "explanation": "New uploads enter Unstable, qualifying packages move into Testing, and a prepared Testing branch eventually becomes the next Stable release."}, {"text": "Stable \u2192 Testing \u2192 Unstable", "correct": false, "explanation": "Stable is the finished production release, not the starting place for new uploads. Development begins in Unstable."}, {"text": "Testing \u2192 Stable \u2192 Unstable", "correct": false, "explanation": "This puts Unstable after the finished release. In Debian's development flow, new packages enter Unstable before reaching Testing."}]}, {"id": "recognize-apt-purpose", "question": "What is the main purpose of Debian's APT toolset?", "options": [{"text": "Install, update, remove, and manage software packages", "correct": true, "explanation": "APT manages software packages from Debian repositories. It provides a consistent way to install, update, and remove software."}, {"text": "Compile a new Linux kernel for every update", "correct": false, "explanation": "APT can install packaged kernels, but its purpose is broader package management. It does not require compiling a kernel for every update."}, {"text": "Move the system between branches without configuration", "correct": false, "explanation": "Changing Debian branches requires deliberate repository and upgrade decisions. APT does not automatically choose or switch the system's release branch."}]}, {"id": "transfer-debian-knowledge", "question": "Why can Debian package-management knowledge transfer to some other distributions?", "options": [{"text": "Every Linux distribution uses identical packages and repositories", "correct": false, "explanation": "Linux distributions can use different package formats, tools, and repositories. Debian knowledge transfers most directly within the Debian family."}, {"text": "Debian-based systems often share `.deb` and APT traditions", "correct": true, "explanation": "Distributions built from Debian commonly retain its package format and related tools. The exact repositories may differ, but the core concepts transfer."}, {"text": "Every Debian-based system follows the same release schedule", "correct": false, "explanation": "Derived distributions can set their own release schedules and policies. Shared packaging traditions, not identical timing, explain the transferable knowledge."}]}]}, {"lesson_id": "red-hat-enterprise-linux", "course_id": "getting-started", "title": "Red Hat Enterprise Linux", "order_index": 4, "description": "Learn how RHEL combines enterprise support, predictable lifecycles, and RPM-based software management.", "content": "## What Is Red Hat Enterprise Linux?\n\nRed Hat Enterprise Linux, often called **RHEL**, is a commercial Linux distribution built by Red Hat for enterprise use. It is designed for organizations that need long support windows, predictable releases, security maintenance, and professional support.\n\nRHEL is one of the most important enterprise Linux distributions because it is used across servers, data centers, cloud systems, and regulated business environments. Its role is different from more general-purpose community distros because supportability and long-term lifecycle planning are central to its value.\n\n## Why RHEL Is Important\n\nRHEL is important because it gives organizations a stable and supported platform for production workloads. That includes not only the operating system itself, but also certification programs, hardware and software compatibility, and support policies that matter in enterprise environments.\n\nThis is what makes RHEL different from community-first distributions. The focus is not simply on having Linux, but on having Linux with enterprise expectations around reliability and support.\n\n## RHEL and Fedora\n\nRHEL is closely connected to the broader Red Hat ecosystem. Fedora is the community project where many new technologies first appear, while RHEL is the enterprise product built with a more conservative release philosophy. This relationship helps explain why Fedora feels more current and RHEL feels more controlled.\n\nIf you want to compare the two paths, see [Fedora](https://labex.io/lesson/fedora). For a broader overview of distro families, see [Choosing a Linux Distribution](https://labex.io/lesson/choosing-a-linux-distribution).\n\n## Package Management\n\nRHEL uses the RPM package format and tools such as DNF to install, update, and manage software. This places it in the same general package family as Fedora and openSUSE, though each distribution has its own tooling choices and ecosystem details.\n\nPackage management is a core operational skill for RHEL administrators because long-term maintenance and predictable updates are central to how enterprise systems are run.\n\n## Enterprise Support\n\nOne of the biggest reasons organizations choose RHEL is enterprise support. That includes long lifecycle planning, access to security updates, and a lifecycle that is designed to extend across many years for each major release.\n\nFor businesses, this support model can matter as much as the technical features of the distribution itself.\n\n## Certifications and Professional Use\n\nRHEL is also closely associated with professional training and certification. Credentials such as RHCSA and RHCE are well known in Linux administration and are part of why RHEL remains highly visible in professional environments.\n\nIf your goal is to learn Linux for enterprise operations, RHEL is one of the most important distributions to understand.\n\n## Further Reading\n\n- [Red Hat Enterprise Linux Overview](https://developers.redhat.com/products/rhel/overview)\n- [Why choose Red Hat Enterprise Linux?](https://www.redhat.com/en/topics/linux/why-choose-red-hat-enterprise-linux)\n- [RHEL lifecycle](https://www.redhat.com/en/blog/understanding-red-hat-enterprise-linux-rhel-lifecycle)\n- [Red Hat Certification](https://www.redhat.com/en/services/certification)\n\nTo keep learning after this RHEL introduction, we recommend these LabEx courses:\n\n1. **[Red Hat System Administration (RH124) Certification Labs](https://labex.io/courses/red-hat-system-administration-rh124-labs)** - Start with RHEL-focused administration practice.\n2. **[RHCSA Certification Exam Practice Exercises](https://labex.io/courses/rhcsa-certification-exam-practice-exercises)** - Reinforce the practical skills commonly associated with RHEL administration.\n3. **[RPM and DNF Package Management](https://labex.io/courses/rpm-and-dnf-package-management)** - Practice RPM- and DNF-related package management concepts.\n\n## Summary\n\nYou can now explain why RHEL is designed for long-lived, supported enterprise environments.\n\n1. Identify the enterprise priorities RHEL addresses.\n2. Describe the upstream relationship between Fedora and RHEL.\n3. Explain how RPM packages and DNF work together.\n4. Recognize the planning value of a published support lifecycle.", "code_snippets": [], "quizzes": [{"id": "match-rhel-priorities", "question": "Which need most directly matches the design goals of RHEL?", "options": [{"text": "Continuous feature changes with no support lifecycle", "correct": false, "explanation": "RHEL follows a conservative, published lifecycle rather than continuous unsupported change. Predictability is part of its enterprise value."}, {"text": "Predictable releases with long-term professional support", "correct": true, "explanation": "RHEL is designed for organizations that need planned lifecycles, maintenance, and professional support. These qualities help production systems remain supportable over time."}, {"text": "An experimental system intended only for personal projects", "correct": false, "explanation": "RHEL can support many workloads, but its defining purpose is supported enterprise operation. It is not positioned only as an experimental hobby system."}]}, {"id": "compare-fedora-and-rhel", "question": "How does Fedora relate to RHEL in the Red Hat ecosystem?", "options": [{"text": "Fedora is an older RHEL release kept without security maintenance", "correct": false, "explanation": "Fedora is a separate community distribution, not an expired RHEL version. It has its own releases and a faster pace."}, {"text": "Fedora is an upstream community project for technologies that may reach RHEL", "correct": true, "explanation": "Fedora is the faster-moving upstream community project. Red Hat draws from that ecosystem when developing its more conservative enterprise platform."}, {"text": "Fedora is the package manager used to install software on RHEL", "correct": false, "explanation": "Fedora is a Linux distribution rather than a package-management command. RHEL uses RPM packages with higher-level tools such as DNF."}]}, {"id": "relate-rpm-and-dnf", "question": "How do RPM and DNF work together on RHEL?", "options": [{"text": "RPM defines packaged software, while DNF manages repository content and dependencies", "correct": true, "explanation": "RHEL software is distributed as RPM packages, and DNF is the higher-level tool commonly used to find, install, update, and remove that content."}, {"text": "DNF defines packaged software, while RPM manages the graphical desktop", "correct": false, "explanation": "This reverses and misstates their roles. RPM is the package system, while DNF performs higher-level software management."}, {"text": "RPM controls release lifecycles, while DNF provides professional certification", "correct": false, "explanation": "Release policy and certification are separate Red Hat programs. RPM and DNF both belong to software packaging and management."}]}, {"id": "use-published-lifecycle", "question": "Why is a published support lifecycle valuable to an organization?", "options": [{"text": "It guarantees that every application will run without testing", "correct": false, "explanation": "A supported operating system does not guarantee compatibility with every application. Organizations still need compatibility checks and testing."}, {"text": "It removes the need to install security updates during support", "correct": false, "explanation": "A support lifecycle provides access to maintenance and security updates; it does not make those updates unnecessary. Systems still need active maintenance."}, {"text": "It helps teams plan maintenance, upgrades, and supported operation", "correct": true, "explanation": "A known lifecycle gives teams a time frame for updates and future migrations. That reduces uncertainty around long-running production systems."}]}]}, {"lesson_id": "ubuntu", "course_id": "getting-started", "title": "Ubuntu", "order_index": 5, "description": "Learn how Ubuntu combines Debian foundations with approachable desktop, server, and release options.", "content": "## What Is Ubuntu?\n\nUbuntu is one of the most widely used Linux distributions. Developed by Canonical, it is built on Debian and is known for its accessible design, large user community, and broad hardware and software support.\n\nUbuntu has become a common starting point for people who want to learn Linux without beginning from a more manual or advanced setup. It is used on personal computers, development systems, cloud platforms, and servers, which gives it a reach that few other distros can match.\n\n## Why Ubuntu Is Popular\n\nUbuntu is popular because it tries to make Linux practical for everyday use. It offers a polished installer, strong documentation, predictable releases, and a large ecosystem of tutorials and third-party support. For many users, that combination makes Ubuntu one of the easiest Linux distros to live with.\n\nAnother reason Ubuntu is so visible is that it works across many environments. You will see it on laptops and desktops, in virtual machines, on servers, and across cloud platforms. That broad adoption reinforces its reputation as a general-purpose Linux distribution.\n\n## Ubuntu and Debian\n\nUbuntu is a Debian-based distribution, which means it inherits much of its package management model and software packaging approach from Debian. If you learn how `apt` works in Ubuntu, that knowledge will also help you understand other Debian-based systems.\n\nAt the same time, Ubuntu is not just \"Debian with a desktop.\" It has its own release schedule, defaults, support model, and ecosystem. If you want to compare it with other options, see [Choosing a Linux Distribution](https://labex.io/lesson/choosing-a-linux-distribution) or learn more about [Debian](https://labex.io/lesson/debian).\n\n## Ubuntu Releases\n\nUbuntu uses two main release types. It publishes a new release every six months, and every two years one of those releases becomes a Long Term Support, or LTS, release. LTS releases are commonly chosen for desktops, workstations, and servers that need a more stable base.\n\nThis release model helps explain Ubuntu's appeal. Users who want a dependable base often choose LTS, while users who want newer features can use the interim releases that arrive on a faster schedule.\n\n## Package Management\n\nAs a Debian-based system, Ubuntu uses the `.deb` package format and the `apt` package manager for installing, updating, and removing software. This gives users access to a very large software ecosystem and a familiar command-line workflow.\n\nPackage management is one of Ubuntu's practical strengths because it combines mature Debian tooling with a large, widely documented software environment.\n\n## Desktop and Server Use\n\nUbuntu is used on both desktop and server systems. On the desktop side, it is known for a polished GNOME-based experience and relatively approachable defaults. On the server side, it is widely deployed in development, web infrastructure, and cloud environments.\n\nThat range makes Ubuntu attractive to users who want one Linux distribution that can scale from learning on a laptop to running workloads in production.\n\n## Why Beginners Choose Ubuntu\n\nUbuntu is often recommended to beginners because it is easier to install and troubleshoot than many other Linux distros. The large user base means there are many tutorials, forum posts, and guides available when something goes wrong.\n\nFor users who want a beginner-friendly Linux distro without giving up long-term flexibility, Ubuntu remains a common starting point.\n\n## Further Reading\n\n- [Ubuntu Desktop](https://ubuntu.com/desktop)\n- [Ubuntu Server](https://ubuntu.com/server)\n- [Ubuntu release cycle](https://ubuntu.com/releaseendoflife)\n- [Ubuntu releases documentation](https://documentation.ubuntu.com/project/release-team/ubuntu-releases/)\n\nTo keep learning after this Ubuntu introduction, we recommend these LabEx courses:\n\n1. **[Quick Start with Linux](https://labex.io/courses/quick-start-with-linux)** - Build a practical foundation in Linux basics and command-line skills.\n2. **[Linux for Noobs](https://labex.io/courses/linux-for-noobs)** - Follow a beginner-friendly path for understanding Linux basics step by step.\n3. **[Become a Junior System Administrator](https://labex.io/courses/become-a-junior-system-administrator)** - Continue into practical Linux administration skills once you are comfortable with the basics.\n\n## Summary\n\nYou can now explain how Ubuntu builds on Debian while offering its own releases and user experience.\n\n1. Identify Debian as Ubuntu's foundation.\n2. Recognize support qualities that help beginners.\n3. Compare LTS and interim Ubuntu releases.\n4. Use `apt` as Ubuntu's package-management tool.", "code_snippets": [], "quizzes": [{"id": "identify-ubuntu-base", "question": "Which distribution provides Ubuntu's foundation?", "options": [{"text": "The Debian distribution", "correct": true, "explanation": "Ubuntu is built from Debian and inherits much of Debian's packaging approach. Ubuntu then adds its own releases, defaults, and support model."}, {"text": "The Fedora distribution", "correct": false, "explanation": "Fedora belongs to the Red Hat ecosystem rather than forming Ubuntu's base. Ubuntu is part of the Debian family."}, {"text": "The Arch distribution", "correct": false, "explanation": "Arch Linux is a separate distribution with its own package system and release approach. Ubuntu is based on Debian."}]}, {"id": "recognize-beginner-support", "question": "Which Ubuntu quality most directly helps a beginner solve problems?", "options": [{"text": "Required manual compilation for each installed program", "correct": false, "explanation": "Ubuntu normally provides packaged software rather than requiring every program to be compiled manually. Extra build work would not simplify troubleshooting."}, {"text": "Extensive documentation and a large user community", "correct": true, "explanation": "Documentation and community discussions give beginners many places to find explanations and troubleshooting help. This lowers the barrier to learning."}, {"text": "Limited guidance available only to experienced administrators", "correct": false, "explanation": "Ubuntu's visibility comes partly from widely available guidance for many skill levels. Restricting help to experts would work against beginner accessibility."}]}, {"id": "choose-ubuntu-lts", "question": "Which Ubuntu release type best fits a system that needs a longer-lived, predictable base?", "options": [{"text": "An interim release", "correct": false, "explanation": "Interim releases arrive more frequently and expose newer features sooner. Their shorter support period does not match the stated priority."}, {"text": "An LTS release", "correct": true, "explanation": "LTS releases are intended for longer support and are commonly selected for systems that prioritize a dependable base."}, {"text": "A package update", "correct": false, "explanation": "A package update changes software within an installed release. It is not one of Ubuntu's two operating-system release types."}]}, {"id": "identify-ubuntu-package-tool", "question": "Which item is the package-management tool used to install software on Ubuntu?", "options": [{"text": "`.deb`", "correct": false, "explanation": "`.deb` identifies the package format used by Debian-based systems. It is not the command-line package-management tool."}, {"text": "`LTS`", "correct": false, "explanation": "LTS labels a Long Term Support release. It does not install or manage software packages."}, {"text": "`apt`", "correct": true, "explanation": "Ubuntu uses `apt` to install, update, and remove packages. The tool works with software packaged in Debian's `.deb` format."}]}]}, {"lesson_id": "fedora", "course_id": "getting-started", "title": "Fedora", "order_index": 6, "description": "Learn how Fedora delivers current Linux technology through a community project connected to Red Hat.", "content": "## What Is Fedora?\n\nFedora is a community-driven Linux distribution sponsored by Red Hat. It is known for shipping modern technologies, a polished desktop experience, and strong support for developers and technical users.\n\nFedora has a reputation for moving faster than more conservative distros while still aiming for quality and usability. That balance makes it appealing to users who want a modern Linux system without building everything from scratch.\n\n## Why Fedora Stands Out\n\nFedora stands out because it often adopts new Linux features earlier than enterprise-focused distributions. That makes it attractive to developers, open-source contributors, and desktop users who want a current system with strong upstream ties.\n\nIt is also well known for offering a clean default experience. Fedora Workstation is especially popular among developers who want a modern desktop, current tooling, and good support for containers, virtualization, and other development workflows.\n\n## Fedora and Red Hat\n\nFedora plays an important role in the Red Hat ecosystem. New technologies and changes often appear in Fedora first, and some of that work later influences Red Hat Enterprise Linux. This relationship helps explain why Fedora feels more current while RHEL is more conservative and enterprise-focused.\n\nIf you want to compare Fedora with enterprise-oriented options, see [Red Hat Enterprise Linux](https://labex.io/lesson/red-hat-enterprise-linux). If you are still comparing families of distros, [Choosing a Linux Distribution](https://labex.io/lesson/choosing-a-linux-distribution) gives a broader overview.\n\n## Fedora Releases\n\nFedora follows a regular release cycle, with two major releases in most years and about thirteen months of support for each release. Compared with more conservative distributions, Fedora tends to deliver newer kernels, desktop environments, and developer tools on a faster schedule.\n\nThat makes Fedora a good fit for users who want up-to-date software but still want an organized, mainstream Linux distribution rather than a more manual rolling-release system.\n\n## Package Management\n\nFedora uses the RPM package format and the DNF package manager to install, update, and remove software. DNF is a central part of the Fedora experience and is one of the main tools users rely on for keeping the system current.\n\nPackage management in Fedora is straightforward, and it fits naturally with the broader Red Hat family of systems.\n\n## Common Uses\n\nFedora is commonly used on developer workstations, technical desktops, and laptops. It is especially attractive to users who want a modern Linux environment for coding, containers, virtual machines, and general desktop work.\n\nWhile Fedora can also be used on servers, its strongest identity is usually as a current, developer-friendly Linux distribution.\n\n## Is Fedora Beginner-Friendly?\n\nFedora can be beginner-friendly, but it is usually a better fit for users who are comfortable with a somewhat faster-moving system. It is easier to approach than highly manual distros, but it may feel less conservative than Debian or less beginner-centered than Ubuntu or Linux Mint.\n\nFor users who want a modern Linux distro and do not mind learning a little as they go, Fedora is a strong option.\n\n## Further Reading\n\n- [Fedora Workstation](https://fedoraproject.org/workstation/)\n- [Fedora Docs](https://docs.fedoraproject.org/)\n- [Fedora release life cycle](https://docs.fedoraproject.org/en-US/releases/lifecycle/)\n- [Fedora Workstation Working Group](https://docs.fedoraproject.org/en-US/workstation-working-group/)\n\nTo build real Linux skills after learning about Fedora, we recommend these LabEx courses:\n\n1. **[Quick Start with Linux](https://labex.io/courses/quick-start-with-linux)** - Cover the Linux basics that apply across many distributions.\n2. **[Linux Commands Practice Online](https://labex.io/courses/linux-basic-commands-practice-online)** - Strengthen the command-line habits that matter in everyday Linux work.\n3. **[RPM and DNF Package Management](https://labex.io/courses/rpm-and-dnf-package-management)** - Practice RPM- and DNF-related package management concepts.\n\n## Summary\n\nYou can now explain Fedora's place as a current, community-driven distribution in the Red Hat ecosystem.\n\n1. Describe Fedora's community and sponsorship model.\n2. Recognize the users and workflows Fedora Workstation supports.\n3. Explain Fedora's upstream relationship with RHEL.\n4. Plan for Fedora's regular release upgrades.\n5. Identify DNF as Fedora's package-management tool.", "code_snippets": [], "quizzes": [{"id": "identify-fedora-project-model", "question": "Which statement correctly describes the Fedora Project?", "options": [{"text": "It is a discontinued version of Red Hat Enterprise Linux", "correct": false, "explanation": "Fedora is an active distribution with its own releases. It is upstream of RHEL rather than an obsolete RHEL version."}, {"text": "It is a distribution maintained by one hardware manufacturer", "correct": false, "explanation": "Fedora collaborates with hardware vendors, but its development is community driven and sponsored by Red Hat."}, {"text": "It is a community project sponsored by Red Hat", "correct": true, "explanation": "Fedora is built by a community with sponsorship and support from Red Hat. It remains a distinct community distribution."}]}, {"id": "match-fedora-user", "question": "Which user goal is the best match for Fedora Workstation?", "options": [{"text": "Keep one enterprise release unchanged for many years", "correct": false, "explanation": "A long, conservative enterprise lifecycle is closer to RHEL's role. Fedora moves on a faster release and upgrade schedule."}, {"text": "Use current developer tools in a polished desktop system", "correct": true, "explanation": "Fedora Workstation combines a curated desktop with current tools for development, containers, and virtualization. That directly matches this goal."}, {"text": "Build every system component manually from source code", "correct": false, "explanation": "Fedora provides a complete packaged system and does not require users to build every component. That goal better describes a more specialized workflow."}]}, {"id": "explain-fedora-upstream-role", "question": "What does Fedora's upstream relationship with RHEL mean?", "options": [{"text": "RHEL releases are copied unchanged into Fedora afterward", "correct": false, "explanation": "This reverses the relationship. Fedora moves faster and serves as an upstream source rather than a later copy of RHEL."}, {"text": "Fedora and RHEL always ship identical software versions", "correct": false, "explanation": "The distributions have different release goals and schedules. RHEL selects and stabilizes technology rather than matching every Fedora version."}, {"text": "Work developed in Fedora can later influence RHEL", "correct": true, "explanation": "Fedora is a place where newer technologies are integrated earlier. Some of that work later contributes to Red Hat's enterprise platform."}]}, {"id": "plan-fedora-upgrades", "question": "What maintenance should a Fedora user expect from its release model?", "options": [{"text": "No version upgrades for the lifetime of the computer", "correct": false, "explanation": "Fedora versions have a limited support period. Staying supported requires moving to newer releases over time."}, {"text": "Regular upgrades to remain on a supported release", "correct": true, "explanation": "Fedora releases move on a relatively fast schedule and receive updates for about thirteen months. Users should plan regular version upgrades."}, {"text": "Continuous package changes without distinct system releases", "correct": false, "explanation": "Fedora publishes distinct major releases rather than operating as a conventional rolling release. Its packages are current, but releases still matter."}]}, {"id": "identify-fedora-package-tool", "question": "Which tool does Fedora use for higher-level package management?", "options": [{"text": "APT", "correct": false, "explanation": "APT is associated with Debian-based distributions. Fedora belongs to the RPM package family and uses DNF."}, {"text": "DNF", "correct": true, "explanation": "DNF installs, updates, and removes packages from Fedora repositories. Fedora packages use the RPM format underneath."}, {"text": "Pacman", "correct": false, "explanation": "Pacman is the package manager used by Arch Linux. Fedora's higher-level package tool is DNF."}]}]}, {"lesson_id": "linux-mint", "course_id": "getting-started", "title": "Linux Mint", "order_index": 7, "description": "Learn how Linux Mint provides an approachable desktop experience with familiar Debian-family tools.", "content": "## What Is Linux Mint?\n\nLinux Mint is a desktop-focused Linux distribution known for being comfortable, familiar, and easy to use. It is especially popular with beginners and with users who want a traditional desktop layout instead of a more opinionated interface.\n\nIts reputation comes from practical decisions rather than technical complexity. Linux Mint aims to provide a complete desktop experience with sensible defaults, which is one reason it is often recommended to people moving from Windows.\n\n## Why Linux Mint Is Popular\n\nLinux Mint is popular because it keeps the desktop experience straightforward. Users often choose it when they want Linux to feel familiar, stable, and ready to use without much extra setup.\n\nIt also benefits from a reputation for being approachable. That makes Mint a natural recommendation in any broader guide about how to [choose a Linux distro](https://labex.io/lesson/choosing-a-linux-distribution).\n\n## Linux Mint and Ubuntu\n\nThe main Linux Mint editions use Ubuntu LTS as their package base, which gives them access to a large software ecosystem and mature package management. Linux Mint also maintains Linux Mint Debian Edition (LMDE), which is based directly on Debian. In both cases, Mint presents its own desktop experience on top of a Debian-family foundation.\n\nIf you want to understand that family relationship better, see [Ubuntu](https://labex.io/lesson/ubuntu) and [Debian](https://labex.io/lesson/debian).\n\n## Package Management\n\nBecause Linux Mint is Ubuntu-based, it uses the `.deb` package format and APT for package management. Users can install software from the command line or through graphical tools such as the Software Manager.\n\nThis gives Linux Mint a familiar and well-documented software workflow, which is one of the reasons it works well for newcomers.\n\n## Desktop Experience\n\nLinux Mint is primarily designed for desktop and laptop systems. Its Cinnamon desktop is especially well known for offering a classic layout with a panel, application menu, and a workflow that feels familiar to many users.\n\nThat desktop-first focus is a major part of Mint's identity. Unlike some distros that try to cover every use case equally, Mint is best understood as a practical desktop Linux distribution.\n\n## Common Uses\n\nLinux Mint is a good fit for everyday desktop computing, web browsing, office work, media playback, and general learning. It is less commonly chosen for servers or highly customized development environments, but it is very strong as a personal desktop system.\n\n## Is Linux Mint Good for Beginners?\n\nYes. Linux Mint is one of the most beginner-friendly Linux distros because it combines a gentle learning curve with a capable and stable base. Users who want an easy desktop introduction to Linux often find it more comfortable than more technical or faster-moving distributions.\n\n## Further Reading\n\n- [Linux Mint](https://linuxmint.com/)\n- [Linux Mint Download](https://linuxmint.com/download.php)\n- [Linux Mint Installation Guide](https://linuxmint-installation-guide.readthedocs.io/en/latest/)\n- [Linux Mint User Guide](https://linuxmint-user-guide.readthedocs.io/en/latest/)\n\nTo keep learning after this Linux Mint overview, we recommend these LabEx courses:\n\n1. **[Quick Start with Linux](https://labex.io/courses/quick-start-with-linux)** - Learn the Linux basics through guided hands-on practice.\n2. **[Linux for Noobs](https://labex.io/courses/linux-for-noobs)** - Follow a beginner-friendly Linux course with hands-on practice.\n3. **[Linux Terminal Basics](https://labex.io/courses/linux-terminal-basics)** - Build confidence with the terminal while keeping a beginner-friendly pace.\n\n## Summary\n\nYou can now explain how Linux Mint combines a familiar desktop with Debian-family software management.\n\n1. Identify the desktop goals Linux Mint emphasizes.\n2. Explain the Ubuntu LTS base of the main Mint editions.\n3. Recognize LMDE as the directly Debian-based edition.\n4. Identify APT and the Cinnamon desktop experience.", "code_snippets": [], "quizzes": [{"id": "match-linux-mint-goal", "question": "Which goal is the best match for Linux Mint?", "options": [{"text": "Use a familiar desktop with practical defaults", "correct": true, "explanation": "Linux Mint focuses on an approachable desktop experience with familiar navigation and useful defaults. That directly matches this goal."}, {"text": "Run a minimal server without a desktop interface", "correct": false, "explanation": "Linux Mint is primarily designed for desktop and laptop use. A server-focused distribution would better match a minimal headless system."}, {"text": "Build every installed component manually from source", "correct": false, "explanation": "Mint supplies a complete packaged desktop and does not require users to build every component. Its goal is practical usability rather than manual assembly."}]}, {"id": "identify-main-mint-base", "question": "Which distribution provides the package base for the main Linux Mint editions?", "options": [{"text": "Ubuntu LTS", "correct": true, "explanation": "The main Linux Mint editions use an Ubuntu LTS package base. LMDE is the separate edition based directly on Debian."}, {"text": "Fedora Linux", "correct": false, "explanation": "Fedora belongs to the RPM package family and does not provide Mint's package base. Main Mint editions use Ubuntu LTS."}, {"text": "Arch Linux", "correct": false, "explanation": "Arch has a different package system and rolling-release model. It is not the base for the main Linux Mint editions."}]}, {"id": "identify-mint-package-tool", "question": "Which tool manages packages from the command line on Linux Mint?", "options": [{"text": "DNF", "correct": false, "explanation": "DNF is used by Fedora and RHEL-family systems. Linux Mint uses Debian-family package tools instead."}, {"text": "APT", "correct": true, "explanation": "Linux Mint uses APT for command-line package management. Its software is distributed in the Debian-family `.deb` format."}, {"text": "Pacman", "correct": false, "explanation": "Pacman is associated with Arch Linux. It is not Linux Mint's package-management tool."}]}, {"id": "recognize-cinnamon-layout", "question": "Which feature describes the Cinnamon desktop experience highlighted here?", "options": [{"text": "A command-only interface with no graphical desktop", "correct": false, "explanation": "Linux Mint can use a terminal, but Cinnamon is a graphical desktop environment. A command-only interface does not describe it."}, {"text": "A classic layout with a panel and application menu", "correct": true, "explanation": "Cinnamon is known for a familiar panel-and-menu layout. This contributes to Mint's approachable desktop experience."}, {"text": "A server console designed without desktop applications", "correct": false, "explanation": "Mint's Cinnamon edition is built for personal desktop use. It is not presented as a desktop-free server console."}]}]}, {"lesson_id": "gentoo", "course_id": "getting-started", "title": "Gentoo", "order_index": 8, "description": "Learn how Gentoo uses Portage, source-based builds, and USE flags for detailed system control.", "content": "## What Is Gentoo?\n\nGentoo is a Linux distribution designed for users who want deep control over how their system is built. Unlike most mainstream distros, Gentoo is best known for its source-based approach, where software is often compiled on the local machine instead of simply installed as prebuilt binaries.\n\nThat design makes Gentoo especially attractive to advanced users who enjoy tuning, learning, and customizing their systems in detail.\n\n## Why Gentoo Is Different\n\nGentoo is different because it treats customization as a core part of the distro, not as an extra feature. Users can make detailed choices about optional features, dependencies, and build behavior in a way that most Linux distributions do not expose as directly.\n\nThis makes Gentoo powerful, but it also means Gentoo asks more from the user. It is not mainly designed to be the easiest path into Linux.\n\n## Portage\n\nAt the center of Gentoo is **Portage**, its package management system. Portage handles software installation and maintenance, and it is closely tied to Gentoo's source-based design.\n\nOne of Portage's most distinctive features is the use of **USE flags**, which let users enable or disable optional features before building software. This gives users a very fine level of control over the resulting system.\n\n## Source-Based Customization\n\nBecause software is often built locally, Gentoo can be tailored closely to specific needs and preferences. Users who want to strip away unnecessary features or optimize for a particular workflow often find this especially appealing.\n\nThis source-based model also makes Gentoo an educational distro. It teaches users more about dependencies, compilation, and system design than many mainstream distros do.\n\n## Performance and Control\n\nGentoo is often associated with performance and efficiency, but the bigger advantage is control. The ability to shape the system at a detailed level is usually more important than small performance gains alone.\n\nFor users who value that level of control, Gentoo can be deeply rewarding.\n\n## Who Should Use Gentoo?\n\nGentoo is best suited for advanced users and committed learners who enjoy detailed configuration and do not mind spending more time on setup and maintenance. If you want a gentler starting point, a distro such as [Ubuntu](https://labex.io/lesson/ubuntu) or [Linux Mint](https://labex.io/lesson/linux-mint) is usually easier. If you want a hands-on distro with less compilation, [Arch Linux](https://labex.io/lesson/arch-linux) may be a closer fit.\n\n## Further Reading\n\n- [Gentoo](https://www.gentoo.org/)\n- [Gentoo Handbook](https://wiki.gentoo.org/wiki/Handbook:Main_Page)\n- [Portage](https://wiki.gentoo.org/wiki/Portage)\n- [USE flags](https://wiki.gentoo.org/wiki/USE_flag)\n\nTo prepare for the deeper technical work Gentoo often involves, we recommend these LabEx courses:\n\n1. **[Linux Commands Practice Online](https://labex.io/courses/linux-basic-commands-practice-online)** - Strengthen the command-line habits that matter in hands-on Linux work.\n2. **[Shell Scripting Fundamentals](https://labex.io/courses/shell-scripting-fundamentals)** - Build more control over your environment through shell automation.\n3. **[Become a Junior System Administrator](https://labex.io/courses/become-a-junior-system-administrator)** - Develop a broader Linux administration foundation.\n\n## Summary\n\nYou can now explain why Gentoo trades convenience for detailed control over a Linux system.\n\n1. Recognize the users Gentoo is designed to serve.\n2. Identify Portage as Gentoo's package manager.\n3. Explain how USE flags control optional package features.\n4. Describe the trade-off of source-based customization.", "code_snippets": [], "quizzes": [{"id": "match-gentoo-user", "question": "Which user is the best match for Gentoo?", "options": [{"text": "A committed learner who wants detailed system control", "correct": true, "explanation": "Gentoo rewards users who want to make detailed build and configuration choices. That control also requires more time and involvement."}, {"text": "A beginner who wants the least possible setup work", "correct": false, "explanation": "Gentoo expects substantial configuration and maintenance from the user. A distribution with more prepared defaults better fits minimal setup."}, {"text": "A user who never wants to manage software choices", "correct": false, "explanation": "Software and feature choices are central to Gentoo's design. Avoiding those decisions would remove much of the reason to choose it."}]}, {"id": "identify-portage-role", "question": "What is Portage's role in Gentoo?", "options": [{"text": "It provides only the graphical desktop and application menu", "correct": false, "explanation": "A desktop environment controls the graphical interface. Portage manages software across the Gentoo system."}, {"text": "It manages software installation, dependencies, and maintenance", "correct": true, "explanation": "Portage is Gentoo's package-management system. It coordinates packages and the choices involved in building and maintaining them."}, {"text": "It replaces the Linux kernel with a different operating system", "correct": false, "explanation": "Portage can manage kernel-related packages, but it does not replace Linux with another operating system. Its role is package management."}]}, {"id": "explain-use-flags", "question": "What do Gentoo USE flags control?", "options": [{"text": "The physical amount of memory installed in the computer", "correct": false, "explanation": "Installed memory is a hardware property. USE flags configure software features rather than changing physical components."}, {"text": "Optional features and dependencies included when building packages", "correct": true, "explanation": "USE flags express which optional capabilities a package should support. Those choices can also change the dependencies Portage installs."}, {"text": "The username displayed when a person signs in", "correct": false, "explanation": "Account names are managed through user configuration. USE flags describe optional package functionality."}]}, {"id": "recognize-source-build-tradeoff", "question": "What trade-off comes with Gentoo's source-based customization?", "options": [{"text": "More control requires more build time and user decisions", "correct": true, "explanation": "Local build and feature choices provide detailed control, but they also demand time and attention from the user."}, {"text": "Less control removes the need to understand dependencies", "correct": false, "explanation": "Gentoo exposes more dependency and build choices, not fewer. Understanding those choices is part of its learning value."}, {"text": "Automatic setup eliminates ongoing package maintenance work", "correct": false, "explanation": "Gentoo does not eliminate maintenance through automatic setup. Its customized system still requires active package management."}]}]}, {"lesson_id": "arch-linux", "course_id": "getting-started", "title": "Arch Linux", "order_index": 9, "description": "Learn how Arch Linux combines rolling releases, Pacman, and user-managed system configuration.", "content": "## What Is Arch Linux?\n\nArch Linux is a lightweight, independently developed Linux distribution known for user control and a hands-on approach. It is popular with users who want to build their system more deliberately instead of relying on heavy defaults.\n\nUnlike distributions with scheduled major releases, Arch follows a rolling release model. That means the system receives continuous updates rather than waiting for big version jumps.\n\n## Why Arch Linux Is Popular\n\nArch Linux is popular because it gives users a high degree of control. Many people choose it not because it is the easiest Linux distro, but because it encourages them to understand what is installed, how the system is configured, and how the pieces fit together.\n\nThat makes Arch a common recommendation for curious intermediate and advanced users, even though it is usually not the first distro suggested to beginners comparing options in [Choosing a Linux Distribution](https://labex.io/lesson/choosing-a-linux-distribution).\n\n## Rolling Releases\n\nArch uses a rolling release model, so packages are updated continuously. This gives users access to current software without reinstalling the system for each major release, but it also means updates require more attention than on conservative point-release distros.\n\nFor users who want a system that stays current, rolling releases are a major attraction. For users who prioritize maximum predictability, a distro such as [Debian](https://labex.io/lesson/debian) may feel more comfortable.\n\n## Pacman and Package Management\n\nArch uses Pacman as its package manager. Pacman installs, updates, removes, and tracks software on the system, and it is one of the most recognizable parts of the Arch Linux experience.\n\nA common command is `sudo pacman -Syu`, which synchronizes package databases and performs a full upgrade of packages from configured repositories. Arch does not support partial upgrades, so users should avoid refreshing package databases without completing the corresponding system upgrade. Pacman is valued because it is direct, fast, and closely aligned with Arch's minimalist design.\n\n## The Arch Philosophy\n\nArch is often associated with minimalism, modernity, and user centrality. In practice, that means the distro tries to avoid unnecessary abstraction and expects users to take responsibility for setup and maintenance.\n\nThis philosophy is a major reason Arch attracts committed users. It is not trying to hide complexity as much as possible. It is trying to make the system understandable.\n\n## Who Should Use Arch Linux?\n\nArch Linux is best suited for users who want a hands-on Linux distro and do not mind reading documentation, configuring parts of the system manually, and taking responsibility for updates. It is an excellent learning environment for users who want deeper system knowledge.\n\nFor complete beginners, Arch is usually better as a later step than a first step.\n\n## Further Reading\n\n- [Arch Linux](https://archlinux.org/)\n- [ArchWiki](https://wiki.archlinux.org/)\n- [Pacman](https://wiki.archlinux.org/title/Pacman)\n- [Arch Linux Installation Guide](https://wiki.archlinux.org/title/Installation_guide)\n\nTo build the command-line confidence Arch Linux expects, we recommend these LabEx courses:\n\n1. **[Linux Commands Practice Online](https://labex.io/courses/linux-basic-commands-practice-online)** - Strengthen the command-line habits that matter in a hands-on Linux environment.\n2. **[Shell for Beginners](https://labex.io/courses/shell-for-beginners)** - Improve your comfort with the shell and terminal workflow.\n3. **[Shell Scripting Fundamentals](https://labex.io/courses/shell-scripting-fundamentals)** - Go deeper once you want more control over your Linux environment.\n\n## Summary\n\nYou can now explain how Arch Linux combines continuous upgrades with direct user responsibility.\n\n1. Describe the Arch rolling release model.\n2. Recognize the users Arch is designed to serve.\n3. Identify Pacman as Arch's package manager.\n4. Explain why Arch requires full system upgrades.", "code_snippets": [], "quizzes": [{"id": "recognize-rolling-release", "question": "What does Arch Linux's rolling release model mean?", "options": [{"text": "The installed system receives continuous package upgrades", "correct": true, "explanation": "Arch evolves through ongoing package upgrades rather than separate major system releases. A maintained installation can stay current over time."}, {"text": "The system waits for fixed multi-year upgrade editions", "correct": false, "explanation": "Fixed major editions describe a point-release model. Arch instead updates the installed system continuously."}, {"text": "The system replaces all packages only during reinstallation", "correct": false, "explanation": "Arch users update an existing installation with Pacman. Reinstalling is not the normal way to receive each set of upgrades."}]}, {"id": "match-arch-user", "question": "Which user is the best match for Arch Linux?", "options": [{"text": "A beginner who wants every decision handled automatically", "correct": false, "explanation": "Arch deliberately leaves many choices to the user. A distribution with more prepared defaults better fits fully automatic setup."}, {"text": "A user who never wants to review software updates", "correct": false, "explanation": "A rolling Arch system requires active maintenance and attention to update notices. Ignoring updates conflicts with that responsibility."}, {"text": "A hands-on learner willing to read and maintain the system", "correct": true, "explanation": "Arch is intended for users with a do-it-yourself attitude who will consult documentation and take responsibility for configuration and maintenance."}]}, {"id": "identify-pacman-role", "question": "What is Pacman's role on Arch Linux?", "options": [{"text": "Choose the desktop layout without managing software", "correct": false, "explanation": "Desktop configuration is separate from package management. Pacman manages the software packages that can provide desktop components."}, {"text": "Replace the rolling release model with fixed editions", "correct": false, "explanation": "Pacman supports Arch's rolling system through package upgrades. It does not convert Arch into a point-release distribution."}, {"text": "Install, update, remove, and track software packages", "correct": true, "explanation": "Pacman is Arch Linux's package manager. It maintains installed packages and works with the distribution's repositories."}]}, {"id": "avoid-partial-upgrades", "question": "Why should an Arch user complete a full upgrade after refreshing package databases?", "options": [{"text": "Partial upgrades are the recommended way to preserve old libraries", "correct": false, "explanation": "Arch explicitly does not support partial upgrades. Mixing newer libraries with older dependent packages can break the system."}, {"text": "Refreshing package databases automatically reinstalls the operating system", "correct": false, "explanation": "A database refresh only updates package information. It does not reinstall Arch, but it should be followed by the matching full upgrade."}, {"text": "Repository packages are maintained as one consistent system state", "correct": true, "explanation": "Arch repositories move together as a rolling system. A full upgrade keeps installed libraries and dependent packages aligned."}]}]}, {"lesson_id": "opensuse", "course_id": "getting-started", "title": "openSUSE", "order_index": 10, "description": "Learn how openSUSE offers regular and rolling releases with Zypper and YaST administration tools.", "content": "## What Is openSUSE?\n\nopenSUSE is a long-running Linux distribution known for flexibility, strong administration tools, and multiple release options. It is a community project with a reputation for being polished and capable on both desktops and technical systems.\n\nOne reason openSUSE stands out is that it offers different paths for different users. Some users want a stable base, while others want a faster-moving rolling release.\n\n## Leap and Tumbleweed\n\nopenSUSE is known for two main release approaches: Leap and Tumbleweed. Leap is the more conservative option and is aimed at users who want stability and a traditional release model. Tumbleweed is a rolling release for users who want newer software delivered continuously.\n\nThat split gives openSUSE unusual flexibility. Users can choose the style that fits them instead of switching to a different distro family entirely.\n\n## Package Management\n\nopenSUSE uses the RPM package format and tools such as `zypper` to install, update, and remove software. This puts it in a different package family from Debian and Ubuntu, which use `.deb` packages and APT.\n\nUnderstanding package families is helpful when you compare Linux distros. If you want a broader comparison, see [Choosing a Linux Distribution](https://labex.io/lesson/choosing-a-linux-distribution).\n\n## YaST\n\nOne of the best-known features of openSUSE is **YaST**. YaST is an administration and setup tool that helps manage software, services, storage, networking, and other system tasks from a central interface.\n\nThis is a major reason openSUSE appeals to users who want powerful system administration tools without having to configure everything manually.\n\n## Common Uses\n\nopenSUSE works well on desktops, development systems, and technical workstations. It is also attractive to users who want strong control over system configuration while still having polished tooling.\n\nCompared with more beginner-focused distros, openSUSE often appeals to users who want a little more structure and admin visibility.\n\n## Who Should Use openSUSE?\n\nopenSUSE is a strong option for users who want flexibility in release style and appreciate powerful management tools. It can work for beginners, especially those who like graphical administration, but it is often especially appealing to intermediate users and technical desktop users.\n\n## Further Reading\n\n- [openSUSE Desktop Distributions](https://get.opensuse.org/desktop/)\n- [Tumbleweed](https://get.opensuse.org/tumbleweed/)\n- [Leap](https://get.opensuse.org/leap/)\n- [YaST](https://yast.opensuse.org/)\n\nTo continue after this openSUSE introduction, we recommend these LabEx courses:\n\n1. **[Quick Start with Linux](https://labex.io/courses/quick-start-with-linux)** - Learn the Linux basics through guided hands-on practice.\n2. **[Linux Commands Practice Online](https://labex.io/courses/linux-basic-commands-practice-online)** - Build comfort with the Linux command line.\n3. **[Become a Junior System Administrator](https://labex.io/courses/become-a-junior-system-administrator)** - Continue into broader Linux administration topics.\n\n## Summary\n\nYou can now compare openSUSE release options and identify its main administration tools.\n\n1. Choose between Leap and Tumbleweed by release preference.\n2. Explain how Tumbleweed delivers continuous updates.\n3. Identify Zypper as the package-management tool.\n4. Recognize YaST as the central configuration interface.", "code_snippets": [], "quizzes": [{"id": "choose-opensuse-leap", "question": "Which openSUSE option best fits a user who wants a traditional, regular release?", "options": [{"text": "Tumbleweed", "correct": false, "explanation": "Tumbleweed is openSUSE's continuously updated rolling release. It better fits users who prioritize newer packages."}, {"text": "YaST", "correct": false, "explanation": "YaST is an installation and configuration tool, not an openSUSE release model. It can be used to administer the system."}, {"text": "Leap", "correct": true, "explanation": "Leap follows a regular release model and emphasizes a more conservative system base. That matches the stated preference."}]}, {"id": "recognize-tumbleweed-model", "question": "What distinguishes Tumbleweed from Leap?", "options": [{"text": "It delivers tested package updates continuously", "correct": true, "explanation": "Tumbleweed is a rolling release that publishes tested snapshots continuously. Users receive new software without waiting for a regular major release."}, {"text": "It receives software only through fixed major releases", "correct": false, "explanation": "Fixed regular releases describe Leap's approach more closely. Tumbleweed updates continuously."}, {"text": "It removes package management from the operating system", "correct": false, "explanation": "Tumbleweed still manages software packages and system updates. Rolling release describes update timing, not the absence of package management."}]}, {"id": "identify-zypper-role", "question": "What is `zypper` used for on openSUSE?", "options": [{"text": "Selecting between graphical desktop wallpaper themes", "correct": false, "explanation": "Desktop appearance is configured through desktop tools. `zypper` manages software packages instead."}, {"text": "Installing, updating, and removing software packages", "correct": true, "explanation": "`zypper` is openSUSE's command-line package-management tool. It works with software distributed through RPM repositories."}, {"text": "Changing Tumbleweed into a fixed Debian release", "correct": false, "explanation": "Package management does not turn openSUSE into another distribution family. Leap and Tumbleweed remain openSUSE release choices."}]}, {"id": "identify-yast-purpose", "question": "What is YaST designed to provide?", "options": [{"text": "A rolling repository containing only the newest applications", "correct": false, "explanation": "Tumbleweed provides the rolling repository model. YaST is an administration and configuration tool rather than a software branch."}, {"text": "A package format shared with Debian and Ubuntu systems", "correct": false, "explanation": "openSUSE uses RPM packages, while Debian-based systems use `.deb`. YaST itself is not a package format."}, {"text": "A central interface for installation and system configuration", "correct": true, "explanation": "YaST combines installation with modules for configuring many parts of an openSUSE system. It is available through graphical and terminal interfaces."}]}]}, {"lesson_id": "best-linux-distro-for-cybersecurity", "course_id": "getting-started", "title": "Linux for Cybersecurity", "order_index": 11, "description": "Learn how to choose a security-focused Linux distribution for an authorized task and skill level.", "content": "## What Is a Cybersecurity Linux Distro?\n\nA cybersecurity Linux distro is a Linux distribution designed for security-focused work such as penetration testing, digital forensics, privacy protection, vulnerability assessment, and security research. These distros often include preinstalled tools, custom configurations, or safer defaults that make them more useful for security tasks than a general-purpose desktop Linux system.\n\nThat does not mean everyone needs one. Many security professionals use standard Linux distributions for daily work and only switch to a security-focused distro when they need a specialized environment.\n\n## Do You Need a Security-Focused Distro?\n\nIf you are learning Linux for the first time, a security distro is not always the best place to start. In many cases, a beginner-friendly distro such as [Ubuntu](https://labex.io/lesson/ubuntu) or a stable distro such as [Debian](https://labex.io/lesson/debian) is a better first step. You can always add tools later or move into a more specialized environment once you understand the basics.\n\nSecurity distros make the most sense when you already know why you need them. For example, you may want a ready-made penetration testing toolkit, a privacy-focused live system, or a large collection of offensive security tools without having to build the environment by hand.\n\nSecurity tools must be used only on systems you own or have explicit permission to test. A specialized distribution provides tools, not authorization, judgment, or the skills needed to use them safely.\n\n## Best Linux Distros for Cybersecurity\n\nThere is no single best Linux distro for cybersecurity because different security tasks have different needs. Some users want a penetration testing platform, some want a privacy-focused operating system, and some want a highly customizable environment for advanced work.\n\nIn practice, the most widely discussed options are:\n\n- **Kali Linux** for penetration testing and security auditing\n- **Parrot OS** for security work with a lighter and more privacy-oriented feel\n- **BlackArch** for advanced users who want a huge Arch-based security toolkit\n- **Tails** for privacy, anonymity, and safer use on untrusted computers\n\n## Kali Linux\n\n[Kali Linux](https://www.kali.org/) is the best-known cybersecurity Linux distro. It is a Debian-based distribution built for penetration testing and security auditing, and its official documentation makes clear that it is specifically tailored for experienced penetration testers and security specialists.\n\nKali stands out because it provides a large collection of security tools in one place and is available across many platforms, including virtual machines and ARM devices. It is often the default answer when people search for the best Linux distro for ethical hacking or penetration testing.\n\nAt the same time, Kali is not recommended as a general-purpose Linux desktop for new users. Even Kali's own documentation warns that it is not the right distribution for people who are unfamiliar with Linux or just want a normal desktop environment.\n\n## Parrot OS\n\n[Parrot OS](https://www.parrotsec.org/) is another major security-focused Linux distro. It is widely used by penetration testers, researchers, students, and users who care about both security and privacy. The Parrot project also emphasizes that the system is lightweight, modular, up to date, and suitable for cloud and virtual environments.\n\nCompared with Kali, Parrot often feels a little broader in scope. It is still security-focused, but it also puts more visible emphasis on privacy, lightweight operation, and flexibility. That makes it appealing to users who want a security distro that can still feel practical for daily technical work.\n\n## BlackArch\n\n[BlackArch](https://www.blackarch.org/) is an Arch Linux-based penetration testing distribution aimed at penetration testers and security researchers. Its official site highlights a very large repository of security tools and notes that BlackArch can also be used on top of an existing Arch installation.\n\nBlackArch is powerful, but it is not a beginner-first option. Its own FAQ says that if you are not familiar with Arch Linux, or Linux in general, you should avoid BlackArch because of the learning curve. This makes it a better fit for advanced users who already understand Arch and want a massive security toolkit.\n\n## Tails and Privacy-Focused Use\n\n[Tails](https://tails.net/) is different from Kali, Parrot, and BlackArch. It is not mainly a penetration testing distro. Instead, Tails is a portable operating system designed to protect against surveillance and censorship. It uses the Tor network, runs from removable media, and is built to leave no trace on the computer when shut down.\n\nThis makes Tails an important security-focused Linux distro, but for a different reason. If your goal is privacy, anonymity, or safer use from untrusted computers, Tails may be the best fit. If your goal is penetration testing, Kali or Parrot is usually a more direct choice.\n\n## Which One Should You Choose?\n\nIf you want the most widely recognized penetration testing distro, start with **Kali Linux**. If you want a security distro with a stronger privacy and lightweight angle, look at **Parrot OS**. If you are already comfortable with Arch and want an enormous security tool repository, **BlackArch** is the advanced option. If you care most about anonymity and leaving no trace, choose **Tails**.\n\nFor most learners, the best path is not to install every security distro at once. Choose one that matches your real goal, then build hands-on skills around it. If you are still comparing general-purpose Linux options, [Choosing a Linux Distribution](https://labex.io/lesson/choosing-a-linux-distribution) gives a broader overview.\n\n## Further Reading\n\n- [What is Kali Linux?](https://www.kali.org/docs/introduction/what-is-kali-linux/)\n- [Should I Use Kali Linux?](https://www.kali.org/docs/introduction/should-i-use-kali-linux/)\n- [Parrot Security](https://www.parrotsec.org/)\n- [BlackArch Linux](https://www.blackarch.org/index.html)\n- [Tails](https://tails.net/)\n\nTo continue learning after comparing security-focused Linux distros, we recommend these LabEx courses:\n\n1. **[Kali Linux for Beginners](https://labex.io/courses/kali-linux-for-beginners)** - Start with a guided introduction to Kali Linux and its common use cases.\n2. **[Penetration Testing for Beginners](https://labex.io/courses/penetration-testing-for-beginners)** - Build a practical foundation in offensive security concepts.\n3. **[Nmap for Beginners](https://labex.io/courses/nmap-for-beginners)** - Learn one of the most common tools used in security-focused Linux environments.\n\n## Summary\n\nYou can now compare security-focused Linux distributions by task, experience, and authorization.\n\n1. Confirm authorization before using security-testing tools.\n2. Match Kali to experienced penetration-testing work.\n3. Recognize the Arch knowledge BlackArch expects.\n4. Choose Tails for portable privacy-focused use.", "code_snippets": [], "quizzes": [{"id": "confirm-testing-authorization", "question": "What must you confirm before using penetration-testing tools on a system?", "options": [{"text": "You own the system or have explicit permission to test it", "correct": true, "explanation": "Security testing requires clear authorization from the system owner. Having a tool or distribution does not create permission to use it against other systems."}, {"text": "The security distribution includes the tool you want to run", "correct": false, "explanation": "Tool availability does not establish permission. Authorization must come from the owner of the system being tested."}, {"text": "The target is reachable from your current network connection", "correct": false, "explanation": "Network access does not imply consent to test. You still need ownership or explicit authorization before running security assessments."}]}, {"id": "match-kali-use-case", "question": "Which situation is the strongest match for Kali Linux?", "options": [{"text": "An experienced tester needs a prepared security-auditing environment", "correct": true, "explanation": "Kali is tailored for penetration testing and security auditing by users who already understand Linux and the work they are performing."}, {"text": "A new Linux user wants a general desktop for everyday tasks", "correct": false, "explanation": "Kali's own documentation does not recommend it as a first general-purpose desktop. A beginner-friendly distribution is a better match."}, {"text": "A privacy user wants a removable system that routes through Tor", "correct": false, "explanation": "A portable, Tor-focused environment describes Tails rather than Kali. Kali's primary role is security assessment."}]}, {"id": "match-blackarch-user", "question": "Which background best prepares someone to use BlackArch?", "options": [{"text": "No Linux experience and no interest in system administration", "correct": false, "explanation": "BlackArch is not designed as a first introduction to Linux. Its Arch foundation and large toolkit require substantial prior knowledge."}, {"text": "Existing confidence with Arch Linux and its maintenance model", "correct": true, "explanation": "BlackArch builds on Arch and assumes users can handle that environment. Its own guidance warns newcomers about the learning curve."}, {"text": "Only experience with graphical tools on a general desktop", "correct": false, "explanation": "A graphical background alone does not prepare a user for BlackArch's Arch-based maintenance and security tooling. Linux command-line experience is important."}]}, {"id": "match-tails-use-case", "question": "Which goal is the strongest match for Tails?", "options": [{"text": "Load a large Arch-based repository of penetration-testing tools", "correct": false, "explanation": "An Arch-based security-tool repository describes BlackArch. Tails focuses on portable privacy and censorship resistance."}, {"text": "Use a portable system designed for privacy and minimal local traces", "correct": true, "explanation": "Tails routes internet activity through Tor and is designed to leave no trace on the computer after shutdown. Its focus is privacy rather than penetration testing."}, {"text": "Run a general desktop intended for a first Linux installation", "correct": false, "explanation": "Tails is a specialized privacy system rather than an ordinary first desktop installation. A general-purpose beginner distribution better fits that goal."}]}]}]}, {"course_id": "command-line", "course_name": "Command Line", "icon": "\ud83d\udc1a", "description": "Master the shell, navigation, file viewing, operations, and discovery.", "lessons_count": 19, "quizzes_count": 88, "lessons": [{"lesson_id": "the-shell", "course_id": "command-line", "title": "The Shell", "order_index": 1, "description": "Learn what the Linux shell is and how commands are executed.", "content": "## What is the Linux Shell\n\nWelcome to your Linux journey! The first step is understanding the Linux shell. A shell is a program that accepts commands you type, asks the operating system to run them, and then prints the result back to your terminal.\n\nIf you have used a graphical user interface, you are used to clicking windows, menus, and buttons. In the command line, you type precise instructions instead. Applications named \"Terminal\", \"Console\", or \"Konsole\" usually open a shell session for you.\n\nThe terminal is the window or app you type into, while the shell is the program running inside it.\n\nThe shell is useful because it is fast, scriptable, and available on almost every Linux system. As you learn more commands, you can combine them to inspect files, manage directories, search text, install software, and automate repeated work.\n\n## Interacting with the Bash Shell\n\nFor this course, we will focus on Bash, short for Bourne Again Shell. Bash is one of the most common Linux shells and is a good foundation even if you later use `zsh`, `fish`, or another shell.\n\nWhen you open a terminal, you will be greeted by the shell prompt. Its appearance can vary, but it often shows your username, host name, and current directory.\n\n```plaintext\npete@icebox:/home/pete $\n```\n\nThe `$` symbol indicates that the shell is ready to accept your input as a normal user. You do not type this symbol when entering commands; it is shown by the shell. If you see `#` instead, you are usually working as the root user, which has more power and more risk.\n\nCommands often follow this pattern:\n\n```bash\ncommand options arguments\n```\n\nFor example, in `echo Hello World`, `echo` is the command and `Hello World` is the text passed to it.\n\n## Your First Linux Command\n\nLet's start with one of the most basic Linux commands for beginners: `echo`. This command displays the text you provide back to the terminal.\n\n```bash\n$ echo Hello World\nHello World\n```\n\nTry a few more examples:\n\n```bash\n$ echo Linux is fun\nLinux is fun\n$ echo \"Hello from Bash\"\nHello from Bash\n```\n\nQuotes are useful when you want the shell to treat several words as one piece of text.\n\nTo practice these skills, explore the comprehensive [Shell Learning Path](https://labex.io/learn/shell).\n\n## Common Beginner Tips\n\n- Press `Enter` to run a command.\n- Use the `Up Arrow` key to recall a previous command.\n- Commands and filenames are case-sensitive in Linux.\n- Spaces matter. `echo hello` and `echohello` are different.\n- If a command seems stuck, `Ctrl-C` often cancels it.\n\n## Summary\n\nYou can now explain the role of a shell and interact with a basic shell prompt.\n\n1. Distinguish between a terminal and a shell.\n2. Identify a command prompt.\n3. Run a simple command with `echo`.", "code_snippets": ["pete@icebox:/home/pete $", "command options arguments", "$ echo Hello World\nHello World", "$ echo Linux is fun\nLinux is fun\n$ echo \"Hello from Bash\"\nHello from Bash"], "quizzes": [{"id": "distinguish-shell-and-terminal", "question": "Which statement correctly describes the relationship between a terminal and a shell?", "options": [{"text": "The terminal provides the window, while the shell runs inside it.", "correct": true, "explanation": "The terminal is the interface you use, and the shell is the command-processing program running inside it."}, {"text": "The terminal accepts commands, while the shell only displays their output.", "correct": false, "explanation": "This reverses their roles. The terminal provides the interface, while the shell accepts and runs commands."}, {"text": "The terminal and the shell are two names for the same program.", "correct": false, "explanation": "They work together, but they are not the same program. A terminal opens a session in which a shell runs."}]}, {"id": "interpret-dollar-prompt", "question": "What does the `$` at the end of the example prompt indicate?", "options": [{"text": "The shell is running with the privileges of the root user.", "correct": false, "explanation": "A root prompt usually ends with `#`, not `$`. Root access carries additional power and risk."}, {"text": "The shell is waiting for input from a normal user.", "correct": true, "explanation": "The `$` marks a normal user prompt and shows that the shell is ready for a command."}, {"text": "The next command must begin with a dollar sign.", "correct": false, "explanation": "The `$` belongs to the prompt. You type the command that follows it, without copying the symbol."}]}, {"id": "identify-command-name", "question": "In `echo Hello World`, which part is the command name?", "options": [{"text": "`Hello`", "correct": false, "explanation": "`Hello` comes after the command name, so it is part of the text passed to `echo`."}, {"text": "`World`", "correct": false, "explanation": "`World` is also text passed to `echo`, not the name of the command being run."}, {"text": "`echo`", "correct": true, "explanation": "`echo` names the program the shell should run. The words after it are passed to that program as arguments."}]}, {"id": "group-words-with-quotes", "question": "Which command makes the shell treat `Hello from Bash` as one quoted piece of text?", "options": [{"text": "`echo \"Hello from Bash\"`", "correct": true, "explanation": "The quotation marks group the three words into one argument passed to `echo`."}, {"text": "`echo Hello from Bash`", "correct": false, "explanation": "This prints the same visible words, but the shell treats them as separate arguments because they are not quoted."}, {"text": "`\"echo Hello from Bash\"`", "correct": false, "explanation": "Quoting the entire line makes the shell look for a command with that full name instead of running `echo` with text."}]}]}, {"lesson_id": "print-working-directory-pwd-command", "course_id": "command-line", "title": "pwd (Print Working Directory)", "order_index": 2, "description": "Learn how to use pwd to identify your current location in the Linux filesystem.", "content": "In Linux, files and directories are organized in a hierarchy called the filesystem. Before you can move around confidently, you need to know where you are. The `pwd` command answers that question by printing your current working directory.\n\n## The Directory Tree in Linux\n\nThe entire filesystem starts from a single top-level directory called the root directory, represented by a forward slash (`/`). From the root, the directory tree branches into subdirectories, which can contain files and more subdirectories.\n\nHere is a simplified example of what this structure looks like:\n\n```plaintext\n/\n|-- bin\n|   |-- file1\n|   |-- file2\n|-- etc\n|   |-- file3\n|   `-- directory1\n|       |-- file4\n|       `-- file5\n|-- home\n|-- var\n```\n\n## Understanding File Paths\n\nThe location of any file or directory is described by its path. A path is a sequence of directories that leads from a starting point to a specific destination.\n\nFor example, if you have a folder named `pete` inside `/home`, and a `Movies` folder inside `pete`, the full path is:\n\n```plaintext\n/home/pete/Movies\n```\n\nA path that starts with `/` is an absolute path because it starts at the root directory. A path such as `Movies` is relative because it depends on your current location.\n\n## What is the Full Form of PWD in Linux?\n\nThe full form of `pwd` is \"print working directory.\" Your working directory is the directory where your shell is currently located. Commands that use relative paths start from this location.\n\n## Using the pwd Command\n\nTo find your current directory, type `pwd` and press Enter.\n\n```bash\n$ pwd\n/home/pete\n```\n\nThe output is an absolute path. In this example, the shell is currently in the `pete` user's home directory.\n\nThe exact output may be different on your system because your username, home directory, and current location may differ. The `pwd` command only prints information; it does not change your working directory. By contrast, `cd` changes the directory where your shell is located.\n\n## Why pwd is Useful\n\nUse `pwd` when:\n\n- You are following instructions and need to confirm your location.\n- A command failed because a file path was wrong.\n- You moved through several directories and lost track of where you are.\n- You want to copy the current directory path into another command.\n\nFor example:\n\n```bash\n$ pwd\n/home/pete/projects\n$ ls\napp.py  README.md\n```\n\nThis tells you that `app.py` and `README.md` are located in `/home/pete/projects`.\n\nTo reinforce your understanding of Linux filesystem navigation and identifying your current location, try these hands-on labs:\n\n1. **[Linux pwd Command: Directory Displaying](https://labex.io/labs/linux-linux-pwd-command-directory-displaying-209734)** - This lab provides a focused overview and practical usage of the `pwd` command, directly aligning with the lesson's introduction to finding your current directory.\n2. **[Linux Directory Navigation](https://labex.io/labs/linux-directory-navigation-387844)** - Put your basic Linux command-line skills to the test by navigating through various directories, solidifying your understanding of paths and the file system structure.\n3. **[Linux cd Command: Directory Changing](https://labex.io/labs/linux-linux-cd-command-directory-changing-209733)** - Learn to efficiently navigate your file system using the `cd` command, understanding different techniques for changing directories and exploring the file structure.\n\n## Summary\n\nYou can now use `pwd` to identify your current location in the Linux filesystem.\n\n1. Recognize the root of the directory tree.\n2. Distinguish an absolute path from a relative path.\n3. Explain what `pwd` means and what it reports.\n4. Check your working directory without changing it.", "code_snippets": ["/\n|-- bin\n|   |-- file1\n|   |-- file2\n|-- etc\n|   |-- file3\n|   `-- directory1\n|       |-- file4\n|       `-- file5\n|-- home\n|-- var", "/home/pete/Movies", "$ pwd\n/home/pete", "$ pwd\n/home/pete/projects\n$ ls\napp.py  README.md"], "quizzes": [{"id": "identify-root-subdirectories", "question": "In the directory tree above, how are `home` and `etc` related to `/`?", "options": [{"text": "They are subdirectories that branch from `/`.", "correct": true, "explanation": "Both directories appear directly below `/` in the tree. The filesystem branches into subdirectories from its root."}, {"text": "They are files stored inside the `bin` directory.", "correct": false, "explanation": "The tree places `home` and `etc` alongside `bin`, not inside it. They are directories rather than files in this example."}, {"text": "They are alternate names for the root directory.", "correct": false, "explanation": "Linux has one filesystem root represented by `/`. `home` and `etc` are directories beneath it."}]}, {"id": "recognize-absolute-path", "question": "What makes `/home/pete/Movies` an absolute path?", "options": [{"text": "It contains several directory names separated by `/`.", "correct": false, "explanation": "Both absolute and relative paths can contain several directory names. The starting point, not the number of names, determines the path type."}, {"text": "It ends at a directory named `Movies`.", "correct": false, "explanation": "The destination name does not determine whether a path is absolute. An absolute path is identified by its root starting point."}, {"text": "It starts at root with a leading `/`.", "correct": true, "explanation": "An absolute path starts from the root directory. The leading `/` shows that starting point."}]}, {"id": "expand-pwd-name", "question": "What does `pwd` stand for?", "options": [{"text": "Print working directory", "correct": true, "explanation": "The name describes exactly what the command does: it prints the shell's current working directory."}, {"text": "Present working directory", "correct": false, "explanation": "The current location can be called the present directory in ordinary speech, but that is not the expansion of `pwd`."}, {"text": "Print whole directory", "correct": false, "explanation": "`pwd` reports the current directory's path. It does not print the directory's entire contents."}]}, {"id": "check-location-without-changing-it", "question": "Which action checks your current directory without changing it?", "options": [{"text": "Run `cd` and read the directory it moves to.", "correct": false, "explanation": "The `cd` command changes the working directory. It does not meet the requirement to check your location without changing it."}, {"text": "Enter `/home/pete` and use the path as a command.", "correct": false, "explanation": "An absolute path identifies a location, but the path by itself is not the command that reports your current directory."}, {"text": "Run `pwd` and read the absolute path it prints.", "correct": true, "explanation": "`pwd` reports the shell's current location without moving it. This makes it safe to use whenever you need to confirm where you are."}]}]}, {"lesson_id": "change-directory-cd-command", "course_id": "command-line", "title": "cd (Change Directory)", "order_index": 3, "description": "Learn how to use cd with paths and shortcuts to move through the Linux filesystem.", "content": "To move around the Linux filesystem, you use paths to specify your destination. The primary tool for this is the `cd` command, short for change directory. It changes the shell's current working directory.\n\nThe destination must be a directory rather than a regular file. If the directory does not exist, its name is typed incorrectly, or you lack permission to enter it, `cd` reports an error instead of changing location.\n\nThe basic syntax is:\n\n```bash\ncd [DIRECTORY]\n```\n\n## Understanding Paths\n\nThere are two ways to specify a path: absolute and relative.\n\n- **Absolute path**: The full path starting from the root directory (`/`). For example: `/home/pete/Desktop`.\n\n- **Relative path**: A path based on your current location. If you are in `/home/pete/Documents` and want to access a subdirectory named `taxes`, you can use `taxes/`.\n\n## Using the cd Command\n\nTo change to a specific directory using an absolute path, type:\n\n```bash\n$ cd /home/pete/Pictures\n```\n\nThis command moves you directly to the `Pictures` directory.\n\nYou can confirm your location with `pwd`:\n\n```bash\n$ pwd\n/home/pete/Pictures\n```\n\n## Navigating to a Subdirectory\n\nIf you are already in a directory and want to move to a subdirectory, use a relative path. For instance, if your current location is `/home/pete/Pictures` and it contains a folder named `Hawaii`, you can navigate into it with:\n\n```bash\n$ cd Hawaii\n```\n\nNotice we only used the folder's name. This is because we were already in its parent directory, `/home/pete/Pictures`.\n\n## Essential Navigation Shortcuts\n\nNavigating with full paths can be tedious. Fortunately, the shell provides several shortcuts to make moving around much faster.\n\n- `.` (current directory): Represents the directory you are currently in.\n- `..` (parent directory): Moves you one level up to the directory containing your current one.\n- `~` (home directory): A shortcut to your personal home directory, like `/home/pete`.\n- `-` (previous directory): Takes you back to the last directory you were in.\n\nYou can use these shortcuts with `cd`:\n\n```bash\n$ cd .\n$ cd ..\n$ cd ~\n$ cd -\n```\n\nExperiment with these shortcuts to become more efficient on the command line.\n\n## Practical cd Examples\n\nGo to your home directory:\n\n```bash\n$ cd\n```\n\nRunning `cd` with no directory argument also takes you to your home directory.\n\nGo up two levels:\n\n```bash\n$ cd ../..\n```\n\nGo to a directory whose name contains spaces by quoting it:\n\n```bash\n$ cd \"Vacation Photos\"\n```\n\nGo back to the previous directory:\n\n```bash\n$ cd -\n/home/pete/Documents\n```\n\nTo reinforce your understanding of Linux directory navigation, try these hands-on labs:\n\n1. **[Linux cd Command: Directory Changing](https://labex.io/labs/linux-linux-cd-command-directory-changing-209733)** - Learn the Linux `cd` command to efficiently navigate your file system, including various techniques for changing directories, understanding paths, and exploring the file structure.\n2. **[Linux Directory Navigation](https://labex.io/labs/linux-directory-navigation-387844)** - Put your basic Linux command-line skills to the test by navigating through directories using essential commands.\n3. **[Setting Up a New Project Structure](https://labex.io/labs/linux-setting-up-a-new-project-structure-387859)** - Practice your Linux directory management skills by creating a specific project structure and navigating through it using essential commands like `mkdir` and `cd`.\n\n## Summary\n\nYou can now use `cd` to move between directories with full paths and shell shortcuts.\n\n1. Distinguish absolute paths from relative paths.\n2. Change directories and verify the result with `pwd`.\n3. Move to parent, home, and previous directories.\n4. Enter directory names that contain spaces.\n5. Recognize common path and permission errors.", "code_snippets": ["cd [DIRECTORY]", "$ cd /home/pete/Pictures", "$ pwd\n/home/pete/Pictures", "$ cd Hawaii", "$ cd .\n$ cd ..\n$ cd ~\n$ cd -", "$ cd", "$ cd ../..", "$ cd \"Vacation Photos\"", "$ cd -\n/home/pete/Documents"], "quizzes": [{"id": "recognize-absolute-cd-path", "question": "Which statement correctly describes an absolute path?", "options": [{"text": "It begins at whichever directory the shell currently uses", "correct": false, "explanation": "A path that depends on the shell's current location is relative. It does not necessarily begin at the root."}, {"text": "It contains only the final directory name without parents", "correct": false, "explanation": "A single destination name is normally interpreted relative to the current directory. An absolute path includes its route from `/`."}, {"text": "It begins at the root directory, represented by `/`", "correct": true, "explanation": "An absolute path starts at the filesystem root. The leading `/` makes its starting point independent of the current directory."}]}, {"id": "verify-changed-directory", "question": "Which command confirms the shell's current location after `cd`?", "options": [{"text": "`cd`", "correct": false, "explanation": "`cd` changes the current directory but does not normally print the resulting full path. Use `pwd` to confirm it."}, {"text": "`ls`", "correct": false, "explanation": "`ls` displays directory contents. It can help you inspect a location, but `pwd` reports the location itself."}, {"text": "`pwd`", "correct": true, "explanation": "`pwd` prints the current working directory. It lets you verify where `cd` moved the shell."}]}, {"id": "move-to-parent-directory", "question": "From `/home/pete/Pictures`, which command moves to `/home/pete`?", "options": [{"text": "`cd .`", "correct": false, "explanation": "`.` represents the current directory. This command leaves the shell in `/home/pete/Pictures`."}, {"text": "`cd -`", "correct": false, "explanation": "`-` returns to the previous working directory, which is not necessarily the parent. Use `..` when the destination is one level up."}, {"text": "`cd ..`", "correct": true, "explanation": "`..` represents the parent of the current directory. From `Pictures`, its parent is `/home/pete`."}]}, {"id": "return-to-previous-directory", "question": "Which command returns to the directory used immediately before the current one?", "options": [{"text": "`cd -`", "correct": true, "explanation": "`cd -` switches to the previous working directory. That directory can be anywhere in the filesystem."}, {"text": "`cd ..`", "correct": false, "explanation": "`cd ..` moves to the parent directory. The parent and previous directory are not always the same place."}, {"text": "`cd ~`", "correct": false, "explanation": "`cd ~` moves to your home directory. It does not track the directory you visited immediately before."}]}, {"id": "enter-directory-with-spaces", "question": "Which command treats `Vacation Photos` as one directory name?", "options": [{"text": "`cd Vacation Photos`", "correct": false, "explanation": "Without quotes, the shell passes `Vacation` and `Photos` as separate arguments rather than one directory name."}, {"text": "`\"cd Vacation Photos\"`", "correct": false, "explanation": "Quoting the entire line makes the shell treat it as one command name. The command itself must remain outside the path quotes."}, {"text": "`cd \"Vacation Photos\"`", "correct": true, "explanation": "The quotation marks group both words into one path argument for `cd`."}]}]}, {"lesson_id": "list-directories-ls-command", "course_id": "command-line", "title": "ls (List Directories)", "order_index": 4, "description": "Learn how to use ls options to inspect files, hidden entries, details, sizes, and sort order.", "content": "Now that we know how to move around the filesystem, how do we figure out what is available to us? The `ls` command lists files and directories so you can inspect your current location or another path.\n\n## Basic Usage of the ls Command\n\nBy default, the `ls` command will list the directories and files in your current directory. However, you can also specify a path to list the contents of a different directory.\n\n```bash\n$ ls\n$ ls /home/pete\n```\n\nYou can list a specific file too:\n\n```bash\n$ ls /etc/hosts\n/etc/hosts\n```\n\n## Viewing Hidden Files\n\nNot all files in a directory are visible by default. In Linux, filenames that start with a dot (`.`) are hidden. You can view them with the `-a` option, which stands for all.\n\n```bash\n$ ls -a\n.  ..  .bashrc  Documents  Pictures\n```\n\nDotfiles are hidden by default and often store configuration, such as `.bashrc`.\n\n## Getting Detailed Information\n\nAnother essential `ls` option is `-l` for long format. It shows file permissions, number of links, owner, group, size, modification time, and name.\n\n```bash\n$ ls -l\n```\n\nHere is an example of the output:\n\n```plaintext\npete@icebox:~$ ls -l\ntotal 80\ndrwxr-x--- 7 pete penguingroup   4096 Nov 20 16:37 Desktop\ndrwxr-x--- 2 pete penguingroup   4096 Oct 19 10:46  Documents\ndrwxr-x--- 4 pete penguingroup   4096 Nov 20 09:30 Downloads\ndrwxr-x--- 2 pete penguingroup   4096 Oct  7 13:13   Music\ndrwxr-x--- 2 pete penguingroup   4096 Sep 21 14:02 Pictures\ndrwxr-x--- 2 pete penguingroup   4096 Jul 27 12:41   Public\ndrwxr-x--- 2 pete penguingroup   4096 Jul 27 12:41   Templates\ndrwxr-x--- 2 pete penguingroup   4096 Jul 27 12:41   Videos\n```\n\nFor easier file sizes, add `-h` for human-readable output:\n\n```bash\n$ ls -lh\n```\n\n## Sorting in Reverse Order\n\nSometimes you may want to change the sort order. The `-r` option lists files and directories in reverse order.\n\n```bash\n$ ls -r\n```\n\nYou can sort by modification time with `-t`, then reverse it with `-r`:\n\n```bash\n$ ls -lt\n$ ls -ltr\n```\n\n## Combining Command Flags\n\nCommands have flags, also called options, to add more functionality. As we saw with `-a` and `-l`, you can combine them into a single command like `ls -la`. The order of the flags often does not matter, so `ls -al` works the same way.\n\n```bash\n$ ls -la\n```\n\nUseful combinations include:\n\n```bash\n$ ls -lh\n$ ls -la\n$ ls -ltr\n```\n\n## Common ls Options\n\n- `-a`: Show all files, including hidden files.\n- `-l`: Use long format.\n- `-h`: Show human-readable sizes with `-l`.\n- `-r`: Reverse the sort order.\n- `-t`: Sort by modification time.\n- `-S`: Sort by file size.\n- `-d`: List the directory itself instead of its contents.\n\nSome systems display `ls` output in different colors for different file types. This behavior commonly comes from an alias or environment setting, so colors may vary between systems.\n\nTo reinforce your understanding of the `ls` command, try this hands-on lab:\n\n- **[Linux ls Command: Content Listing](https://labex.io/labs/linux-linux-ls-command-content-listing-219205)** - Practice using the `ls` command to efficiently list and analyze file and directory contents. You'll learn various options for detailed listings, hidden file display, human-readable sizes, and sorting techniques to enhance your command-line skills.\n\nThis lab will help you apply the concepts in a real scenario and build confidence with directory listing in Linux.\n\n## Summary\n\nYou can now use `ls` to inspect directory contents and control how entries are displayed.\n\n1. List the current directory or another path.\n2. Include hidden files in a listing.\n3. Show detailed information with readable sizes.\n4. Sort entries by modification time in reverse order.\n5. List a directory entry without listing its contents.", "code_snippets": ["$ ls\n$ ls /home/pete", "$ ls /etc/hosts\n/etc/hosts", "$ ls -a\n.  ..  .bashrc  Documents  Pictures", "$ ls -l", "pete@icebox:~$ ls -l\ntotal 80\ndrwxr-x--- 7 pete penguingroup   4096 Nov 20 16:37 Desktop\ndrwxr-x--- 2 pete penguingroup   4096 Oct 19 10:46  Documents\ndrwxr-x--- 4 pete penguingroup   4096 Nov 20 09:30 Downloads\ndrwxr-x--- 2 pete penguingroup   4096 Oct  7 13:13   Music\ndrwxr-x--- 2 pete penguingroup   4096 Sep 21 14:02 Pictures\ndrwxr-x--- 2 pete penguingroup   4096 Jul 27 12:41   Public\ndrwxr-x--- 2 pete penguingroup   4096 Jul 27 12:41   Templates\ndrwxr-x--- 2 pete penguingroup   4096 Jul 27 12:41   Videos", "$ ls -lh", "$ ls -r", "$ ls -lt\n$ ls -ltr", "$ ls -la", "$ ls -lh\n$ ls -la\n$ ls -ltr"], "quizzes": [{"id": "list-another-directory", "question": "Which command lists the contents of `/home/pete` without changing into it?", "options": [{"text": "`ls /home/pete`", "correct": true, "explanation": "Passing a directory path to `ls` lists that directory's contents. The shell remains in its current working directory."}, {"text": "`cd /home/pete`", "correct": false, "explanation": "`cd` changes the shell's working directory. It does not perform the requested listing by itself."}, {"text": "`pwd /home/pete`", "correct": false, "explanation": "`pwd` reports the current working directory and does not take a destination to list. Use `ls` with the path instead."}]}, {"id": "show-hidden-files", "question": "Which command includes hidden files in the listing?", "options": [{"text": "`ls -l`", "correct": false, "explanation": "The `-l` option adds detailed columns but does not include hidden names by itself."}, {"text": "`ls -r`", "correct": false, "explanation": "The `-r` option reverses the sort order. It does not change whether hidden files are included."}, {"text": "`ls -a`", "correct": true, "explanation": "The `-a` option means all, so `ls` includes names that begin with a dot."}]}, {"id": "show-readable-file-details", "question": "Which command shows long-format details with human-readable sizes?", "options": [{"text": "`ls -la`", "correct": false, "explanation": "This combines long format with hidden files. It does not request human-readable size units."}, {"text": "`ls -lh`", "correct": true, "explanation": "`-l` selects long format and `-h` makes sizes easier to read. The flags can be combined in one command."}, {"text": "`ls -ltr`", "correct": false, "explanation": "This combines long format, modification-time sorting, and reverse order. It does not include the `-h` size option."}]}, {"id": "show-newest-files-last", "question": "Which command sorts by modification time and then places the newest entries last?", "options": [{"text": "`ls -ltr`", "correct": true, "explanation": "`-t` sorts by modification time, while `-r` reverses that order. Together they place older entries before newer ones."}, {"text": "`ls -lt`", "correct": false, "explanation": "This sorts by modification time but keeps the default newest-first direction. It does not place the newest entries last."}, {"text": "`ls -lr`", "correct": false, "explanation": "This uses long format and reverses the default name sort. Without `-t`, modification time does not control the order."}]}, {"id": "list-directory-entry-itself", "question": "Which command lists the `projects/` directory entry instead of its contents?", "options": [{"text": "`ls -d projects/`", "correct": true, "explanation": "The `-d` option tells `ls` to show the directory entry itself rather than opening it for a content listing."}, {"text": "`ls projects/`", "correct": false, "explanation": "Without `-d`, passing a directory path makes `ls` display the entries inside that directory."}, {"text": "`cd projects/`", "correct": false, "explanation": "`cd` changes the working directory. It does not list the directory entry requested here."}]}]}, {"lesson_id": "touch-command", "course_id": "command-line", "title": "touch", "order_index": 5, "description": "Learn how to create empty files and manage file timestamps with the touch command.", "content": "The `touch` command changes file timestamps. It is also commonly used to create one or more empty files.\n\nThe basic syntax is:\n\n```bash\ntouch [OPTIONS] FILE...\n```\n\n## Creating Empty Files\n\nIf the named file does not exist, `touch` creates it as an empty file:\n\n```bash\n$ touch mysuperduperfile\n```\n\nYou can create several files in one command by listing each name:\n\n```bash\n$ touch file1.txt file2.txt file3.log\n```\n\nThis is useful for creating placeholders, but `touch` does not add text to a file. Use a text editor or another command designed to write content when you need a nonempty file.\n\n## Updating File Timestamps\n\nFiles track several timestamps. By default, running `touch` on an existing file changes both its access time and modification time to the current time. It leaves the file's contents unchanged.\n\nYou can compare the displayed modification time before and after running the command:\n\n```bash\n$ ls -l mysuperduperfile\n$ touch mysuperduperfile\n$ ls -l mysuperduperfile\n```\n\nThe `ls -l` output normally displays the modification time, not the access time.\n\n## Controlling Which Timestamp Changes\n\nUse `-a` to change only the access time or `-m` to change only the modification time:\n\n```bash\n$ touch -a notes.txt\n$ touch -m notes.txt\n```\n\n## Setting or Copying a Time\n\nThe `-d` option accepts a date string instead of using the current time:\n\n```bash\n$ touch -d \"2026-06-23 12:30:00\" mysuperduperfile\n```\n\nTo give a file the same access and modification times as a reference file, use `-r`:\n\n```bash\n$ touch -r file1.txt file2.txt\n```\n\nHere, `file1.txt` supplies the timestamps and `file2.txt` is the file that changes. The `-t` option is another way to provide a time, using a compact numeric format.\n\n## Avoiding File Creation\n\nNormally, `touch` creates a file when the named path does not exist. Add `-c` when you want to update a file only if it already exists:\n\n```bash\n$ touch -c existing-file.txt\n```\n\nIf `existing-file.txt` is missing, this command does not create it. This behavior can be useful in scripts that should update a timestamp without introducing a new file.\n\n## Summary\n\nYou can now use `touch` to create empty files and control file timestamps.\n\n1. Create one or more empty files.\n2. Update timestamps without changing file contents.\n3. Select the access time or modification time.\n4. Set a specific time or copy a reference file's timestamps.\n5. Prevent creation of a missing file.", "code_snippets": ["touch [OPTIONS] FILE...", "$ touch mysuperduperfile", "$ touch file1.txt file2.txt file3.log", "$ ls -l mysuperduperfile\n$ touch mysuperduperfile\n$ ls -l mysuperduperfile", "$ touch -a notes.txt\n$ touch -m notes.txt", "$ touch -d \"2026-06-23 12:30:00\" mysuperduperfile", "$ touch -r file1.txt file2.txt", "$ touch -c existing-file.txt"], "quizzes": [{"id": "create-several-empty-files", "question": "Which command creates three empty files named `one`, `two`, and `three` if they do not already exist?", "options": [{"text": "`touch \"one two three\"`", "correct": false, "explanation": "The quotation marks make this a single filename containing spaces. This command addresses one file rather than three."}, {"text": "`mkdir one two three`", "correct": false, "explanation": "`mkdir` creates directories rather than empty regular files. Use `touch` for the files requested here."}, {"text": "`touch one two three`", "correct": true, "explanation": "`touch` accepts multiple file operands. It creates each missing file without adding content."}]}, {"id": "touch-existing-file", "question": "What happens when you run `touch report.txt` and `report.txt` already exists?", "options": [{"text": "Its timestamps are updated without replacing its contents.", "correct": true, "explanation": "By default, `touch` updates the access and modification times of an existing file. It does not overwrite the file's data."}, {"text": "Its contents are deleted and the file becomes empty.", "correct": false, "explanation": "Creating an empty file is the missing-file behavior. An existing file keeps its contents when `touch` updates its timestamps."}, {"text": "The command fails because the filename is already in use.", "correct": false, "explanation": "`touch` is designed to operate on existing files as well as missing ones. An existing name is not itself an error."}]}, {"id": "change-modification-time-only", "question": "Which command updates only the modification time of `notes.txt`?", "options": [{"text": "`touch -a notes.txt`", "correct": false, "explanation": "The `-a` option changes only the access time. It does not select the modification time requested here."}, {"text": "`touch -m notes.txt`", "correct": true, "explanation": "The `-m` option limits the change to the modification time. The access time is left unchanged."}, {"text": "`touch -c notes.txt`", "correct": false, "explanation": "The `-c` option controls whether a missing file is created. It does not limit the update to one timestamp."}]}, {"id": "copy-reference-timestamps", "question": "Which command copies the timestamps of `source.txt` to `target.txt`?", "options": [{"text": "`touch -r source.txt target.txt`", "correct": true, "explanation": "With `-r`, the next operand is the reference file and the final operand is the file whose timestamps are updated."}, {"text": "`touch -r target.txt source.txt`", "correct": false, "explanation": "This reverses the roles of the files. It would use `target.txt` as the reference and update `source.txt`."}, {"text": "`touch -d source.txt target.txt`", "correct": false, "explanation": "The `-d` option expects a date string, not a reference filename. Use `-r` to copy another file's timestamps."}]}, {"id": "update-without-creating", "question": "Which command updates `status.log` if it exists but does not create it if it is missing?", "options": [{"text": "`touch -a status.log`", "correct": false, "explanation": "The `-a` option selects the access time, but a missing file can still be created. It does not provide the required no-create behavior."}, {"text": "`touch -m status.log`", "correct": false, "explanation": "The `-m` option selects the modification time, but it does not prevent creation of a missing file. Use `-c` for that condition."}, {"text": "`touch -c status.log`", "correct": true, "explanation": "The `-c` option suppresses creation of a missing file. An existing file can still have its timestamps updated."}]}]}, {"lesson_id": "file-command", "course_id": "command-line", "title": "file", "order_index": 6, "description": "Learn how to identify a file's likely content type without relying on its name or extension.", "content": "In the previous lesson, you used `touch` to create a file without adding an extension. Linux filenames do not have to describe what a file contains: a file named `funny.gif` is not necessarily a GIF image.\n\nUse the `file` command to inspect a file and report its likely type:\n\n```bash\n$ file banana.jpg\nbanana.jpg: JPEG image data\n```\n\n## Why File Extensions Are Not Enough\n\nLinux tools usually do not require a file extension to determine a file's type. A shell script can be named `backup`, a text file can be named `README`, and an image can have a misleading extension. The `file` command examines properties such as filesystem metadata and recognizable patterns in the content.\n\n```bash\n$ file README\nREADME: ASCII text\n$ file /bin/ls\n/bin/ls: ELF 64-bit LSB executable\n```\n\nIts result is a classification, not a guarantee. An unusual, incomplete, or damaged file may receive a broad description such as `data` instead of a precise type.\n\n A file named `report.jpg` may not contain an image. Which command checks its likely content type?\n\n## Checking Multiple Files\n\nYou can check several files at once:\n\n```bash\n$ file notes.txt image.png archive.tar.gz\nnotes.txt: ASCII text\nimage.png: PNG image data\narchive.tar.gz: gzip compressed data\n```\n\nYou can also pass a shell wildcard. The shell expands `*` into matching names before `file` examines them:\n\n```bash\n$ file *\n```\n\n## Showing MIME Information\n\nThe `-i` option prints MIME-style information, including a media type and, when available, a character set. This form is useful when another program expects values such as `text/html`.\n\n```bash\n$ file -i index.html\nindex.html: text/html; charset=us-ascii\n```\n\n## Useful file Options\n\n- `-i`: Show MIME-style information.\n- `-b`: Use brief mode and omit the filename from the output.\n- `-L`: Follow symbolic links and classify their targets.\n- `-z`: Try to examine the contents of compressed files.\n\nFor example:\n\n```bash\n$ file -b notes.txt\nASCII text\n```\n\n## Summary\n\nYou can now use `file` to investigate what a file is likely to contain.\n\n1. Classify a file without trusting its extension.\n2. Inspect multiple pathnames in one command.\n3. Request MIME-style information.\n4. Adjust how links, compressed data, and output labels are handled.", "code_snippets": ["$ file banana.jpg\nbanana.jpg: JPEG image data", "$ file README\nREADME: ASCII text\n$ file /bin/ls\n/bin/ls: ELF 64-bit LSB executable", "$ file notes.txt image.png archive.tar.gz\nnotes.txt: ASCII text\nimage.png: PNG image data\narchive.tar.gz: gzip compressed data", "$ file *", "$ file -i index.html\nindex.html: text/html; charset=us-ascii", "$ file -b notes.txt\nASCII text"], "quizzes": [{"id": "identify-misleading-extension", "question": "A file named `report.jpg` may not contain an image. Which command checks its likely content type?", "options": [{"text": "`ls report.jpg`", "correct": false, "explanation": "`ls` confirms that the name exists and can show metadata, but it does not classify the file's contents."}, {"text": "`file report.jpg`", "correct": true, "explanation": "The `file` command examines the file and reports a likely type. It does not rely only on the `.jpg` suffix."}, {"text": "`touch report.jpg`", "correct": false, "explanation": "`touch` updates timestamps or creates a missing file. It does not identify the content type."}]}, {"id": "inspect-multiple-files", "question": "Which command asks `file` to inspect every non-hidden name matched by `*` in the current directory?", "options": [{"text": "`file *`", "correct": true, "explanation": "The shell expands `*` to matching non-hidden names, and `file` inspects each resulting operand."}, {"text": "`file .`", "correct": false, "explanation": "A single dot names the current directory itself. This command classifies that directory rather than each entry inside it."}, {"text": "`file -b`", "correct": false, "explanation": "The `-b` option changes output formatting, but this command does not supply the files to inspect."}]}, {"id": "show-mime-information", "question": "Which command reports MIME-style information for `index.html`?", "options": [{"text": "`file -b index.html`", "correct": false, "explanation": "The `-b` option omits the filename from the usual description. It does not specifically request MIME-style output."}, {"text": "`file -i index.html`", "correct": true, "explanation": "The `-i` option requests MIME-style output, such as `text/html` plus character-set information."}, {"text": "`file -L index.html`", "correct": false, "explanation": "The `-L` option controls symbolic-link handling. It does not select the MIME output format."}]}, {"id": "omit-filename-from-output", "question": "Which command classifies `notes.txt` but omits its filename from the output?", "options": [{"text": "`file -i notes.txt`", "correct": false, "explanation": "The `-i` option requests MIME-style information. The output still normally includes the filename."}, {"text": "`file -z notes.txt`", "correct": false, "explanation": "The `-z` option asks `file` to look inside compressed data when possible. It does not enable brief output."}, {"text": "`file -b notes.txt`", "correct": true, "explanation": "Brief mode, selected with `-b`, prints the classification without the filename prefix."}]}]}, {"lesson_id": "cat-command", "course_id": "command-line", "title": "cat", "order_index": 7, "description": "Learn how to display, concatenate, and redirect file content safely with the cat command.", "content": "After learning to identify files, the next step is to read their contents. The `cat` command displays files and joins their contents; its name is short for \"concatenate.\"\n\n## Viewing File Contents\n\nThe simplest use of `cat` displays a file directly in the terminal:\n\n```bash\n$ cat myfile.txt\n```\n\nThe command writes the entire file to standard output. This works well for short text, but a long file may scroll past too quickly.\n\n## Concatenating Files\n\nWhen you give `cat` several files, it reads them in operand order and writes their contents one after another:\n\n```bash\n$ cat dogfile birdfile\n```\n\nThis displays `dogfile` first and `birdfile` second. To save the combined output in a new file, redirect standard output with `>`:\n\n```bash\n$ cat dogfile birdfile > animals\n```\n\nThe shell creates `animals` or truncates it before running `cat`, then sends the combined output there. Do not use one of the input files as this destination, because it may be emptied before `cat` reads it.\n\n## Reading Terminal Input into a File\n\nWhen no input file is given, `cat` reads standard input. You can combine that behavior with `>` to enter text from the terminal and write it to a file:\n\n```bash\n$ cat > newfile.txt\n```\n\nAfter running the command, type the desired text. Press `Ctrl+D` to send an end-of-file signal and return to the shell. Be careful: if `newfile.txt` already exists, `>` truncates its previous contents.\n\nUse `>>` to append new input instead of replacing existing contents:\n\n```bash\n$ cat >> notes.txt\n```\n\n You want to type more text at the end of an existing `notes.txt`. Which command starts that operation without truncating the file?\n\n## Formatting the Output\n\nSeveral options make output easier to inspect:\n\n- `-n`: Number all output lines, starting from 1.\n- `-b`: Number only non-empty output lines.\n- `-s`: Squeeze multiple blank lines into one blank line.\n- `-A`: Show nonprinting characters, tabs, and line endings.\n\nExamples:\n\n```bash\n$ cat -n script.sh\n$ cat -b notes.txt\n$ cat -s messy.txt\n```\n\n## Choosing a Viewer for Long Files\n\nUse `cat` when you want the entire output at once. For a long file, `less` is usually more convenient because it lets you scroll, search, and quit without flooding the terminal:\n\n```bash\n$ less /var/log/syslog\n```\n\nTo practice displaying and combining file content, try these hands-on labs:\n\n1. **[Linux cat Command: File Concatenating](https://labex.io/labs/linux-linux-cat-command-file-concatenating-210986)** - Learn the `cat` command for viewing, concatenating, and manipulating text files, enhancing your command-line skills for efficient text file handling.\n2. **[Viewing Log and Configuration Files in Linux](https://labex.io/labs/linux-viewing-log-and-configuration-files-in-linux-387914)** - Practice using commands like `cat` to efficiently view and navigate text files, including system logs and configuration files, to extract critical information.\n\n## Summary\n\nYou can now use `cat` to display and combine file content while choosing safe redirection.\n\n1. Display the complete contents of a short file.\n2. Concatenate files in a chosen order.\n3. Replace or append to a destination deliberately.\n4. Number or simplify output lines.\n5. Choose `less` when interactive reading is more suitable.", "code_snippets": ["$ cat myfile.txt", "$ cat dogfile birdfile", "$ cat dogfile birdfile > animals", "$ cat > newfile.txt", "$ cat >> notes.txt", "$ cat -n script.sh\n$ cat -b notes.txt\n$ cat -s messy.txt", "$ less /var/log/syslog"], "quizzes": [{"id": "display-short-file", "question": "Which command displays all of `myfile.txt` in the terminal?", "options": [{"text": "`file myfile.txt`", "correct": false, "explanation": "`file` reports the likely file type. It does not print the complete text stored in the file."}, {"text": "`touch myfile.txt`", "correct": false, "explanation": "`touch` updates timestamps or creates a missing file. It does not display the file's contents."}, {"text": "`cat myfile.txt`", "correct": true, "explanation": "`cat` reads `myfile.txt` and writes its contents to standard output, which is the terminal here."}]}, {"id": "combine-files-in-order", "question": "Which command writes `part1` followed by `part2` into a new or replaced file named `whole`?", "options": [{"text": "`cat whole > part1 part2`", "correct": false, "explanation": "Redirection has one destination, while the other words become operands to `cat`. This does not express the requested input and output order."}, {"text": "`cat part1 part2 > whole`", "correct": true, "explanation": "`cat` emits the two files in the order listed, and `>` redirects that combined output to `whole`."}, {"text": "`cat part2 part1 > whole`", "correct": false, "explanation": "This writes the same two inputs to `whole`, but it reads `part2` before `part1`. Operand order controls output order."}]}, {"id": "append-terminal-input", "question": "You want to type more text at the end of an existing `notes.txt`. Which command starts that operation without truncating the file?", "options": [{"text": "`cat > notes.txt`", "correct": false, "explanation": "A single `>` redirects input after truncating the destination. Existing text in `notes.txt` would be lost."}, {"text": "`cat >> notes.txt`", "correct": true, "explanation": "The `>>` operator opens the destination for appending, so text read by `cat` is added after the existing contents."}, {"text": "`cat notes.txt > notes.txt`", "correct": false, "explanation": "Using the same file as input and a `>` destination can truncate it before `cat` reads it. It is not a safe append operation."}]}, {"id": "number-nonempty-lines", "question": "Which command numbers only the nonempty output lines from `notes.txt`?", "options": [{"text": "`cat -b notes.txt`", "correct": true, "explanation": "The `-b` option numbers nonempty output lines while leaving empty lines unnumbered."}, {"text": "`cat -n notes.txt`", "correct": false, "explanation": "The `-n` option numbers every output line, including empty ones. It does not meet the nonempty-only condition."}, {"text": "`cat -s notes.txt`", "correct": false, "explanation": "The `-s` option reduces repeated blank lines to one. It does not add line numbers."}]}, {"id": "choose-viewer-for-long-file", "question": "Which command is better suited to interactively reading a long log file?", "options": [{"text": "`less /var/log/syslog`", "correct": true, "explanation": "`less` provides scrolling, searching, and a controlled exit, making it suitable for interactive reading of long files."}, {"text": "`cat /var/log/syslog`", "correct": false, "explanation": "`cat` writes the entire log to the terminal at once. A long file may scroll past before you can inspect it."}, {"text": "`touch /var/log/syslog`", "correct": false, "explanation": "`touch` changes timestamps and may require permissions. It is not a command for reading the log."}]}]}, {"lesson_id": "less-command", "course_id": "command-line", "title": "less", "order_index": 8, "description": "Learn how to navigate, search, and follow long text files interactively with less.", "content": "When a text file is too long for one screen, `less` lets you read it without sending the entire file scrolling through the terminal. Its name inspired the old Unix joke, \"less is more,\" because `more` is another pager.\n\n## Opening a File\n\nStart the pager by passing it a filename:\n\n```bash\n$ less /home/pete/Documents/text1\n```\n\nWhile `less` is active, keystrokes control the pager rather than starting ordinary shell commands. You return to the shell when you quit the pager.\n\n## Navigating in less\n\nUse these keys while the pager is open:\n\n- Use `Up`, `Down`, `Page Up`, and `Page Down` to move by lines or screens.\n- Press `g` to jump to the beginning.\n- Press `G` to jump to the end.\n- Press `u` to move up half a screen or `d` to move down half a screen.\n- Press `h` to open the built-in help.\n\n## Searching in less\n\nType `/` followed by a pattern and press Enter to search forward. Begin with `?` to search backward.\n\n- `/search_term`: Search forward for `search_term`.\n- `?search_term`: Search backward for `search_term`.\n- `n`: Repeat the search in the same direction.\n- `N`: Repeat the search in the opposite direction.\n\n After a forward search for `error`, which key repeats the search in the same direction?\n\n## Leaving less\n\nPress `q` to quit `less` and return to the shell prompt.\n\n## Starting less with Options\n\nOptions and initial commands can change how the pager starts:\n\n```bash\n$ less -N file.txt\n$ less +G file.txt\n$ less +F /var/log/syslog\n```\n\n- `-N`: Show line numbers.\n- `+G`: Open at the end of the file.\n- `+F`: Follow new content as it is added, similar to `tail -f`.\n\nWhile following a file with `+F`, press `Ctrl+C` to stop following and return to normal navigation, then press `q` to quit. Use `-i` for searches that ignore case unless the pattern contains an uppercase letter, or `-I` to ignore case regardless of the pattern.\n\nCommands can also send output through a pipe to `less`:\n\n```bash\n$ dmesg | less\n```\n\nTo practice paging, searching, and reading system text, try these hands-on labs:\n\n1. **[Linux less Command: File Paging](https://labex.io/labs/linux-linux-less-command-file-paging-214301)** - Learn the Linux 'less' command for efficient text file viewing and navigation, including search, line numbers, and pattern matching.\n2. **[Viewing Log and Configuration Files in Linux](https://labex.io/labs/linux-viewing-log-and-configuration-files-in-linux-387914)** - Learn essential Linux command-line skills for efficiently viewing and navigating text files, including system logs and configuration files, using commands like `cat`, `more`, and `less`.\n\n## Summary\n\nYou can now use `less` to inspect long files without flooding the terminal.\n\n1. Open a file or piped command output in the pager.\n2. Navigate to specific parts of the input.\n3. Search forward or backward and repeat a search.\n4. Show line numbers or follow growing content.\n5. Quit safely and return to the shell.", "code_snippets": ["$ less /home/pete/Documents/text1", "$ less -N file.txt\n$ less +G file.txt\n$ less +F /var/log/syslog", "$ dmesg | less"], "quizzes": [{"id": "open-long-file", "question": "Which command opens `/var/log/syslog` in an interactive pager?", "options": [{"text": "`less /var/log/syslog`", "correct": true, "explanation": "`less` opens the file in a pager so you can move through it, search it, and quit back to the shell."}, {"text": "`cat /var/log/syslog`", "correct": false, "explanation": "`cat` sends the whole file to standard output at once. It does not provide interactive paging controls."}, {"text": "`file /var/log/syslog`", "correct": false, "explanation": "`file` reports a likely content type. It does not open the log for interactive reading."}]}, {"id": "jump-to-file-end", "question": "Which key jumps directly to the end of a file in `less`?", "options": [{"text": "`g`", "correct": false, "explanation": "Lowercase `g` jumps to the beginning of the file. The uppercase form goes in the opposite direction."}, {"text": "`G`", "correct": true, "explanation": "Uppercase `G` jumps to the end of the input. The command is case-sensitive."}, {"text": "`h`", "correct": false, "explanation": "The `h` key opens the pager's help screen. It does not jump to the end of the file."}]}, {"id": "repeat-search-direction", "question": "After a forward search for `error`, which key repeats the search in the same direction?", "options": [{"text": "`n`", "correct": true, "explanation": "Lowercase `n` repeats the most recent search in its original direction. Here, that direction is forward."}, {"text": "`N`", "correct": false, "explanation": "Uppercase `N` repeats the most recent search in the opposite direction. After a forward search, it moves backward through matches."}, {"text": "`g`", "correct": false, "explanation": "The `g` key jumps to the beginning of the input. It does not repeat a search."}]}, {"id": "quit-less", "question": "Which key exits `less` and returns to the shell?", "options": [{"text": "`q`", "correct": true, "explanation": "The `q` command quits the pager and restores the shell prompt."}, {"text": "`h`", "correct": false, "explanation": "The `h` key opens help inside `less`. It does not return directly to the shell."}, {"text": "`G`", "correct": false, "explanation": "Uppercase `G` moves to the end of the input. The pager remains open."}]}, {"id": "follow-growing-log", "question": "Which command opens `/var/log/syslog` and follows new content as it arrives?", "options": [{"text": "`less +F /var/log/syslog`", "correct": true, "explanation": "The `+F` initial command enters follow mode, so `less` displays new content appended to the log."}, {"text": "`less +G /var/log/syslog`", "correct": false, "explanation": "The `+G` initial command opens at the end, but it does not keep following content that arrives later."}, {"text": "`less -N /var/log/syslog`", "correct": false, "explanation": "The `-N` option displays line numbers. It does not enable continuous following."}]}]}, {"lesson_id": "history-command", "course_id": "command-line", "title": "history", "order_index": 9, "description": "Learn how to inspect, search, reuse, and manage command history in Bash.", "content": "Interactive shells can keep a record of commands you enter. This lesson focuses on Bash, where the `history` builtin displays and manages that record. Other shells may use different shortcuts, files, or settings.\n\n## Viewing Bash History\n\nRun `history` to display the current history list:\n\n```bash\n$ history\n  101  pwd\n  102  ls -la\n  103  cat notes.txt\n```\n\nEach line has a history number followed by the command.\n\n## Reusing Previous Commands\n\nBash provides several shortcuts for recalling or immediately executing commands:\n\n- **Up Arrow**: Recall earlier commands for review or editing.\n- **`!!`**: Expand to and execute the most recent command.\n- **Run by number**: Use `!102` to run command number 102 from your history.\n- **Run by prefix**: Use `!cat` to run the most recent command that started with `cat`.\n\nHistory expansion forms that begin with `!` can run a command as soon as you press Enter. Inspect the match first when there is any doubt, especially before adding elevated privileges or operating on important files.\n\n## Searching History Interactively\n\nPress `Ctrl+R` to start a reverse incremental search, then type part of the command you want. Press `Ctrl+R` again to move to an older match.\n\nPress Enter to execute the displayed match. If you want to review or edit it first, use an arrow key to place the command on the editing line instead.\n\n You remember part of an earlier Bash command and want to find it interactively. What should you press first?\n\n## Managing the History List\n\nThe `history` builtin can modify or save the current list:\n\n- `history -c`: Clear the current in-memory history list.\n- `history -w`: Write the current list to the configured history file, commonly `~/.bash_history`.\n- `history -d <offset>`: Delete the entry at the given history position.\n\nExamples:\n\n```bash\n$ history -d 101\n$ history -w\n```\n\nClearing the in-memory list does not by itself guarantee that older commands have disappeared from every file, backup, or other active shell. History behavior also depends on Bash settings and when sessions read or write their files.\n\n## Clearing the Display and Completing Names\n\nUse `clear` when you want a fresh visible terminal area:\n\n```bash\n$ clear\n```\n\nThis does not erase the Bash history list. Depending on the terminal, older display content may also remain available in scrollback.\n\nTab completion is another way to avoid retyping. Start a command, filename, or directory name and press Tab. Bash may complete an unambiguous match or show possible completions when more than one exists.\n\nCommand lines can be stored in history, so do not place passwords, tokens, or other secrets directly in commands when a safer input method is available.\n\n You want to refresh the visible terminal without deleting the in-memory command history. Which command should you run?\n\n## Summary\n\nYou can now find and reuse Bash commands while managing history deliberately.\n\n1. Display the current numbered history list.\n2. Recall or expand a previous command carefully.\n3. Search history interactively with `Ctrl+R`.\n4. Delete, clear, or write history entries.\n5. Distinguish command history from the terminal display.", "code_snippets": ["$ history\n  101  pwd\n  102  ls -la\n  103  cat notes.txt", "$ history -d 101\n$ history -w", "$ clear"], "quizzes": [{"id": "show-command-history", "question": "Which Bash command displays the current numbered history list?", "options": [{"text": "`clear`", "correct": false, "explanation": "`clear` refreshes the visible terminal area. It does not display previous commands."}, {"text": "`history -w`", "correct": false, "explanation": "`history -w` writes the current list to the history file. Its purpose is saving rather than displaying the list."}, {"text": "`history`", "correct": true, "explanation": "The `history` builtin prints commands in the current history list, normally with their history numbers."}]}, {"id": "repeat-most-recent-command", "question": "Which Bash history expansion repeats the most recently executed command?", "options": [{"text": "`!102`", "correct": false, "explanation": "This expansion selects the command with history number 102. That entry is not necessarily the most recent command."}, {"text": "`!cat`", "correct": false, "explanation": "This selects the most recent command whose text begins with `cat`. It does not mean the most recent command of any kind."}, {"text": "`!!`", "correct": true, "explanation": "In Bash, `!!` expands to the previous command and executes it after you submit the line."}]}, {"id": "search-before-executing", "question": "You remember part of an earlier Bash command and want to find it interactively. What should you press first?", "options": [{"text": "`Ctrl+D`", "correct": false, "explanation": "`Ctrl+D` signals end of input in many terminal contexts and may exit an idle shell. It does not begin a history search."}, {"text": "`Ctrl+C`", "correct": false, "explanation": "`Ctrl+C` normally interrupts or cancels the current operation. It does not search command history."}, {"text": "`Ctrl+R`", "correct": true, "explanation": "`Ctrl+R` begins a reverse incremental search through command history. Typing more characters narrows the match."}]}, {"id": "save-current-history-list", "question": "Which command writes the current Bash history list to its configured history file?", "options": [{"text": "`history -c`", "correct": false, "explanation": "The `-c` option clears the in-memory list. It does not request that the current list be saved."}, {"text": "`history -d 101`", "correct": false, "explanation": "The `-d` option removes one selected history entry. It is not the operation for saving the complete list."}, {"text": "`history -w`", "correct": true, "explanation": "The `-w` option writes the current history list to the configured history file."}]}, {"id": "distinguish-clear-from-history-clear", "question": "You want to refresh the visible terminal without deleting the in-memory command history. Which command should you run?", "options": [{"text": "`clear`", "correct": true, "explanation": "`clear` refreshes the visible terminal area while leaving Bash's in-memory history list intact."}, {"text": "`history -c`", "correct": false, "explanation": "This removes entries from the current in-memory history list. It changes history rather than only refreshing the display."}, {"text": "`history -d 1`", "correct": false, "explanation": "This asks Bash to delete a selected history entry. It does not clear the visible terminal area."}]}]}, {"lesson_id": "copy-cp-command", "course_id": "command-line", "title": "cp (Copy)", "order_index": 10, "description": "Learn how to copy files and directory trees while controlling overwrites and preserved attributes.", "content": "The `cp` command copies files and directories while leaving the source in place. Its basic syntax is:\n\n```bash\ncp [OPTIONS] SOURCE DESTINATION\n```\n\nYou can copy one file to another path, copy several files into a directory, or recursively copy a directory tree.\n\n## Copying One File\n\nPlace the source first and the destination second:\n\n```bash\n$ cp mycoolfile /home/pete/Documents/cooldocs\n```\n\nIf `/home/pete/Documents/cooldocs` is an existing directory, the copy is created inside it as `mycoolfile`. You can instead provide a new destination filename:\n\n```bash\n$ cp mycoolfile /home/pete/Documents/mycoolfile_backup\n```\n\nIn the second example, the copied data receives the name `mycoolfile_backup`.\n\n## Copying Multiple Files into a Directory\n\nList every source first and put the destination directory last:\n\n```bash\n$ cp report.txt notes.txt summary.txt /home/pete/Documents/\n```\n\nThe final argument must be a directory when you provide more than one source.\n\n## Selecting Files with Wildcards\n\nThe shell can expand wildcard patterns into several source pathnames:\n\n- `*`: Matches any sequence of characters.\n- `?`: Matches any single character.\n- `[]`: Matches any one of the characters enclosed in the brackets.\n\nFor example, copy names ending in `.jpg` from the current directory to `Pictures`:\n\n```bash\n$ cp *.jpg /home/pete/Pictures\n```\n\nPreview the matches before a bulk copy, especially when the destination contains important data:\n\n```bash\n$ ls *.jpg\nbeach.jpg  lunch.jpg  profile.jpg\n$ cp *.jpg /home/pete/Pictures\n```\n\n Before copying `*.jpg`, which command shows the non-hidden names that the pattern currently matches?\n\n## Copying Directory Trees\n\nCopying a directory and everything below it requires recursive operation. Use `-r` or `-R`:\n\n```bash\n$ cp -r Pumpkin/ /home/pete/Documents\n```\n\nThis copies the `Pumpkin` directory and its descendants into `Documents`.\n\nUppercase `-R` also requests recursive copying:\n\n```bash\n$ cp -R website /home/pete/backups/\n```\n\nArchive mode, `-a`, is useful for backup-style copies. It copies recursively while preserving links and many file attributes:\n\n```bash\n$ cp -a project/ project-backup/\n```\n\n You want a recursive backup-style copy of `project/` that preserves links and many attributes. Which command fits that goal?\n\n## Controlling Overwrites\n\nBy default, `cp` can replace an existing destination file. Use `-i` to request confirmation before an overwrite:\n\n```bash\n$ cp -i mycoolfile /home/pete/Pictures\ncp: overwrite '/home/pete/Pictures/mycoolfile'? n\n```\n\nUse `-n` when an existing destination should not be overwritten:\n\n```bash\n$ cp -n mycoolfile /home/pete/Pictures\n```\n\nThe `-f` option tells GNU `cp` to try removing an existing destination if it cannot open that file for writing, then retry the copy. It is not a substitute for checking targets carefully. Shell aliases can also add options such as `-i`, so inspect an unexpected prompt rather than assuming a particular configuration.\n\n## Preserving or Refreshing Files\n\nUse `-p` to preserve the source file's mode, ownership when permitted, and timestamps:\n\n```bash\n$ cp -p mycoolfile /home/pete/backups/\n```\n\nUse `-u` to copy a source only when the destination is missing or the source is newer:\n\n```bash\n$ cp -u *.txt /home/pete/Documents/\n```\n\nOther common options include:\n\n- `-f`: Force overwriting by removing the destination first if needed.\n- `-v`: Show each file as it is copied.\n\nTo practice copying files and directory trees, try these hands-on labs:\n\n1. **[Linux cp Command: File Copying](https://labex.io/labs/linux-linux-cp-command-file-copying-209744)** - Practice basic usage, advanced options like recursive copying, preserving attributes, and using wildcards to efficiently copy files and directories.\n2. **[Organizing Files and Directories](https://labex.io/labs/linux-organizing-files-and-directories-387877)** - Practice essential Linux file management skills by using `cp`, `mv`, and `rm` commands to organize a project structure, move files, and clean up unnecessary directories.\n\n## Summary\n\nYou can now copy files and directory trees while controlling how destinations are handled.\n\n1. Place source operands before the destination.\n2. Preview wildcard matches before a bulk copy.\n3. Copy directory trees recursively or in archive mode.\n4. Confirm, skip, or deliberately replace existing destinations.\n5. Preserve attributes or copy only newer sources when needed.", "code_snippets": ["cp [OPTIONS] SOURCE DESTINATION", "$ cp mycoolfile /home/pete/Documents/cooldocs", "$ cp mycoolfile /home/pete/Documents/mycoolfile_backup", "$ cp report.txt notes.txt summary.txt /home/pete/Documents/", "$ cp *.jpg /home/pete/Pictures", "$ ls *.jpg\nbeach.jpg  lunch.jpg  profile.jpg\n$ cp *.jpg /home/pete/Pictures", "$ cp -r Pumpkin/ /home/pete/Documents", "$ cp -R website /home/pete/backups/", "$ cp -a project/ project-backup/", "$ cp -i mycoolfile /home/pete/Pictures\ncp: overwrite '/home/pete/Pictures/mycoolfile'? n", "$ cp -n mycoolfile /home/pete/Pictures", "$ cp -p mycoolfile /home/pete/backups/", "$ cp -u *.txt /home/pete/Documents/"], "quizzes": [{"id": "copy-file-under-new-name", "question": "Which command copies `draft.txt` to a file named `final.txt` while keeping `draft.txt`?", "options": [{"text": "`mv draft.txt final.txt`", "correct": false, "explanation": "`mv` renames or moves the original pathname. It does not leave the requested source copy in place."}, {"text": "`cp final.txt draft.txt`", "correct": false, "explanation": "Source and destination are reversed here. This would copy from `final.txt` to `draft.txt`."}, {"text": "`cp draft.txt final.txt`", "correct": true, "explanation": "`cp` reads `draft.txt` and creates or replaces `final.txt`, while the source remains available."}]}, {"id": "copy-multiple-files", "question": "Which command copies `a.txt` and `b.txt` into the existing `archive/` directory?", "options": [{"text": "`cp archive/ a.txt b.txt`", "correct": false, "explanation": "For this form of `cp`, the destination directory belongs at the end. Placing it first changes how the operands are interpreted."}, {"text": "`cp a.txt b.txt archive/`", "correct": true, "explanation": "With multiple sources, `cp` treats the final existing directory as the destination for every preceding file."}, {"text": "`cp a.txt archive/ b.txt`", "correct": false, "explanation": "All source operands must come before the destination. The existing directory should be the final operand."}]}, {"id": "preview-copy-pattern", "question": "Before copying `*.jpg`, which command shows the non-hidden names that the pattern currently matches?", "options": [{"text": "`cp *.jpg`", "correct": false, "explanation": "This attempts a copy without a clear destination when several names match. It is not a preview operation."}, {"text": "`ls *.jpg`", "correct": true, "explanation": "The shell expands the same pattern for `ls`, letting you inspect the matching names before copying them."}, {"text": "`file '*.jpg'`", "correct": false, "explanation": "The quotes prevent wildcard expansion, so `file` receives the literal characters `*.jpg`. This does not preview the normal matches."}]}, {"id": "archive-directory-tree", "question": "You want a recursive backup-style copy of `project/` that preserves links and many attributes. Which command fits that goal?", "options": [{"text": "`cp -p project/ project-backup/`", "correct": false, "explanation": "`-p` preserves selected attributes, but it does not by itself make a directory copy recursive."}, {"text": "`cp -u project/ project-backup/`", "correct": false, "explanation": "`-u` controls when files are copied based on destination state. It does not by itself enable recursive directory copying."}, {"text": "`cp -a project/ project-backup/`", "correct": true, "explanation": "Archive mode includes recursive copying and preserves links and a broad set of attributes for a backup-style result."}]}, {"id": "skip-existing-destination", "question": "Which command copies `report.txt` into `backup/` but skips an existing destination of the same name?", "options": [{"text": "`cp -n report.txt backup/`", "correct": true, "explanation": "The `-n` option prevents `cp` from overwriting an existing destination file."}, {"text": "`cp -i report.txt backup/`", "correct": false, "explanation": "`-i` asks before an overwrite, so the result depends on the response. It does not automatically skip every existing destination."}, {"text": "`cp -f report.txt backup/`", "correct": false, "explanation": "`-f` can help replace a destination that cannot initially be opened. It does not provide no-clobber behavior."}]}]}, {"lesson_id": "move-mv-command", "course_id": "command-line", "title": "mv (Move)", "order_index": 11, "description": "Learn how to rename and move files or directories while avoiding unintended overwrites.", "content": "The `mv` command renames a file or directory, or moves it to another location. Unlike `cp`, it does not leave the original pathname in place after a successful move.\n\nThe basic syntax is:\n\n```bash\nmv [OPTIONS] SOURCE DESTINATION\n```\n\n## Renaming Files and Directories\n\nTo rename an item, put its current pathname first and its new pathname second.\n\nTo rename a file:\n\n```bash\n$ mv oldfile newfile\n```\n\nThe same operand order renames a directory:\n\n```bash\n$ mv old_directory_name new_directory_name\n```\n\n## Moving Items to a Directory\n\nWhen the final operand is an existing directory, `mv` places the source inside it:\n\n```bash\n$ mv file2 /home/pete/Documents\n```\n\nTo move several sources, list them first and put the target directory last:\n\n```bash\n$ mv file_1 file_2 somedirectory/\n```\n\nGNU `mv` also provides `-t` for placing the target directory before the sources:\n\n```bash\n$ mv -t somedirectory/ file_1 file_2\n```\n\nUnlike `cp`, `mv` does not need a recursive option for a directory.\n\n## Controlling Existing Destinations\n\nBy default, `mv` can replace an existing destination. Inspect source and destination pathnames before running a move, then choose an overwrite policy when necessary:\n\n- `-i`: Ask for confirmation before replacing an existing destination.\n\n  ```bash\n  $ mv -i source_file destination_directory\n  ```\n\n- `-n`: Do not overwrite an existing destination.\n\n  ```bash\n  $ mv -n source_file destination_directory\n  ```\n\n- `-b`: On GNU/Linux, make a backup of a destination that would otherwise be replaced. The default backup suffix is usually `~`.\n\n  ```bash\n  $ mv -b file1 directory_with_file1\n  ```\n\n- `-v`: Print each move as it happens.\n\n```bash\n$ mv -v file1 file2 somedirectory/\n```\n\n## Moving Directories and Wildcard Matches\n\nA directory can be moved without `-r`:\n\n```bash\n$ mv project /home/pete/Documents/\n```\n\nShell wildcards can select multiple sources:\n\n```bash\n$ ls *.txt\n$ mv *.txt notes/\n```\n\nPreviewing the matches with `ls` helps you catch an overly broad pattern before changing several pathnames.\n\n You plan to run `mv *.txt notes/`. Which command previews the pathnames selected by the same wildcard?\n\nTo practice moving and renaming items, try these hands-on labs:\n\n1. **[Linux mv Command: File Moving and Renaming](https://labex.io/labs/linux-linux-mv-command-file-moving-and-renaming-209743)** - Practice using the `mv` command to move and rename files and directories, including understanding its various options and behaviors.\n2. **[Organizing Files and Directories](https://labex.io/labs/linux-organizing-files-and-directories-387877)** - Apply your knowledge of `mv` (along with `cp` and `rm`) in a practical challenge to organize a project structure, move files, and clean up directories.\n\n## Summary\n\nYou can now rename and move files or directories while protecting existing destinations.\n\n1. Put the source before its new pathname.\n2. Place a target directory after multiple sources.\n3. Ask, skip, or back up before replacing a destination.\n4. Move directories without a recursive option.\n5. Preview wildcard matches before a bulk move.", "code_snippets": ["mv [OPTIONS] SOURCE DESTINATION", "$ mv oldfile newfile", "$ mv old_directory_name new_directory_name", "$ mv file2 /home/pete/Documents", "$ mv file_1 file_2 somedirectory/", "$ mv -t somedirectory/ file_1 file_2", "$ mv -i source_file destination_directory", "$ mv -n source_file destination_directory", "$ mv -b file1 directory_with_file1", "$ mv -v file1 file2 somedirectory/", "$ mv project /home/pete/Documents/", "$ ls *.txt\n$ mv *.txt notes/"], "quizzes": [{"id": "rename-file-with-mv", "question": "Which command renames `cat` to `dog` in the current directory?", "options": [{"text": "`mv cat dog`", "correct": true, "explanation": "`mv` treats `cat` as the source pathname and `dog` as its new destination pathname."}, {"text": "`mv dog cat`", "correct": false, "explanation": "The operand order is reversed. This would try to rename an existing `dog` to `cat`."}, {"text": "`cp cat dog`", "correct": false, "explanation": "`cp` would create a copy named `dog` while keeping `cat`. It would not perform the requested rename."}]}, {"id": "move-multiple-files", "question": "Which command moves both `file_1` and `file_2` into the existing `archive/` directory?", "options": [{"text": "`mv archive/ file_1 file_2`", "correct": false, "explanation": "Without GNU `-t`, a multi-source move expects the target directory last. This operand order is not the standard multi-source form."}, {"text": "`mv -r file_1 file_2 archive/`", "correct": false, "explanation": "`mv` does not use `-r` to move files or directories. The normal multi-source form already handles the requested move."}, {"text": "`mv file_1 file_2 archive/`", "correct": true, "explanation": "With multiple sources, the existing target directory is the final operand and receives both files."}]}, {"id": "move-without-overwriting", "question": "Which command moves `draft.txt` into `finished/` only when it will not overwrite an existing destination?", "options": [{"text": "`mv -i draft.txt finished/`", "correct": false, "explanation": "The `-i` option asks what to do when a destination exists. An overwrite can still occur if the user confirms it."}, {"text": "`mv -b draft.txt finished/`", "correct": false, "explanation": "The `-b` option permits replacement while keeping a backup of the previous destination. It does not prevent overwriting."}, {"text": "`mv -n draft.txt finished/`", "correct": true, "explanation": "The `-n` option skips a move that would overwrite an existing destination."}]}, {"id": "move-directory-without-recursion", "question": "Which command moves the `project/` directory into `/srv/archive/`?", "options": [{"text": "`mv -r project/ /srv/archive/`", "correct": false, "explanation": "`mv` does not need or support `-r` for this purpose. Directories are handled by the ordinary move operation."}, {"text": "`mv project/ /srv/archive/`", "correct": true, "explanation": "The ordinary `mv` syntax moves a directory to an existing target directory without a recursive flag."}, {"text": "`cp project/ /srv/archive/`", "correct": false, "explanation": "A plain `cp` does not move the directory and would require a recursive option to copy it. The original would also remain."}]}, {"id": "preview-text-file-move", "question": "You plan to run `mv *.txt notes/`. Which command previews the pathnames selected by the same wildcard?", "options": [{"text": "`ls '*.txt'`", "correct": false, "explanation": "Quotes keep the shell from expanding `*`, so this looks for a literal name containing an asterisk rather than previewing the move set."}, {"text": "`ls *.txt`", "correct": true, "explanation": "The shell expands `*.txt` for `ls` just as it would for `mv`, allowing you to inspect the selected non-hidden names first."}, {"text": "`mv -v *.txt notes/`", "correct": false, "explanation": "Verbose mode reports moves while they happen. It performs the operation instead of providing a read-only preview."}]}]}, {"lesson_id": "make-directory-mkdir-command", "course_id": "command-line", "title": "mkdir (Make Directory)", "order_index": 12, "description": "Learn how to create single, multiple, and nested directories with mkdir options.", "content": "The `mkdir` command, short for make directory, creates directories for organizing files and other directories.\n\nThe basic syntax is:\n\n```bash\nmkdir [OPTIONS] DIRECTORY...\n```\n\n## Creating One Directory\n\nPass a pathname to create one directory. This example creates `documents` in the current working directory:\n\n```bash\n$ mkdir documents\n```\n\nIf an entry named `documents` already exists, `mkdir` reports an error rather than replacing it. Use `ls -ld documents` to inspect the existing entry.\n\n## Creating Multiple Directories\n\nList several pathnames to create several directories in one command:\n\n```bash\n$ mkdir books paintings\n```\n\n## Creating Missing Parent Directories\n\nWithout an option, `mkdir books/hemingway/favorites` fails if an intermediate directory is missing. Add `-p` to create missing parent directories along the path:\n\n```bash\n$ mkdir -p books/hemingway/favorites\n```\n\nThis creates the missing parts of the path. It also does not report an error merely because the final directory already exists, although other errors such as insufficient permissions can still occur.\n\n None of `projects/app/src` exists yet. Which command creates the complete directory path?\n\n## Setting the Initial Mode\n\nUse `-m MODE` to specify permissions for a newly created directory:\n\n```bash\n$ mkdir -m 755 public\n```\n\nYou will study permission modes later. In this example, mode `755` gives the owner read, write, and search permissions, while the group and others receive read and search permissions.\n\nAdd `-v` to print a message for each directory as it is created:\n\n```bash\n$ mkdir -pv projects/app/src\nmkdir: created directory 'projects'\nmkdir: created directory 'projects/app'\nmkdir: created directory 'projects/app/src'\n```\n\nTo practice creating and organizing directories, try these hands-on labs:\n\n1. **[Linux mkdir Command: Directory Creating](https://labex.io/labs/linux-linux-mkdir-command-directory-creating-209739)** - Learn how to use the `mkdir` command in Linux to create directories, set permissions, and organize your file system. This lab covers basic and advanced usage, including creating nested directories.\n2. **[Setting Up a New Project Structure](https://labex.io/labs/linux-setting-up-a-new-project-structure-387859)** - Practice your Linux directory management skills by creating a specific project structure and navigating through it using essential commands like `mkdir` and `cd`.\n\n## Summary\n\nYou can now create directory structures with deliberate names, parents, and modes.\n\n1. Create one or more directories in a single command.\n2. Recognize an error caused by an existing pathname.\n3. Build missing parent directories with `-p`.\n4. Set a new directory's mode with `-m`.", "code_snippets": ["mkdir [OPTIONS] DIRECTORY...", "$ mkdir documents", "$ mkdir books paintings", "$ mkdir -p books/hemingway/favorites", "$ mkdir -m 755 public", "$ mkdir -pv projects/app/src\nmkdir: created directory 'projects'\nmkdir: created directory 'projects/app'\nmkdir: created directory 'projects/app/src'"], "quizzes": [{"id": "create-one-directory", "question": "Which command creates a directory named `documents` in the current working directory?", "options": [{"text": "`mkdir documents`", "correct": true, "explanation": "`mkdir` creates the requested directory at the relative pathname `documents`."}, {"text": "`touch documents`", "correct": false, "explanation": "`touch` creates an empty regular file when the pathname is missing. It does not create a directory."}, {"text": "`cd documents`", "correct": false, "explanation": "`cd` attempts to enter an existing directory. It does not create a missing one."}]}, {"id": "create-separate-directories", "question": "Which command creates two sibling directories named `books` and `paintings`?", "options": [{"text": "`mkdir books/paintings`", "correct": false, "explanation": "This pathname describes `paintings` inside `books`, not two sibling directories. It also fails if `books` is missing."}, {"text": "`mkdir \"books paintings\"`", "correct": false, "explanation": "Quoting combines the words into one pathname, so this requests a single directory whose name contains a space."}, {"text": "`mkdir books paintings`", "correct": true, "explanation": "Separate operands tell `mkdir` to create `books` and `paintings` as two directories."}]}, {"id": "create-nested-path", "question": "None of `projects/app/src` exists yet. Which command creates the complete directory path?", "options": [{"text": "`mkdir -p projects/app/src`", "correct": true, "explanation": "The `-p` option creates each missing parent directory before creating the final directory."}, {"text": "`mkdir projects/app/src`", "correct": false, "explanation": "Without `-p`, `mkdir` cannot create `src` when the intermediate directories do not exist."}, {"text": "`mkdir -m projects/app/src`", "correct": false, "explanation": "The `-m` option requires a mode argument and does not request creation of missing parents."}]}, {"id": "set-directory-mode", "question": "Which command creates `public` with permission mode `755`?", "options": [{"text": "`mkdir -p 755 public`", "correct": false, "explanation": "The `-p` option treats the remaining words as directory pathnames, so this would not set permission mode `755`."}, {"text": "`mkdir -v 755 public`", "correct": false, "explanation": "The `-v` option prints creation messages. It does not interpret `755` as a permission mode."}, {"text": "`mkdir -m 755 public`", "correct": true, "explanation": "The `-m` option takes the requested mode, and `public` is the directory pathname to create."}]}]}, {"lesson_id": "remove-rm-command", "course_id": "command-line", "title": "rm (Remove)", "order_index": 13, "description": "Learn how to remove files and directories while checking targets and choosing safer rm options.", "content": "The `rm` command removes filesystem entries. Command-line removal normally does not send items to a desktop trash folder, and `rm` has no built-in undo, so confirm every target before you run it.\n\nThe basic syntax is:\n\n```bash\nrm [OPTIONS] FILE...\n```\n\n## Removing Files\n\nPass one or more file pathnames to `rm`:\n\n```bash\n$ rm file1\n```\n\n```bash\n$ rm notes.txt old-report.txt draft.md\n```\n\nCheck spelling and location before pressing Enter. A backup or version-control copy is a more dependable recovery plan than filesystem-recovery tools after deletion.\n\n After confirming the target, which command removes the file `old-report.txt`?\n\n## Previewing Wildcard Targets\n\nThe shell can expand a wildcard into several operands. For example, `*.tmp` selects matching non-hidden names in the current directory:\n\n```bash\n$ rm *.tmp\n```\n\nPreview the same unquoted pattern with `ls` before removing anything:\n\n```bash\n$ ls *.tmp\ncache.tmp  test.tmp\n$ rm *.tmp\n```\n\nThe shell expands the pattern before `rm` starts. If the preview includes an unexpected file, correct the pattern instead of proceeding.\n\n You plan to remove `*.tmp`. Which command first shows the non-hidden pathnames selected by that pattern without deleting them?\n\n## Requesting Confirmation\n\nThe `-i` option asks before each removal:\n\n```bash\n$ rm -i important.txt\nrm: remove regular file 'important.txt'? y\n```\n\nThe `-I` option is a less intrusive safeguard in GNU `rm`: it prompts once when a command would remove more than three files or operate recursively.\n\n## Ignoring Missing Files with -f\n\nThe `-f` option ignores missing operands and suppresses prompts:\n\n```bash\n$ rm -f old-cache.txt\n```\n\nThis can make scripted cleanup idempotent when a generated file may already be absent. Because it removes confirmation, do not add `-f` merely to silence an error you have not understood.\n\n## Removing Directories\n\nPlain `rm` does not remove a directory:\n\n```bash\n$ rm projects\nrm: cannot remove 'projects': Is a directory\n```\n\nUse `-r` or `-R` only when you intend to remove a directory tree and all of its contents:\n\n```bash\n$ rm -r old-project\n```\n\nFor an empty directory, `rmdir` is a narrower alternative:\n\n```bash\n$ rmdir empty-directory\n```\n\n`rmdir` fails when the directory is not empty, which protects its contents from recursive deletion.\n\n## Checking a Recursive Removal\n\nRecursive removal can erase a complete tree. Combining `-r` with `-f` also removes prompts, so `rm -rf` deserves especially careful target validation. Before any recursive removal, check:\n\n- Are you in the directory you think you are in? Use `pwd`.\n- Does `ls -ld -- TARGET` show the intended top-level path?\n- If a wildcard is involved, did a read-only preview match exactly what you expect?\n- Is the path absolute or relative? `/tmp/cache` and `tmp/cache` are very different.\n- Is there an accidental space? `rm -rf old-project` and `rm -rf old project` target different paths.\n\nUse `--` before a target that could begin with a hyphen so it is not interpreted as an option:\n\n```bash\n$ rm -- -old-name\n```\n\nDo not reach for `sudo` simply because `rm` reports a permissions error. First verify the target and determine why your account cannot modify its containing directory. Elevated recursive removal can damage the operating system or other users' data.\n\nUse `-v` when you want `rm` to report each successful removal:\n\n```bash\n$ rm -rv old-project\nremoved 'old-project/notes.txt'\nremoved directory 'old-project'\n```\n\n After verifying the complete target, which command removes `old-project/` and everything below it while still allowing normal prompts?\n\nTo practice removal in a controlled environment, try these hands-on labs:\n\n1. **[Linux rm Command: File Removing](https://labex.io/labs/linux-linux-rm-command-file-removing-209741)** - Learn how to use the `rm` command for removing files and directories, including various options like `-r` and `-i`, and practice safe and effective file deletion.\n2. **[Organizing Files and Directories](https://labex.io/labs/linux-organizing-files-and-directories-387877)** - Practice essential Linux file management skills, including using the `rm` command to clean up unnecessary directories, in a practical challenge.\n\n## Summary\n\nYou can now remove filesystem entries while treating every target as irreversible.\n\n1. Confirm file pathnames before removal.\n2. Preview wildcard expansions with a read-only command.\n3. Ask for confirmation with `-i` or `-I`.\n4. Prefer `rmdir` when a directory must be empty.\n5. Validate an entire target before using recursive removal.", "code_snippets": ["rm [OPTIONS] FILE...", "$ rm file1", "$ rm notes.txt old-report.txt draft.md", "$ rm *.tmp", "$ ls *.tmp\ncache.tmp  test.tmp\n$ rm *.tmp", "$ rm -i important.txt\nrm: remove regular file 'important.txt'? y", "$ rm -f old-cache.txt", "$ rm projects\nrm: cannot remove 'projects': Is a directory", "$ rm -r old-project", "$ rmdir empty-directory", "$ rm -- -old-name", "$ rm -rv old-project\nremoved 'old-project/notes.txt'\nremoved directory 'old-project'"], "quizzes": [{"id": "remove-one-file", "question": "After confirming the target, which command removes the file `old-report.txt`?", "options": [{"text": "`rm old-report.txt`", "correct": true, "explanation": "`rm` removes the named file entry. The operation normally does not place the file in a trash folder."}, {"text": "`rmdir old-report.txt`", "correct": false, "explanation": "`rmdir` operates on empty directories, not regular files. It is not the command for this target."}, {"text": "`mv old-report.txt`", "correct": false, "explanation": "`mv` needs a destination and changes a pathname rather than deleting it. This incomplete command does not perform the requested removal."}]}, {"id": "preview-removal-pattern", "question": "You plan to remove `*.tmp`. Which command first shows the non-hidden pathnames selected by that pattern without deleting them?", "options": [{"text": "`rm -v *.tmp`", "correct": false, "explanation": "Verbose mode reports removals as they happen. It still deletes the matched files and is not a read-only preview."}, {"text": "`ls '*.tmp'`", "correct": false, "explanation": "Quotes prevent wildcard expansion, so this looks for a literal name containing `*` rather than previewing the intended targets."}, {"text": "`ls *.tmp`", "correct": true, "explanation": "The shell expands `*.tmp` for `ls`, allowing you to inspect the same set of non-hidden matches before removal."}]}, {"id": "confirm-each-removal", "question": "Which command asks for confirmation before removing each named file?", "options": [{"text": "`rm -i important.txt`", "correct": true, "explanation": "The `-i` option prompts before each removal, giving you a chance to reject the operation."}, {"text": "`rm -f important.txt`", "correct": false, "explanation": "The `-f` option suppresses prompts and ignores a missing operand. It removes rather than adds confirmation."}, {"text": "`rm -v important.txt`", "correct": false, "explanation": "The `-v` option reports what was removed, but it does not ask for approval first."}]}, {"id": "remove-empty-directory-only", "question": "Which command removes `old-cache/` only if that directory is empty?", "options": [{"text": "`rm -r old-cache/`", "correct": false, "explanation": "Recursive `rm` removes the directory and its contents. It does not enforce the empty-directory condition."}, {"text": "`rmdir old-cache/`", "correct": true, "explanation": "`rmdir` succeeds only for an empty directory, so it does not recursively delete contained files."}, {"text": "`rm -f old-cache/`", "correct": false, "explanation": "The `-f` option does not make plain `rm` remove a directory. It also suppresses safeguards rather than checking emptiness."}]}, {"id": "remove-nonempty-tree", "question": "After verifying the complete target, which command removes `old-project/` and everything below it while still allowing normal prompts?", "options": [{"text": "`rm old-project/`", "correct": false, "explanation": "Plain `rm` does not descend into a directory. It cannot remove a nonempty tree."}, {"text": "`rm -r old-project/`", "correct": true, "explanation": "The `-r` option recursively removes the directory tree. Unlike `-rf`, this form does not add `-f` to suppress prompts."}, {"text": "`rmdir old-project/`", "correct": false, "explanation": "`rmdir` requires an empty directory. It fails when the project still contains entries."}]}]}, {"lesson_id": "find-command", "course_id": "command-line", "title": "find", "order_index": 14, "description": "Learn how to search directory trees by name, type, size, and time, then act on verified matches.", "content": "The `find` command walks a directory tree and tests each entry against criteria such as its name, type, size, or modification time.\n\n## Choosing Where to Search\n\nThe basic syntax is:\n\n```bash\nfind [PATH] [EXPRESSION]\n```\n\nThe path chooses the starting point, and the expression selects or acts on entries below it.\n\nThis command searches `/home` and its descendants for entries named `puppies.jpg`:\n\n```bash\n$ find /home -name puppies.jpg\n```\n\nRecursion is the default. Use `.` as the starting path when you want to search the current directory tree.\n\n## Matching Names and Types\n\nThe `-name` test accepts an exact basename or a shell-style pattern. Quote wildcard patterns so the current shell passes them unchanged to `find`:\n\n```bash\n$ find . -name \"*.txt\"\n```\n\nWithout the quotes, the shell may expand `*.txt` against the current directory before `find` begins. Use `-iname` instead of `-name` when the name match should ignore letter case.\n\nAdd `-type d` to select directories or `-type f` to select regular files:\n\n```bash\n$ find /home -type d -name MyFolder\n```\n\nBoth tests must be true here: the entry must be a directory and its basename must be `MyFolder`.\n\n## Matching Size and Modification Time\n\nUse `-size` with `+` for greater than the specified unit or `-` for less than it:\n\n```bash\n$ find . -type f -size +10M\n$ find . -type f -size -1k\n```\n\nHere, uppercase `M` represents 1,048,576-byte units, while lowercase `k` represents 1,024-byte units. `find` rounds sizes up to the selected unit before applying the numeric comparison, so boundary behavior is based on those units.\n\nUse `-mtime` to test the number of complete 24-hour periods since the file was modified:\n\n```bash\n$ find . -type f -mtime -7\n$ find . -type f -mtime +30\n```\n\n`-mtime -7` matches a value less than 7, while `-mtime +30` matches a value greater than 30. Because complete 24-hour periods are used, these tests are not based on calendar-midnight boundaries.\n\n## Printing and Acting on Matches\n\nIf no action is supplied, GNU `find` prints matching paths. You can write `-print` explicitly when you want the expression's action to be clear:\n\nPrint matches explicitly:\n\n```bash\n$ find . -name \"*.log\" -print\n```\n\nUse `-exec` to run another command for matches:\n\n```bash\n$ find . -name \"*.log\" -exec ls -l {} \\;\n```\n\nFor the `\\;` form, `{}` is replaced by one matching pathname for each command invocation. The semicolon terminates the `-exec` action and is escaped so the shell passes it to `find`.\n\nBefore using a destructive action such as `-delete` or an `-exec` command that changes files, run the same tests with `-print` and inspect every result. A narrower starting path and `-maxdepth N` can also limit the search.\n\n You are developing a `find` command that may later delete old `.log` files. What should you do first?\n\n` before invoking `ls -l`.\"}\n\nPermission-denied messages usually mean the current account cannot search part of the tree. Prefer a narrower, relevant starting path; do not add elevated privileges until you understand and intend the expanded access.\n\nTo practice building search expressions, try these hands-on labs:\n\n1. **[Linux find Command: File Searching](https://labex.io/labs/linux-linux-find-command-file-searching-219191)** - This lab provides an introduction to the `find` command, a versatile utility for searching and locating files and directories based on various criteria. You'll practice using `find` to locate specific files.\n2. **[Discover Critical System Resources](https://labex.io/labs/linux-discover-critical-system-resources-388032)** - Learn essential Linux commands for locating files and executables, including `find`. You'll practice efficiently navigating the file system and discovering critical system resources.\n\n## Summary\n\nYou can now build focused `find` expressions and verify results before taking action.\n\n1. Choose the narrowest useful starting path.\n2. Quote name patterns and combine them with type tests.\n3. Filter by size or complete 24-hour modification periods.\n4. Limit recursion depth when appropriate.\n5. Print and inspect matches before destructive actions.", "code_snippets": ["find [PATH] [EXPRESSION]", "$ find /home -name puppies.jpg", "$ find . -name \"*.txt\"", "$ find /home -type d -name MyFolder", "$ find . -type f -size +10M\n$ find . -type f -size -1k", "$ find . -type f -mtime -7\n$ find . -type f -mtime +30", "$ find . -name \"*.log\" -print", "$ find . -name \"*.log\" -exec ls -l {} \\;"], "quizzes": [{"id": "search-current-tree", "question": "Which command searches the current directory and its descendants for entries named `notes.txt`?", "options": [{"text": "`find . -name notes.txt`", "correct": true, "explanation": "The dot selects the current directory as the starting path, and `-name` tests each entry's basename."}, {"text": "`find / -name notes.txt`", "correct": false, "explanation": "A starting path of `/` searches from the filesystem root, which is much broader than the current directory tree."}, {"text": "`find notes.txt .`", "correct": false, "explanation": "`find` expects starting paths before the expression. This order does not express the requested search."}]}, {"id": "find-text-regular-files", "question": "Which command finds regular files whose names end in `.txt` below the current directory?", "options": [{"text": "`find . -type f -name \"*.txt\"`", "correct": true, "explanation": "`-type f` selects regular files, while the quoted `-name` pattern is evaluated by `find` for every entry."}, {"text": "`find . -type d -name \"*.txt\"`", "correct": false, "explanation": "The pattern is quoted correctly, but `-type d` selects directories rather than regular files."}, {"text": "`find . -type f -name *.txt`", "correct": false, "explanation": "The unquoted wildcard can be expanded by the current shell before `find` runs, changing the intended expression."}]}, {"id": "find-recent-regular-files", "question": "Which command finds regular files below `.` whose modification age is less than seven complete 24-hour periods?", "options": [{"text": "`find . -type f -mtime -7`", "correct": true, "explanation": "`-type f` selects regular files, and `-mtime -7` selects modification ages below seven complete 24-hour periods."}, {"text": "`find . -type f -mtime +7`", "correct": false, "explanation": "The plus sign selects ages greater than seven units. It looks for older rather than recent files."}, {"text": "`find . -type d -mtime -7`", "correct": false, "explanation": "The time test is recent, but `-type d` restricts the results to directories instead of regular files."}]}, {"id": "verify-before-delete", "question": "You are developing a `find` command that may later delete old `.log` files. What should you do first?", "options": [{"text": "Add `-delete` immediately and check which files disappear.", "correct": false, "explanation": "Deletion is not a safe preview and has no built-in undo. Verify the complete match set before adding it."}, {"text": "Run the same tests with `-print` and inspect every match.", "correct": true, "explanation": "A read-only listing verifies the starting path and tests before a destructive action is introduced."}, {"text": "Search from `/` so the command cannot miss any log files.", "correct": false, "explanation": "Starting at `/` broadens the scope and can include unrelated or protected paths. Use the narrowest appropriate starting point."}]}, {"id": "run-ls-for-each-match", "question": "In `find . -name \"*.log\" -exec ls -l {} \\;`, what does `{}` represent?", "options": [{"text": "The current matching pathname supplied to `ls -l`.", "correct": true, "explanation": "For this `-exec` form, `find` substitutes the current match for `{}` before invoking `ls -l`."}, {"text": "The directory where the `find` command was started.", "correct": false, "explanation": "The starting directory is the dot near the beginning of the command. The braces have a different role inside `-exec`."}, {"text": "The semicolon that ends the `-exec` expression.", "correct": false, "explanation": "The escaped semicolon terminates the `-exec` action. The braces are the pathname placeholder."}]}]}, {"lesson_id": "help-command", "course_id": "command-line", "title": "help", "order_index": 15, "description": "Learn how to choose built-in help, program usage output, or manual pages for a command.", "content": "You do not need to memorize every command option. Bash and many installed programs can explain their syntax directly in the terminal, but the right help source depends on what kind of command you are using.\n\n## Getting Help for Bash Builtins\n\nBash provides the `help` builtin for commands implemented by the shell itself. Examples include `cd`, `history`, and `type`.\n\nPass the builtin name as an argument:\n\n```bash\n$ help echo\n```\n\nThe output describes the builtin's syntax and behavior. Running `help` without an argument lists the builtins for which Bash has help.\n\n## Requesting a Program's Usage Summary\n\nMany external programs follow the convention of accepting `--help` and printing a usage summary:\n\n```bash\n$ ls --help\n```\n\nThis convention is common but not universal. Read the output and exit status rather than assuming every program supports the same option.\n\n## Finding How Bash Resolves a Name\n\nUse `type` to see whether Bash resolves a name as a builtin, alias, function, keyword, or executable file:\n\n```bash\n$ type cd\ncd is a shell builtin\n$ type ls\nls is /usr/bin/ls\n```\n\nThe exact result can vary with aliases, functions, installed programs, and `PATH`. Use `type -a NAME` when you want Bash to show all known resolutions rather than only the one it would use first.\n\n You do not know whether `deploy` is an alias, function, builtin, or executable. Which Bash command checks how the name resolves?\n\n## Choosing the Level of Detail\n\n- Use `help COMMAND` for a Bash builtin.\n- Use `COMMAND --help` for a quick summary from many external commands.\n- Use `man COMMAND` for an installed manual page with more detailed documentation.\n- Use `whatis COMMAND` for a one-line description.\n\nThe next lessons examine manual pages and one-line descriptions in more detail.\n\n You need detailed documentation for the external command `ls`, not only a short usage summary. Which command should you try?\n\n## Summary\n\nYou can now choose a help source based on how Bash resolves a command.\n\n1. Use `help` for Bash builtins.\n2. Try `--help` for a program's quick usage output.\n3. Inspect name resolution with `type`.\n4. Open detailed documentation with `man`.", "code_snippets": ["$ help echo", "$ ls --help", "$ type cd\ncd is a shell builtin\n$ type ls\nls is /usr/bin/ls"], "quizzes": [{"id": "help-for-bash-cd", "question": "Which command displays Bash's help entry for its `cd` builtin?", "options": [{"text": "`cd --help`", "correct": false, "explanation": "Some builtins may recognize options, but Bash's dedicated documentation interface is `help` followed by the builtin name."}, {"text": "`help cd`", "correct": true, "explanation": "Bash's `help` builtin looks up the documentation for the named builtin, which is `cd` here."}, {"text": "`type cd`", "correct": false, "explanation": "`type` explains how Bash resolves the name `cd`. It identifies the command but does not show the full help entry."}]}, {"id": "quick-ls-usage", "question": "Which command commonly prints a quick usage summary provided by the external `ls` program?", "options": [{"text": "`help ls`", "correct": false, "explanation": "Bash `help` documents shell builtins. On a typical system, it does not provide the external `ls` program's usage page."}, {"text": "`ls --help`", "correct": true, "explanation": "GNU `ls` follows the common `--help` convention and prints its usage and options."}, {"text": "`type --help ls`", "correct": false, "explanation": "This asks the `type` builtin about its own option handling, not `ls` to explain its usage."}]}, {"id": "identify-command-resolution", "question": "You do not know whether `deploy` is an alias, function, builtin, or executable. Which Bash command checks how the name resolves?", "options": [{"text": "`type deploy`", "correct": true, "explanation": "The `type` builtin reports how Bash interprets the command name in the current shell environment."}, {"text": "`help deploy`", "correct": false, "explanation": "`help` looks for Bash builtin documentation. It does not generally identify aliases, functions, and external files."}, {"text": "`deploy --help`", "correct": false, "explanation": "This attempts to run the command and depends on its own option support. It does not first explain how Bash resolved the name."}]}, {"id": "choose-detailed-manual", "question": "You need detailed documentation for the external command `ls`, not only a short usage summary. Which command should you try?", "options": [{"text": "`man ls`", "correct": true, "explanation": "`man ls` opens the installed manual page, which normally provides a fuller description of syntax, options, and behavior."}, {"text": "`whatis ls`", "correct": false, "explanation": "`whatis` is designed to show concise manual-page descriptions. It is not the detailed documentation requested."}, {"text": "`type ls`", "correct": false, "explanation": "`type` reports how Bash resolves `ls`. It does not display the program's detailed manual."}]}]}, {"lesson_id": "man-command", "course_id": "command-line", "title": "man", "order_index": 16, "description": "Learn how to open, navigate, search, and select sections of installed manual pages.", "content": "Many Linux commands, interfaces, configuration files, and administrative tools have installed reference documentation called manual pages, or man pages. The `man` command finds and displays those pages.\n\n## Opening a Manual Page\n\nPass a topic name to `man`. For example, open the page for `ls` with:\n\n```bash\n$ man ls\n```\n\nManual pages commonly include a synopsis, description, options, related files, and cross-references, although the exact sections vary.\n\n## Navigating and Searching a Page\n\nOn many systems, `man` displays pages through a pager such as `less`. While a page is open, you can scroll with arrow or page keys and use these controls:\n\nInside a man page:\n\n- Type `/pattern` and press Enter to search forward.\n- Press `n` to repeat the search in the same direction.\n- Press `N` to repeat it in the opposite direction.\n- Press `q` to quit.\n\nThe pager can differ by system or environment, so its exact keys are not guaranteed everywhere. The controls above apply to the common `less` setup.\n\n With a man page open in `less`, what starts a forward search for `--recursive`?\n\n With a man page open in the usual pager, which key returns to the shell?\n\n## Selecting a Manual Section\n\nThe manual is organized into numbered sections. Common sections include:\n\n- `1`: User commands.\n- `2`: System calls.\n- `3`: Library functions.\n- `5`: File formats.\n- `8`: System administration commands.\n\nThe same topic can appear in more than one section. Put the section before the topic to select one explicitly:\n\n```bash\n$ man 5 passwd\n$ man 1 passwd\n```\n\nThe first command opens the `passwd` file-format page from section 5. The second opens the user command page from section 1. References such as `passwd(5)` use the same `topic(section)` notation.\n\n## When a Page Is Missing\n\nNot every command name has a separately installed manual page. If `man` reports that no entry exists:\n\n- Run `type NAME` to see how Bash resolves the name.\n- Use `help NAME` when it is a Bash builtin.\n- Try `NAME --help` when an external program supports that convention.\n- Check whether your distribution offers a separate documentation package.\n\n `type cd` reports that `cd` is a Bash builtin, and no separate man page is available. Which command should you try next?\n\n## Summary\n\nYou can now locate and navigate installed manual documentation.\n\n1. Open a page by topic name.\n2. Search and move through a page in the usual pager.\n3. Quit the pager and return to the shell.\n4. Select a numbered manual section.\n5. Choose another help source when a page is unavailable.", "code_snippets": ["$ man ls", "$ man 5 passwd\n$ man 1 passwd"], "quizzes": [{"id": "open-ls-manual", "question": "Which command opens the installed manual page for `ls`?", "options": [{"text": "`help ls`", "correct": false, "explanation": "Bash `help` documents shell builtins and normally does not open the external `ls` manual page."}, {"text": "`man ls`", "correct": true, "explanation": "`man` looks up the topic `ls` in the manual database and displays the matching page."}, {"text": "`ls --help`", "correct": false, "explanation": "This asks `ls` for its own usage summary. It does not open the installed manual page."}]}, {"id": "search-man-page", "question": "With a man page open in `less`, what starts a forward search for `--recursive`?", "options": [{"text": "Type `?--recursive` and press Enter.", "correct": false, "explanation": "A question mark begins a backward search. It looks in the opposite direction from the one requested."}, {"text": "Type `/--recursive` and press Enter.", "correct": true, "explanation": "A slash begins a forward search in `less`, and Enter submits the pattern."}, {"text": "Type `n--recursive` and press Enter.", "correct": false, "explanation": "The `n` key repeats an existing search. It does not introduce a new search pattern this way."}]}, {"id": "leave-man-page", "question": "With a man page open in the usual pager, which key returns to the shell?", "options": [{"text": "`G`", "correct": false, "explanation": "Uppercase `G` moves to the end of the page in `less`. It does not close the pager."}, {"text": "`n`", "correct": false, "explanation": "The `n` key repeats the most recent search. It keeps the manual page open."}, {"text": "`q`", "correct": true, "explanation": "The `q` key quits the usual pager and returns control to the shell."}]}, {"id": "open-passwd-file-format", "question": "Which command opens the section 5 page that documents the `passwd` file format?", "options": [{"text": "`man passwd 5`", "correct": false, "explanation": "The section selector belongs before the topic in this command form. This operand order does not request `passwd(5)`."}, {"text": "`man 5 passwd`", "correct": true, "explanation": "Placing section `5` before `passwd` selects the file-format page specifically."}, {"text": "`man 1 passwd`", "correct": false, "explanation": "Section 1 contains user commands, so this selects the `passwd` command page rather than the file-format page."}]}, {"id": "missing-builtin-manual", "question": "`type cd` reports that `cd` is a Bash builtin, and no separate man page is available. Which command should you try next?", "options": [{"text": "`whatis cd`", "correct": false, "explanation": "`whatis` summarizes entries from the manual database. It cannot supply a missing dedicated page for the builtin."}, {"text": "`file cd`", "correct": false, "explanation": "`file` classifies filesystem objects, but `cd` is being resolved as a shell builtin rather than a pathname here."}, {"text": "`help cd`", "correct": true, "explanation": "Bash's `help` builtin provides the shell's own documentation for `cd`."}]}]}, {"lesson_id": "whatis-command", "course_id": "command-line", "title": "whatis", "order_index": 17, "description": "Learn how to retrieve concise manual-page descriptions and interpret their section numbers.", "content": "When you recognize a command name but forget its purpose, `whatis` can provide a short reminder from the manual-page database.\n\n## Looking Up an Exact Name\n\nPass one or more exact topic names to `whatis`. Each result is derived from the `NAME` section recorded for an installed manual page:\n\n```bash\n$ whatis cat\ncat (1)              - concatenate files and print on the standard output\n```\n\nThe output is a description, not a list of command options or examples. Use `man cat` or `cat --help` when you need more detail.\n\n You know the name `cat` and want its one-line manual-page description. Which command should you run?\n\n## Reading Section Numbers\n\nIf the same topic has manual pages in several sections, `whatis` can display more than one result:\n\n```bash\n$ whatis passwd\npasswd (1)           - change user password\npasswd (5)           - the password file\n```\n\nThe number in parentheses is the manual section. Here, `passwd(1)` describes the user command and `passwd(5)` describes a file format. You can open one explicitly with `man 1 passwd` or `man 5 passwd`.\n\n## Choosing between whatis, man, and apropos\n\n- `whatis NAME`: Show concise descriptions for an exact manual topic name.\n- `man NAME`: Open a full manual page.\n- `apropos KEYWORD`: Search manual-page names and descriptions for a keyword.\n\nFor example:\n\n```bash\n$ apropos password\n```\n\nUse `apropos` when you know the task but not the command name. Use `whatis` when you already know the name.\n\n You do not know a command's name, but you want to search manual descriptions for the keyword `password`. Which command fits that task?\n\n## When No Description Appears\n\nIf `whatis` reports that nothing is appropriate, the topic may not have an installed manual page or the manual database may be out of date. This result does not prove that no executable, alias, function, or builtin with that name exists. Use `type NAME` to see how Bash resolves a command name, then choose an appropriate help source.\n\n `whatis deploy` finds no manual description. Which command checks whether Bash resolves `deploy` as an alias, function, builtin, or executable?\n\n## Summary\n\nYou can now retrieve and interpret concise descriptions from the manual database.\n\n1. Look up an exact topic with `whatis`.\n2. Read the manual section shown in parentheses.\n3. Use `man` when you need the full page.\n4. Use `apropos` when you know a keyword rather than a name.", "code_snippets": ["$ whatis cat\ncat (1)              - concatenate files and print on the standard output", "$ whatis passwd\npasswd (1)           - change user password\npasswd (5)           - the password file", "$ apropos password"], "quizzes": [{"id": "describe-known-command", "question": "You know the name `cat` and want its one-line manual-page description. Which command should you run?", "options": [{"text": "`man cat`", "correct": false, "explanation": "`man cat` opens the full manual page. It provides more than the requested one-line reminder."}, {"text": "`apropos cat`", "correct": false, "explanation": "`apropos` searches descriptions for a keyword and can return many related topics. It is broader than an exact-name lookup."}, {"text": "`whatis cat`", "correct": true, "explanation": "`whatis` looks up the exact topic name and prints its concise description from the manual database."}]}, {"id": "interpret-whatis-section", "question": "In the output `passwd (5) - the password file`, what does `(5)` identify?", "options": [{"text": "The fifth option accepted by the `passwd` command.", "correct": false, "explanation": "The number is not an option position. Options are documented inside a selected manual page."}, {"text": "The manual section containing the file-format page.", "correct": true, "explanation": "Section 5 is used for file formats and conventions, so `passwd(5)` refers to that manual section."}, {"text": "Five manual pages that share the name `passwd`.", "correct": false, "explanation": "Multiple results may exist, but the parenthesized value identifies one section rather than a page count."}]}, {"id": "search-by-purpose", "question": "You do not know a command's name, but you want to search manual descriptions for the keyword `password`. Which command fits that task?", "options": [{"text": "`apropos password`", "correct": true, "explanation": "`apropos` searches manual-page names and descriptions for the keyword, helping discover relevant topics."}, {"text": "`whatis password`", "correct": false, "explanation": "`whatis` looks for an exact manual topic named `password`. It is not the general keyword-search interface."}, {"text": "`man password`", "correct": false, "explanation": "`man` attempts to open a page with that topic name. It does not perform the requested description search."}]}, {"id": "whatis-versus-type", "question": "`whatis deploy` finds no manual description. Which command checks whether Bash resolves `deploy` as an alias, function, builtin, or executable?", "options": [{"text": "`whatis -r deploy`", "correct": false, "explanation": "Changing the manual-database query does not show all of Bash's aliases, functions, builtins, and path resolution."}, {"text": "`man 5 deploy`", "correct": false, "explanation": "This attempts to open a section 5 page. It does not determine how Bash resolves the command name."}, {"text": "`type deploy`", "correct": true, "explanation": "Bash `type` reports how the current shell resolves a command name, independently of whether a manual description is installed."}]}]}, {"lesson_id": "alias-command", "course_id": "command-line", "title": "alias", "order_index": 18, "description": "Learn how to create, inspect, persist, bypass, and remove command aliases in Bash.", "content": "An alias tells an interactive shell to replace one command word with another string before executing the line. This can shorten a frequent command or provide a preferred set of options.\n\n## Creating an Alias in the Current Shell\n\nIn Bash, define an alias with `alias NAME='REPLACEMENT'`. Do not put spaces around the equals sign:\n\n```bash\n$ alias ll='ls -la'\n```\n\nAfter this definition, entering `ll` as a command expands to `ls -la`. The quotes keep the replacement together while the alias is being defined.\n\nAliases are best for simple command-prefix substitutions. Use a shell function when you need to process arguments in a more structured way.\n\n## Loading an Alias in Future Bash Sessions\n\nAn alias defined at the prompt belongs to the current shell and disappears when that shell exits. Interactive non-login Bash sessions normally read `~/.bashrc`, so that file is the usual place for personal Bash aliases:\n\n```bash\nalias ll='ls -la'\n```\n\nAfter editing the file, start a new interactive Bash session or reload it in the current shell:\n\n```bash\n$ source ~/.bashrc\n```\n\nShell startup behavior can vary by shell, login mode, and distribution configuration. A Zsh user, for example, would normally use Zsh configuration rather than Bash's `.bashrc`.\n\n## Inspecting Aliases and Name Resolution\n\nRun `alias` without arguments to list aliases in the current shell:\n\n```bash\n$ alias\nalias ll='ls -la'\nalias grep='grep --color=auto'\n```\n\nUse `type NAME` to inspect how Bash resolves a particular name:\n\n```bash\n$ type ll\nll is aliased to 'ls -la'\n```\n\n## Bypassing and Removing an Alias\n\nTo bypass an alias for one command line, prefix the command name with a backslash or put it after Bash's `command` builtin:\n\n```bash\n$ \\ls\n$ command ls\n```\n\nThis is useful when you need the underlying command's normal behavior. Keep aliases short and predictable, and avoid hiding surprising or destructive behavior behind familiar command names.\n\n The current Bash session has an alias named `ls`. Which command bypasses that alias for one invocation?\n\nRemove an alias from the current shell with `unalias`:\n\n```bash\n$ unalias ll\n```\n\nIf the definition remains in `~/.bashrc`, a future shell can create it again. Remove or change that configuration line as well when you want the alias gone permanently.\n\n## Summary\n\nYou can now customize Bash with simple, inspectable aliases.\n\n1. Define a temporary alias with correct quoting.\n2. Load personal aliases from `~/.bashrc` in future sessions.\n3. Inspect aliases and command resolution.\n4. Bypass an alias for one invocation.\n5. Remove both the active and saved definition when required.", "code_snippets": ["$ alias ll='ls -la'", "alias ll='ls -la'", "$ source ~/.bashrc", "$ alias\nalias ll='ls -la'\nalias grep='grep --color=auto'", "$ type ll\nll is aliased to 'ls -la'", "$ \\ls\n$ command ls", "$ unalias ll"], "quizzes": [{"id": "define-ll-alias", "question": "Which Bash command defines `ll` as an alias for `ls -la` in the current shell?", "options": [{"text": "`alias ll = 'ls -la'`", "correct": false, "explanation": "Spaces around `=` split the definition into separate shell words, so Bash does not receive a valid alias assignment."}, {"text": "`alias ll='ls -la'`", "correct": true, "explanation": "This uses the required `NAME=REPLACEMENT` form and quotes the replacement containing a space."}, {"text": "`unalias ll='ls -la'`", "correct": false, "explanation": "`unalias` removes existing alias names. It does not create a replacement."}]}, {"id": "persist-bash-alias", "question": "Where should a personal alias normally be defined so future interactive non-login Bash sessions load it?", "options": [{"text": "In the user's `~/.bashrc` file.", "correct": true, "explanation": "Interactive non-login Bash normally reads `~/.bashrc`, making it the conventional location for personal Bash aliases."}, {"text": "In the executable file used by the aliased command.", "correct": false, "explanation": "Changing an installed executable is unrelated to shell alias expansion and can damage managed system files."}, {"text": "In the current terminal's scrollback history.", "correct": false, "explanation": "Scrollback only records displayed text. Bash does not execute it as startup configuration."}]}, {"id": "inspect-command-alias", "question": "Which command shows whether Bash currently resolves `ll` as an alias, function, builtin, or executable?", "options": [{"text": "`file ll`", "correct": false, "explanation": "`file` classifies a filesystem pathname. An alias exists in shell state and need not correspond to a file named `ll`."}, {"text": "`type ll`", "correct": true, "explanation": "The `type` builtin reports how the current Bash session resolves the name `ll`."}, {"text": "`whatis ll`", "correct": false, "explanation": "`whatis` queries manual-page descriptions. Personal aliases normally have no manual database entry."}]}, {"id": "bypass-ls-alias", "question": "The current Bash session has an alias named `ls`. Which command bypasses that alias for one invocation?", "options": [{"text": "`alias ls`", "correct": false, "explanation": "This prints the definition of the `ls` alias. It does not invoke the underlying command."}, {"text": "`command ls`", "correct": true, "explanation": "Because `command` is the command word, Bash does not expand the following `ls` as an alias and invokes normal command resolution."}, {"text": "`source ls`", "correct": false, "explanation": "`source` reads a file as shell code in the current shell. It is not a safe or appropriate way to bypass an alias."}]}, {"id": "remove-current-alias", "question": "Which command removes the alias `ll` from the current Bash session?", "options": [{"text": "`unalias ll`", "correct": true, "explanation": "`unalias` deletes the named alias from the current shell's alias table."}, {"text": "`alias ll=''`", "correct": false, "explanation": "This replaces the alias with an empty expansion rather than removing its definition."}, {"text": "`command ll`", "correct": false, "explanation": "`command` can bypass alias expansion on that line, but it does not delete the alias from shell state."}]}]}, {"lesson_id": "exit-command", "course_id": "command-line", "title": "exit", "order_index": 19, "description": "Learn how to leave the current shell and choose the status it returns to its caller.", "content": "Shells can be nested: a graphical terminal starts a shell, an SSH connection starts a remote shell, and a shell can start another shell. Leaving one normally returns control to whatever started that current shell.\n\n## Leaving the Current Shell\n\nThe `exit` command asks the current shell to terminate:\n\n```bash\n$ exit\n```\n\nIf that shell is the main process in a graphical terminal tab, the tab may close according to the terminal's settings. In an SSH session, leaving the remote shell normally returns you to the local shell. If you started a nested shell, `exit` returns to its parent shell.\n\n You started Bash inside another shell and now want to return to the parent shell. Which command should you run in the nested Bash session?\n\n## Returning an Exit Status\n\nAn optional numeric argument sets the status returned to the shell's caller:\n\n```bash\n$ exit 0\n```\n\nBy convention, `0` means success and a nonzero value represents failure or another condition defined by the program. If Bash receives no numeric argument, it exits with the status of the last command executed before `exit`.\n\n## Using logout in a Login Shell\n\nThe Bash `logout` builtin exits a login shell:\n\n```bash\n$ logout\n```\n\nIn a non-login Bash shell, `logout` reports that it is not a login shell; use `exit` instead.\n\n## Using Ctrl+D or Closing a Terminal\n\nAt an empty interactive prompt, pressing `Ctrl+D` normally supplies the terminal's end-of-file input character. Bash commonly interprets that condition as a request to exit. It is not a signal, and shell settings such as Bash's `ignoreeof` can change the behavior.\n\nClosing a graphical terminal window asks the terminal application to close its processes and can affect running jobs. Prefer an orderly `exit` when practical, and check for active work before closing a session.\n\n## Summary\n\nYou can now leave the current shell and communicate its completion status.\n\n1. Use `exit` to return to the current shell's caller.\n2. Supply `0` for success or a defined nonzero status otherwise.\n3. Understand the status used by a bare `exit`.\n4. Use `logout` only for a login shell.\n5. Recognize `Ctrl+D` as end-of-file input rather than a signal.", "code_snippets": ["$ exit", "$ exit 0", "$ logout"], "quizzes": [{"id": "leave-current-shell", "question": "You started Bash inside another shell and now want to return to the parent shell. Which command should you run in the nested Bash session?", "options": [{"text": "`clear`", "correct": false, "explanation": "`clear` refreshes the visible terminal area but leaves the current shell running."}, {"text": "`exit`", "correct": true, "explanation": "`exit` terminates the current shell, allowing its parent shell to resume."}, {"text": "`history -c`", "correct": false, "explanation": "This clears Bash's in-memory history list. It does not terminate the current shell."}]}, {"id": "return-success-status", "question": "Which command terminates the current shell and explicitly reports success to its caller?", "options": [{"text": "`exit 0`", "correct": true, "explanation": "Status `0` conventionally reports successful completion to the caller."}, {"text": "`exit 1`", "correct": false, "explanation": "A nonzero status conventionally indicates failure or another exceptional result rather than success."}, {"text": "`logout 0`", "correct": false, "explanation": "Bash `logout` is for a login shell and does not use this form to set the requested status."}]}, {"id": "exit-without-number", "question": "In Bash, what status does `exit` return when you do not provide a number?", "options": [{"text": "It always returns a successful status of `0`.", "correct": false, "explanation": "A successful convention does not force a bare `exit` to return zero. Bash preserves a previous status in this case."}, {"text": "It always returns a failure status of `1`.", "correct": false, "explanation": "Bash does not assign failure status `1` to every bare `exit`. The preceding command determines the value."}, {"text": "It returns the previous command's exit status.", "correct": true, "explanation": "Without an explicit numeric argument, Bash exits using the most recent command status."}]}, {"id": "leave-login-shell", "question": "Which Bash builtin is specifically intended to leave a login shell?", "options": [{"text": "`logout`", "correct": true, "explanation": "Bash provides `logout` for terminating a login shell."}, {"text": "`unalias`", "correct": false, "explanation": "`unalias` removes alias definitions from the current shell. It does not end the session."}, {"text": "`source`", "correct": false, "explanation": "`source` reads commands from a file into the current shell. It does not terminate that shell."}]}]}]}, {"course_id": "text-fu", "course_name": "Text-Fu", "icon": "\ud83e\udd4b", "description": "Master data streams, redirection, pipes, environment variables, and text filters.", "lessons_count": 16, "quizzes_count": 76, "lessons": [{"lesson_id": "stdout-standard-out-redirect", "course_id": "text-fu", "title": "stdout (Standard Out)", "order_index": 1, "description": "Learn how standard output flows to the terminal and how Bash redirects it to files.", "content": "Programs communicate through input/output streams. Standard output, abbreviated **stdout**, is the stream a program normally uses for its regular results. In a terminal, the shell initially connects that stream to the terminal display.\n\n## Writing to Standard Output\n\nThe `echo` command writes its arguments to stdout:\n\n```bash\n$ echo Hello World\nHello World\n```\n\nStdout is file descriptor `1`, a number that becomes useful when you redirect more than one stream. Programs can also have standard input, or stdin, and standard error, or stderr; the next lessons examine those streams.\n\n Without redirection, where does `echo Hello World` normally send its regular output in an interactive terminal?\n\n## Replacing a File with >\n\nBash interprets `>` as an output-redirection operator. It opens the destination file and connects the command's stdout to it:\n\n```bash\n$ echo Hello World > peanuts.txt\n```\n\nThe text no longer appears on the terminal because stdout goes to `peanuts.txt`. If the file is missing, the shell creates it. If it exists, the shell truncates it before the command writes, so the previous contents are lost.\n\nUse `cat` to inspect the result:\n\n```bash\n$ cat peanuts.txt\nHello World\n```\n\n `notes.txt` already contains text. What does `echo new > notes.txt` do?\n\nBecause the shell opens the destination before the command runs, verify the pathname before pressing Enter. A misspelled or unintended existing file can be truncated even if the command later fails.\n\n## Appending to a File with >>\n\nUse `>>` when new stdout should be added after a file's existing contents:\n\n```bash\n$ echo Another line >> peanuts.txt\n$ cat peanuts.txt\nHello World\nAnother line\n```\n\nLike `>`, `>>` creates a missing destination. The difference is how an existing file is opened: `>>` appends instead of truncating.\n\n## Redirection Belongs to the Shell\n\nThe shell recognizes `>` and `>>`, removes those operators from the arguments passed to the program, opens the file, and arranges the stream connection. The command itself simply writes to stdout as usual.\n\nThis means the same redirection syntax works with many commands:\n\n```bash\n$ pwd > current-directory.txt\n$ ls -la >> directory-list.txt\n```\n\nTo practice standard-stream redirection, try this hands-on lab:\n\n1. **[Redirecting Input and Output in Linux](https://labex.io/labs/comptia-redirecting-input-and-output-in-linux-590840)** - Practice controlling data flow from commands by manipulating standard output (stdout), standard error (stderr), and standard input (stdin) using operators like `>`, `>>`, `2>`, and the `tee` command.\n\n## Summary\n\nYou can now redirect a command's standard output without confusing replacement and append behavior.\n\n1. Recognize stdout as the stream for regular command results.\n2. Replace a file's contents with `>`.\n3. Preserve existing contents and append with `>>`.\n4. Verify a destination before the shell opens it.", "code_snippets": ["$ echo Hello World\nHello World", "$ echo Hello World > peanuts.txt", "$ cat peanuts.txt\nHello World", "$ echo Another line >> peanuts.txt\n$ cat peanuts.txt\nHello World\nAnother line", "$ pwd > current-directory.txt\n$ ls -la >> directory-list.txt"], "quizzes": [{"id": "stdout-default-destination", "question": "Without redirection, where does `echo Hello World` normally send its regular output in an interactive terminal?", "options": [{"text": "To a file named `stdout` in the current directory.", "correct": false, "explanation": "Standard output is a stream, not an automatically created file named `stdout`. A file is used only when you redirect to one."}, {"text": "To the terminal through standard output.", "correct": true, "explanation": "The shell normally connects a command's stdout to the terminal, so `echo` is displayed there."}, {"text": "To the command's standard input stream.", "correct": false, "explanation": "Standard input carries data into a program. `echo` sends its regular result out through stdout."}]}, {"id": "stdout-replace-file", "question": "`notes.txt` already contains text. What does `echo new > notes.txt` do?", "options": [{"text": "It replaces the file's contents with `new`.", "correct": true, "explanation": "The shell truncates the existing destination for `>` and directs `echo` output into the now-empty file."}, {"text": "It adds `new` after the existing text.", "correct": false, "explanation": "Appending requires `>>`. A single `>` does not preserve the destination's previous contents."}, {"text": "It displays `new` without changing the file.", "correct": false, "explanation": "The redirection sends stdout to `notes.txt`, so the normal output does not remain on the terminal."}]}, {"id": "stdout-append-file", "question": "Which command adds `Finished` to the end of `status.log` without erasing existing content?", "options": [{"text": "`echo Finished > status.log`", "correct": false, "explanation": "A single `>` truncates an existing destination before writing. It would erase the earlier log content."}, {"text": "`echo Finished >> status.log`", "correct": true, "explanation": "`echo` produces the text, and `>>` appends that stdout to the destination file."}, {"text": "`cat Finished >> status.log`", "correct": false, "explanation": "This asks `cat` to read a file named `Finished`. It does not produce the requested text as stdout."}]}, {"id": "stdout-shell-role", "question": "Who normally interprets `>` in `pwd > current-directory.txt`?", "options": [{"text": "The `pwd` command after receiving `>` as an argument.", "correct": false, "explanation": "The shell consumes the redirection syntax, so `pwd` normally does not receive `>` or the destination as ordinary arguments."}, {"text": "The Bash shell before it starts `pwd`.", "correct": true, "explanation": "Bash opens the destination and connects file descriptor 1 before executing the command."}, {"text": "The terminal after `pwd` has printed the path on screen.", "correct": false, "explanation": "The stream is redirected before output is written, so the terminal never receives that stdout in the first place."}]}]}, {"lesson_id": "stdin-standard-in-redirect", "course_id": "text-fu", "title": "stdin (Standard In)", "order_index": 2, "description": "Learn how programs read standard input and how Bash connects that stream to a file.", "content": "Standard input, abbreviated **stdin**, is the stream a program normally reads for incoming data. In an interactive terminal, the shell usually connects stdin to your terminal input, so a program can read what you type.\n\n## Standard Input and File Descriptor 0\n\nBy convention, the three standard streams use these file descriptor numbers:\n\n- `0`: standard input (`stdin`)\n- `1`: standard output (`stdout`)\n- `2`: standard error (`stderr`)\n\nA program can choose whether and how to use these streams. A command designed to read stdin often waits for terminal input when no file operand or other input source is supplied.\n\n## Redirecting a File into stdin\n\nThe `<` operator tells Bash to open a file for reading and connect it to the command's stdin:\n\n```bash\n$ cat < peanuts.txt\nHello World\n```\n\nThe shell handles `< peanuts.txt`; `cat` simply reads file descriptor 0. The pathname is not passed to `cat` as a normal file operand.\n\nIf the input file does not exist or cannot be opened, the shell reports the redirection error and does not start the command with that input.\n\n## File Operand versus Input Redirection\n\nSome commands accept either a filename operand or stdin, but the results can differ slightly. For example:\n\n```bash\n$ wc -l peanuts.txt\n1 peanuts.txt\n$ wc -l < peanuts.txt\n1\n```\n\nBoth forms count lines in the same data. In the first form, `wc` knows the filename because it received it as an argument. In the second, it only receives a stream on stdin, so it has no filename to print.\n\n## Combining Input and Output Redirection\n\nOne command line can redirect more than one stream:\n\n```bash\n$ cat < peanuts.txt > banana.txt\n```\n\nThe shell performs two independent connections:\n\n1. `< peanuts.txt` opens `peanuts.txt` as `cat`'s stdin.\n2. `> banana.txt` creates or truncates `banana.txt` and connects it to `cat`'s stdout.\n\n`cat` reads bytes from stdin and writes them to stdout, so `banana.txt` receives the source content. For ordinary file copying, `cp peanuts.txt banana.txt` communicates the intent more directly; this example is about stream connections.\n\nTo practice input and output redirection, try these hands-on labs:\n\n1. **[Redirecting Input and Output in Linux](https://labex.io/labs/comptia-redirecting-input-and-output-in-linux-590840)** - Practice controlling data flow from commands by manipulating standard output (stdout), standard error (stderr), and standard input (stdin) using operators like >, >>, 2>, and the tee command.\n2. **[Data Stream Redirection](https://labex.io/labs/linux-data-stream-redirection-17995)** - Learn the art of Linux stream redirection. Manipulate standard input, output, and error streams, combine outputs, and utilize /dev/null for advanced file operations.\n## Summary\n\nYou can now connect a command's standard input to a file through the shell.\n\n1. Recognize stdin as file descriptor 0.\n2. Redirect a readable file with `<`.\n3. Distinguish a filename operand from redirected input.\n4. Combine stdin and stdout redirections deliberately.", "code_snippets": ["$ cat < peanuts.txt\nHello World", "$ wc -l peanuts.txt\n1 peanuts.txt\n$ wc -l < peanuts.txt\n1", "$ cat < peanuts.txt > banana.txt"], "quizzes": [{"id": "stdin-descriptor-number", "question": "Which file descriptor conventionally represents standard input?", "options": [{"text": "`0`", "correct": true, "explanation": "Standard input is conventionally file descriptor 0."}, {"text": "`1`", "correct": false, "explanation": "File descriptor 1 conventionally represents standard output, the stream for regular results."}, {"text": "`2`", "correct": false, "explanation": "File descriptor 2 conventionally represents standard error, not standard input."}]}, {"id": "stdin-from-file", "question": "Which command makes `sort` read its standard input from `names.txt`?", "options": [{"text": "`sort < names.txt`", "correct": true, "explanation": "Bash opens `names.txt` for reading and connects it to `sort` on file descriptor 0."}, {"text": "`sort > names.txt`", "correct": false, "explanation": "A greater-than operator redirects stdout to the file and can truncate it. It does not supply the file as input."}, {"text": "`sort names.txt >`", "correct": false, "explanation": "This includes an incomplete output redirection. It does not express the requested stdin connection."}]}, {"id": "stdin-not-command-argument", "question": "Why does `wc -l < peanuts.txt` normally omit `peanuts.txt` from its output?", "options": [{"text": "`wc` deletes the filename after it finishes counting lines.", "correct": false, "explanation": "The command does not rename or delete the source file. Only its input connection differs."}, {"text": "The `<` operator hides every word printed by the command.", "correct": false, "explanation": "Input redirection does not filter stdout. The filename is absent because `wc` never received it as an argument."}, {"text": "Bash provides the file as stdin rather than as a filename argument.", "correct": true, "explanation": "The shell consumes the redirection and connects the file to descriptor 0, so `wc` is not given the pathname as an operand."}]}, {"id": "stdin-and-stdout-files", "question": "In `cat < input.txt > output.txt`, which file supplies stdin and which receives stdout?", "options": [{"text": "`output.txt` supplies stdin; `input.txt` receives stdout.", "correct": false, "explanation": "This reverses the meanings of the redirection operators. The arrows point toward the command for input and toward the file for output."}, {"text": "`input.txt` supplies stdin; `output.txt` receives stdout.", "correct": true, "explanation": "The `<` redirection opens `input.txt` for descriptor 0, and `>` opens `output.txt` for descriptor 1."}, {"text": "Both files supply stdin, and stdout stays on the terminal.", "correct": false, "explanation": "The two operators affect different standard streams. `>` redirects stdout away from the terminal."}]}]}, {"lesson_id": "stderr-standard-error-redirect", "course_id": "text-fu", "title": "stderr (Standard Error)", "order_index": 3, "description": "Learn how to redirect standard error separately or combine it with standard output in Bash.", "content": "Programs normally write regular results to standard output and diagnostics to a separate stream called standard error, or **stderr**. Keeping the streams separate lets you save useful data without mixing error messages into it.\n\n## Separating Regular Output from Errors\n\nConsider a command whose pathname does not exist:\n\n```bash\n$ ls /fake/directory > peanuts.txt\nls: cannot access '/fake/directory': No such file or directory\n```\n\nThe `>` operator redirects stdout only. The diagnostic is written to stderr, which is still connected to the terminal. Meanwhile, the shell creates or truncates `peanuts.txt` for stdout even though `ls` produces no regular result.\n\nThe standard streams conventionally use these file descriptors:\n\n- `0`: stdin (standard input)\n- `1`: stdout (standard output)\n- `2`: stderr (standard error)\n\n## Redirecting stderr with 2>\n\nPut file descriptor `2` before `>` to redirect stderr:\n\n```bash\n$ ls /fake/directory 2> errors.txt\n```\n\nThe shell creates or truncates `errors.txt` and connects it to descriptor 2. Stdout keeps its previous destination. Use `2>> errors.txt` instead when error output should be appended.\n\n## Combining stdout and stderr\n\nTo place both output streams in one file, first redirect stdout, then duplicate stderr to stdout's current destination:\n\n```bash\n$ ls /fake/directory /etc/passwd > combined.txt 2>&1\n```\n\nRedirections are processed from left to right:\n\n1. `> combined.txt` connects stdout to the file.\n2. `2>&1` connects stderr to wherever stdout points at that moment.\n\nReversing the order changes the result:\n\n```bash\n$ ls /fake/directory /etc/passwd 2>&1 > regular.txt\n```\n\nHere, stderr first duplicates stdout's original terminal destination. Stdout then moves to `regular.txt`, so the two streams end in different places.\n\nBash also provides `&>` as a shorter syntax for replacing a file with both streams:\n\n```bash\n$ ls /fake/directory /etc/passwd &> combined.txt\n```\n\nUse `&>>` to append both streams in Bash. The explicit `> file 2>&1` form is useful to recognize because it also appears in shell scripts and documentation.\n\n## Discarding a Stream Deliberately\n\n`/dev/null` is a special device that discards data written to it. Redirect stderr there only when you have determined that those diagnostics are expected and unnecessary:\n\n```bash\n$ ls /fake/directory 2> /dev/null\n```\n\nThis does not make the command succeed or change its exit status; it only hides the diagnostic stream. During troubleshooting, preserve or display stderr instead of discarding the information you need.\n\nTo practice managing all three standard streams, try this hands-on lab:\n\n1. **[Redirecting Input and Output in Linux](https://labex.io/labs/comptia-redirecting-input-and-output-in-linux-590840)** - In this lab, you will learn to redirect input and output in the Linux shell. You will practice controlling data flow from commands by manipulating standard output (stdout), standard error (stderr), and standard input (stdin) using operators like >, >>, 2>, and the tee command.\n\n## Summary\n\nYou can now keep diagnostics separate or combine them with regular command output.\n\n1. Recognize stderr as file descriptor 2.\n2. Replace or append an error log with `2>` or `2>>`.\n3. Apply multiple redirections from left to right.\n4. Combine both output streams with deliberate syntax.\n5. Discard diagnostics only when their loss is acceptable.", "code_snippets": ["$ ls /fake/directory > peanuts.txt\nls: cannot access '/fake/directory': No such file or directory", "$ ls /fake/directory 2> errors.txt", "$ ls /fake/directory /etc/passwd > combined.txt 2>&1", "$ ls /fake/directory /etc/passwd 2>&1 > regular.txt", "$ ls /fake/directory /etc/passwd &> combined.txt", "$ ls /fake/directory 2> /dev/null"], "quizzes": [{"id": "stderr-not-in-stdout-file", "question": "Why does the error from `ls /missing > results.txt` normally remain on the terminal?", "options": [{"text": "`>` redirects stdout, while the diagnostic is written to stderr.", "correct": true, "explanation": "A plain `>` changes file descriptor 1 only. File descriptor 2 keeps its existing terminal destination."}, {"text": "`ls` waits until the file closes before printing any error.", "correct": false, "explanation": "The issue is not timing. The regular and diagnostic messages use different output streams."}, {"text": "`results.txt` can store regular text but cannot store diagnostics.", "correct": false, "explanation": "An ordinary file can store either stream. The command line simply did not redirect stderr to it."}]}, {"id": "stderr-to-error-file", "question": "Which command replaces `errors.log` with diagnostics from `find /restricted` while leaving stdout on its existing destination?", "options": [{"text": "`find /restricted > errors.log`", "correct": false, "explanation": "A plain `>` redirects descriptor 1, so it captures regular results rather than specifically redirecting diagnostics."}, {"text": "`find /restricted < errors.log`", "correct": false, "explanation": "The less-than operator supplies the file as stdin. It does not capture either output stream."}, {"text": "`find /restricted 2> errors.log`", "correct": true, "explanation": "The leading `2` selects stderr, and `>` creates or truncates the destination for that stream."}]}, {"id": "stderr-combine-order", "question": "Which Bash redirection sends both stdout and stderr from `command` to `all.log`?", "options": [{"text": "`command 2>&1 > all.log`", "correct": false, "explanation": "This first connects stderr to stdout's old destination, then redirects only stdout to the file. The streams end up separated."}, {"text": "`command 2> all.log > /dev/null`", "correct": false, "explanation": "This sends stderr to `all.log` but discards stdout. It does not combine both streams in the file."}, {"text": "`command > all.log 2>&1`", "correct": true, "explanation": "Stdout goes to the file first, and stderr then duplicates that current stdout destination."}]}, {"id": "stderr-bash-short-form", "question": "Which Bash command appends both stdout and stderr from `build` to `build.log`?", "options": [{"text": "`build &> build.log`", "correct": false, "explanation": "Bash `&>` redirects both streams but replaces an existing file instead of appending to it."}, {"text": "`build 2>> build.log`", "correct": false, "explanation": "This appends stderr only. Stdout retains its previous destination."}, {"text": "`build &>> build.log`", "correct": true, "explanation": "In Bash, `&>>` appends file descriptors 1 and 2 to the same destination."}]}, {"id": "stderr-dev-null-effect", "question": "What does `check-data 2> /dev/null` change?", "options": [{"text": "It discards stdout and converts every error into success.", "correct": false, "explanation": "Descriptor 2 is stderr, not stdout, and redirection does not rewrite the program's exit status."}, {"text": "It discards stderr but does not force a successful exit status.", "correct": true, "explanation": "The redirection changes where diagnostics go. The program still determines its own success or failure status."}, {"text": "It saves stderr in a hidden file named `/dev/null`.", "correct": false, "explanation": "`/dev/null` discards written data; it is not a storage file for later recovery."}]}]}, {"lesson_id": "pipe-tee-redirect", "course_id": "text-fu", "title": "pipe and tee", "order_index": 4, "description": "Learn how pipelines connect commands and how tee saves a stream while passing it onward.", "content": "Pipelines connect small commands so data can flow between them without an intermediate file. The `tee` command can copy part of that flow to a file while continuing to send it onward.\n\n## Connecting Commands with |\n\nSuppose a directory listing is too long to read at once:\n\n```bash\n$ ls -la /etc\n```\n\nPlace the pipe operator, `|`, between commands to connect the left command's stdout to the right command's stdin:\n\n```bash\n$ ls -la /etc | less\n```\n\nThe shell starts the pipeline commands and arranges the stream connection. The commands can work concurrently: `less` can begin reading before `ls` has produced its entire listing.\n\n## Keeping stderr Separate\n\nA plain `|` carries stdout only. Stderr from the left command keeps its previous destination, which is often the terminal:\n\n```bash\n$ find /etc -name \"*.conf\" | less\n```\n\nMatching pathnames go through the pipe, while permission diagnostics can still appear directly on the terminal. Redirect stderr separately when you need different behavior:\n\n```bash\n$ find /etc -name \"*.conf\" 2> find-errors.log | less\n```\n\n## Copying a Stream with tee\n\n`tee` reads stdin, writes a copy to each named file, and also writes the same data to stdout:\n\n```bash\n$ ls | tee listing.txt\n```\n\nHere, `listing.txt` receives the listing and `tee`'s stdout remains connected to the terminal. By default, `tee` creates or truncates the named file, just like `>`.\n\nUse `-a` when the file should be appended instead of replaced:\n\n```bash\n$ date | tee -a activity.log\n```\n\n## Saving an Intermediate Result\n\nPut `tee` in the middle of a pipeline to save an intermediate stream and continue processing it:\n\n```bash\n$ ls -la /etc | tee etc-listing.txt | grep \"conf\"\n```\n\nThis pipeline:\n\n1. Produces the complete long listing.\n2. Saves that complete stream in `etc-listing.txt`.\n3. Sends the same stream to `grep`, which prints only lines containing `conf`.\n\nThe file contains the data before `grep` filters it. If you want only the filtered lines in the file, put `tee` after `grep`.\n\nTo practice pipelines and stream copying, try these hands-on labs:\n\n1. **[Redirecting Input and Output in Linux](https://labex.io/labs/comptia-redirecting-input-and-output-in-linux-590840)** - Practice controlling data flow from commands by manipulating standard output (stdout), standard error (stderr), and standard input (stdin) using operators like `>`, `>>`, `2>`, and the `tee` command.\n2. **[Sequence Control and Pipeline](https://labex.io/labs/linux-sequence-control-and-pipeline-17994)** - Learn to control command execution sequences, utilize pipelines, and leverage powerful text processing tools like `cut`, `grep`, `wc`, `sort`, and `uniq`.\n3. **[Data Stream Redirection](https://labex.io/labs/linux-data-stream-redirection-17995)** - Learn the art of Linux stream redirection, including manipulating standard input, output, and error streams, combining outputs, and utilizing `/dev/null`.\n\n## Summary\n\nYou can now connect commands and preserve selected points in a data stream.\n\n1. Pipe stdout from one command into another command's stdin.\n2. Redirect stderr separately when required.\n3. Copy input to both a file and stdout with `tee`.\n4. Append with `tee -a` instead of replacing a file.\n5. Position `tee` before or after a filter deliberately.", "code_snippets": ["$ ls -la /etc", "$ ls -la /etc | less", "$ find /etc -name \"*.conf\" | less", "$ find /etc -name \"*.conf\" 2> find-errors.log | less", "$ ls | tee listing.txt", "$ date | tee -a activity.log", "$ ls -la /etc | tee etc-listing.txt | grep \"conf\""], "quizzes": [{"id": "pipe-stream-connection", "question": "In `ls -la /etc | less`, which streams does `|` connect by default?", "options": [{"text": "`ls` stdin to `less` stdout.", "correct": false, "explanation": "This reverses both the producer and consumer. Data flows from the left command's output to the right command's input."}, {"text": "`ls` stderr to both streams of `less`.", "correct": false, "explanation": "A plain pipe does not connect the left command's stderr, and it does not target both streams of the right command."}, {"text": "`ls` stdout to `less` stdin.", "correct": true, "explanation": "A standard pipeline connects file descriptor 1 of the left command to file descriptor 0 of the right command."}]}, {"id": "pipe-left-stderr", "question": "In `find /etc -name \"*.conf\" | less`, where does `find`'s stderr normally go if no other redirection is present?", "options": [{"text": "Into `less` through the same pipe as stdout.", "correct": false, "explanation": "The ordinary pipe connects stdout only. Stderr is not automatically combined with it."}, {"text": "Into a file named `stderr` in the current directory.", "correct": false, "explanation": "No error-file redirection is present, so the shell does not create such a file."}, {"text": "To its existing destination, usually the terminal.", "correct": true, "explanation": "Because descriptor 2 is unchanged, diagnostics normally remain connected to the terminal."}]}, {"id": "tee-display-and-save", "question": "Which command displays `generate-report` output and also replaces `report.txt` with the same output?", "options": [{"text": "`generate-report > report.txt`", "correct": false, "explanation": "A plain output redirection writes the file but does not keep a copy flowing to the terminal."}, {"text": "`generate-report | tee report.txt`", "correct": true, "explanation": "`tee` copies stdin to `report.txt` and to its stdout, which remains the terminal in this pipeline."}, {"text": "`tee generate-report | report.txt`", "correct": false, "explanation": "This treats `generate-report` as a destination filename and attempts to execute `report.txt` as a command. The producer belongs on the left."}]}, {"id": "tee-append-log", "question": "Which command displays the current date and appends it to `activity.log`?", "options": [{"text": "`date | tee -a activity.log`", "correct": true, "explanation": "The `-a` option makes `tee` append to the file while it continues copying the input to stdout."}, {"text": "`date | tee activity.log`", "correct": false, "explanation": "Without `-a`, `tee` replaces the existing file rather than preserving its earlier entries."}, {"text": "`date > activity.log`", "correct": false, "explanation": "This replaces the file and sends no copy to the terminal. It meets neither the append nor display requirement."}]}, {"id": "tee-before-filter-result", "question": "What does `all.txt` contain after `produce | tee all.txt | grep error` finishes successfully?", "options": [{"text": "Only the lines that `grep` matched.", "correct": false, "explanation": "`tee` runs before `grep`, so it writes the unfiltered input rather than the downstream match set."}, {"text": "Only stderr from `produce`.", "correct": false, "explanation": "A plain pipe carries `produce` stdout. Its stderr is not the input to `tee`."}, {"text": "All stdout produced before filtering.", "correct": true, "explanation": "`tee` saves every byte it receives, then passes that same stream to `grep` for filtering."}]}]}, {"lesson_id": "env-environment", "course_id": "text-fu", "title": "env (Environment)", "order_index": 5, "description": "Learn how Bash expands, exports, inspects, and temporarily overrides environment variables.", "content": "Every process has an environment: a collection of name-value strings inherited from its parent process. Shells use environment variables to pass configuration such as language settings and executable search paths to the programs they start.\n\n## Expanding Variable Values in Bash\n\nBash expands `$NAME` or `${NAME}` to a variable's value before it runs a command. Quote the expansion to preserve the value as one argument:\n\n```bash\n$ printf '%s\\n' \"$HOME\"\n/home/pete\n```\n\nCommon environment variables include:\n\n- `HOME`: The current user's home-directory path.\n- `USER`: A username supplied by the login environment on many systems.\n- `PWD`: The shell's current working directory.\n- `PATH`: Directories searched for command names.\n\nValues depend on the current process environment; they are not universal constants. An unset variable expands to an empty string unless stricter shell behavior is enabled.\n\n## Inspecting the Current Environment\n\nRun `env` without operands to print the environment inherited by that `env` process:\n\n```bash\n$ env\n```\n\nThe output contains `NAME=value` records, for example:\n\n```plaintext\nPATH=/usr/local/sbin:/usr/local/bin:/usr/sbin:/bin\nPWD=/home/user\nUSER=pete\n```\n\nEnvironment variables can contain credentials, tokens, internal paths, or other sensitive data. Do not paste complete `env` output into public issues or logs without reviewing and redacting it.\n\n## Finding Commands through PATH\n\n`PATH` is a colon-separated list of directories that Bash searches when a command name contains no slash:\n\n```bash\n$ printf '%s\\n' \"$PATH\"\n```\n\nThe order matters: Bash uses the first suitable command it finds according to its resolution rules. Use `type -a NAME` to inspect how the current shell resolves a name.\n\nTo add `/opt/coolapp/bin` for the current shell and its future children while retaining the existing search path:\n\n```bash\n$ export PATH=\"/opt/coolapp/bin:$PATH\"\n```\n\nDo not replace `PATH` accidentally with only the new directory, and do not add untrusted writable directories. Either mistake can prevent normal commands from resolving or cause an unexpected executable to run.\n\n## Exporting a Variable to Child Processes\n\nBash variables are not automatically part of the environment given to child processes. Mark a name for export with `export`:\n\n```bash\n$ export TEST=test\n```\n\nThe current Bash process now has a variable named `TEST`, and commands it starts inherit `TEST=test`. A child process cannot use this mechanism to change its parent's environment.\n\n```bash\n$ printenv TEST\ntest\n```\n\nThe assignment normally lasts until you unset it or the shell exits. It does not modify a system-wide environment.\n\n## Setting a Value for One Command\n\nPlace assignments before a command to supply values only to that command's environment:\n\n```bash\n$ LANG=C sort names.txt\n```\n\nThe current shell's `LANG` value is not permanently changed. The `env` utility provides another explicit form:\n\n```bash\n$ env LANG=C sort names.txt\n```\n\nUse `env -i COMMAND` to start a command with an initially empty environment, then add any required assignments. Many programs rely on environment values, so use that option deliberately.\n\n## Loading Personal Values in Future Sessions\n\nTo recreate an exported variable in future interactive Bash sessions, place a suitable `export` line in the startup file those sessions actually read, commonly `~/.bashrc` for interactive non-login Bash:\n\n```bash\nexport TEST=test\n```\n\nZsh commonly uses `~/.zshrc`, while Fish uses different syntax and configuration. Login and non-interactive shells can read other files, so identify the shell and session type instead of assuming one file configures every process.\n\nTo practice environment inheritance and shell configuration, try these hands-on labs:\n\n1. **[Manage Shell Environment and Configuration in Linux](https://labex.io/labs/comptia-manage-shell-environment-and-configuration-in-linux-590838)** - Practice creating and managing local and environment variables, understanding inheritance, and making configurations persistent by modifying the `.bashrc` file.\n2. **[Environment Variables in Linux](https://labex.io/labs/linux-environment-variables-in-linux-385274)** - Learn the concept and usage of environment variables, how to create, modify, and manage them, and their role in system configuration.\n## Summary\n\nYou can now inspect and control the environment passed from Bash to child processes.\n\n1. Expand variable values with deliberate quoting.\n2. Review exported values without exposing secrets.\n3. Preserve and order command directories in `PATH`.\n4. Export a shell variable for future child processes.\n5. Override a value for one command without changing the parent shell.", "code_snippets": ["$ printf '%s\\n' \"$HOME\"\n/home/pete", "$ env", "PATH=/usr/local/sbin:/usr/local/bin:/usr/sbin:/bin\nPWD=/home/user\nUSER=pete", "$ printf '%s\\n' \"$PATH\"", "$ export PATH=\"/opt/coolapp/bin:$PATH\"", "$ export TEST=test", "$ printenv TEST\ntest", "$ LANG=C sort names.txt", "$ env LANG=C sort names.txt", "export TEST=test"], "quizzes": [{"id": "env-print-home-value", "question": "Which Bash command prints the value of `HOME` while preserving it as one argument?", "options": [{"text": "`printf '%s\\n' '$HOME'`", "correct": false, "explanation": "Single quotes prevent parameter expansion, so this prints the literal characters `$HOME`."}, {"text": "`printf '%s\\n' \"$HOME\"`", "correct": true, "explanation": "Bash expands `$HOME` inside double quotes, and `printf` receives the complete value as one argument."}, {"text": "`printf '%s\\n' HOME`", "correct": false, "explanation": "Without a dollar sign or parameter syntax, `HOME` is ordinary text rather than a variable expansion."}]}, {"id": "env-list-exported-values", "question": "Which command prints the environment visible to a newly started process?", "options": [{"text": "`env`", "correct": true, "explanation": "With no command or assignments, `env` prints the name-value environment it received."}, {"text": "`alias`", "correct": false, "explanation": "`alias` lists shell alias definitions, which are shell state rather than exported environment records."}, {"text": "`history`", "correct": false, "explanation": "`history` displays the shell's remembered command lines. It does not enumerate exported variables."}]}, {"id": "env-prepend-path-directory", "question": "Which command adds `/opt/coolapp/bin` before the existing `PATH` for the current Bash process and its future children?", "options": [{"text": "`export PATH=\"/opt/coolapp/bin\"`", "correct": false, "explanation": "This discards every existing search directory, which can make ordinary commands difficult to find."}, {"text": "`export PATH=\"/opt/coolapp/bin:$PATH\"`", "correct": true, "explanation": "This prepends the new directory, retains the previous value, and exports the result for child processes."}, {"text": "`PATH='$PATH:/opt/coolapp/bin'`", "correct": false, "explanation": "Single quotes preserve the literal text `$PATH`, and the assignment is not exported to future child processes."}]}, {"id": "env-export-inheritance", "question": "What is the main effect of `export TEST=test` in Bash?", "options": [{"text": "It writes `TEST` into every user's system configuration.", "correct": false, "explanation": "The assignment affects the current shell and inheritance by its children, not every user or the whole operating system."}, {"text": "It marks `TEST=test` for inheritance by future child processes.", "correct": true, "explanation": "`export` adds the shell variable to the environment Bash passes to commands it starts."}, {"text": "It changes the environment of processes that are already running.", "correct": false, "explanation": "Existing unrelated or child processes keep their own environments. Export affects processes started afterward."}]}, {"id": "env-one-command-value", "question": "Which command runs `sort names.txt` with `LANG=C` without permanently changing the current shell's `LANG`?", "options": [{"text": "`env LANG=C sort names.txt`", "correct": true, "explanation": "`env` adds the assignment to the environment of the command it starts, while the parent shell keeps its prior value."}, {"text": "`export LANG=C; sort names.txt`", "correct": false, "explanation": "This exports `LANG=C` in the current shell and leaves it changed after `sort` finishes."}, {"text": "`env -i sort names.txt`", "correct": false, "explanation": "This starts with an empty environment but does not set the requested `LANG=C` value."}]}]}, {"lesson_id": "cut-command", "course_id": "text-fu", "title": "cut", "order_index": 6, "description": "Learn how to select character positions or delimited fields from each line with cut.", "content": "The `cut` command selects specified character positions or fields from each input line. It works best with consistently structured text whose delimiters and field positions are known.\n\nCreate a small tab-separated file for the examples. `printf` interprets `\\t` as a literal tab and `\\n` as a newline:\n\n```bash\n$ printf 'name\\trole\\nalice\\tadmin\\nbob\\tviewer\\n' > team.tsv\n```\n\n## Selecting Character Positions\n\nUse `-c LIST` to select positions from each line. Positions start at 1:\n\n```bash\n$ cut -c 1 team.tsv\nn\na\nb\n```\n\nThe list can contain individual positions and ranges:\n\n```bash\n$ cut -c 1-4 team.tsv\nname\nalic\nbob\n$ cut -c 1,3 team.tsv\nnm\nai\nbb\n```\n\nSpaces, tabs, and punctuation occupy positions too. `cut` processes each line independently.\n\n## Selecting Tab-Delimited Fields\n\nUse `-f LIST` to select fields. The default delimiter is a tab:\n\n```bash\n$ cut -f 2 team.tsv\nrole\nadmin\nviewer\n```\n\nAs with character selection, a list can include values such as `1`, `1,3`, `2-4`, `-3`, or `2-`.\n\n## Choosing a Custom Delimiter\n\nUse `-d CHARACTER` with `-f` when fields use a delimiter other than a tab. This example creates semicolon-separated data:\n\n```bash\n$ printf 'alice;admin\\nbob;viewer\\n' > team.txt\n$ cut -d ';' -f 1 team.txt\nalice\nbob\n```\n\nThe delimiter for this form is one character. Quote `;` because an unquoted semicolon has control meaning in the shell.\n\n## Handling Lines without the Delimiter\n\nIn field mode, `cut` normally prints a line unchanged when it contains no delimiter. Add `-s` to suppress those lines:\n\n```bash\n$ printf 'alice;admin\\nheader\\nbob;viewer\\n' | cut -s -d ';' -f 2\nadmin\nviewer\n```\n\nThis does not validate a general CSV file. CSV can contain quoted delimiters, embedded newlines, and escaping rules that a single-character split does not understand; use a CSV-aware tool for such data.\n\n## Reading from stdin\n\nWhen no file is named, or when `-` is used as an input operand, `cut` reads stdin. That makes it a natural pipeline stage:\n\n```bash\n$ printf 'red:1\\nblue:2\\n' | cut -d ':' -f 1\nred\nblue\n```\n\nTo practice positional and field selection, try these hands-on labs:\n\n1. **[Linux cut Command: Text Cutting](https://labex.io/labs/linux-linux-cut-command-text-cutting-219187)** - This lab provides a direct, hands-on introduction to the `cut` command, allowing you to practice extracting specific columns or fields from text files, just as discussed in the lesson.\n2. **[Sequence Control and Pipeline](https://labex.io/labs/linux-sequence-control-and-pipeline-17994)** - Enhance your command-line efficiency by learning to control command execution sequences, utilize pipelines, and leverage powerful text processing tools like `cut`, `grep`, `wc`, `sort`, and `uniq`.\n\n## Summary\n\nYou can now select predictable positions from line-oriented text with `cut`.\n\n1. Select individual character positions or ranges.\n2. Extract tab-delimited fields with `-f`.\n3. Supply a one-character delimiter with `-d`.\n4. Suppress undelimited lines when appropriate.\n5. Read structured text from files or stdin.", "code_snippets": ["$ printf 'name\\trole\\nalice\\tadmin\\nbob\\tviewer\\n' > team.tsv", "$ cut -c 1 team.tsv\nn\na\nb", "$ cut -c 1-4 team.tsv\nname\nalic\nbob\n$ cut -c 1,3 team.tsv\nnm\nai\nbb", "$ cut -f 2 team.tsv\nrole\nadmin\nviewer", "$ printf 'alice;admin\\nbob;viewer\\n' > team.txt\n$ cut -d ';' -f 1 team.txt\nalice\nbob", "$ printf 'alice;admin\\nheader\\nbob;viewer\\n' | cut -s -d ';' -f 2\nadmin\nviewer", "$ printf 'red:1\\nblue:2\\n' | cut -d ':' -f 1\nred\nblue"], "quizzes": [{"id": "cut-first-character", "question": "Which command prints the first character from every line of `names.txt`?", "options": [{"text": "`cut -c 1 names.txt`", "correct": true, "explanation": "The `-c` option selects character positions, and position 1 is the first character of each line."}, {"text": "`cut -f 1 names.txt`", "correct": false, "explanation": "The `-f` option selects the first tab-delimited field, which can contain more than one character."}, {"text": "`cut -d 1 names.txt`", "correct": false, "explanation": "The `-d` option specifies a field delimiter and must be paired with field selection. It does not select a character position."}]}, {"id": "cut-second-tab-field", "question": "Which command prints the second tab-delimited field from every line of `team.tsv`?", "options": [{"text": "`cut -c 2 team.tsv`", "correct": false, "explanation": "This selects the second character position from each line, not the second tab-separated field."}, {"text": "`cut -f 2 team.tsv`", "correct": true, "explanation": "Without `-d`, field mode uses a tab delimiter, and `-f 2` selects the second field."}, {"text": "`cut -d 2 team.tsv`", "correct": false, "explanation": "This tries to use `2` as a delimiter but supplies no field list. It does not select field 2."}]}, {"id": "cut-semicolon-role-field", "question": "Which command prints the second semicolon-delimited field from `team.txt`?", "options": [{"text": "`cut -d ':' -f 2 team.txt`", "correct": false, "explanation": "This selects fields separated by colons, but the file uses semicolons."}, {"text": "`cut -d ';' -f 2 team.txt`", "correct": true, "explanation": "The quoted semicolon sets the delimiter, and `-f 2` selects the second field from each line."}, {"text": "`cut -c 2 -f ';' team.txt`", "correct": false, "explanation": "This mixes character selection with an invalid field argument. The delimiter belongs after `-d`, and the field number after `-f`."}]}, {"id": "cut-suppress-undelimited", "question": "What does `-s` do with `cut -d ':' -f 1`?", "options": [{"text": "It sorts the selected fields before printing them.", "correct": false, "explanation": "`cut` does not sort input, and `-s` is unrelated to ordering."}, {"text": "It treats consecutive delimiters as one separator.", "correct": false, "explanation": "`cut` does not use `-s` to collapse delimiters. Empty fields remain meaningful positions."}, {"text": "It suppresses lines that contain no selected delimiter.", "correct": true, "explanation": "In field mode, `-s` prevents undelimited lines from being passed through unchanged."}]}, {"id": "cut-pipeline-input", "question": "In `generate-data | cut -d ':' -f 1`, where does `cut` read its input?", "options": [{"text": "From the stdout of `generate-data` through the pipe.", "correct": true, "explanation": "The pipe connects the producer's stdout to `cut`'s stdin, and no separate input file is named."}, {"text": "From a file whose literal name is `generate-data`.", "correct": false, "explanation": "`generate-data` is executed as the left pipeline command. It is not passed to `cut` as a filename."}, {"text": "From `cut`'s standard error stream.", "correct": false, "explanation": "A normal pipe feeds standard input from the previous command's stdout, not from `cut`'s stderr."}]}]}, {"lesson_id": "paste-command", "course_id": "text-fu", "title": "paste", "order_index": 7, "description": "Learn how to merge corresponding lines or serialize lines with configurable delimiters using paste.", "content": "The `paste` command combines lines as columns. By default, it takes one line from each input file, joins those lines with a tab, and repeats until all inputs reach end-of-file.\n\n## Merging Files Side by Side\n\nCreate two small files:\n\n```bash\n$ printf 'alice\\nbob\\n' > names.txt\n$ printf 'admin\\nviewer\\n' > roles.txt\n```\n\nPass both files to `paste`:\n\n```bash\n$ paste names.txt roles.txt\nalice\tadmin\nbob\tviewer\n```\n\nThe visible spacing between columns is a tab. Unlike `cat`, which writes one complete file after another, `paste` combines corresponding input lines.\n\n `first.txt` contains `A` then `B`, and `second.txt` contains `1` then `2`. What does `paste first.txt second.txt` produce by default?\n\n## Choosing a Delimiter\n\nUse `-d LIST` to replace the default tab separator. For a colon:\n\n```bash\n$ paste -d ':' names.txt roles.txt\nalice:admin\nbob:viewer\n```\n\nQuote delimiters that have shell meaning. `paste` can cycle through multiple delimiter characters when the list contains more than one, but a single character is easiest when building two columns.\n\n## Serializing Lines from One File\n\nThe `-s` option processes each input file serially, joining its lines into one output line. Create a file with one word per line:\n\n```bash\n$ printf 'The\\nquick\\nbrown\\nfox\\n' > words.txt\n$ paste -s words.txt\nThe\tquick\tbrown\tfox\n```\n\nCombine `-s` with `-d` to choose the separator:\n\n```bash\n$ paste -s -d ' ' words.txt\nThe quick brown fox\n```\n\nIf several files are supplied with `-s`, each file becomes its own output line.\n\n## Handling Unequal Input Lengths\n\nWhen parallel input files have different numbers of lines, `paste` continues until the longest file ends. Missing values from a shorter file become empty fields:\n\n```bash\n$ printf 'A\\nB\\nC\\n' > letters.txt\n$ printf '1\\n2\\n' > numbers.txt\n$ paste -d ':' letters.txt numbers.txt\nA:1\nB:2\nC:\n```\n\n## Reading One Input from stdin\n\nUse `-` as a file operand to read that position from stdin:\n\n```bash\n$ printf 'admin\\nviewer\\n' | paste -d ':' names.txt -\nalice:admin\nbob:viewer\n```\n\nTo practice merging line-oriented data, try this hands-on lab:\n\n1. **[Simple Text Processing](https://labex.io/labs/linux-simple-text-processing-18004)** - Learn to use powerful commands like `tr`, `col`, `join`, and `paste` to manipulate and analyze text data efficiently.\n## Summary\n\nYou can now combine line-oriented inputs with predictable alignment and delimiters.\n\n1. Merge corresponding lines from several files.\n2. Replace the default tab separator with `-d`.\n3. Serialize one file's lines with `-s`.\n4. Interpret empty fields from shorter inputs.\n5. Use `-` when one input comes from stdin.", "code_snippets": ["$ printf 'alice\\nbob\\n' > names.txt\n$ printf 'admin\\nviewer\\n' > roles.txt", "$ paste names.txt roles.txt\nalice\tadmin\nbob\tviewer", "$ paste -d ':' names.txt roles.txt\nalice:admin\nbob:viewer", "$ printf 'The\\nquick\\nbrown\\nfox\\n' > words.txt\n$ paste -s words.txt\nThe\tquick\tbrown\tfox", "$ paste -s -d ' ' words.txt\nThe quick brown fox", "$ printf 'A\\nB\\nC\\n' > letters.txt\n$ printf '1\\n2\\n' > numbers.txt\n$ paste -d ':' letters.txt numbers.txt\nA:1\nB:2\nC:", "$ printf 'admin\\nviewer\\n' | paste -d ':' names.txt -\nalice:admin\nbob:viewer"], "quizzes": [{"id": "paste-corresponding-lines", "question": "`first.txt` contains `A` then `B`, and `second.txt` contains `1` then `2`. What does `paste first.txt second.txt` produce by default?", "options": [{"text": "`A`, `B`, `1`, and `2` on four consecutive lines.", "correct": false, "explanation": "That resembles writing the files one after another. `paste` instead combines corresponding lines."}, {"text": "`A`, `B`, `1`, and `2` on one line with no separators.", "correct": false, "explanation": "One-line serialization requires `-s`, and the default separator is a tab rather than nothing."}, {"text": "`A` with `1`, then `B` with `2`, separated by tabs.", "correct": true, "explanation": "Default parallel mode takes one line from each file for every output line and separates the fields with a tab."}]}, {"id": "paste-colon-delimiter", "question": "Which command joins corresponding lines from `names.txt` and `roles.txt` with a colon?", "options": [{"text": "`paste -d ':' names.txt roles.txt`", "correct": true, "explanation": "The `-d` option replaces the default tab with the supplied colon for each pair of fields."}, {"text": "`paste -s ':' names.txt roles.txt`", "correct": false, "explanation": "The `-s` option selects serial mode, and `:` would be treated as another input pathname rather than a delimiter."}, {"text": "`paste names.txt ':' roles.txt`", "correct": false, "explanation": "Without `-d`, every operand is treated as an input file. This would try to open a file named `:`."}]}, {"id": "paste-serialize-with-spaces", "question": "Which command joins every line of `words.txt` into one space-separated output line?", "options": [{"text": "`paste -d ' ' words.txt`", "correct": false, "explanation": "In default parallel mode, a single input file still produces one output line per input line. The delimiter has nothing to join across files."}, {"text": "`paste -s words.txt roles.txt`", "correct": false, "explanation": "This serializes two files separately with the default tab, producing two output lines rather than the requested one-file space-separated result."}, {"text": "`paste -s -d ' ' words.txt`", "correct": true, "explanation": "`-s` serializes the file's lines, and `-d ' '` uses a space between them."}]}, {"id": "paste-unequal-files", "question": "What happens when one file passed to parallel `paste` ends before another?", "options": [{"text": "`paste` uses empty fields for that file until the longest input ends.", "correct": true, "explanation": "Parallel mode continues until all files are exhausted, representing missing lines from shorter inputs as empty fields."}, {"text": "`paste` stops immediately and discards remaining lines.", "correct": false, "explanation": "`paste` continues through the longest input, so remaining lines are not discarded merely because another file ended."}, {"text": "`paste` repeats the shorter file from its beginning.", "correct": false, "explanation": "The command does not cycle input records. An exhausted input contributes empty fields."}]}, {"id": "paste-stdin-operand", "question": "In `producer | paste names.txt -`, what does the `-` operand mean?", "options": [{"text": "Write the merged result to stderr.", "correct": false, "explanation": "A hyphen here identifies an input source. It does not redirect an output stream."}, {"text": "Remove delimiters between the two columns.", "correct": false, "explanation": "Delimiter selection is controlled with `-d`. The hyphen does not change the separator."}, {"text": "Read that input column from stdin.", "correct": true, "explanation": "The hyphen tells `paste` to use its standard input at that operand position."}]}]}, {"lesson_id": "head-command", "course_id": "text-fu", "title": "head", "order_index": 8, "description": "Learn how to display a controlled number of lines or bytes from the beginning of input.", "content": "The `head` command displays the beginning of a file or input stream. It is useful for checking headers, previewing structured data, or sampling output without printing everything.\n\n## Displaying the First Ten Lines\n\nWith no count option, `head` prints the first 10 lines of each named file:\n\n```bash\n$ head events.log\n```\n\nThe file is not modified. If it contains fewer than 10 lines, all available lines are printed.\n\n## Choosing a Line Count\n\nUse `-n NUMBER` to choose how many lines to print:\n\n```bash\n$ head -n 15 events.log\n```\n\nGNU `head` also accepts the compact form `-15`, but `-n 15` states the option's meaning more clearly.\n\n## Choosing a Byte Count\n\nUse `-c NUMBER` when you need bytes rather than complete lines:\n\n```bash\n$ head -c 20 archive.bin\n```\n\nThis prints the first 20 bytes. The output may end in the middle of a text line or, for multibyte text, in the middle of an encoded character. Use line mode for ordinary text previews.\n\n## Reading stdin and Multiple Files\n\nWhen no file operand is supplied, `head` reads stdin:\n\n```bash\n$ generate-report | head -n 5\n```\n\nWhen several files are named, `head` normally adds a header identifying each file's output:\n\n```bash\n$ head -n 2 january.txt february.txt\n==> january.txt <==\n...\n\n==> february.txt <==\n...\n```\n\nUse `-q` to suppress these headers or `-v` to show a header even for one file.\n\nTo practice previewing file beginnings, try these hands-on labs:\n\n1. **[Linux head Command: File Beginning Display](https://labex.io/labs/linux-linux-head-command-file-beginning-display-214302)** - This lab will guide you through using the `head` command to display the initial lines of text files, including modifying the line count.\n2. **[Viewing Log and Configuration Files in Linux](https://labex.io/labs/linux-viewing-log-and-configuration-files-in-linux-387914)** - Practice essential Linux command-line skills for efficiently viewing and navigating text files, including system logs and configuration files, which often require commands like `head`.\n3. **[Rapid Threat Detection](https://labex.io/labs/linux-rapid-threat-detection-387930)** - Apply your knowledge of `head` (and `tail`) to quickly extract and analyze recent log entries, simulating real-world cybersecurity analysis.\n\n## Summary\n\nYou can now preview the beginning of files and command output with `head`.\n\n1. Use the default first-ten-line view.\n2. Select a line count with `-n`.\n3. Select a byte count with `-c` when appropriate.\n4. Read from stdin in a pipeline.\n5. Control headers when several files are shown.", "code_snippets": ["$ head events.log", "$ head -n 15 events.log", "$ head -c 20 archive.bin", "$ generate-report | head -n 5", "$ head -n 2 january.txt february.txt\n==> january.txt <==\n...\n\n==> february.txt <==\n..."], "quizzes": [{"id": "head-default-lines", "question": "What does `head events.log` print by default?", "options": [{"text": "The last 10 lines, or all lines if the file is shorter.", "correct": false, "explanation": "Displaying the end of input is the role of `tail`. `head` selects from the beginning."}, {"text": "The first 10 lines, or all lines if the file is shorter.", "correct": true, "explanation": "Without a count option, `head` selects up to the first ten lines of the input."}, {"text": "Only the first line, regardless of the file length.", "correct": false, "explanation": "One line requires an explicit count such as `-n 1`; the default count is ten."}]}, {"id": "head-five-lines", "question": "Which command displays the first five lines of `report.txt`?", "options": [{"text": "`head -c 5 report.txt`", "correct": false, "explanation": "The `-c` option counts bytes rather than lines, so it may stop within the first line."}, {"text": "`head -n 5 report.txt`", "correct": true, "explanation": "The `-n` option selects a line count, and `5` requests the first five lines."}, {"text": "`tail -n 5 report.txt`", "correct": false, "explanation": "This displays the final five lines of the file, not the beginning."}]}, {"id": "head-first-bytes", "question": "Which command writes the first 100 bytes of `payload.bin` to stdout?", "options": [{"text": "`head -c 100 payload.bin`", "correct": true, "explanation": "The `-c` option selects a byte count, so exactly the available first 100 bytes are requested."}, {"text": "`head -n 100 payload.bin`", "correct": false, "explanation": "The `-n` option counts lines, not bytes. It can produce far more or less than 100 bytes."}, {"text": "`cut -c 100 payload.bin`", "correct": false, "explanation": "This selects position 100 from each line rather than the first 100 bytes of the entire input."}]}, {"id": "head-pipeline-preview", "question": "In `generate-report | head -n 5`, what does `head` read?", "options": [{"text": "The stdout of `generate-report` through stdin.", "correct": true, "explanation": "The pipe connects the producer's stdout to `head`'s stdin, from which the first five lines are selected."}, {"text": "The first five filenames in the current directory.", "correct": false, "explanation": "No directory-listing command is involved. `head` receives a stream through the pipeline."}, {"text": "Five bytes from a file named `generate-report`.", "correct": false, "explanation": "The left side is executed as a command, and `-n` counts lines rather than bytes."}]}, {"id": "head-suppress-filename-headers", "question": "Which option suppresses filename headers when `head` reads several files?", "options": [{"text": "`-v`", "correct": false, "explanation": "The `-v` option requests headers even when only one file is supplied, the opposite of suppression."}, {"text": "`-c`", "correct": false, "explanation": "The `-c` option changes the selection unit to bytes. It does not control filename headers."}, {"text": "`-q`", "correct": true, "explanation": "The `-q` or quiet option prevents `head` from printing per-file header labels."}]}]}, {"lesson_id": "tail-command", "course_id": "text-fu", "title": "tail", "order_index": 9, "description": "Learn how to view the end of input and follow files as new content is appended.", "content": "The `tail` command displays the end of a file or input stream. It can also remain active and show data appended to a file, which is useful when observing logs.\n\n## Displaying the Last Ten Lines\n\nWith no count option, `tail` prints the last 10 lines of each named file:\n\n```bash\n$ tail application.log\n```\n\nIf the file contains fewer than 10 lines, all available lines are printed. The file itself is not changed.\n\n## Choosing a Line or Byte Count\n\nUse `-n NUMBER` to select a different number of final lines:\n\n```bash\n$ tail -n 20 application.log\n```\n\nUse `-c NUMBER` when you need the final bytes instead:\n\n```bash\n$ tail -c 100 payload.bin\n```\n\nByte mode can begin in the middle of a text line or encoded character, so line mode is usually clearer for text.\n\n## Starting at a Particular Line\n\nA count prefixed with `+` changes the meaning: `tail -n +N` starts with line N and prints through the end.\n\n```bash\n$ tail -n +5 report.txt\n```\n\nThis skips the first four lines and begins at line 5. It is useful for removing a known number of header lines from a stream.\n\n## Following Appended Data\n\nWith `-f`, `tail` prints the initial ending and remains active, showing data as it is appended:\n\n```bash\n$ tail -f application.log\n```\n\nPress `Ctrl+C` to interrupt `tail` and return to the shell. Following a file only displays new content; it does not guarantee that the application producing the log is healthy or that every relevant event uses that file.\n\n## Following a Rotated Log by Name\n\nLog rotation can rename an old file and create a new file at the original pathname. GNU `tail -F` is equivalent to following by name while retrying, so it can reopen a file that is replaced or temporarily missing:\n\n```bash\n$ tail -F application.log\n```\n\nUse `-f` when following the currently opened file is the desired behavior, and `-F` when a named log is expected to rotate. These are GNU behaviors; other implementations can differ.\n\n On GNU/Linux, which option is better suited to following `application.log` across common rename-and-recreate log rotation?\n\nWhen no file is named, `tail` reads stdin, so it can select the end of command output. Multiple named files receive identifying headers by default, as with `head`.\n\nTo practice viewing and following file endings, try these hands-on labs:\n\n1. **[Linux tail Command: File End Display](https://labex.io/labs/linux-linux-tail-command-file-end-display-214303)** - Learn the Linux `tail` command for viewing and monitoring the end of text files, including the `-f` option for real-time updates.\n2. **[Viewing Log and Configuration Files in Linux](https://labex.io/labs/linux-viewing-log-and-configuration-files-in-linux-387914)** - Practice using `tail` (along with `cat` and `more`) to efficiently view and navigate log and configuration files, which is crucial for system monitoring.\n3. **[Rapid Threat Detection](https://labex.io/labs/linux-rapid-threat-detection-387930)** - Apply your knowledge of `tail` to quickly extract and analyze recent log entries, simulating rapid threat detection in a cybersecurity context.\n\n## Summary\n\nYou can now inspect file endings and observe newly appended content with `tail`.\n\n1. Display the final ten lines by default.\n2. Select a line or byte count explicitly.\n3. Start output at a numbered line with `-n +N`.\n4. Follow appended content with `-f` and stop with `Ctrl+C`.\n5. Use GNU `-F` when a named log may be rotated.", "code_snippets": ["$ tail application.log", "$ tail -n 20 application.log", "$ tail -c 100 payload.bin", "$ tail -n +5 report.txt", "$ tail -f application.log", "$ tail -F application.log"], "quizzes": [{"id": "tail-default-lines", "question": "What does `tail application.log` display by default?", "options": [{"text": "Up to the initial 10 lines of the file.", "correct": false, "explanation": "The beginning of a file is selected by `head`. `tail` works from the end."}, {"text": "Every line added after the command starts.", "correct": false, "explanation": "Continuous following requires `-f` or a related option. Plain `tail` prints a snapshot and exits."}, {"text": "Up to the final 10 lines of the file.", "correct": true, "explanation": "Without a count option, `tail` selects the final ten lines, or every line when fewer are available."}]}, {"id": "tail-twenty-lines", "question": "Which command displays the final 20 lines of `application.log`?", "options": [{"text": "`tail -n 20 application.log`", "correct": true, "explanation": "The `-n` option selects a line count, and `tail` takes those lines from the end."}, {"text": "`head -n 20 application.log`", "correct": false, "explanation": "This selects 20 lines from the beginning rather than from the end."}, {"text": "`tail -c 20 application.log`", "correct": false, "explanation": "The `-c` option selects the final 20 bytes, which is not the same as 20 lines."}]}, {"id": "tail-start-line-five", "question": "Which command prints `report.txt` starting with line 5?", "options": [{"text": "`tail -n +5 report.txt`", "correct": true, "explanation": "The `+5` count tells `tail` to begin at line 5 and continue through the end."}, {"text": "`tail -n 5 report.txt`", "correct": false, "explanation": "Without a plus sign, this selects the final five lines, regardless of their absolute line numbers."}, {"text": "`head -n +5 report.txt`", "correct": false, "explanation": "This is not the `tail` start-at-line form. Use `tail -n +5` for the requested range."}]}, {"id": "tail-follow-file", "question": "Which command shows the current end of `application.log` and keeps waiting for appended content?", "options": [{"text": "`tail -f application.log`", "correct": true, "explanation": "The `-f` option keeps `tail` active and displays data appended to the file."}, {"text": "`tail -n 0 application.log`", "correct": false, "explanation": "This initially prints no lines and exits because no follow option is present."}, {"text": "`less application.log`", "correct": false, "explanation": "`less` provides interactive paging, but this form does not remain in `tail`-style follow mode."}]}, {"id": "tail-follow-rotated-name", "question": "On GNU/Linux, which option is better suited to following `application.log` across common rename-and-recreate log rotation?", "options": [{"text": "`-n`", "correct": false, "explanation": "The `-n` option changes the number of lines displayed. It does not retry a replaced pathname."}, {"text": "`-c`", "correct": false, "explanation": "The `-c` option changes the selection unit to bytes. It does not provide rotation-aware following."}, {"text": "`-F`", "correct": true, "explanation": "GNU `-F` follows by name and retries, allowing `tail` to reopen a log that is replaced or temporarily absent."}]}]}, {"lesson_id": "expand-unexpand-command", "course_id": "text-fu", "title": "expand and unexpand", "order_index": 10, "description": "Learn how tab stops control conversion between tabs and spaces with expand and unexpand.", "content": "Tabs store movement to a tab stop rather than a fixed number of visible spaces. Their displayed width depends on the current column and the tab-stop settings. The `expand` and `unexpand` commands convert between tab characters and spaces while accounting for those positions.\n\n## Converting Tabs to Spaces\n\n`expand` reads input, replaces tabs with the spaces needed to reach the appropriate tab stops, and writes the result to stdout:\n\n```bash\n$ expand sample.txt\n```\n\nBy default, tab stops occur every 8 columns. A tab at column 1 therefore expands differently from a tab at column 6; it is not always replaced by eight spaces.\n\n With default settings, how does `expand` replace a tab character?\n\n## Choosing Tab Stops\n\nUse `-t NUMBER` to place tab stops every specified number of columns. For four-column stops:\n\n```bash\n$ expand -t 4 sample.txt\n```\n\nGNU `expand` also accepts a comma-separated list of explicit tab positions. Use `-i` when only tabs before the first nonblank character on each line should be converted.\n\n## Saving Converted Output Safely\n\n`expand` does not edit its input file. Redirect stdout to a different pathname when you want to save the converted text:\n\n```bash\n$ expand sample.txt > result.txt\n```\n\nDo not use `expand sample.txt > sample.txt`. The shell truncates the destination before `expand` can read it, so the source data can be lost. After verifying a separately written result, you can deliberately replace the original using an appropriate file-management step.\n\n## Converting Spaces to Tabs\n\n`unexpand` replaces eligible spaces with tabs while preserving alignment at the selected tab stops. By default, GNU `unexpand` converts only initial blanks before the first nonblank character on a line:\n\n```bash\n$ unexpand result.txt\n```\n\nUse `-a` to consider suitable blanks throughout each line:\n\n```bash\n$ unexpand -a result.txt\n```\n\nThis does not simply replace every run of eight spaces. Conversion depends on column positions and tab stops, just as it does for `expand`. Use `-t 4` or another tab-stop specification when the file follows a different convention.\n\n Without `-a`, which spaces does GNU `unexpand` normally consider for conversion?\n\nBoth commands read stdin when no file is named, so they can be used in pipelines. Remember that converting to spaces and back may not reconstruct the original choice of tabs and spaces even when the displayed alignment is unchanged.\n\n## Summary\n\nYou can now convert tabs and spaces while preserving tab-stop alignment.\n\n1. Expand tabs to the next configured stop.\n2. Set custom tab stops with `-t`.\n3. Save output to a different file before replacing an input.\n4. Convert leading blanks with `unexpand` by default.\n5. Use `-a` when blanks throughout each line should be considered.", "code_snippets": ["$ expand sample.txt", "$ expand -t 4 sample.txt", "$ expand sample.txt > result.txt", "$ unexpand result.txt", "$ unexpand -a result.txt"], "quizzes": [{"id": "expand-default-tab-stops", "question": "With default settings, how does `expand` replace a tab character?", "options": [{"text": "It inserts enough spaces to reach the next default tab stop.", "correct": true, "explanation": "`expand` preserves tab-stop alignment by calculating the spaces needed from the current column."}, {"text": "It always inserts exactly eight spaces.", "correct": false, "explanation": "Default stops are eight columns apart, but the number of spaces depends on the current column."}, {"text": "It removes the tab without adding any characters.", "correct": false, "explanation": "The command replaces the tab with spaces so later text remains aligned at the selected tab stop."}]}, {"id": "expand-four-column-stops", "question": "Which command converts tabs using tab stops every four columns?", "options": [{"text": "`expand -i 4 sample.txt`", "correct": false, "explanation": "The `-i` option limits conversion to initial tabs and does not take `4` as the tab-stop interval."}, {"text": "`unexpand -t 4 sample.txt`", "correct": false, "explanation": "`unexpand` converts suitable spaces to tabs, the reverse direction from the requested operation."}, {"text": "`expand -t 4 sample.txt`", "correct": true, "explanation": "The `-t` option sets the tab-stop interval, and `4` requests stops every four columns."}]}, {"id": "expand-safe-output-file", "question": "Which command saves expanded text without truncating `sample.txt` before it is read?", "options": [{"text": "`expand sample.txt > sample.txt`", "correct": false, "explanation": "The shell opens and truncates `sample.txt` for output before starting `expand`, which can erase the input."}, {"text": "`expand sample.txt > result.txt`", "correct": true, "explanation": "The input and output pathnames differ, so the shell can create `result.txt` without destroying the source."}, {"text": "`> sample.txt expand result.txt`", "correct": false, "explanation": "This still truncates `sample.txt` and does not express a safe conversion from the original file."}]}, {"id": "unexpand-default-scope", "question": "Without `-a`, which spaces does GNU `unexpand` normally consider for conversion?", "options": [{"text": "Every group of spaces anywhere in the file.", "correct": false, "explanation": "Considering blanks throughout the line requires `-a`, and conversion still depends on tab-stop positions."}, {"text": "Only spaces that appear after the final word.", "correct": false, "explanation": "The default scope concerns initial blanks, not specifically trailing whitespace."}, {"text": "Only initial blanks before the first nonblank character.", "correct": true, "explanation": "Default GNU `unexpand` behavior is limited to leading blank space on each line."}]}, {"id": "unexpand-all-blanks", "question": "Which option tells GNU `unexpand` to consider blanks after the first nonblank character too?", "options": [{"text": "`-i`", "correct": false, "explanation": "For `expand`, `-i` limits work to initial tabs. It is not the all-blanks option for `unexpand`."}, {"text": "`-a`", "correct": true, "explanation": "The `-a` option enables conversion of suitable blanks throughout each input line."}, {"text": "`-t`", "correct": false, "explanation": "The `-t` option sets tab stops. Although GNU behavior can imply broader conversion with it, `-a` explicitly requests all blanks."}]}]}, {"lesson_id": "join-split-command", "course_id": "text-fu", "title": "join and split", "order_index": 11, "description": "Learn how to join two sorted text files by a key and split one file into named pieces.", "content": "The `join` and `split` commands solve different file-processing problems. `join` combines related records from two sorted text inputs, while `split` divides one input into a sequence of smaller files.\n\n## Joining Two Files by Their First Field\n\nBy default, `join` compares the first blank-separated field in exactly two input files. Consider these already sorted files:\n\n`people.txt`:\n\n```text\n1 John\n2 Jane\n3 Mary\n```\n\n`surnames.txt`:\n\n```text\n1 Doe\n2 Doe\n3 Sue\n```\n\nJoin records whose key fields are equal:\n\n```bash\n$ join people.txt surnames.txt\n1 John Doe\n2 Jane Doe\n3 Mary Sue\n```\n\nThe output contains the shared key once, then the remaining fields from the first and second files. `join` processes two files at a time; it does not accept three ordinary file operands as a three-way relational join.\n\n With no field options, which records does `join first.txt second.txt` combine?\n\n## Sorting the Join Keys\n\nEach input must be ordered by its join field using compatible comparison rules. For default field 1, prepare copies with `sort -k 1,1`:\n\n```bash\n$ LC_ALL=C sort -k 1,1 people-raw.txt > people.txt\n$ LC_ALL=C sort -k 1,1 surnames-raw.txt > surnames.txt\n$ LC_ALL=C join people.txt surnames.txt\n```\n\nUsing the same locale for sorting and joining keeps collation rules consistent. Do not redirect a sort back to its own input pathname, because the shell would truncate that file first.\n\n## Selecting Different Join Fields\n\nUse `-1 FIELD` for the first file's key and `-2 FIELD` for the second file's key. Suppose the first input contains:\n\n```text\nJohn 1\nJane 2\nMary 3\n```\n\nThe second contains:\n\n```text\n1 Doe\n2 Doe\n3 Sue\n```\n\nAfter sorting the first file by field 2 and the second by field 1, run:\n\n```bash\n$ join -1 2 -2 1 people.txt surnames.txt\n1 John Doe\n2 Jane Doe\n3 Mary Sue\n```\n\nUse `-t CHARACTER` when a single nonblank character, such as `:`, separates fields. Options such as `-a 1` or `-a 2` can include unpaired lines from one input; the default output contains matched keys only.\n\n## Splitting by Line Count\n\n`split` writes consecutive portions of one input to separate output files. It is not the inverse of a key-based `join` operation.\n\n```bash\n$ split large.txt\n```\n\nThe default GNU behavior writes up to 1000 lines per output file and uses the prefix `x`, producing names such as `xaa`, `xab`, and `xac`.\n\nUse `-l NUMBER` to choose a line count and add a final operand to choose the output prefix:\n\n```bash\n$ split -l 500 large.txt part-\n```\n\nThis produces `part-aa`, `part-ab`, and so on, with at most 500 lines in each piece.\n\n## Splitting by Size\n\nUse `-b SIZE` to divide input by byte size. GNU suffixes such as `K`, `M`, and `G` represent powers of 1024 in this context:\n\n```bash\n$ split -b 10M archive.bin chunk-\n```\n\nThis requests pieces of 10 mebibytes except for a potentially smaller final piece. `split` does not create an archive manifest or reassembly metadata; preserve the ordering of suffixes and concatenate the pieces in order when reconstruction is appropriate.\n\nTo practice keyed joins and structured data processing, try these hands-on labs:\n\n1. **[Linux join Command: File Joining](https://labex.io/labs/linux-linux-join-command-file-joining-219193)** - This lab provides a direct, hands-on introduction to the `join` command, allowing you to practice merging lines from two sorted text files based on a common field, just as discussed in the lesson.\n2. **[Processing Employees Data](https://labex.io/labs/linux-processing-employees-data-388132)** - Apply your knowledge of `join` and other powerful Linux command-line utilities like `awk` to combine and process data from multiple sources, simulating a real-world data analysis scenario.\n## Summary\n\nYou can now combine sorted records or divide one input into ordered pieces.\n\n1. Join exactly two files by equal key fields.\n2. Sort both inputs consistently by their join keys.\n3. Select nondefault key fields with `-1` and `-2`.\n4. Split by line count with `-l`.\n5. Split by byte size with `-b` and a clear prefix.", "code_snippets": ["1 John\n2 Jane\n3 Mary", "1 Doe\n2 Doe\n3 Sue", "$ join people.txt surnames.txt\n1 John Doe\n2 Jane Doe\n3 Mary Sue", "$ LC_ALL=C sort -k 1,1 people-raw.txt > people.txt\n$ LC_ALL=C sort -k 1,1 surnames-raw.txt > surnames.txt\n$ LC_ALL=C join people.txt surnames.txt", "John 1\nJane 2\nMary 3", "1 Doe\n2 Doe\n3 Sue", "$ join -1 2 -2 1 people.txt surnames.txt\n1 John Doe\n2 Jane Doe\n3 Mary Sue", "$ split large.txt", "$ split -l 500 large.txt part-", "$ split -b 10M archive.bin chunk-"], "quizzes": [{"id": "join-default-key", "question": "With no field options, which records does `join first.txt second.txt` combine?", "options": [{"text": "Lines whose first blank-separated fields are equal.", "correct": true, "explanation": "Default `join` behavior compares field 1 from each of the two sorted inputs."}, {"text": "Lines that occupy the same physical line number.", "correct": false, "explanation": "Matching is based on key-field values, not simply on record positions."}, {"text": "Every line from the first file with every line from the second.", "correct": false, "explanation": "`join` emits records for matching keys rather than an unrestricted Cartesian product of all lines."}]}, {"id": "join-sort-requirement", "question": "What preparation does `join` normally require for reliable matching?", "options": [{"text": "Both files must contain exactly the same number of physical lines.", "correct": false, "explanation": "Input lengths can differ. Key matches, not equal line counts, determine joined output."}, {"text": "Both files must use filenames that sort next to each other alphabetically.", "correct": false, "explanation": "The content keys need sorting; the lexical relationship between the two filenames is irrelevant."}, {"text": "Both files must be sorted by their respective join fields with compatible ordering.", "correct": true, "explanation": "`join` advances through ordered keys, so each input must use an ordering consistent with the comparison it performs."}]}, {"id": "join-different-fields", "question": "Which options join field 2 of the first file to field 1 of the second?", "options": [{"text": "`-1 1 -2 2`", "correct": false, "explanation": "This selects field 1 from the first input and field 2 from the second, the reverse of the requested mapping."}, {"text": "`-1 2 -2 1`", "correct": true, "explanation": "`-1 2` chooses field 2 from file one, and `-2 1` chooses field 1 from file two."}, {"text": "`-f 2 -d 1`", "correct": false, "explanation": "Those resemble field and delimiter options from other text tools. They are not the `join` field selectors."}]}, {"id": "split-lines-with-prefix", "question": "Which command splits `large.txt` into pieces of at most 500 lines named with the prefix `part-`?", "options": [{"text": "`split -b 500 large.txt part-`", "correct": false, "explanation": "The `-b` option selects bytes, so these pieces would be far smaller than 500 lines in ordinary text."}, {"text": "`split -l 500 large.txt part-`", "correct": true, "explanation": "`-l 500` sets the maximum line count, and the final operand supplies the output filename prefix."}, {"text": "`join -l 500 large.txt part-`", "correct": false, "explanation": "`join` combines keyed records from two files. It does not divide one input into pieces."}]}, {"id": "split-ten-mebibytes", "question": "Which command splits `archive.bin` into pieces of 10 MiB using the prefix `chunk-`?", "options": [{"text": "`split -l 10M archive.bin chunk-`", "correct": false, "explanation": "The `-l` option expects a line count, not a byte-size suffix for binary chunks."}, {"text": "`join -b 10M archive.bin chunk-`", "correct": false, "explanation": "`join` does not split binary input or accept this piece-size operation."}, {"text": "`split -b 10M archive.bin chunk-`", "correct": true, "explanation": "The `-b` option selects piece size, `10M` requests 10\u00d71024\u00d71024 bytes, and `chunk-` is the output prefix."}]}]}, {"lesson_id": "sort-command", "course_id": "text-fu", "title": "sort", "order_index": 12, "description": "Learn how to order text lines by lexical, numeric, or selected field values with sort.", "content": "The `sort` command reads complete lines, orders them according to selected comparison rules, and writes the result to stdout. It does not change an input file unless you explicitly choose an output operation.\n\n## Sorting Complete Lines\n\nConsider `animals.txt`:\n\n```text\ndog\ncow\ncat\nelephant\nbird\n```\n\nSort the lines in ascending order:\n\n```bash\n$ sort animals.txt\nbird\ncat\ncow\ndog\nelephant\n```\n\nText ordering follows the current locale, which can affect case, accents, and punctuation. Use a consistent locale such as `LC_ALL=C` when a script requires reproducible byte-oriented collation:\n\n```bash\n$ LC_ALL=C sort animals.txt\n```\n\n## Reversing the Result\n\nAdd `-r` to reverse the comparison result:\n\n```bash\n$ sort -r animals.txt\nelephant\ndog\ncow\ncat\nbird\n```\n\n## Comparing Numbers\n\nLexical order compares characters, so `10` normally comes before `2`. Use `-n` for ordinary numeric comparison:\n\n```bash\n$ printf '10\\n2\\n30\\n' | sort -n\n2\n10\n30\n```\n\nCombine options when needed. `sort -nr scores.txt` compares numerically and places larger values first.\n\n## Sorting by a Field\n\nUse `-k START[,END]` to choose a key. Fields are separated by runs of blanks by default. For colon-separated records, use `-t ':'`:\n\n```bash\n$ printf 'alice:30\\nbob:8\\ncarol:20\\n' | sort -t ':' -k 2,2n\nbob:8\ncarol:20\nalice:30\n```\n\nHere, `-t ':'` selects the delimiter, `-k 2,2` limits the key to field 2, and the attached `n` compares that key numerically. Without the ending `,2`, a key starting at field 2 normally continues through the rest of the line.\n\n## Removing Duplicates and Saving Output\n\nUse `-u` to output one line for each equal comparison key:\n\n```bash\n$ sort -u names.txt\n```\n\nThis both sorts and removes duplicates under the selected comparison rules. If you only want to remove adjacent duplicates from already sorted data, the `uniq` command covered later can do that.\n\nTo write the result to a file, ordinary redirection is fine when the destination differs from the input:\n\n```bash\n$ sort names.txt > names-sorted.txt\n```\n\nDo not run `sort names.txt > names.txt`; the shell truncates the input before `sort` reads it. GNU `sort -o names.txt names.txt` safely arranges its own output when you intentionally want the same pathname:\n\n```bash\n$ sort -o names.txt names.txt\n```\n\nKeep a backup or write and verify a separate result when the original data matters.\n\n On GNU/Linux, which command asks `sort` to safely write the sorted result back to `names.txt` without shell redirection truncating it first?\n\nTo practice ordering and analyzing line-oriented data, try these hands-on labs:\n\n1. **[Linux sort Command: Text Sorting](https://labex.io/labs/linux-linux-sort-command-text-sorting-219196)** - This lab provides a direct introduction to the `sort` command, allowing you to practice sorting lines of text files in various ways, including ascending and descending order.\n2. **[Word Count and Sorting](https://labex.io/labs/linux-word-count-and-sorting-388125)** - In this challenge, you'll apply your knowledge of sorting along with word counting to analyze text data, helping you find frequent patterns and sort data efficiently.\n\n## Summary\n\nYou can now choose comparison rules and destinations for sorted text.\n\n1. Sort complete lines under an explicit locale when reproducibility matters.\n2. Reverse results with `-r`.\n3. Compare numeric values with `-n`.\n4. Select a bounded field key with `-t` and `-k`.\n5. Remove duplicates or save output without truncating the input.", "code_snippets": ["dog\ncow\ncat\nelephant\nbird", "$ sort animals.txt\nbird\ncat\ncow\ndog\nelephant", "$ LC_ALL=C sort animals.txt", "$ sort -r animals.txt\nelephant\ndog\ncow\ncat\nbird", "$ printf '10\\n2\\n30\\n' | sort -n\n2\n10\n30", "$ printf 'alice:30\\nbob:8\\ncarol:20\\n' | sort -t ':' -k 2,2n\nbob:8\ncarol:20\nalice:30", "$ sort -u names.txt", "$ sort names.txt > names-sorted.txt", "$ sort -o names.txt names.txt"], "quizzes": [{"id": "sort-lines-ascending", "question": "What does `sort animals.txt` do with no key or numeric option?", "options": [{"text": "Orders complete input lines according to the current locale.", "correct": true, "explanation": "Default `sort` compares whole lines using the active locale's collation rules."}, {"text": "Orders words within each line but keeps the line order fixed.", "correct": false, "explanation": "`sort` treats each line as a record. It does not rearrange words inside individual lines."}, {"text": "Rewrites `animals.txt` in place automatically.", "correct": false, "explanation": "The sorted result goes to stdout by default, and the input file remains unchanged."}]}, {"id": "sort-reverse-order", "question": "Which command sorts `animals.txt` in reverse order?", "options": [{"text": "`sort -n animals.txt`", "correct": false, "explanation": "The `-n` option requests numeric comparison. It does not mean reverse order."}, {"text": "`sort -u animals.txt`", "correct": false, "explanation": "The `-u` option suppresses duplicate keys. It does not reverse the output."}, {"text": "`sort -r animals.txt`", "correct": true, "explanation": "The `-r` option reverses the ordering chosen by the other comparison rules."}]}, {"id": "sort-numbers-descending", "question": "Which command sorts numeric lines in `scores.txt` from largest to smallest?", "options": [{"text": "`sort -n scores.txt`", "correct": false, "explanation": "Numeric comparison is selected, but the default direction places smaller values first."}, {"text": "`sort -nr scores.txt`", "correct": true, "explanation": "`-n` selects numeric comparison and `-r` reverses it, producing descending numeric order."}, {"text": "`sort -r scores.txt`", "correct": false, "explanation": "This reverses text collation but does not request numeric comparison, so values such as `10` and `2` can order unexpectedly."}]}, {"id": "sort-second-colon-field", "question": "Which command sorts `users.txt` numerically by only its second colon-separated field?", "options": [{"text": "`sort -n -k 1,1 users.txt`", "correct": false, "explanation": "This uses default blank-separated fields and selects field 1, not the second colon-separated field."}, {"text": "`cut -d ':' -f 2 users.txt`", "correct": false, "explanation": "`cut` extracts field 2 but does not sort the original records by that key."}, {"text": "`sort -t ':' -k 2,2n users.txt`", "correct": true, "explanation": "The colon sets field boundaries, `2,2` confines the key to field 2, and `n` gives that key numeric comparison."}]}, {"id": "sort-safe-same-file", "question": "On GNU/Linux, which command asks `sort` to safely write the sorted result back to `names.txt` without shell redirection truncating it first?", "options": [{"text": "`sort -o names.txt names.txt`", "correct": true, "explanation": "GNU `sort` manages `-o` output after reading as needed, so the shell does not pre-truncate the input through `>`."}, {"text": "`sort names.txt > names.txt`", "correct": false, "explanation": "The shell truncates `names.txt` before starting `sort`, so the command can lose the input."}, {"text": "`sort -u names.txt`", "correct": false, "explanation": "This writes unique sorted lines to stdout and leaves the input file unchanged."}]}]}, {"lesson_id": "tr-translate-command", "course_id": "text-fu", "title": "tr (Translate)", "order_index": 13, "description": "Learn how to translate, delete, and squeeze character sets in a standard input stream.", "content": "The `tr` command, short for translate, translates, deletes, or squeezes characters read from stdin. It does not accept ordinary input-file operands, so use a pipe or input redirection to provide data.\n\nThe basic syntax is:\n\n```bash\ntr [OPTIONS] SET1 [SET2]\n```\n\n`tr` works with character sets rather than words or general regular expressions. Use another tool when a transformation depends on a complete word, line structure, or surrounding context.\n\n## Translating Characters\n\nWith two sets, characters from `SET1` map by position to characters in `SET2`:\n\n```bash\n$ echo \"hello world\" | tr a-z A-Z\nHELLO WORLD\n```\n\nHere, lowercase range positions map to corresponding uppercase positions. Quote set expressions so the shell passes them unchanged.\n\nYou can also translate one character to another:\n\n```bash\n$ echo \"2026-06-23\" | tr '-' '/'\n2026/06/23\n```\n\n```bash\n$ echo \"abc123\" | tr 'abc' 'ABC'\nABC123\n```\n\nCharacters not in `SET1` pass through unchanged.\n\n## Deleting Characters\n\nUse `-d` with one set to remove every matching character:\n\n```bash\n$ echo \"My address is 123 Main Street\" | tr -d '0-9'\nMy address is  Main Street\n```\n\nEvery digit is removed independently; `tr` is not identifying a complete number token.\n\nCharacter classes can describe groups defined by the current locale:\n\n```bash\n$ echo \"Hello, world!\" | tr -d '[:punct:]'\nHello world\n```\n\nDeleting newlines joins input lines without inserting a replacement separator:\n\n```bash\n$ printf \"one\\ntwo\\nthree\\n\" | tr -d '\\n'\nonetwothree\n```\n\n## Squeezing Repeated Characters\n\nUse `-s SET` to replace each run of a listed character with one instance of that character:\n\n```bash\n$ echo \"Hello      World,   how   are   you?\" | tr -s ' '\nHello World, how are you?\n```\n\nThis set contains an ordinary space, so tabs and newlines are not squeezed by that command.\n\nYou can squeeze repeated newlines too:\n\n```bash\n$ printf \"one\\n\\n\\nTwo\\n\" | tr -s '\\n'\none\nTwo\n```\n\n## Using Character Classes and Complements\n\nCharacter classes make intent clearer than hand-written ranges in many locales. Common classes include:\n\n- `[:lower:]`: Lowercase letters.\n- `[:upper:]`: Uppercase letters.\n- `[:digit:]`: Digits.\n- `[:alpha:]`: Letters.\n- `[:alnum:]`: Letters and digits.\n- `[:space:]`: Whitespace characters.\n- `[:punct:]`: Punctuation characters.\n\nFor example, convert lowercase text to uppercase with character classes:\n\n```bash\n$ echo \"linux journey\" | tr '[:lower:]' '[:upper:]'\nLINUX JOURNEY\n```\n\nThe `-c` option complements `SET1`, meaning every character not in the set. Combine it with `-d` to retain only selected kinds of characters:\n\n```bash\n$ echo \"user@example.com!\" | tr -cd '[:alnum:]'\nuserexamplecom\n```\n\nThis also removes the newline because a newline is not alphanumeric. Add or preserve separators deliberately when record boundaries matter.\n\n## Building Stream Transformations\n\nSeveral `tr` processes can be connected when transformations are clearer as separate stages:\n\n```bash\n$ echo \"Hello,,,     world!!!\" | tr -d '[:punct:]' | tr -s ' '\nHello world\n```\n\nFor simple tab-separated input, translate tab characters into commas:\n\n```bash\n$ printf \"name\\tlevel\\npete\\tbeginner\\n\" | tr '\\t' ','\nname,level\npete,beginner\n```\n\nBecause `tr` reads stdin, a file can be provided with `<`:\n\n```bash\n$ tr '[:lower:]' '[:upper:]' < names.txt\n```\n\nRedirect stdout to a different file if you need to save the result. Do not redirect back to the input pathname, because the shell would truncate it before `tr` reads.\n\nTo practice character-level stream transformations, try this hands-on lab:\n\n1. **[Linux tr Command: Character Translating](https://labex.io/labs/linux-linux-tr-command-character-translating-219198)** - Learn the Linux `tr` command for character-level transformations in text streams. You'll practice translating characters, deleting specific characters, working with character classes, and squeezing repeated characters.\n\n## Summary\n\nYou can now transform character streams with focused `tr` operations.\n\n1. Map characters between corresponding sets.\n2. Delete selected characters with `-d`.\n3. Squeeze repeated characters with `-s`.\n4. Use locale-aware classes and complements deliberately.\n5. Supply input through stdin rather than a filename operand.", "code_snippets": ["tr [OPTIONS] SET1 [SET2]", "$ echo \"hello world\" | tr a-z A-Z\nHELLO WORLD", "$ echo \"2026-06-23\" | tr '-' '/'\n2026/06/23", "$ echo \"abc123\" | tr 'abc' 'ABC'\nABC123", "$ echo \"My address is 123 Main Street\" | tr -d '0-9'\nMy address is  Main Street", "$ echo \"Hello, world!\" | tr -d '[:punct:]'\nHello world", "$ printf \"one\\ntwo\\nthree\\n\" | tr -d '\\n'\nonetwothree", "$ echo \"Hello      World,   how   are   you?\" | tr -s ' '\nHello World, how are you?", "$ printf \"one\\n\\n\\nTwo\\n\" | tr -s '\\n'\none\nTwo", "$ echo \"linux journey\" | tr '[:lower:]' '[:upper:]'\nLINUX JOURNEY", "$ echo \"user@example.com!\" | tr -cd '[:alnum:]'\nuserexamplecom", "$ echo \"Hello,,,     world!!!\" | tr -d '[:punct:]' | tr -s ' '\nHello world", "$ printf \"name\\tlevel\\npete\\tbeginner\\n\" | tr '\\t' ','\nname,level\npete,beginner", "$ tr '[:lower:]' '[:upper:]' < names.txt"], "quizzes": [{"id": "tr-map-characters", "question": "What does `printf '%s\\n' 'abc123' | tr 'abc' 'ABC'` print?", "options": [{"text": "`ABCABC`", "correct": false, "explanation": "Digits are not members of the source set, so `tr` does not replace them with letters."}, {"text": "`ABC123`", "correct": true, "explanation": "Each of `a`, `b`, and `c` maps to the character at the same position in `ABC`; the digits are unchanged."}, {"text": "`abc123ABC`", "correct": false, "explanation": "`tr` translates matching input characters. It does not append the destination set to the stream."}]}, {"id": "tr-squeeze-spaces", "question": "Which command reduces every run of ordinary spaces in stdin to one space?", "options": [{"text": "`tr -s ' '`", "correct": true, "explanation": "The `-s` option squeezes repeated members of the supplied set, which contains one ordinary space."}, {"text": "`tr -d ' '`", "correct": false, "explanation": "The `-d` option removes all ordinary spaces rather than preserving one per run."}, {"text": "`tr ' ' ''`", "correct": false, "explanation": "An empty translation set is not the clear, portable way to request squeezing. Use `-s` for repeated characters."}]}, {"id": "tr-keep-alphanumeric", "question": "What does `tr -cd '[:alnum:]'` do to stdin?", "options": [{"text": "Deletes alphanumeric characters and keeps everything else.", "correct": false, "explanation": "The complement changes which characters `-d` targets. The alphanumeric set itself is retained."}, {"text": "Deletes every character that is not alphanumeric.", "correct": true, "explanation": "`-c` complements the alphanumeric set, and `-d` deletes the resulting non-alphanumeric set."}, {"text": "Converts every letter and digit to uppercase.", "correct": false, "explanation": "No destination translation set is present, so this command does not perform case conversion."}]}]}, {"lesson_id": "uniq-unique-command", "course_id": "text-fu", "title": "uniq (Unique)", "order_index": 14, "description": "Learn how to collapse, count, or filter adjacent groups of equal lines with uniq.", "content": "The `uniq` command compares each input line with the preceding line. It can collapse, count, or select groups of adjacent equal lines, but it does not search the entire file for separated duplicates.\n\n## Collapsing Adjacent Duplicate Lines\n\nSuppose `reading.txt` contains grouped values:\n\n```plaintext\nbook\nbook\npaper\npaper\narticle\narticle\nmagazine\n```\n\nRun `uniq` with no filtering option to print one representative line from each adjacent group:\n\n```bash\n$ uniq reading.txt\nbook\npaper\narticle\nmagazine\n```\n\nThe input file remains unchanged because the result goes to stdout.\n\n## Counting Adjacent Groups\n\nUse `-c` to prefix each output group with its number of consecutive input lines:\n\n```bash\n$ uniq -c reading.txt\n      2 book\n      2 paper\n      2 article\n      1 magazine\n```\n\nThese are run lengths, not global totals unless all equal lines have first been made adjacent.\n\n## Selecting Unique or Repeated Groups\n\nUse `-u` to print only groups containing exactly one line:\n\n```bash\n$ uniq -u reading.txt\nmagazine\n```\n\nUse `-d` to print one representative line from each adjacent group containing more than one line:\n\n```bash\n$ uniq -d reading.txt\nbook\npaper\narticle\n```\n\nGNU `uniq -D` prints every line from repeated groups, whereas lowercase `-d` prints each repeated group's value once.\n\n## Grouping Separated Duplicates\n\nIf equal lines are separated, they form different groups:\n\n```plaintext\nbook\npaper\nbook\npaper\narticle\nmagazine\narticle\n```\n\nRunning `uniq` on this file will produce a surprising result:\n\n```bash\n$ uniq reading.txt\nbook\npaper\nbook\npaper\narticle\nmagazine\narticle\n```\n\nNo lines are collapsed because neighboring values differ. Sort first when changing the order is acceptable and you want equal complete lines grouped together:\n\n```bash\n$ sort reading.txt | uniq\narticle\nbook\nmagazine\npaper\n```\n\nUse a consistent locale and comparison policy across both steps. `sort -u reading.txt` can also sort and retain one line per equal sort key in a single command.\n\n Equal lines are scattered through `reading.txt`, and output order may change. Which pipeline produces one sorted copy of each distinct complete line?\n\n`uniq` reads stdin when no input file is named, which is why it fits naturally after `sort`. GNU options such as `-i` can ignore case, while `-f`, `-s`, and `-w` can skip or limit comparison regions; use them only when equality should be defined by part of each line.\n\nTo practice grouping, counting, and filtering duplicates, try these hands-on labs:\n\n1. **[Linux uniq Command: Duplicate Filtering](https://labex.io/labs/linux-linux-uniq-command-duplicate-filtering-219199)** - Learn how to use the Linux `uniq` command in combination with `sort` to identify, filter, and analyze duplicate lines in text files.\n2. **[Linux sort Command: Text Sorting](https://labex.io/labs/linux-linux-sort-command-text-sorting-219196)** - Practice using the `sort` command to organize lines of text files, a crucial step before using `uniq` effectively.\n3. **[Word Count and Sorting](https://labex.io/labs/linux-word-count-and-sorting-388125)** - Learn the essential Linux text processing tools `wc` (word count) and `sort` in this hands-on challenge. Learn to count lines, words, and characters, find frequent patterns, and sort data efficiently for various text analysis tasks.\n\n## Summary\n\nYou can now analyze adjacent groups of equal lines with `uniq`.\n\n1. Collapse each adjacent duplicate group to one line.\n2. Count consecutive occurrences with `-c`.\n3. Select singleton groups with `-u`.\n4. Select repeated groups with `-d` or GNU `-D`.\n5. Sort first when separated duplicates must be grouped.", "code_snippets": ["book\nbook\npaper\npaper\narticle\narticle\nmagazine", "$ uniq reading.txt\nbook\npaper\narticle\nmagazine", "$ uniq -c reading.txt\n      2 book\n      2 paper\n      2 article\n      1 magazine", "$ uniq -u reading.txt\nmagazine", "$ uniq -d reading.txt\nbook\npaper\narticle", "book\npaper\nbook\npaper\narticle\nmagazine\narticle", "$ uniq reading.txt\nbook\npaper\nbook\npaper\narticle\nmagazine\narticle", "$ sort reading.txt | uniq\narticle\nbook\nmagazine\npaper"], "quizzes": [{"id": "uniq-collapse-adjacent", "question": "What does `uniq reading.txt` do by default?", "options": [{"text": "Sorts the complete file and then removes every repeated value.", "correct": false, "explanation": "`uniq` preserves input order and does not sort. Separated copies remain separate groups."}, {"text": "Prints one line from each adjacent group of equal lines.", "correct": true, "explanation": "Default `uniq` collapses consecutive equal lines into one output line."}, {"text": "Deletes duplicate lines directly from `reading.txt`.", "correct": false, "explanation": "The command writes filtered text to stdout by default and does not edit the input file."}]}, {"id": "uniq-count-groups", "question": "What does the count from `uniq -c` represent?", "options": [{"text": "The number of characters in each input line.", "correct": false, "explanation": "Character counting is not the purpose of `uniq -c`; tools such as `wc` handle character and byte totals."}, {"text": "The number of consecutive equal lines in each group.", "correct": true, "explanation": "`-c` prefixes each collapsed adjacent group with the number of lines it contained."}, {"text": "The total number of matching lines anywhere in the file.", "correct": false, "explanation": "Separated equal lines form separate groups unless the data is sorted or otherwise grouped first."}]}, {"id": "uniq-only-singletons", "question": "Which command prints only adjacent groups that occur exactly once?", "options": [{"text": "`uniq -c reading.txt`", "correct": false, "explanation": "This prints every group with a count, including repeated and singleton groups."}, {"text": "`uniq -d reading.txt`", "correct": false, "explanation": "Lowercase `-d` prints one line for each repeated group, the opposite selection."}, {"text": "`uniq -u reading.txt`", "correct": true, "explanation": "The `-u` option selects groups whose adjacent run length is exactly one."}]}, {"id": "uniq-one-per-duplicate-group", "question": "Which command prints one line for each adjacent group that appears more than once?", "options": [{"text": "`uniq -d reading.txt`", "correct": true, "explanation": "The `-d` option selects repeated adjacent groups and emits one representative line per group."}, {"text": "`uniq -D reading.txt`", "correct": false, "explanation": "GNU uppercase `-D` prints all lines belonging to repeated groups, not only one representative."}, {"text": "`uniq -u reading.txt`", "correct": false, "explanation": "The `-u` option selects singleton groups rather than repeated ones."}]}, {"id": "uniq-separated-duplicates", "question": "Equal lines are scattered through `reading.txt`, and output order may change. Which pipeline produces one sorted copy of each distinct complete line?", "options": [{"text": "`sort reading.txt | uniq`", "correct": true, "explanation": "Sorting groups equal complete lines, then `uniq` collapses each adjacent group to one line."}, {"text": "`uniq reading.txt | sort`", "correct": false, "explanation": "`uniq` runs before equal separated lines become adjacent, so later sorting can leave duplicate output lines."}, {"text": "`uniq -c reading.txt | head`", "correct": false, "explanation": "This counts existing adjacent groups and then limits output. It does not globally group separated duplicates."}]}]}, {"lesson_id": "nl-wc-command", "course_id": "text-fu", "title": "wc and nl", "order_index": 15, "description": "Learn how to count lines, words, bytes, or characters with wc and number lines with nl.", "content": "The `wc` command counts properties of text streams, while `nl` writes input with generated line numbers. Both read files or stdin and send their results to stdout.\n\n## Reading the Default wc Output\n\nWith no count option, `wc` prints the number of newline characters, words, and bytes, followed by the filename when one was supplied:\n\n```bash\n$ printf 'red blue\\ngreen\\n' > colors.txt\n$ wc colors.txt\n 2  3 15 colors.txt\n```\n\nFrom left to right:\n\n1. `2` newline characters, reported as lines.\n2. `3` whitespace-delimited words.\n3. `15` bytes in this ASCII example.\n\nA final text line without a terminating newline is not counted by `wc -l`, because that option counts newline characters rather than visually perceived lines.\n\n## Requesting One Count\n\nSelect only the measurement you need:\n\n- `-l`: Count newline characters.\n- `-w`: Count words.\n- `-c`: Count bytes.\n- `-m`: Count characters according to the current locale.\n\nFor example:\n\n```bash\n$ wc -w colors.txt\n3 colors.txt\n```\n\nByte and character counts are equal for ASCII text but can differ for multibyte encodings such as UTF-8. When stdin is used without a filename operand, `wc` normally omits a filename label:\n\n```bash\n$ printf 'one two\\n' | wc -w\n2\n```\n\nWhen multiple files are named, `wc` prints one result per file and a `total` line. GNU `wc -L` reports the maximum display width of an input line.\n\n## Numbering Nonempty Lines with nl\n\nBy default, `nl` numbers nonempty lines in the logical body of its input. Suppose `notes.txt` contains a blank second line:\n\n```text\nalpha\n\nbeta\n```\n\nThe blank line is preserved but receives no number:\n\n```bash\n$ nl notes.txt\n\t 1\talpha\n\n\t 2\tbeta\n```\n\n`nl` writes numbered output; it does not modify `notes.txt`.\n\n## Numbering Every Line\n\nUse `-ba` to select body numbering style `a`, which numbers all lines:\n\n```bash\n$ nl -ba notes.txt\n\t 1\talpha\n\t 2\n\t 3\tbeta\n```\n\nOther options control formatting. For example, `-w 3` sets the number field width and `-s ': '` changes the separator after the number.\n\nTo practice counting and numbering text, try these hands-on labs:\n\n1. **[Linux wc Command: Text Counting](https://labex.io/labs/linux-linux-wc-command-text-counting-219200)** - Practice counting words, lines, and characters in text files using the `wc` command.\n2. **[Linux nl Command: Line Numbering](https://labex.io/labs/linux-linux-nl-command-line-numbering-210988)** - Learn to number lines in text files with the `nl` command.\n3. **[Word Count and Sorting](https://labex.io/labs/linux-word-count-and-sorting-388125)** - Apply your knowledge of `wc` to count lines, words, and characters, and combine it with sorting for practical text analysis tasks.\n\n## Summary\n\nYou can now measure text streams and add visible line numbers without editing the source.\n\n1. Interpret the default lines, words, and bytes columns from `wc`.\n2. Select one count with `-l`, `-w`, `-c`, or `-m`.\n3. Distinguish byte counts from character counts.\n4. Number nonempty lines with default `nl` behavior.\n5. Number blank lines too with `nl -ba`.", "code_snippets": ["$ printf 'red blue\\ngreen\\n' > colors.txt\n$ wc colors.txt\n 2  3 15 colors.txt", "$ wc -w colors.txt\n3 colors.txt", "$ printf 'one two\\n' | wc -w\n2", "alpha\n\nbeta", "$ nl notes.txt\n\t 1\talpha\n\n\t 2\tbeta", "$ nl -ba notes.txt\n\t 1\talpha\n\t 2\n\t 3\tbeta"], "quizzes": [{"id": "wc-default-columns", "question": "In the default output from `wc file.txt`, what do the first three numbers represent?", "options": [{"text": "Lines, words, and bytes, in that order.", "correct": true, "explanation": "Default `wc` output reports newline count, word count, and byte count before the filename."}, {"text": "Bytes, words, and lines, in that order.", "correct": false, "explanation": "These are the same measurements in the wrong order. The line count appears first."}, {"text": "Files, characters, and paragraphs, in that order.", "correct": false, "explanation": "The default columns do not count files or paragraphs, and the third default measurement is bytes."}]}, {"id": "wc-word-count-only", "question": "Which command reports only the word count for `essay.txt`?", "options": [{"text": "`wc -l essay.txt`", "correct": false, "explanation": "The `-l` option reports newline characters, not words."}, {"text": "`wc -w essay.txt`", "correct": true, "explanation": "The `-w` option selects the word count measurement."}, {"text": "`wc -c essay.txt`", "correct": false, "explanation": "The `-c` option reports bytes rather than whitespace-delimited words."}]}, {"id": "wc-characters-not-bytes", "question": "Which option asks `wc` to count characters rather than bytes in the current locale?", "options": [{"text": "`-m`", "correct": true, "explanation": "The `-m` option reports characters, which may differ from bytes for multibyte text."}, {"text": "`-c`", "correct": false, "explanation": "The `-c` option reports bytes. One character can occupy several bytes in encodings such as UTF-8."}, {"text": "`-w`", "correct": false, "explanation": "The `-w` option counts words rather than characters or bytes."}]}, {"id": "nl-default-blank-lines", "question": "How does `nl notes.txt` handle blank body lines by default?", "options": [{"text": "It omits each blank line from the output entirely.", "correct": false, "explanation": "The blank line remains in the output, but it is not assigned a number by default."}, {"text": "It preserves them without line numbers.", "correct": true, "explanation": "Default body style numbers nonempty lines and passes blank lines through unnumbered."}, {"text": "It numbers them in the same sequence as nonempty lines.", "correct": false, "explanation": "Numbering every body line requires a different style such as `-ba`."}]}, {"id": "nl-number-all-lines", "question": "Which command numbers every body line in `notes.txt`, including blank lines?", "options": [{"text": "`nl -w 3 notes.txt`", "correct": false, "explanation": "This changes the number field width but keeps the default nonempty-line numbering rule."}, {"text": "`nl -ba notes.txt`", "correct": true, "explanation": "The `-b` option chooses the body style, and style `a` numbers all body lines."}, {"text": "`wc -l notes.txt`", "correct": false, "explanation": "This prints a count of newline characters and does not reproduce the file with line numbers."}]}]}, {"lesson_id": "grep-command", "course_id": "text-fu", "title": "grep", "order_index": 16, "description": "Learn how to select lines with fixed strings or regular expressions and interpret grep results.", "content": "The `grep` command selects input lines that match a pattern. It can search named files or stdin, print matching context, count selected lines, and communicate whether a match was found through its exit status.\n\n## Matching Lines in a File\n\nPass a pattern followed by one or more input files:\n\n```bash\n$ grep 'fox' sample.txt\n```\n\nBy default, GNU `grep` interprets the pattern as a basic regular expression and prints every selected line. Quote patterns to keep spaces and shell metacharacters from being interpreted by the shell first.\n\nUse `-F` when the pattern should be treated as a fixed string rather than a regular expression:\n\n```bash\n$ grep -F 'price: $5.00' products.txt\n```\n\n## Selecting Pattern Syntax\n\nGNU `grep` offers three commonly used pattern modes:\n\n- Default: Basic regular expressions.\n- `-E`: Extended regular expressions, including operators such as `|`, `+`, and `?` without backslashes.\n- `-F`: Fixed strings with no regular-expression operators.\n\nAnchors such as `^` and `$` match the beginning and end of a line. To match filenames ending in the literal suffix `.txt` within a text list:\n\n```bash\n$ grep -E '\\.txt$' filenames.txt\n```\n\nThe backslash makes the dot literal; an unescaped `.` in a regular expression matches any single character.\n\n## Supplying Patterns Safely\n\nUse `-e PATTERN` to supply a pattern explicitly. This is particularly useful when the pattern begins with `-`, because quoting alone does not stop option parsing:\n\n```bash\n$ grep -e '-v' settings.conf\n```\n\nYou can repeat `-e` to select lines matching any supplied pattern. Use `-f patterns.txt` to read one pattern per line from a file.\n\n## Controlling Selected Output\n\n- `-i`: Ignore case distinctions.\n- `-n`: Prefix selected lines with line numbers.\n- `-v`: Select lines that do not match.\n- `-c`: Print the count of selected lines for each input file.\n- `-o`: Print only each nonempty matching part rather than the full selected line.\n\nFor example, count lines containing `fox`, ignoring case:\n\n```bash\n$ grep -ic 'fox' sample.txt\n```\n\n`-c` counts selected lines, not the total number of matches within those lines. A line containing `fox fox` contributes one to the count. When you specifically need nonoverlapping match occurrences with GNU `grep`, `grep -o PATTERN | wc -l` is one possible pipeline.\n\n `data.txt` has one line containing `error error` and two lines with no match. What does `grep -c 'error' data.txt` report?\n\n## Filtering stdin and Searching Directories\n\nWhen no input file is named, `grep` reads stdin and fits naturally in a pipeline:\n\n```bash\n$ env | grep '^USER='\n```\n\nUse `-r` to search readable files recursively below a directory:\n\n```bash\n$ grep -r 'listen_port' config/\n```\n\nDiagnostics such as permission errors go to stderr and are not matching input. Narrow the search path and understand permissions rather than immediately elevating access.\n\n## Interpreting the Exit Status\n\nFor ordinary searches, GNU `grep` returns status `0` when at least one line is selected, `1` when no line is selected, and `2` for an error. This lets scripts test for a match without treating \u201cno match\u201d as the same condition as an unreadable file or invalid pattern.\n\nOptions such as `-q` suppress normal output and stop after a match is found, which is useful for condition checks. Do not infer success from empty display alone: `-q`, redirection, no match, and an error can all produce little or no stdout, while their statuses differ.\n\nTo practice fixed-string and regular-expression searches, try these hands-on labs:\n\n1. **[Search Text with grep in Linux](https://labex.io/labs/comptia-search-text-with-grep-in-linux-590841)** - Practice basic searches, display line numbers, use anchors, and harness both basic and extended regular expressions for complex pattern matching with `grep`.\n2. **[Linux grep Command: Pattern Searching](https://labex.io/labs/linux-linux-grep-command-pattern-searching-219192)** - Learn to use `grep` for searching and matching patterns within text files, and explore regular expressions to define complex search patterns.\n3. **[Needle in the Haystack](https://labex.io/labs/linux-needle-in-the-haystack-388109)** - Learn the power of the `grep` command to search for specific patterns, count occurrences, extract unique values, and combine multiple search criteria across various log files.\n\n## Summary\n\nYou can now search line-oriented text and distinguish matches from errors.\n\n1. Choose basic, extended, or fixed-string matching.\n2. Quote patterns and use `-e` for a leading hyphen.\n3. Count selected lines without confusing them with occurrences.\n4. Filter stdin or recursively search a focused directory.\n5. Interpret exit statuses for match, no match, and error.", "code_snippets": ["$ grep 'fox' sample.txt", "$ grep -F 'price: $5.00' products.txt", "$ grep -E '\\.txt$' filenames.txt", "$ grep -e '-v' settings.conf", "$ grep -ic 'fox' sample.txt", "$ env | grep '^USER='", "$ grep -r 'listen_port' config/"], "quizzes": [{"id": "grep-fixed-string", "question": "Which command searches `products.txt` for the literal text `price: $5.00` without treating pattern characters as regular-expression syntax?", "options": [{"text": "`grep -F 'price: $5.00' products.txt`", "correct": true, "explanation": "`-F` selects fixed-string matching, and single quotes protect the dollar sign from shell expansion."}, {"text": "`grep -E 'price: $5.00' products.txt`", "correct": false, "explanation": "`-E` enables extended regular expressions, where `$` and `.` have special meanings rather than being literal."}, {"text": "`grep -v 'price: $5.00' products.txt`", "correct": false, "explanation": "`-v` selects nonmatching lines and still uses regular-expression interpretation by default."}]}, {"id": "grep-literal-txt-suffix", "question": "Which extended regular expression matches lines ending with the literal suffix `.txt`?", "options": [{"text": "`'.txt$'`", "correct": false, "explanation": "The dot is unescaped, so it matches any one character before `txt`, not specifically a literal period."}, {"text": "`'\\.txt$'`", "correct": true, "explanation": "`\\.` matches a literal period and `$` anchors the match at the end of the line."}, {"text": "`'^.txt'`", "correct": false, "explanation": "This anchors at the beginning and still uses an unescaped dot, so it expresses a different match."}]}, {"id": "grep-hyphen-pattern", "question": "Which command searches `settings.conf` for the pattern `-v` rather than interpreting it as an option?", "options": [{"text": "`grep '-v' settings.conf`", "correct": false, "explanation": "Quotes protect characters from shell expansion, but `grep` can still interpret the resulting `-v` argument as its invert-match option."}, {"text": "`grep -v settings.conf`", "correct": false, "explanation": "This enables inverted matching and does not supply `settings.conf` as both a pattern and an input in the requested way."}, {"text": "`grep -e '-v' settings.conf`", "correct": true, "explanation": "The `-e` option declares that the following argument is a pattern even though it starts with a hyphen."}]}, {"id": "grep-count-lines", "question": "`data.txt` has one line containing `error error` and two lines with no match. What does `grep -c 'error' data.txt` report?", "options": [{"text": "`2`, because the word occurs twice on one line.", "correct": false, "explanation": "`-c` counts selected lines, not individual matches within a line."}, {"text": "`1`, because exactly one line matches.", "correct": true, "explanation": "The single line is selected once even though the pattern appears twice within it."}, {"text": "`3`, because the file contains three lines total.", "correct": false, "explanation": "Only selected lines contribute to `grep -c`; nonmatching lines are excluded."}]}, {"id": "grep-pipeline-input", "question": "In `generate-report | grep 'failed'`, what input does `grep` search?", "options": [{"text": "A file named `generate-report` in the current directory.", "correct": false, "explanation": "The left-hand word is executed as a command and is not passed to `grep` as a file operand."}, {"text": "The stdout stream produced by `generate-report`.", "correct": true, "explanation": "The pipe connects the producer's stdout to `grep`'s stdin."}, {"text": "The stderr stream produced by `generate-report`.", "correct": false, "explanation": "A plain pipe carries stdout. Stderr remains separate unless it is explicitly redirected."}]}]}]}, {"course_id": "advanced-text-fu", "course_name": "Advanced Text-Fu", "icon": "\ud83c\udfaf", "description": "Vim editor mastery, Emacs buffers, and Regular Expressions.", "lessons_count": 13, "quizzes_count": 65, "lessons": [{"lesson_id": "regular-expressions-regex", "course_id": "advanced-text-fu", "title": "regex (Regular Expressions)", "order_index": 1, "description": "Learn how anchors, character sets, repetition, and regex flavors control text pattern matching.", "content": "Regular expressions, often shortened to **regex**, describe text patterns. Tools such as `grep`, `sed`, and `awk` use regex, but their supported syntax can differ, so always identify the tool and regex flavor.\n\nGNU `grep` uses basic regular expressions (BRE) by default and extended regular expressions (ERE) with `-E`. This lesson introduces constructs shared by both, then notes common ERE additions.\n\nUse this input in the examples:\n\n```text\nsally sells seashells\nby the seashore\n```\n\n## Matching Literal Text\n\nMost ordinary characters match themselves. The pattern `seashells` selects a line containing that exact sequence anywhere:\n\n```bash\n$ grep 'seashells' sample.txt\nsally sells seashells\n```\n\nQuote regex patterns so the shell does not expand or split them before the matching tool receives them. Regex is also different from shell pathname expansion: in a regex, `*` repeats the preceding atom; in a shell glob, `*` is itself a wildcard for a string of pathname characters.\n\n## Anchoring a Match\n\nOutside a bracket expression, `^` at the beginning of a pattern anchors the match at the beginning of a line:\n\n```plaintext\n^by\n```\n\nThe `$` anchor matches at the end of a line:\n\n```plaintext\nseashore$\n```\n\nCombine both anchors when the entire line must fit the pattern:\n\n```text\n^by the seashore$\n```\n\n## Matching One Character\n\nThe dot matches one character in ordinary line-oriented regex mode:\n\n```plaintext\nb.\n```\n\nThis matches `by`, but it could also match `ba` or `b7`. It does not match a lone `b` because one character is required after it. To match a literal period, escape it as `\\.` or place it in a suitable bracket expression.\n\n## Using Bracket Expressions\n\nA bracket expression matches one character from a specified set:\n\n```plaintext\ns[ae]lls\n```\n\nThis matches `sells` or `salls` at that position.\n\nWhen `^` is the first character after `[`, it negates the set:\n\n```plaintext\ns[^e]lls\n```\n\nThis matches `salls` but not `sells` because the character after the first `s` cannot be `e`.\n\nRanges can describe characters between endpoints:\n\n```plaintext\nd[a-c]g\n```\n\nThis can match `dag`, `dbg`, or `dcg`. Range behavior can depend on locale collation. Character classes such as `[[:lower:]]`, `[[:upper:]]`, and `[[:digit:]]` often express intent more clearly.\n\n## Repeating and Combining Patterns\n\nIn both BRE and ERE, `*` means zero or more repetitions of the preceding atom:\n\n```text\nseashells*\n```\n\nThis matches `seashell` followed by zero or more additional `s` characters. In ERE mode with `grep -E`, common operators include:\n\n- `+`: One or more repetitions.\n- `?`: Zero or one repetition.\n- `|`: Either the expression on the left or the right.\n- `(...)`: Group expressions.\n\nFor example:\n\n```bash\n$ grep -E '^(cat|dog)s?$' animals.txt\n```\n\nThis selects complete lines equal to `cat`, `cats`, `dog`, or `dogs`. In BRE mode, these operators have different escaping rules, so do not copy a pattern between flavors without checking it.\n\nTo practice regex selection with Linux text tools, try these hands-on labs:\n\n1. **[Search Text with grep in Linux](https://labex.io/labs/comptia-search-text-with-grep-in-linux-590841)** - In this lab, you will learn to search for text in files on a Linux system using the `grep` command. You will perform basic searches, display line numbers, use anchors like `^` and `$` to match line positions, and harness both basic and extended regular expressions for complex pattern matching.\n2. **[Text Processing and Regular Expressions](https://labex.io/labs/linux-text-processing-and-regular-expressions-18003)** - Learn the powerful text processing tools grep, sed, and awk. Learn to use regular expressions for efficient text manipulation and pattern matching in Linux.\n3. **[Extracting Mails and Numbers](https://labex.io/labs/linux-extracting-mails-and-numbers-17991)** - In this challenge, you will learn how to use grep and regular expressions to extract email addresses and numbers from a file, demonstrating essential Linux text processing skills.\n\n## Summary\n\nYou can now read and build foundational line-oriented regular expressions.\n\n1. Distinguish regex operators from shell pathname wildcards.\n2. Anchor matches at the beginning or end of a line.\n3. Match one character with a dot or bracket expression.\n4. Negate sets and use locale-aware character classes.\n5. Choose BRE or ERE syntax deliberately.", "code_snippets": ["sally sells seashells\nby the seashore", "$ grep 'seashells' sample.txt\nsally sells seashells", "^by", "seashore$", "^by the seashore$", "b.", "s[ae]lls", "s[^e]lls", "d[a-c]g", "seashells*", "$ grep -E '^(cat|dog)s?$' animals.txt"], "quizzes": [{"id": "regex-versus-shell-star", "question": "What does `*` do in a regular expression such as `ab*`?", "options": [{"text": "It matches any filename in the current directory.", "correct": false, "explanation": "That describes shell pathname expansion in a command context, not the meaning of `*` inside a regex."}, {"text": "It repeats the preceding `b` zero or more times.", "correct": true, "explanation": "A regex quantifier applies to the atom immediately before it, so `ab*` matches `a`, `ab`, `abb`, and so on."}, {"text": "It repeats the complete string `ab` exactly two times.", "correct": false, "explanation": "The star applies only to the preceding atom and allows zero or more repetitions, not exactly two complete-string repetitions."}]}, {"id": "regex-complete-line", "question": "Which pattern matches only a line whose complete text is `by the seashore`?", "options": [{"text": "`^by the seashore$`", "correct": true, "explanation": "The caret requires the match to start at the beginning, and the dollar sign requires it to end with the line."}, {"text": "`by the seashore`", "correct": false, "explanation": "Without anchors, this sequence can match inside a longer line with additional text before or after it."}, {"text": "`$by the seashore^`", "correct": false, "explanation": "The end anchor cannot precede the text that must match, and the start anchor cannot follow it in this intended pattern."}]}, {"id": "regex-dot-character", "question": "Which string is not matched by the complete-line pattern `^b.$`?", "options": [{"text": "`by`", "correct": false, "explanation": "The dot matches `y`, so the two-character line satisfies the pattern."}, {"text": "`b`", "correct": true, "explanation": "The dot requires one character after `b`, but this string ends immediately."}, {"text": "`b7`", "correct": false, "explanation": "The dot matches the digit `7`, so this two-character line satisfies the pattern."}]}, {"id": "regex-negated-bracket", "question": "What does `[^e]` match?", "options": [{"text": "Exactly one character other than `e`.", "correct": true, "explanation": "A leading caret inside brackets complements the listed set, while the bracket expression still consumes one character."}, {"text": "The beginning of a line followed by `e`.", "correct": false, "explanation": "Inside a bracket expression, a leading caret negates the set rather than anchoring a line."}, {"text": "Zero or more occurrences of the letter `e`.", "correct": false, "explanation": "Repetition would require a quantifier such as `*`; this bracket expression matches one non-`e` character."}]}, {"id": "regex-extended-alternation", "question": "Which command enables extended regex syntax for the pattern `^(cat|dog)s?$`?", "options": [{"text": "`grep -F '^(cat|dog)s?$' animals.txt`", "correct": false, "explanation": "`-F` treats every regex operator as literal text, so grouping, alternation, and optional repetition are disabled."}, {"text": "`grep -E '^(cat|dog)s?$' animals.txt`", "correct": true, "explanation": "`-E` selects extended regular expressions, enabling the shown grouping, alternation, and optional `s`."}, {"text": "`grep '^(cat|dog)s?$' animals.txt`", "correct": false, "explanation": "Default grep uses BRE, where these unescaped grouping and alternation characters do not have the intended ERE meanings."}]}]}, {"lesson_id": "text-editors-vim-or-emacs", "course_id": "advanced-text-fu", "title": "Text Editors", "order_index": 2, "description": "Learn how to choose and configure a terminal text editor for Linux administration and development.", "content": "Linux configuration, scripts, source code, and logs are commonly stored as plain text. A terminal editor lets you work with those files in a local terminal, a remote SSH session, or an environment without a graphical desktop.\n\n## Choosing an Editor for the Environment\n\nNo single editor is best for every person or task. Graphical editors, terminal editors, and integrated development environments can all be appropriate. For command-line work, choose an editor that is installed, that you can exit safely, and whose basic editing model you understand.\n\nDo not assume Vim or Emacs is installed. Check command resolution in the current shell:\n\n```bash\n$ command -v vim\n/usr/bin/vim\n$ command -v emacs\n/usr/bin/emacs\n```\n\nAn empty result with a nonzero status means that name was not found through the current command search. Minimal systems may provide `vi`, while others include Nano or no interactive editor at all.\n\n## Understanding Vim's Model\n\nVim is a modal editor. The same key can have different meanings depending on the current mode:\n\n- Normal mode interprets keys as navigation and editing commands.\n- Insert mode inserts typed text.\n- Command-line mode accepts commands such as writing or quitting.\n\nThis model makes repeated keyboard editing efficient after practice, but new users must keep track of the active mode. Later lessons introduce Vim one operation at a time.\n\n## Understanding Emacs's Model\n\nEmacs commonly uses key combinations and named commands within an extensible environment. Files are visited in buffers, and major and minor modes customize behavior for different content and tasks. Emacs can run in a terminal or a graphical frame.\n\nVim and Emacs both support far more than basic editing through configuration and extensions. Begin with opening, changing, saving, and closing a plain-text file before adding customization.\n\n## Setting a Preferred Editor\n\nMany command-line programs consult `VISUAL` or `EDITOR` when they need to start an editor. For example, choose Vim for commands launched from the current Bash session and its children:\n\n```bash\n$ export VISUAL=vim\n$ export EDITOR=\"$VISUAL\"\n```\n\nThese variables express a preference; they do not install the program. Use a command that actually exists, and place the exports in the appropriate shell startup file only after testing them.\n\n## Practicing without Risking Important Files\n\nLearn on a disposable file in a directory you own:\n\n```bash\n$ printf 'first line\\nsecond line\\n' > editor-practice.txt\n$ vim editor-practice.txt\n```\n\nAvoid beginning with system configuration or another user's data. Make a backup before changing an important file, understand how to save and exit, and review the result with a read-only command such as `cat` or `diff`.\n\nTo practice opening, editing, and saving terminal text files, try this hands-on lab:\n\n1. **[Edit Text Files in Linux with Vim and Nano](https://labex.io/labs/comptia-edit-text-files-in-linux-with-vim-and-nano-591076)** - Practice creating files, editing text, saving files, and navigating with both vi/vim and nano, essential skills for any Linux user.\n\n## Summary\n\nYou can now choose a terminal editor and prepare a safe practice workflow.\n\n1. Check whether an editor command is available.\n2. Recognize Vim's modal interaction model.\n3. Recognize Emacs buffers and extensible modes.\n4. Set an editor preference without confusing it with installation.\n5. Practice on disposable text before editing important files.", "code_snippets": ["$ command -v vim\n/usr/bin/vim\n$ command -v emacs\n/usr/bin/emacs", "$ export VISUAL=vim\n$ export EDITOR=\"$VISUAL\"", "$ printf 'first line\\nsecond line\\n' > editor-practice.txt\n$ vim editor-practice.txt"], "quizzes": [{"id": "editors-check-availability", "question": "Which command checks whether the current shell can resolve an executable named `vim`?", "options": [{"text": "`vim --install`", "correct": false, "explanation": "Vim does not use this command as a portable installation check, and package installation is distribution-specific."}, {"text": "`file ~/.vimrc`", "correct": false, "explanation": "This classifies one configuration pathname if it exists; it does not determine whether `vim` is resolvable."}, {"text": "`command -v vim`", "correct": true, "explanation": "The shell builtin checks command resolution and prints the resolved form when available."}]}, {"id": "editors-vim-modal-meaning", "question": "What does it mean that Vim is modal?", "options": [{"text": "Every file opens in a separate graphical window.", "correct": false, "explanation": "Windows and buffers are separate concepts. Modal refers to how key behavior changes with editor state."}, {"text": "Vim can edit only one kind of text file at a time.", "correct": false, "explanation": "Vim supports many file types. The word modal describes its interaction model, not a file restriction."}, {"text": "Keys perform different actions depending on the active mode.", "correct": true, "explanation": "For example, a key can issue a command in Normal mode but insert text in Insert mode."}]}, {"id": "editors-emacs-buffer", "question": "In Emacs terminology, where is a visited file's editable text normally held?", "options": [{"text": "In a buffer.", "correct": true, "explanation": "Emacs visits a file in a buffer, which holds the text being viewed or edited."}, {"text": "In the shell's alias table.", "correct": false, "explanation": "Aliases belong to shell command resolution and do not store editor text."}, {"text": "Only in the terminal scrollback.", "correct": false, "explanation": "Terminal scrollback records displayed output, while Emacs manages editable text in buffers."}]}, {"id": "editors-editor-variable", "question": "What does `export EDITOR=vim` do?", "options": [{"text": "It tells future child processes that `vim` is the preferred editor value.", "correct": true, "explanation": "Export places the preference in the environment inherited by commands started from the current shell."}, {"text": "It installs Vim for every user on the system.", "correct": false, "explanation": "Environment-variable assignment does not install packages or change other users' systems."}, {"text": "It makes every program obey Vim's key bindings.", "correct": false, "explanation": "Programs may consult the variable to launch an editor, but it does not replace their own interaction model."}]}, {"id": "editors-first-practice-file", "question": "What is the safest first file for practicing an unfamiliar editor?", "options": [{"text": "A critical boot configuration file opened as root.", "correct": false, "explanation": "An accidental change could prevent normal startup, and elevated access increases the impact of mistakes."}, {"text": "A disposable text file in a directory you own.", "correct": true, "explanation": "A practice file limits the consequences of accidental edits while you learn navigation, saving, and quitting."}, {"text": "A shared production file with no backup.", "correct": false, "explanation": "Unreviewed practice on shared data can disrupt others and offers no simple recovery path."}]}]}, {"lesson_id": "vim-text-editor", "course_id": "advanced-text-fu", "title": "Vim (Vi Improved)", "order_index": 3, "description": "Learn what Vim is, how it relates to vi, and how to open files, help, and guided practice.", "content": "Vim is a configurable text editor whose name means **Vi Improved**. It preserves the modal editing model associated with the original `vi` editor and adds features such as multilevel undo, syntax support, scripting, and an extensive help system.\n\n## Relating Vim and vi\n\n`vi` describes both a historical editor and a common command interface. On one Linux system, `vi` may start Vim in a compatibility-oriented mode; on another, it may start a different vi implementation. Do not assume that every `vi` command provides every Vim feature.\n\nCheck what the current shell resolves:\n\n```bash\n$ command -v vim\n/usr/bin/vim\n$ command -v vi\n/usr/bin/vi\n```\n\nThe resolved path does not by itself identify whether `vi` and `vim` are the same implementation. `type -a vi vim` and the editor's version output can provide more detail.\n\n## Opening Vim and Files\n\nStart Vim with an unnamed buffer:\n\n```bash\n$ vim\n```\n\nPass a pathname to edit that file:\n\n```bash\n$ vim filename.txt\n```\n\nIf `filename.txt` exists and is readable, Vim loads its content into a buffer. If the path does not exist, Vim opens a new buffer associated with that name; no file is created until you successfully write the buffer.\n\nVim does not bypass filesystem permissions. Opening a file does not guarantee that your account can save changes to its pathname.\n\n## Using Built-In Learning Resources\n\nIf the Vim installation includes `vimtutor`, run it from the shell for an interactive practice lesson:\n\n```bash\n$ vimtutor\n```\n\nInside Vim, enter Normal mode with `Esc`, type `:help`, and press Enter to open the help system. A specific topic can follow the command:\n\n```vim\n:help user-manual\n:help :write\n```\n\nHelp tags are precise, so punctuation can matter. Use `Ctrl+]` on a help link to follow it and `Ctrl+T` to return.\n\n## Practicing with a Disposable File\n\nBegin with a file in a directory you own:\n\n```bash\n$ printf 'alpha\\nbeta\\n' > vim-practice.txt\n$ vim vim-practice.txt\n```\n\nThe following lessons introduce searching, navigation, insertion, editing, and saving. Until you know how to leave safely, remember that `Esc` returns to Normal mode and `:q!` followed by Enter abandons unsaved changes in the current window. Use that command only when discarding those changes is intentional.\n\nTo practice opening, editing, and saving with Vim, try this hands-on lab:\n\n1. **[Edit Text Files in Linux with Vim and Nano](https://labex.io/labs/comptia-edit-text-files-in-linux-with-vim-and-nano-591076)** - Practice creating files, editing text, saving files, and navigating with both Vim and Nano in a real Linux environment.\n\n## Summary\n\nYou can now identify Vim, open a buffer, and find safe learning resources.\n\n1. Explain how Vim relates to vi without assuming one implementation.\n2. Check whether the `vim` command is available.\n3. Open an existing file or a new named buffer.\n4. Start `vimtutor` or open Vim's built-in help.\n5. Abandon unsaved practice changes only when intended.", "code_snippets": ["$ command -v vim\n/usr/bin/vim\n$ command -v vi\n/usr/bin/vi", "$ vim", "$ vim filename.txt", "$ vimtutor", ":help user-manual\n:help :write", "$ printf 'alpha\\nbeta\\n' > vim-practice.txt\n$ vim vim-practice.txt"], "quizzes": [{"id": "vim-name-origin", "question": "What does the name Vim mean?", "options": [{"text": "Visual Input Manager", "correct": false, "explanation": "This expansion is not the origin of the editor's name."}, {"text": "Virtual Interface Mode", "correct": false, "explanation": "Vim does use modes, but this phrase is not what the name represents."}, {"text": "Vi Improved", "correct": true, "explanation": "Vim began as an improved vi-compatible editor, which is reflected in its name."}]}, {"id": "vim-check-command", "question": "Which command checks whether Bash can currently resolve the name `vim`?", "options": [{"text": "`vim --create`", "correct": false, "explanation": "This is not the shell-resolution check and is not how Vim is installed or discovered."}, {"text": "`command -v vim`", "correct": true, "explanation": "The shell builtin reports the command that would be used for the name, if one is available."}, {"text": "`file ~/.vimrc`", "correct": false, "explanation": "This examines one possible configuration file and says nothing definitive about whether the Vim executable is available."}]}, {"id": "vim-open-missing-path", "question": "What normally happens when `vim draft.txt` names a path that does not yet exist?", "options": [{"text": "Vim opens a new buffer and creates the file only when it is written.", "correct": true, "explanation": "The pathname is remembered for the buffer, while disk creation is deferred until a successful save."}, {"text": "Vim creates an empty file on disk before opening the interface.", "correct": false, "explanation": "The new buffer is associated with the pathname, but the file is not created until a successful write."}, {"text": "Vim refuses to start because every pathname must already exist.", "correct": false, "explanation": "Vim can open a new buffer for a missing pathname so you can compose a new file."}]}, {"id": "vim-guided-tutorial", "question": "Which shell command starts Vim's guided tutorial when it is installed?", "options": [{"text": "`vim --quiz`", "correct": false, "explanation": "Vim does not use this option as its standard guided tutorial interface."}, {"text": "`vimtutor`", "correct": true, "explanation": "`vimtutor` opens a copy of the interactive tutorial designed for safe hands-on practice."}, {"text": "`help vim`", "correct": false, "explanation": "Bash `help` documents shell builtins; it does not start Vim's interactive tutorial."}]}, {"id": "vim-abandon-practice-changes", "question": "In a disposable practice file, which Vim command quits the current window and discards its unsaved changes?", "options": [{"text": "`:w`", "correct": false, "explanation": "`:w` writes the buffer but does not quit the current window."}, {"text": "`:wq`", "correct": false, "explanation": "`:wq` saves changes before quitting, so it does not discard them."}, {"text": "`:q!`", "correct": true, "explanation": "The `!` tells Vim to abandon the modified-buffer warning and quit without writing those changes."}]}]}, {"lesson_id": "vim-search-patterns", "course_id": "advanced-text-fu", "title": "Vim Search Patterns", "order_index": 4, "description": "Learn how to search forward or backward in Vim and repeat, refine, or clear pattern matches.", "content": "Vim searches from the current cursor position using patterns. Begin in Normal mode, enter a forward or backward search, and then repeat matches without retyping the pattern.\n\n## Searching Forward\n\nIn Normal mode, type `/`, enter a pattern, and press Enter. Vim moves to the next match after the cursor:\n\n```vim\n/pretty\n```\n\nSearches use Vim's regular-expression syntax, so characters such as `.`, `*`, `[`, and `\\` can have special meaning. Use `\\V` at the start when the rest of a pattern should be treated as very nomagic, or escape special characters deliberately.\n\n## Searching Backward\n\nType `?`, enter a pattern, and press Enter to move to the preceding match before the cursor:\n\n```vim\n?pretty\n```\n\nThis does not inherently mean \u201cthe final match in the file.\u201d The result depends on the current cursor position. With Vim's default `wrapscan` setting, a search can wrap at the beginning or end; `:set nowrapscan` disables that wrapping.\n\n## Repeating a Search\n\nAfter either kind of search:\n\n- Press `n` to repeat in the original search direction.\n- Press `N` to repeat in the opposite direction.\n\nTherefore, after `/pretty`, `n` moves forward and `N` backward. After `?pretty`, `n` moves backward and `N` forward.\n\n After running `?error`, which key repeats the search in the same backward direction?\n\n## Searching for the Word under the Cursor\n\nIn Normal mode, place the cursor on a word and use:\n\n- `*` to search forward for that whole word.\n- `#` to search backward for that whole word.\n\nThese commands set the latest search pattern, so `n` and `N` can continue from it.\n\n## Controlling Case and Highlighting\n\nVim options can change case behavior:\n\n- `:set ignorecase` makes searches ignore case.\n- `:set smartcase` makes an uppercase character restore case sensitivity when `ignorecase` is also set.\n- `\\c` inside a pattern forces that search to ignore case.\n- `\\C` forces that search to respect case.\n\nFor example, `/\\cerror` matches `error`, `Error`, and `ERROR` regardless of the current case options.\n\nWhen search highlighting is enabled, `:nohlsearch` clears the current visual highlights without deleting the search pattern. The next search or repeat can highlight matches again.\n\nTo practice Vim navigation and search in a controlled file, try this hands-on lab:\n\n1. **[Edit Text Files in Linux with Vim and Nano](https://labex.io/labs/comptia-edit-text-files-in-linux-with-vim-and-nano-591076)** - Practice creating, editing, saving, and navigating text files with Vim and Nano.\n\n## Summary\n\nYou can now search a Vim buffer and move between matches predictably.\n\n1. Start forward searches with `/` and backward searches with `?`.\n2. Repeat in the same direction with `n` or the opposite direction with `N`.\n3. Search for the whole word under the cursor with `*` or `#`.\n4. Control case behavior for one pattern or through options.\n5. Clear highlights without losing the current search pattern.", "code_snippets": ["/pretty", "?pretty"], "quizzes": [{"id": "vim-search-forward-key", "question": "From Normal mode, which command starts a forward search for `pretty`?", "options": [{"text": "`?pretty` followed by Enter", "correct": false, "explanation": "A question mark begins a backward search from the current cursor position."}, {"text": "`/pretty` followed by Enter", "correct": true, "explanation": "A slash begins a forward search, and Enter submits the pattern."}, {"text": "`:pretty` followed by Enter", "correct": false, "explanation": "A colon enters Command-line mode for an Ex command; `pretty` is not introduced as a search this way."}]}, {"id": "vim-search-backward-key", "question": "Which Normal-mode search prefix looks toward earlier text from the cursor?", "options": [{"text": "`/`", "correct": false, "explanation": "A slash searches forward from the cursor rather than toward preceding text."}, {"text": "`?`", "correct": true, "explanation": "A question mark starts a backward pattern search from the current cursor position."}, {"text": "`:`", "correct": false, "explanation": "A colon starts an Ex command line. It is not the backward-search prefix."}]}, {"id": "vim-repeat-backward-search", "question": "After running `?error`, which key repeats the search in the same backward direction?", "options": [{"text": "`n`", "correct": true, "explanation": "Lowercase `n` repeats the most recent search in its original direction, which is backward here."}, {"text": "`N`", "correct": false, "explanation": "Uppercase `N` reverses the original search direction, so it would move forward after a `?` search."}, {"text": "`/`", "correct": false, "explanation": "A slash starts a new forward search and waits for a pattern rather than repeating the previous one."}]}, {"id": "vim-current-word-forward", "question": "Which Normal-mode key searches forward for the whole word under the cursor?", "options": [{"text": "`#`", "correct": false, "explanation": "The hash key searches backward for the word under the cursor."}, {"text": "`*`", "correct": true, "explanation": "The star command builds a whole-word pattern from the word under the cursor and searches forward."}, {"text": "`n`", "correct": false, "explanation": "The `n` key repeats an existing search; it does not first create a pattern from the current word."}]}, {"id": "vim-force-case-insensitive", "question": "Which pattern forces one Vim search for `error` to ignore case regardless of the current case options?", "options": [{"text": "`/\\Cerror`", "correct": false, "explanation": "Uppercase `\\C` forces case-sensitive matching, the opposite behavior."}, {"text": "`/:error`", "correct": false, "explanation": "A colon inside this pattern is a literal character here and does not select case handling."}, {"text": "`/\\cerror`", "correct": true, "explanation": "The `\\c` atom makes that search case-insensitive, so capitalization variants can match."}]}]}, {"lesson_id": "vim-navigation", "course_id": "advanced-text-fu", "title": "Vim Navigation", "order_index": 5, "description": "Learn how to move by characters, words, lines, and file positions in Vim's Normal mode.", "content": "Vim provides keyboard motions that work in a terminal without requiring a mouse. Some Vim configurations also support mouse input, but learning motions makes navigation composable with editing commands.\n\nPress `Esc` before practicing to return to Normal mode.\n\n## Moving by Characters and Screen Lines\n\nThe foundational Normal-mode motions are:\n\n- `h`: Move one character left.\n- `j`: Move one screen line down.\n- `k`: Move one screen line up.\n- `l`: Move one character right.\n\nArrow keys commonly perform similar movement, but `h`, `j`, `k`, and `l` keep your hands near other commands. On a wrapped display line, `j` and `k` normally move by file lines; `gj` and `gk` move by displayed screen lines.\n\n## Prefixing Motions with Counts\n\nType a positive count before many motions to repeat them. For example:\n\n```text\n5j\n3l\n```\n\n`5j` moves down five lines, while `3l` moves right three character positions when possible. Counts also combine with word and editing commands.\n\n## Moving by Words\n\nUseful word motions include:\n\n- `w`: Move to the beginning of the next word.\n- `b`: Move to the beginning of the current or previous word.\n- `e`: Move to the end of the current or next word.\n\nUppercase `W`, `B`, and `E` use whitespace-delimited WORDS, treating punctuation differently. Prefix a count to move through several words, such as `3w`.\n\n## Moving within a Line\n\nThese motions target positions on the current line:\n\n- `0`: Move to column zero.\n- `^`: Move to the first nonblank character.\n- `$`: Move to the end of the line.\n\nThe difference between `0` and `^` matters on indented lines.\n\n## Moving through the File\n\nUse these Normal-mode commands for larger jumps:\n\n- `gg`: Move to the first line.\n- `G`: Move to the final line.\n- `42G`: Move to line 42.\n- `Ctrl+F`: Move forward approximately one screen.\n- `Ctrl+B`: Move backward approximately one screen.\n\nThe command `:42` followed by Enter is another way to jump to line 42.\n\nTo practice keyboard navigation while editing a disposable file, try this hands-on lab:\n\n1. **[Edit Text Files in Linux with Vim and Nano](https://labex.io/labs/comptia-edit-text-files-in-linux-with-vim-and-nano-591076)** - Practice creating files, editing text, saving files, and navigating with both Vim and Nano in a real Linux environment.\n\n## Summary\n\nYou can now navigate a Vim buffer at several useful scales.\n\n1. Move by characters or lines with `h`, `j`, `k`, and `l`.\n2. Repeat motions with a numeric prefix.\n3. Move between word boundaries with `w`, `b`, and `e`.\n4. Target the start, first text, or end of a line.\n5. Jump to file positions with `gg`, `G`, or a line number.", "code_snippets": ["5j\n3l"], "quizzes": [{"id": "vim-navigation-down", "question": "In Normal mode, which key moves the cursor down one line?", "options": [{"text": "`k`", "correct": false, "explanation": "The `k` motion moves upward one line."}, {"text": "`l`", "correct": false, "explanation": "The `l` motion moves one character to the right."}, {"text": "`j`", "correct": true, "explanation": "The `j` motion moves downward one line in Normal mode."}]}, {"id": "vim-navigation-count", "question": "What does `4k` do in Normal mode?", "options": [{"text": "Moves down four lines when possible.", "correct": false, "explanation": "Downward movement uses `j`; `k` moves in the opposite direction."}, {"text": "Moves up four lines when possible.", "correct": true, "explanation": "The count `4` repeats the upward `k` motion four times."}, {"text": "Deletes four lines above the cursor.", "correct": false, "explanation": "A motion by itself changes the cursor position. Deletion would require an operator such as `d`."}]}, {"id": "vim-navigation-next-words", "question": "Which Normal-mode command moves forward to the beginning of the third following word position?", "options": [{"text": "`3w`", "correct": true, "explanation": "The count applies the next-word motion three times."}, {"text": "`w3`", "correct": false, "explanation": "Counts precede motions in this command form; placing `3` afterward does not express the requested movement."}, {"text": "`3b`", "correct": false, "explanation": "The `b` motion travels toward earlier word beginnings rather than forward."}]}, {"id": "vim-navigation-first-nonblank", "question": "Which motion moves to the first nonblank character of an indented line?", "options": [{"text": "`0`", "correct": false, "explanation": "Zero moves to the first column, which can contain indentation whitespace."}, {"text": "`$`", "correct": false, "explanation": "The dollar motion targets the end of the line."}, {"text": "`^`", "correct": true, "explanation": "The caret motion skips leading blanks and lands on the first nonblank character."}]}, {"id": "vim-navigation-file-end", "question": "Which Normal-mode command moves to the final line of the buffer?", "options": [{"text": "`gg`", "correct": false, "explanation": "Lowercase `gg` moves to the first line, not the final one."}, {"text": "`$`", "correct": false, "explanation": "The dollar motion goes to the end of the current line rather than the end of the file."}, {"text": "`G`", "correct": true, "explanation": "Uppercase `G` with no count jumps to the final line."}]}]}, {"lesson_id": "vim-inserting-appending-text", "course_id": "advanced-text-fu", "title": "Vim Inserting and Appending Text", "order_index": 6, "description": "Learn how Vim enters Insert mode before, after, above, or below the current cursor position.", "content": "In Normal mode, Vim interprets keys as commands. Insert mode inserts typed text into the buffer. Several Normal-mode commands enter Insert mode at different positions, letting you begin typing without separate navigation.\n\nPress `Esc` to leave Insert mode and return to Normal mode. If you are unsure which mode is active, pressing `Esc` is a safe way to reestablish Normal mode, although it can cancel a pending operation.\n\n## Inserting before or after the Cursor\n\nFrom Normal mode:\n\n- `i`: Enter Insert mode before the cursor.\n- `a`: Enter Insert mode after the cursor.\n\nFor example, if the cursor is on `b` in `abc`, `i` begins before `b`, while `a` begins after `b`. Both commands change modes; the text you type afterward performs the insertion.\n\n## Inserting at Line Boundaries\n\nUppercase commands target meaningful positions on the current line:\n\n- `I`: Enter Insert mode before the first nonblank character.\n- `A`: Enter Insert mode at the end of the line.\n\nOn an indented line, `I` skips indentation and begins before the first nonblank text. Use `0i` if you specifically need to insert at column zero.\n\n## Opening a New Line\n\nFrom Normal mode:\n\n- `o`: Open a new line below the current line and enter Insert mode.\n- `O`: Open a new line above the current line and enter Insert mode.\n\nVim applies indentation according to current settings and filetype rules. A count can repeat the open-line operation, but first learn the single-line form so the resulting cursor position is predictable.\n\nTo practice moving between Normal and Insert modes, try this hands-on lab:\n\n1. **[Edit Text Files in Linux with Vim and Nano](https://labex.io/labs/comptia-edit-text-files-in-linux-with-vim-and-nano-591076)** - Practice creating files, editing text, saving files, and navigating with both vi/vim and nano. This lab will help you master the fundamental skills of using Vim's Normal and Insert modes.\n\n## Summary\n\nYou can now enter Insert mode at the position where new text belongs.\n\n1. Return to Normal mode with `Esc`.\n2. Insert before or after the cursor with `i` or `a`.\n3. Insert at the first text or line end with `I` or `A`.\n4. Open a line below with `o`.\n5. Open a line above with `O`.", "code_snippets": [], "quizzes": [{"id": "vim-insert-return-normal", "question": "Which key normally returns from Insert mode to Normal mode?", "options": [{"text": "`Esc`", "correct": true, "explanation": "Escape ends the current insertion and returns Vim to Normal mode."}, {"text": "`Enter`", "correct": false, "explanation": "Enter inserts a line break while remaining in Insert mode."}, {"text": "`Tab`", "correct": false, "explanation": "Tab inserts indentation or triggers configured completion behavior; it does not normally leave Insert mode."}]}, {"id": "vim-insert-before-cursor", "question": "Which Normal-mode key enters Insert mode immediately before the cursor?", "options": [{"text": "`a`", "correct": false, "explanation": "Lowercase `a` appends after the cursor rather than inserting before it."}, {"text": "`o`", "correct": false, "explanation": "Lowercase `o` opens a new line below the current line before entering Insert mode."}, {"text": "`i`", "correct": true, "explanation": "Lowercase `i` begins insertion at the current cursor position, before the character under it."}]}, {"id": "vim-insert-first-nonblank", "question": "Which Normal-mode command begins insertion before the first nonblank character of the current line?", "options": [{"text": "`i`", "correct": false, "explanation": "Lowercase `i` uses the current cursor position and does not first target the line's initial text."}, {"text": "`A`", "correct": false, "explanation": "Uppercase `A` begins insertion at the end of the current line."}, {"text": "`I`", "correct": true, "explanation": "Uppercase `I` moves to the first nonblank character and enters Insert mode before it."}]}, {"id": "vim-append-line-end", "question": "Which Normal-mode command moves to the end of the current line and enters Insert mode?", "options": [{"text": "`A`", "correct": true, "explanation": "Uppercase `A` combines an end-of-line jump with entry into Insert mode."}, {"text": "`$`", "correct": false, "explanation": "The dollar motion reaches the line end but remains in Normal mode."}, {"text": "`a`", "correct": false, "explanation": "Lowercase `a` begins after the current cursor rather than jumping to the line end."}]}, {"id": "vim-open-line-above", "question": "Which Normal-mode command opens a new line above the current line and enters Insert mode?", "options": [{"text": "`o`", "correct": false, "explanation": "Lowercase `o` opens below the current line."}, {"text": "`O`", "correct": true, "explanation": "Uppercase `O` opens a new line above and starts insertion there."}, {"text": "`A`", "correct": false, "explanation": "Uppercase `A` appends at the end of the existing line and does not open a new one above."}]}]}, {"lesson_id": "vim-editing", "course_id": "advanced-text-fu", "title": "Vim Editing", "order_index": 7, "description": "Learn how Vim combines operators, motions, registers, puts, and undo commands for text editing.", "content": "Vim editing commands often combine an operator with a motion or text object. This grammar lets the same actions work at character, word, line, and larger scopes. Press `Esc` before practicing to return to Normal mode.\n\n## Combining an Operator with a Motion\n\nThe general form is:\n\n```text\n[count] operator [count] motion\n```\n\nCommon operators include:\n\n- `d`: Delete text.\n- `c`: Change text, then enter Insert mode.\n- `y`: Yank, or copy, text.\n\nFor example, `dw` deletes through the `w` motion, while `d$` deletes from the cursor through the end of the line. `2dw` applies the delete across two word motions.\n\n## Editing Characters and Lines\n\nSome commands are convenient shortcuts:\n\n- `x`: Delete the character under the cursor.\n- `dd`: Delete the current line linewise.\n- `3dd`: Delete three lines starting with the current line.\n- `cc`: Change the current line and enter Insert mode.\n- `r{char}`: Replace the character under the cursor with `{char}`.\n- `R`: Enter Replace mode until `Esc` is pressed.\n\nRepeating an operator, as in `dd`, makes it linewise. A count expands the number of lines.\n\n## Changing Text and Entering Insert Mode\n\nThe `c` operator removes the selected text and enters Insert mode so you can type a replacement:\n\n- `ce`: Change through the end of the word.\n- `c$`: Change through the end of the line.\n- `cc`: Change the complete current line.\n- `ciw`: Change the inner word under the cursor.\n- `caw`: Change a word text object, including surrounding spacing as Vim defines it.\n\nThe behavior of `cw` has a historical special case and often acts like `ce`. Text objects such as `iw` can make the intended boundary clearer.\n\n## Yanking and Putting Text\n\nVim calls copying **yanking** and pasting **putting**:\n\n- `yw`: Yank through a word motion.\n- `yy`: Yank the current line.\n- `p`: Put after the cursor for characterwise text or below the current line for linewise text.\n- `P`: Put before the cursor or above the current line.\n\nDeletes and changes also store text in registers, so a later `p` may put the most recently deleted text rather than an earlier yank. Named registers let you preserve specific text, but begin by watching what the latest operation stored.\n\n After `yy` yanks the current line, which command puts that line below the current line?\n\n## Undoing, Redoing, and Repeating\n\nIn Normal mode:\n\n- `u`: Undo the most recent change.\n- `Ctrl+R`: Redo an undone change.\n- `.`: Repeat the most recent change at the current location when applicable.\n- `J`: Join the current line with the next line.\n\nUndo history applies to buffer changes, not merely cursor motions. Save checkpoints and review edits rather than depending on an unlimited or permanent undo history.\n\nTo practice operators, motions, and recovery on disposable text, try this hands-on lab:\n\n1. **[Edit Text Files in Linux with Vim and Nano](https://labex.io/labs/comptia-edit-text-files-in-linux-with-vim-and-nano-591076)** - Practice creating files, editing text, saving files, and navigating with both vi/vim and nano. This lab will help you apply concepts like deleting, changing, yanking, and putting text in real scenarios.\n\n## Summary\n\nYou can now compose Vim edits and recover from mistakes in Normal mode.\n\n1. Combine operators with motions, text objects, and counts.\n2. Delete characters or complete lines at a chosen scope.\n3. Change text and enter Insert mode for replacement.\n4. Yank and put characterwise or linewise text.\n5. Undo, redo, or repeat changes deliberately.", "code_snippets": ["[count] operator [count] motion"], "quizzes": [{"id": "vim-edit-operator-motion", "question": "In Normal mode, what does `d$` do?", "options": [{"text": "Deletes the complete file from the cursor onward.", "correct": false, "explanation": "The dollar motion targets the current line's end, not the end of the entire buffer."}, {"text": "Deletes from the cursor through the end of the line.", "correct": true, "explanation": "The `d` operator applies to the `$` end-of-line motion."}, {"text": "Moves to the end of the line without changing text.", "correct": false, "explanation": "`$` alone moves, but the preceding `d` turns the covered range into a deletion."}]}, {"id": "vim-edit-delete-three-lines", "question": "Which Normal-mode command deletes the current line and the next two lines?", "options": [{"text": "`dd3`", "correct": false, "explanation": "The count belongs before the doubled operator in this command form."}, {"text": "`3x`", "correct": false, "explanation": "This deletes three characters under and after the cursor, not three complete lines."}, {"text": "`3dd`", "correct": true, "explanation": "The count applies to the linewise `dd` command, deleting three lines beginning at the current one."}]}, {"id": "vim-edit-change-inner-word", "question": "Which Normal-mode command replaces the inner word under the cursor by deleting it and entering Insert mode?", "options": [{"text": "`diw`", "correct": false, "explanation": "This deletes the inner word but remains in Normal mode instead of starting replacement text."}, {"text": "`yiw`", "correct": false, "explanation": "This yanks the inner word without changing the buffer or entering Insert mode."}, {"text": "`ciw`", "correct": true, "explanation": "The `c` operator changes the `iw` text object and then enters Insert mode."}]}, {"id": "vim-edit-yank-put-line", "question": "After `yy` yanks the current line, which command puts that line below the current line?", "options": [{"text": "`p`", "correct": true, "explanation": "For linewise yanked text, lowercase `p` puts the stored line below the current line."}, {"text": "`P`", "correct": false, "explanation": "Uppercase `P` puts linewise text above the current line."}, {"text": "`u`", "correct": false, "explanation": "Lowercase `u` undoes a change; it does not put the yanked line."}]}, {"id": "vim-edit-redo-change", "question": "Which Normal-mode command redoes a change that was just undone?", "options": [{"text": "`Ctrl+U`", "correct": false, "explanation": "In Normal mode, `Ctrl+U` scrolls upward by about half a screen; it is not redo."}, {"text": "`.`", "correct": false, "explanation": "The dot repeats the latest change as a new action rather than traversing forward through undo history."}, {"text": "`Ctrl+R`", "correct": true, "explanation": "Vim uses `Ctrl+R` in Normal mode to move forward through undo history."}]}]}, {"lesson_id": "vim-saving-and-exiting", "course_id": "advanced-text-fu", "title": "Vim Saving and Exiting", "order_index": 8, "description": "Learn how to write, quit, save under another name, or deliberately discard Vim buffer changes.", "content": "Writing and quitting are separate Vim operations. Before entering an Ex command, press `Esc` to return to Normal mode, type `:`, enter the command, and press Enter. Read Vim's status or error message before assuming a write succeeded.\n\n## Writing the Current Buffer\n\nUse `:w` to write the current buffer to its associated file without closing the window:\n\n```vim\n:w\n```\n\nA write can fail because the buffer has no filename, the directory is not writable, the filesystem is full, or another condition prevents the operation. Check the message Vim reports.\n\nUse `:w copy.txt` to write the current buffer to another pathname while keeping the current buffer's existing name. Use `:saveas copy.txt` when the buffer should adopt the new pathname.\n\n## Quitting an Unmodified Buffer\n\nUse `:q` to close the current window when doing so will not abandon unsaved buffer changes:\n\n```vim\n:q\n```\n\nIf the current buffer is modified and its changes would be lost, Vim normally refuses and reports a warning. This safeguard gives you a chance to write or reconsider.\n\n## Discarding Unsaved Changes\n\nUse `:q!` only when you intentionally want to close the current window and abandon changes that would otherwise block quitting:\n\n```vim\n:q!\n```\n\nThe exclamation mark overrides the unsaved-change warning. Those buffer changes are not written, so verify that they are truly disposable before pressing Enter.\n\n The current buffer has changes you deliberately do not want to save. Which command quits the current window and abandons them?\n\n## Writing and Quitting Together\n\nUse `:wq` when the buffer should be written and the current window should close after a successful write:\n\n```vim\n:wq\n```\n\nIf writing fails, Vim does not complete the requested quit. Resolve the error rather than assuming the data reached disk.\n\n## Using :x and ZZ\n\n`:x` writes the buffer only if it is modified, then quits. In Normal mode, uppercase `ZZ` performs the same write-if-modified-and-quit behavior:\n\n```vim\n:x\n```\n\n```text\nZZ\n```\n\nThis differs subtly from `:wq`, which requests a write even when the buffer is unchanged. Uppercase `ZQ` is the Normal-mode counterpart for quitting without writing, similar to `:q!`.\n\nWhen several windows or buffers are involved, a command may close only the current window. Commands such as `:qa`, `:wqa`, and `:qa!` act across windows, but review every modified buffer before using an all-windows force command.\n\nTo practice writing and quitting on a disposable file, try this hands-on lab:\n\n1. **[Edit Text Files in Linux with Vim and Nano](https://labex.io/labs/comptia-edit-text-files-in-linux-with-vim-and-nano-591076)** - Practice creating files, editing text, saving files, and navigating with both Vim and Nano. This lab will solidify your understanding of basic Vim operations, including how to save and quit.\n\n## Summary\n\nYou can now choose a Vim exit command that matches your intent for unsaved data.\n\n1. Write without quitting with `:w`.\n2. Quit safely with `:q` when no changes would be lost.\n3. Discard changes deliberately with `:q!`.\n4. Write and quit with `:wq`.\n5. Use `:x` or `ZZ` for write-if-modified behavior.", "code_snippets": [":w", ":q", ":q!", ":wq", ":x", "ZZ"], "quizzes": [{"id": "vim-save-without-quit", "question": "Which Vim command writes the current buffer to its associated file without quitting?", "options": [{"text": "`:q`", "correct": false, "explanation": "`:q` requests a quit and does not write a modified buffer."}, {"text": "`:w`", "correct": true, "explanation": "The `:write` command saves the current buffer and leaves the editing window open."}, {"text": "`:q!`", "correct": false, "explanation": "`:q!` abandons unsaved changes and quits; it does not save them."}]}, {"id": "vim-quit-clean-buffer", "question": "Which command quits the current Vim window when no unsaved changes would be lost?", "options": [{"text": "`:w`", "correct": false, "explanation": "This writes the buffer but leaves the current window open."}, {"text": "`:q`", "correct": true, "explanation": "The ordinary quit command closes the window when Vim's modified-buffer safeguards allow it."}, {"text": "`u`", "correct": false, "explanation": "Normal-mode `u` undoes a change and does not close the editor window."}]}, {"id": "vim-quit-discard-changes", "question": "The current buffer has changes you deliberately do not want to save. Which command quits the current window and abandons them?", "options": [{"text": "`:q`", "correct": false, "explanation": "Plain `:q` normally refuses when quitting would lose modified-buffer changes."}, {"text": "`:wq`", "correct": false, "explanation": "`:wq` writes the changes before quitting, the opposite of discarding them."}, {"text": "`:q!`", "correct": true, "explanation": "The bang overrides the modified warning and closes without writing the unsaved changes."}]}, {"id": "vim-write-and-quit", "question": "Which command writes the current buffer and then quits the current window if writing succeeds?", "options": [{"text": "`:wq`", "correct": true, "explanation": "This combines a write with a quit, and the quit depends on successful writing."}, {"text": "`:q!`", "correct": false, "explanation": "This quits while discarding changes rather than writing them."}, {"text": "`:w copy.txt`", "correct": false, "explanation": "This writes another pathname but keeps the editing window open."}]}, {"id": "vim-write-if-modified-quit", "question": "Which Normal-mode command writes only when the buffer is modified and then quits?", "options": [{"text": "`ZZ`", "correct": true, "explanation": "Uppercase `ZZ` performs the write-if-modified-and-quit behavior associated with `:x`."}, {"text": "`zz`", "correct": false, "explanation": "Lowercase `zz` recenters the current line in the window; it does not save or quit."}, {"text": "`ZQ`", "correct": false, "explanation": "Uppercase `ZQ` quits without writing, so it discards unsaved changes rather than saving them."}]}]}, {"lesson_id": "emacs-text-editor", "course_id": "advanced-text-fu", "title": "Emacs", "order_index": 9, "description": "Learn how to start Emacs, interpret its key notation, and distinguish buffers, windows, and frames.", "content": "GNU Emacs is an extensible text editor whose behavior can be customized with Emacs Lisp. It supports plain-text editing, programming modes, file and buffer management, and many optional packages. You can learn its core editing commands without adopting every extension.\n\n## Checking and Starting Emacs\n\nDo not assume Emacs is installed. Check how the shell resolves it:\n\n```bash\n$ command -v emacs\n/usr/bin/emacs\n```\n\nStart Emacs with its normal display selection:\n\n```bash\n$ emacs\n```\n\nIn a graphical session this may create a graphical frame. Use `-nw`, short for no window system, when Emacs should remain inside the current terminal:\n\n```bash\n$ emacs -nw\n```\n\n## Opening a File\n\nPass a pathname to visit a file when Emacs starts:\n\n```bash\n$ emacs notes.txt\n```\n\nIf the file exists, Emacs reads it into a buffer. If it is missing, Emacs creates a new buffer associated with that pathname; the file is created only after a successful save. Filesystem permissions still determine whether a write can succeed.\n\n## Understanding Buffers, Windows, and Frames\n\nEmacs uses related but distinct objects:\n\n- A **buffer** holds text or other editor state. A visited file's content lives in a buffer.\n- A **window** is an area within an Emacs frame that displays a buffer.\n- A **frame** is a top-level Emacs display, such as a graphical frame or terminal frame.\n\nSeveral buffers can exist without being visible, and two windows can display the same buffer. Closing a window does not necessarily kill its buffer or delete a file.\n\n## Reading Emacs Key Notation\n\nEmacs documentation uses compact notation:\n\n- `C-x` means hold Control and press `x`.\n- `M-x` means hold Meta and press `x`; Alt commonly acts as Meta in modern terminals and desktops.\n- `C-x C-f` is a key sequence: press Control+x, then Control+f.\n\nThe exact terminal may intercept or remap some keys. `Esc` followed by a key can often stand in for a Meta chord.\n\n## Starting the Built-In Tutorial\n\nInside Emacs, type `C-h t` to open the interactive tutorial. It teaches movement, insertion, saving, and quitting in a safe practice buffer. `C-h` is the help prefix; `C-h C-h` shows help about using help.\n\nIf Emacs displays a menu or welcome buffer, the tutorial remains the better structured starting point than experimenting on an important file.\n\n## Summary\n\nYou can now start Emacs and interpret its foundational interface concepts.\n\n1. Check whether the `emacs` command is available.\n2. Choose graphical or terminal operation with `-nw`.\n3. Visit an existing or new pathname in a buffer.\n4. Distinguish buffers, windows, and frames.\n5. Read key notation and open the built-in tutorial.", "code_snippets": ["$ command -v emacs\n/usr/bin/emacs", "$ emacs", "$ emacs -nw", "$ emacs notes.txt"], "quizzes": [{"id": "emacs-terminal-start", "question": "Which command starts Emacs inside the current terminal instead of using a graphical window system?", "options": [{"text": "`emacs -w`", "correct": false, "explanation": "This is not the documented no-window-system form introduced here."}, {"text": "`emacs -nw`", "correct": true, "explanation": "The `-nw` option tells Emacs not to use a graphical window system and to run on the terminal."}, {"text": "`command -v emacs`", "correct": false, "explanation": "This checks command resolution and does not start the editor."}]}, {"id": "emacs-open-file-buffer", "question": "What does `emacs notes.txt` normally do when `notes.txt` does not yet exist?", "options": [{"text": "Opens a new buffer associated with that pathname.", "correct": true, "explanation": "The buffer can hold new text for `notes.txt`, while the actual file is deferred until saving."}, {"text": "Creates the file on disk before the editor starts.", "correct": false, "explanation": "Emacs can associate a new buffer with the pathname without creating the disk file until a save succeeds."}, {"text": "Refuses to start because every visited file must exist.", "correct": false, "explanation": "Emacs supports composing new files through buffers associated with missing pathnames."}]}, {"id": "emacs-buffer-definition", "question": "What is an Emacs buffer?", "options": [{"text": "A top-level graphical application frame.", "correct": false, "explanation": "A frame is the top-level display object; a buffer holds editor content or state."}, {"text": "An object that holds editable text or other editor state.", "correct": true, "explanation": "Visited file contents and many non-file views live in Emacs buffers."}, {"text": "A shell history file containing previous commands.", "correct": false, "explanation": "Shell history is separate from Emacs buffer storage."}]}, {"id": "emacs-key-sequence-notation", "question": "How do you enter the Emacs key sequence written `C-x C-f`?", "options": [{"text": "Hold Control for `x`, then hold Control for `f`.", "correct": true, "explanation": "Each `C-` prefix applies to its following key, and the two chords are entered in sequence."}, {"text": "Type the literal characters `C-x C-f` into the buffer.", "correct": false, "explanation": "The notation describes control-key events rather than text to insert."}, {"text": "Hold Control, `x`, and `f` simultaneously as one chord.", "correct": false, "explanation": "The notation contains two successive chords, not one three-key chord."}]}, {"id": "emacs-open-tutorial", "question": "Which Emacs key sequence opens the built-in tutorial?", "options": [{"text": "`C-x C-s`", "correct": false, "explanation": "This sequence saves the current buffer; it does not open the tutorial."}, {"text": "`C-x C-c`", "correct": false, "explanation": "This sequence begins exiting Emacs rather than starting a lesson."}, {"text": "`C-h t`", "correct": true, "explanation": "The help prefix `C-h` followed by `t` starts the Emacs tutorial."}]}]}, {"lesson_id": "emacs-manipulate-files", "course_id": "advanced-text-fu", "title": "Emacs Manipulate Files", "order_index": 10, "description": "Learn how to visit, save, rename, revisit, and review file-backed buffers in Emacs.", "content": "Emacs visits files in buffers. Editing changes the buffer first; saving writes its current contents to the associated pathname. Read minibuffer messages because permissions, conflicting disk changes, or other errors can prevent a write.\n\n## Visiting a File\n\nUse `C-x C-f`, which runs `find-file`, then enter a pathname in the minibuffer and press Enter:\n\n```text\nC-x C-f\n```\n\nEmacs opens an existing readable file in a buffer or prepares a new file-visiting buffer when the pathname is missing. In the second case, no disk file exists until a save succeeds.\n\nYou can use Tab completion while entering a pathname. Visiting a directory normally opens Dired, Emacs's directory editor, rather than treating the directory as a text file.\n\n When `C-x C-f` visits a pathname that does not exist, when is the disk file normally created?\n\n## Saving the Current Buffer\n\nUse `C-x C-s`, which runs `save-buffer`, to save the current file-visiting buffer:\n\n```text\nC-x C-s\n```\n\nIf the buffer has no associated filename, Emacs prompts for one. A successful write clears the buffer's modified indicator; a failure leaves the unsaved data in the buffer and reports an error.\n\n## Writing under Another Name\n\nUse `C-x C-w`, which runs `write-file`, to prompt for a pathname, write the buffer there, and make the buffer visit that new file:\n\n```text\nC-x C-w\n```\n\nThis is the Emacs \u201cSave As\u201d behavior. It differs from merely writing a separate copy while continuing to visit the original pathname.\n\n## Reviewing Several Modified Buffers\n\nUse `C-x s`, which runs `save-some-buffers`, to examine modified file-visiting buffers:\n\n```text\nC-x s\n```\n\nEmacs normally asks whether to save each eligible modified buffer. Read the buffer name and answer deliberately; this is not an unconditional save-all shortcut.\n\n## Reverting from Disk\n\nIf a file changed on disk and you intentionally want to discard the buffer's current contents, run `M-x revert-buffer` and review the confirmation prompt. Reverting can destroy unsaved buffer edits, so use it only after confirming which source should win.\n\nTo compare before deciding, save a separate copy or use version-control and diff tools. Avoid treating reload operations as harmless when the buffer is modified.\n\n## Summary\n\nYou can now manage file-backed buffers without confusing visits and writes.\n\n1. Visit a pathname with `C-x C-f`.\n2. Create a missing file only when its buffer is saved.\n3. Save the current buffer with `C-x C-s`.\n4. Save under a new visited name with `C-x C-w`.\n5. Review several modified buffers with `C-x s`.", "code_snippets": ["C-x C-f", "C-x C-s", "C-x C-w", "C-x s"], "quizzes": [{"id": "emacs-find-file-key", "question": "Which Emacs key sequence prompts for a pathname and visits it?", "options": [{"text": "`C-x C-s`", "correct": false, "explanation": "This saves the current file-visiting buffer and does not prompt to visit another pathname."}, {"text": "`C-x C-c`", "correct": false, "explanation": "This begins exiting Emacs rather than opening a file."}, {"text": "`C-x C-f`", "correct": true, "explanation": "This runs `find-file`, prompting in the minibuffer for the pathname to visit."}]}, {"id": "emacs-find-missing-file", "question": "When `C-x C-f` visits a pathname that does not exist, when is the disk file normally created?", "options": [{"text": "Only after the new buffer is successfully saved.", "correct": true, "explanation": "The buffer can hold edits before any file exists, and saving performs the creation."}, {"text": "Immediately when the pathname is entered.", "correct": false, "explanation": "Emacs first creates a buffer associated with the new pathname; disk creation is deferred."}, {"text": "Only after Emacs itself is closed.", "correct": false, "explanation": "Exiting can prompt to save, but file creation is tied to a successful save rather than necessarily to closing Emacs."}]}, {"id": "emacs-save-current-buffer", "question": "Which key sequence saves the current file-visiting buffer?", "options": [{"text": "`C-x C-s`", "correct": true, "explanation": "`C-x C-s` runs `save-buffer` for the current buffer."}, {"text": "`C-x C-w`", "correct": false, "explanation": "This prompts for another filename and changes which file the buffer visits."}, {"text": "`C-x s`", "correct": false, "explanation": "This checks multiple file-visiting buffers and prompts about saving them rather than targeting only the current one."}]}, {"id": "emacs-write-file-as", "question": "Which key sequence performs the usual Save As operation for the current buffer?", "options": [{"text": "`C-x C-f`", "correct": false, "explanation": "This visits a file, potentially switching to another buffer; it is not Save As for the current buffer."}, {"text": "`C-x k`", "correct": false, "explanation": "This asks to kill a buffer and can prompt about unsaved changes; it does not save under a new name."}, {"text": "`C-x C-w`", "correct": true, "explanation": "`write-file` writes to the chosen pathname and makes the buffer visit that file."}]}, {"id": "emacs-save-some-buffers", "question": "What does `C-x s` normally do?", "options": [{"text": "Prompts about saving modified file-visiting buffers.", "correct": true, "explanation": "`save-some-buffers` reviews eligible modified buffers and asks which should be written."}, {"text": "Silently saves every buffer without showing names.", "correct": false, "explanation": "The normal interactive command prompts rather than unconditionally writing every buffer."}, {"text": "Closes every buffer after saving the current one.", "correct": false, "explanation": "The command concerns saving multiple buffers and does not normally close them."}]}]}, {"lesson_id": "emacs-buffer-navigation", "course_id": "advanced-text-fu", "title": "Emacs Buffer Navigation", "order_index": 11, "description": "Learn how to switch and kill Emacs buffers while splitting, selecting, and closing display windows.", "content": "An Emacs buffer holds text or editor state, while a window displays a buffer. A buffer can exist without being visible, and several windows can display one buffer. Managing one object does not automatically manage the other.\n\n## Switching Buffers\n\nUse `C-x b`, which runs `switch-to-buffer`, to select a buffer by name in the current window:\n\n```text\nC-x b\n```\n\nThe minibuffer offers completion for existing names. Entering a new name can create a non-file buffer with that name; it does not visit a file pathname.\n\nBy default, `C-x Right` runs `next-buffer` and `C-x Left` runs `previous-buffer`, cycling through buffers in the selected window.\n\n## Splitting the Selected Window\n\nUse `C-x 2` to split the selected window into an upper and lower window:\n\n```text\nC-x 2\n```\n\nUse `C-x 3` to split it into left and right windows:\n\n```text\nC-x 3\n```\n\nThe new window initially displays a buffer, often the same one. Switch buffers in either window independently.\n\n## Selecting and Closing Windows\n\nUse `C-x o`, which runs `other-window`, to select the next window:\n\n```text\nC-x o\n```\n\nUse these commands to remove window displays:\n\n- `C-x 0`: Delete the selected window.\n- `C-x 1`: Delete the other windows in the current frame.\n\nDeleting a window normally leaves its displayed buffer alive. You can show that buffer again in another window.\n\n## Killing a Buffer\n\nUse `C-x k`, which runs `kill-buffer`, to prompt for a buffer to remove from Emacs:\n\n```text\nC-x k\n```\n\nThe current buffer is the default choice. If a file-visiting buffer has unsaved changes, Emacs warns before killing it. Read the prompt; killing a modified buffer can discard edits.\n\nKilling a buffer is different from deleting a window. Emacs replaces a killed buffer in any displaying window, while deleting a window can leave its buffer untouched.\n\nPractice these commands with `*scratch*` and disposable buffers. Before killing any file-visiting buffer, confirm whether its modified indicator shows unsaved work.\n\n## Summary\n\nYou can now manage what Emacs stores and what each window displays.\n\n1. Switch buffers in the selected window with `C-x b`.\n2. Split below with `C-x 2` or to the right with `C-x 3`.\n3. Select another window with `C-x o`.\n4. Remove window displays with `C-x 0` or `C-x 1`.\n5. Kill a buffer with `C-x k` only after reviewing unsaved changes.", "code_snippets": ["C-x b", "C-x 2", "C-x 3", "C-x o", "C-x k"], "quizzes": [{"id": "emacs-switch-buffer-key", "question": "Which key sequence prompts for a buffer name to display in the current window?", "options": [{"text": "`C-x C-f`", "correct": false, "explanation": "This prompts for a file pathname and visits it, which is a different operation from choosing an existing buffer by name."}, {"text": "`C-x b`", "correct": true, "explanation": "`switch-to-buffer` reads a buffer name and displays that buffer in the selected window."}, {"text": "`C-x k`", "correct": false, "explanation": "This prompts to kill a buffer rather than switch the selected window to one."}]}, {"id": "emacs-split-side-by-side", "question": "Which key sequence splits the selected Emacs window into left and right windows?", "options": [{"text": "`C-x 1`", "correct": false, "explanation": "This deletes other windows and makes the selected window the only one in its frame."}, {"text": "`C-x 2`", "correct": false, "explanation": "This creates upper and lower windows rather than a side-by-side split."}, {"text": "`C-x 3`", "correct": true, "explanation": "`split-window-right`, bound to `C-x 3`, creates left and right windows."}]}, {"id": "emacs-select-other-window", "question": "Which key sequence moves point and keyboard focus to another Emacs window?", "options": [{"text": "`C-x 0`", "correct": false, "explanation": "This deletes the selected window rather than moving focus to another one."}, {"text": "`C-x o`", "correct": true, "explanation": "`other-window` cycles selection to another window in the frame."}, {"text": "`C-x b`", "correct": false, "explanation": "This changes which buffer the current window displays, not which window is selected."}]}, {"id": "emacs-keep-one-window", "question": "Which key sequence keeps the selected window and deletes the other windows in its frame?", "options": [{"text": "`C-x 1`", "correct": true, "explanation": "`delete-other-windows` makes the selected window the only window in the frame."}, {"text": "`C-x 0`", "correct": false, "explanation": "This deletes the selected window itself rather than preserving it."}, {"text": "`C-x 2`", "correct": false, "explanation": "This adds another window instead of reducing the frame to one."}]}, {"id": "emacs-kill-buffer-key", "question": "Which key sequence prompts to kill an Emacs buffer?", "options": [{"text": "`C-x 0`", "correct": false, "explanation": "This deletes a window display but normally leaves the buffer alive."}, {"text": "`C-x k`", "correct": true, "explanation": "`kill-buffer` removes the selected buffer from Emacs after any required modified-buffer confirmation."}, {"text": "`C-x b`", "correct": false, "explanation": "This switches the current window to a named buffer and does not kill it."}]}]}, {"lesson_id": "emacs-editing", "course_id": "advanced-text-fu", "title": "Emacs Editing", "order_index": 12, "description": "Learn how to move point, activate a region, and use Emacs kill-ring commands to edit text.", "content": "Emacs calls the current cursor position **point**. Movement commands reposition point; editing commands insert, delete, kill, copy, or yank text around it. In the key notation below, `C-` means Control and `M-` means Meta, commonly Alt.\n\n## Moving by Characters and Lines\n\nArrow and other platform navigation keys may work, but Emacs's standard movement commands remain available across terminal and graphical sessions:\n\n- `C-f`: Move forward one character.\n- `C-b`: Move backward one character.\n- `C-n`: Move to the next line.\n- `C-p`: Move to the previous line.\n- `C-a`: Move to the beginning of the line.\n- `C-e`: Move to the end of the line.\n\n## Moving by Words and Buffer Boundaries\n\nMeta commands move across larger units:\n\n- `M-f`: Move forward one word.\n- `M-b`: Move backward one word.\n- `M-<`: Move to the beginning of the buffer.\n- `M->`: Move to the end of the buffer.\n\nOn many keyboards, Alt acts as Meta. When that chord is unavailable, pressing `Esc` and then the following key often sends the equivalent Meta command.\n\n## Defining a Region\n\nThe **mark** is a saved buffer position. The text between point and mark is the **region**. Press `C-SPC`, written `C-space` in some documentation, to run `set-mark-command`, then move point to extend the active region.\n\nIn a terminal, `C-SPC` can be encoded as `C-@`. Highlighting depends on transient-mark settings, but point and mark still define a region.\n\n## Killing or Copying a Region\n\nEmacs stores killed and copied text in the **kill ring**:\n\n- `C-w`: Kill the active region, removing it and adding it to the kill ring.\n- `M-w`: Copy the active region to the kill ring without removing it.\n- `C-k`: Kill from point to the end of the line; repeated use can include the newline.\n\nKilling is more than ordinary deletion because the removed text is retained for later yanking.\n\n## Yanking from the Kill Ring\n\nUse `C-y` to yank the most recent kill-ring entry at point. Immediately after a yank, `M-y` replaces that inserted text with an earlier kill-ring entry; repeating `M-y` cycles through entries.\n\n```text\nC-y\nM-y\n```\n\nIf another unrelated command occurs after `C-y`, `M-y` no longer has the same yank-pop context.\n\nPractice in `*scratch*` or a disposable file: move point, set the mark, copy one region, kill another, and yank both back. Save only when the resulting file is worth keeping.\n\n## Summary\n\nYou can now navigate and rearrange Emacs text using point, mark, and the kill ring.\n\n1. Move by characters or lines with Control commands.\n2. Move by words or buffer boundaries with Meta commands.\n3. Set the mark with `C-SPC` to define a region.\n4. Kill with `C-w` or copy with `M-w`.\n5. Yank with `C-y` and cycle with `M-y` immediately afterward.", "code_snippets": ["C-y\nM-y"], "quizzes": [{"id": "emacs-edit-next-line", "question": "Which Emacs key moves point to the next line?", "options": [{"text": "`C-p`", "correct": false, "explanation": "`C-p` moves to the previous line, in the opposite direction."}, {"text": "`C-n`", "correct": true, "explanation": "`C-n`, for next-line, moves point downward to the next screen line position."}, {"text": "`C-f`", "correct": false, "explanation": "`C-f` moves forward one character rather than to the next line."}]}, {"id": "emacs-edit-buffer-end", "question": "Which Emacs key moves point to the end of the buffer?", "options": [{"text": "`C-e`", "correct": false, "explanation": "`C-e` moves to the end of the current line rather than the entire buffer."}, {"text": "`M-<`", "correct": false, "explanation": "`M-<` moves to the beginning of the buffer."}, {"text": "`M->`", "correct": true, "explanation": "`M->` moves point to the end of the current buffer."}]}, {"id": "emacs-edit-set-mark", "question": "Which key begins defining a region by setting the mark at point?", "options": [{"text": "`C-w`", "correct": false, "explanation": "`C-w` kills an already defined region; it is not the initial mark-setting command."}, {"text": "`C-y`", "correct": false, "explanation": "`C-y` inserts text from the kill ring and does not begin a selection."}, {"text": "`C-SPC`", "correct": true, "explanation": "`set-mark-command` places the mark, after which movement changes the region between mark and point."}]}, {"id": "emacs-edit-copy-region", "question": "Which key copies the active region to the kill ring without removing it?", "options": [{"text": "`M-w`", "correct": true, "explanation": "`kill-ring-save`, bound to `M-w`, copies the region without deleting it."}, {"text": "`C-w`", "correct": false, "explanation": "`C-w` removes the region while saving it to the kill ring."}, {"text": "`C-k`", "correct": false, "explanation": "`C-k` kills text toward the end of the line rather than copying the selected region unchanged."}]}, {"id": "emacs-edit-yank-latest", "question": "Which key inserts the most recent kill-ring entry at point?", "options": [{"text": "`C-y`", "correct": true, "explanation": "`yank`, bound to `C-y`, inserts the latest kill-ring text into the current buffer."}, {"text": "`M-y`", "correct": false, "explanation": "`M-y` normally replaces a just-yanked entry with an earlier one; it depends on the preceding yank context."}, {"text": "`C-d`", "correct": false, "explanation": "`C-d` deletes the character after point and does not retrieve kill-ring text."}]}]}, {"lesson_id": "emacs-exiting-and-help", "course_id": "advanced-text-fu", "title": "Emacs Exiting and Help", "order_index": 13, "description": "Learn how to exit Emacs safely, cancel pending commands, inspect help topics, and undo changes.", "content": "Emacs provides contextual help for keys, functions, variables, and active modes. It also protects modified file-visiting buffers during exit, giving you a chance to save or decline each write.\n\n## Exiting Emacs\n\nUse `C-x C-c`, which runs `save-buffers-kill-terminal`, to request that the Emacs session or terminal connection close:\n\n```text\nC-x C-c\n```\n\nEmacs checks relevant modified file-visiting buffers and asks whether to save them. Read each buffer name and answer deliberately. It may also ask about active processes. Cancel the exit if you need to inspect work before deciding.\n\nIn an `emacsclient` workflow or an Emacs server, the exact frame and server behavior can differ, but modified-buffer prompts still deserve careful attention.\n\n## Opening the Help Dispatcher\n\nThe standard help prefix is `C-h`. Use `C-h C-h`, which runs help for help, to display guidance on available help commands:\n\n```text\nC-h C-h\n```\n\nThe second key chooses the type of help you need.\n\n## Describing Keys and Editor State\n\nUseful help commands include:\n\n- `C-h k KEY`: Describe what a key sequence runs.\n- `C-h f FUNCTION`: Describe an Emacs Lisp function.\n- `C-h v VARIABLE`: Describe an Emacs Lisp variable.\n- `C-h m`: Describe the current major and minor modes.\n- `C-h t`: Open the interactive tutorial.\n\nFor example, type `C-h k C-x C-s` to see documentation for the save-buffer binding.\n\n You want to learn what `C-x C-s` does. Which help prefix should you enter before that key sequence?\n\n## Cancelling a Pending Command\n\nUse `C-g`, bound to `keyboard-quit`, when you are stuck in a prompt, partially entered key sequence, incremental search, or other command you want to cancel:\n\n```text\nC-g\n```\n\nIt does not undo buffer changes that have already happened and does not exit Emacs. It stops the current interaction and returns control to ordinary editing when possible.\n\n## Undoing Buffer Changes\n\nUse `C-/`, `C-_`, or `C-x u` to invoke undo in common Emacs configurations:\n\n```text\nC-/\n```\n\nRepeated undo commands walk backward through recent buffer changes. Cursor movement alone is not normally a buffer change. Emacs versions and configurations can offer `undo-redo` and more advanced history tools; use `C-h k` on your actual undo and redo bindings to verify local behavior.\n\nPractice by opening `*scratch*`, making a disposable change, using undo, asking `C-h k` about an unfamiliar key, and cancelling a minibuffer prompt with `C-g` before exiting normally.\n\n## Summary\n\nYou can now recover help and leave Emacs without ignoring unsaved work.\n\n1. Exit through the modified-buffer checks with `C-x C-c`.\n2. Open help for help with `C-h C-h`.\n3. Describe keys, functions, variables, or active modes.\n4. Cancel a pending command with `C-g`.\n5. Undo recent buffer changes with a verified local binding.", "code_snippets": ["C-x C-c", "C-h C-h", "C-g", "C-/"], "quizzes": [{"id": "emacs-exit-key", "question": "Which key sequence requests a normal Emacs exit and checks modified buffers?", "options": [{"text": "`C-x k`", "correct": false, "explanation": "This kills one selected buffer and does not request that the Emacs session exit."}, {"text": "`C-g`", "correct": false, "explanation": "This cancels a pending command or prompt rather than closing Emacs."}, {"text": "`C-x C-c`", "correct": true, "explanation": "This runs the normal save-buffers-and-exit workflow, including prompts for relevant unsaved work."}]}, {"id": "emacs-help-for-help", "question": "Which key sequence explains how to use the Emacs help system?", "options": [{"text": "`C-h C-h`", "correct": true, "explanation": "The help prefix followed by another `C-h` opens help about the help dispatcher itself."}, {"text": "`C-x C-h`", "correct": false, "explanation": "This is not the help-for-help sequence introduced here."}, {"text": "`C-h t`", "correct": false, "explanation": "This opens the tutorial directly rather than explaining the broader help menu."}]}, {"id": "emacs-describe-key", "question": "You want to learn what `C-x C-s` does. Which help prefix should you enter before that key sequence?", "options": [{"text": "`C-h k`", "correct": true, "explanation": "`describe-key` waits for a key sequence and explains the command bound to it."}, {"text": "`C-h f`", "correct": false, "explanation": "This prompts for a function name rather than reading a key sequence to identify its binding."}, {"text": "`C-h v`", "correct": false, "explanation": "This prompts for a variable name and does not inspect a key binding."}]}, {"id": "emacs-cancel-pending-command", "question": "Which key normally cancels the current Emacs prompt or pending command?", "options": [{"text": "`C-x C-c`", "correct": false, "explanation": "This initiates the Emacs exit workflow rather than merely cancelling the current prompt."}, {"text": "`C-y`", "correct": false, "explanation": "This yanks text from the kill ring and does not cancel a command."}, {"text": "`C-g`", "correct": true, "explanation": "`keyboard-quit` aborts the current command interaction and returns control to Emacs."}]}, {"id": "emacs-undo-change", "question": "Which key sequence is a standard binding for undoing a recent Emacs buffer change?", "options": [{"text": "`C-/`", "correct": true, "explanation": "`C-/` is a standard undo binding, alongside `C-_` and `C-x u` in common configurations."}, {"text": "`C-x C-s`", "correct": false, "explanation": "This saves the current buffer rather than traversing its undo history."}, {"text": "`C-w`", "correct": false, "explanation": "This kills the active region and creates another change instead of undoing one."}]}]}]}, {"course_id": "user-management", "course_name": "User Management", "icon": "\ud83d\udc65", "description": "User accounts, groups, /etc/passwd, /etc/shadow, root, and sudo.", "lessons_count": 6, "quizzes_count": 30, "lessons": [{"lesson_id": "users-and-groups", "course_id": "user-management", "title": "Users and Groups", "order_index": 1, "description": "Learn how Linux identifies users and groups and how process credentials affect access decisions.", "content": "Linux uses user and group identities to label processes, own filesystem objects, and make access-control decisions. Human-readable names help administrators, while the kernel primarily works with numeric identifiers and process credentials.\n\n## Identifying Users with UIDs\n\nEach account has a numeric user ID, or **UID**. Usernames map to UIDs through the system's account databases. Files store numeric ownership, which tools normally display as a corresponding name.\n\nRun `id` to inspect the current process identity information:\n\n```bash\n$ id\nuid=1000(alice) gid=1000(alice) groups=1000(alice),27(sudo)\n```\n\nValues differ by system. Human login accounts commonly have home directories such as `/home/alice`, but accounts can use another path or no ordinary home at all. Service accounts often exist to run software with limited identity rather than to support interactive login.\n\n## Organizing Access with Groups\n\nA group has a numeric group ID, or **GID**. An account normally has one primary group and can belong to supplementary groups. Group membership lets administrators grant access to a set of users without assigning permissions one account at a time.\n\nInspect memberships with:\n\n```bash\n$ id alice\n$ groups alice\n```\n\nThese commands report configured or resolved identity information. Directory services and caches can participate, so directly reading `/etc/group` does not always show the complete effective membership picture.\n\n## Understanding Process Credentials\n\nA process has credentials such as real and effective UIDs and GIDs plus supplementary groups. The effective credentials are central to many permission checks. A process started by a user usually inherits credentials from its parent, but controlled mechanisms can change them.\n\nThis is more precise than saying a process always runs only \u201cas the user who started it.\u201d Set-user-ID executables, service managers, containers, namespaces, and privilege-changing system calls can affect the identities visible or effective in a particular context.\n\n## Recognizing the Root Identity\n\nThe account traditionally named `root` has UID 0. UID 0 is treated specially by many Linux permission mechanisms and carries broad administrative power. Modern Linux can also divide privileges through capabilities, namespaces, mandatory access controls, and service confinement, so \u201cunlimited power in every context\u201d is an oversimplification.\n\nRoutine work should use an unprivileged account. Administrative authority increases the impact of path mistakes, untrusted commands, and compromised software.\n\n## Using sudo under a Policy\n\n`sudo` asks its configured policy whether the invoking user may run a command as a target user. The default target is often root, but a policy or `-u USER` can select another account. Authentication prompts and logging also depend on configuration.\n\nList the commands the current account is allowed to run:\n\n```bash\n$ sudo -l\n```\n\nOnly use an allowed administrative command when the task requires it and you understand its effects. Do not use `sudo` merely to silence a permission error, and do not display password-hash databases such as `/etc/shadow` as a casual exercise.\n\nTo practice account and group administration in a controlled environment, try these hands-on labs:\n\n1. **[Manage Linux User Accounts with useradd, usermod, and userdel](https://labex.io/labs/comptia-manage-linux-user-accounts-with-useradd-usermod-and-userdel-590837)** - Practice the complete lifecycle of user administration, from creating and securing new accounts to modifying and deleting them.\n2. **[Manage Linux Groups with groupadd, usermod, and groupdel](https://labex.io/labs/comptia-manage-linux-groups-with-groupadd-usermod-and-groupdel-590836)** - Gain hands-on experience with core command-line utilities for group administration, including creating new groups, modifying user memberships, and removing groups.\n3. **[Configure User Accounts and Sudo Privileges in Linux](https://labex.io/labs/comptia-configure-user-accounts-and-sudo-privileges-in-linux-590856)** - Learn essential techniques for managing user accounts and `sudo` privileges to enhance the security of a Linux system, including granting administrative permissions.\n\n## Summary\n\nYou can now describe how Linux represents identities and delegates administrative commands.\n\n1. Identify accounts by UID and groups by GID.\n2. Distinguish primary and supplementary group membership.\n3. Relate process credentials to access checks.\n4. Recognize UID 0 as the traditional root identity.\n5. Treat `sudo` as a policy-controlled delegation tool.", "code_snippets": ["$ id\nuid=1000(alice) gid=1000(alice) groups=1000(alice),27(sudo)", "$ id alice\n$ groups alice", "$ sudo -l"], "quizzes": [{"id": "users-uid-purpose", "question": "Which identifier does the kernel primarily use to represent a user identity?", "options": [{"text": "A home-directory pathname", "correct": false, "explanation": "A home path is account configuration and can vary or be absent; it is not the kernel's user identifier."}, {"text": "A numeric UID", "correct": true, "explanation": "Account databases map names to numeric UIDs, which are used in process credentials and ownership records."}, {"text": "A terminal window number", "correct": false, "explanation": "Terminal devices and sessions are separate from numeric user identities."}]}, {"id": "users-primary-supplementary-groups", "question": "How can one Linux account normally participate in groups?", "options": [{"text": "It can belong to exactly one group for its entire lifetime.", "correct": false, "explanation": "Linux processes can carry a primary group plus a list of supplementary groups."}, {"text": "It belongs to every group whose files it can read.", "correct": false, "explanation": "File readability follows permissions and credentials; it does not automatically create group membership."}, {"text": "It has one primary group and may have supplementary groups.", "correct": true, "explanation": "The primary GID is part of the account record, while supplementary memberships provide additional group identities."}]}, {"id": "users-process-access-identity", "question": "Which information is commonly considered when the kernel checks a process against file permissions?", "options": [{"text": "The process's effective UID, effective GID, and supplementary groups.", "correct": true, "explanation": "These credentials are compared with ownership and permission data during ordinary discretionary access checks."}, {"text": "The color theme of the terminal that launched the process.", "correct": false, "explanation": "Display preferences have no role in filesystem permission checks."}, {"text": "The spelling length of the account's username.", "correct": false, "explanation": "The kernel works with numeric credentials; username length does not grant access."}]}, {"id": "users-root-uid", "question": "What numeric UID traditionally identifies the root account?", "options": [{"text": "`0`", "correct": true, "explanation": "Linux and Unix-like systems traditionally reserve UID 0 for the superuser identity."}, {"text": "`1000`", "correct": false, "explanation": "Many distributions assign a value near 1000 to the first regular human account, but this is not the root UID."}, {"text": "`1`", "correct": false, "explanation": "UID 1 can belong to a system account and is not the traditional superuser identity."}]}, {"id": "users-sudo-policy", "question": "What does `sudo` do before running a requested command?", "options": [{"text": "Consults configured policy for permission to use the requested target identity.", "correct": true, "explanation": "`sudo` authorizes according to policy and then establishes the configured target credentials when permitted."}, {"text": "Always grants every local user unrestricted root access.", "correct": false, "explanation": "Authorization is policy-controlled, and denied users or commands do not receive blanket root access."}, {"text": "Changes the invoking account's permanent UID to 0.", "correct": false, "explanation": "`sudo` runs a command with target credentials; it does not permanently rewrite the caller's account identity."}]}]}, {"lesson_id": "root-user", "course_id": "user-management", "title": "root", "order_index": 2, "description": "Learn how su, sudo, and sudoers policy provide controlled access to privileged identities.", "content": "The account traditionally named `root` has UID 0 and broad authority in its security context. Use an unprivileged account for routine work and elevate only for a specific administrative purpose you understand.\n\n## Starting a Shell as Another User with su\n\n`su`, meaning substitute user, starts a shell or command with another account's identity. With no username, the target defaults to root:\n\n```bash\n$ su\n```\n\nAuthentication is controlled by PAM and local policy. A system may ask for the target account's password, restrict who can use `su`, or keep the root password locked. Do not assume knowledge of a password is the only condition.\n\nPlain `su` changes identity while preserving more of the current environment. `su - USER`, also written `su --login USER`, starts a login-style shell and initializes an environment closer to a fresh login for the target account:\n\n```bash\n$ su - operator\n```\n\nExit the subshell when the target-specific work is complete.\n\n## Running a Specific Command with sudo\n\n`sudo COMMAND` requests policy authorization to run one command as a target user, usually root by default. Use `-u USER` to request another target:\n\n```bash\n$ sudo -u postgres id\n```\n\nThis does not mean the request will be permitted. Sudo policy controls the invoking user, host, target identity, command, and other conditions. Authentication may use the invoking user's password, another mechanism, or no prompt depending on configuration.\n\nPrefer one narrowly scoped administrative command over a long-lived privileged shell when practical. The smaller scope makes accidental commands less likely to run with elevated authority.\n\n## Avoiding Persistent Privileged Shells\n\nCommands such as `su -`, `sudo -s`, or `sudo -i` can create a privileged shell when policy permits. Every later command in that shell can have elevated impact until you exit it. Path mistakes, unreviewed scripts, and shell expansions become more dangerous.\n\nAudit behavior is configuration-dependent. `sudo` commonly records invocations, but a single logged shell launch does not automatically provide a complete record of every command typed inside that shell. Shell history, system audit, and sudo I/O logging are separate mechanisms with their own policies.\n\n## Reviewing sudo Authorization\n\nRun `sudo -l` to list what the current account may request under the active policy:\n\n```bash\n$ sudo -l\n```\n\nReview command paths, permitted target users, and argument restrictions. A broad-looking rule should not be treated as permission to perform unrelated work.\n\n## Editing sudoers Policy Safely\n\nThe default sudo policy commonly reads `/etc/sudoers` and may include files under `/etc/sudoers.d/`. Other policy sources are possible. Syntax controls much more than a simple list of users and groups.\n\nUse `visudo` for policy changes because it locks the file and validates syntax before installation:\n\n```bash\n$ sudo visudo\n```\n\nFor a drop-in file, specify its exact path:\n\n```bash\n$ sudo visudo -f /etc/sudoers.d/application-admins\n```\n\nDo not edit sudoers with an ordinary redirection or unvalidated editor workflow. A syntax or permission mistake can remove administrative access. Keep another verified recovery path available when changing remote authorization.\n\nTo practice delegated administration in a controlled environment, try this hands-on lab:\n\n1. **[Configure User Accounts and Sudo Privileges in Linux](https://labex.io/labs/comptia-configure-user-accounts-and-sudo-privileges-in-linux-590856)** - Practice enforcing password policies, locking and unlocking user accounts, securing the root account, and granting administrative permissions, directly relating to the management of superuser access.\n\n## Summary\n\nYou can now distinguish identity switching from policy-controlled command delegation.\n\n1. Use `su - USER` only when a target login shell is intended.\n2. Request a specific sudo target with `-u USER`.\n3. Minimize time spent in a privileged shell.\n4. Review effective sudo rules with `sudo -l`.\n5. Edit sudoers policy only through `visudo`.", "code_snippets": ["$ su", "$ su - operator", "$ sudo -u postgres id", "$ sudo -l", "$ sudo visudo", "$ sudo visudo -f /etc/sudoers.d/application-admins"], "quizzes": [{"id": "root-su-login-shell", "question": "Which command requests a login-style shell as the user `operator`?", "options": [{"text": "`su - operator`", "correct": true, "explanation": "The hyphen requests login-shell behavior and a target-oriented environment for `operator`."}, {"text": "`su operator`", "correct": false, "explanation": "This changes to the target identity but does not request the full login-style initialization introduced here."}, {"text": "`sudo -l operator`", "correct": false, "explanation": "`sudo -l` lists allowed commands under policy; it does not start the requested login shell."}]}, {"id": "root-sudo-target-user", "question": "What does `sudo -u postgres id` request?", "options": [{"text": "Permanently rename the current account to `postgres`.", "correct": false, "explanation": "`sudo` runs a command with target credentials; it does not rename account records."}, {"text": "Run `id` with `postgres` as the target user, subject to policy.", "correct": true, "explanation": "The `-u` option selects the target identity, while sudoers policy decides whether the request is allowed."}, {"text": "List every user whose UID is greater than the current user's.", "correct": false, "explanation": "The `id` command reports identity information for its process; this syntax does not enumerate account UIDs."}]}, {"id": "root-persistent-shell-risk", "question": "Why is a long-lived root shell riskier than elevating one understood command at a time?", "options": [{"text": "Root shells automatically delete every command from all audit systems.", "correct": false, "explanation": "Logging varies by configuration; it is inaccurate to claim that all audit records are automatically erased."}, {"text": "The shell disables filesystem pathnames longer than one component.", "correct": false, "explanation": "Privilege does not impose this pathname restriction; the concern is the authority applied to ordinary operations."}, {"text": "Later commands can retain elevated impact until the shell exits.", "correct": true, "explanation": "A persistent privileged identity expands the window in which a typo or untrusted command can modify protected resources."}]}, {"id": "root-list-sudo-rules", "question": "Which command lists sudo privileges available to the current invoking user?", "options": [{"text": "`sudo -i`", "correct": false, "explanation": "This requests a target login-style shell and can increase privilege scope; it is not a read-only policy listing."}, {"text": "`sudo -l`", "correct": true, "explanation": "The lowercase `-l` option asks sudo to list the commands allowed by its current policy."}, {"text": "`su -l`", "correct": false, "explanation": "This invokes login-shell behavior for `su` rather than listing sudo authorization."}]}, {"id": "root-edit-sudoers-safely", "question": "Which tool should be used to edit and syntax-check the main sudoers policy?", "options": [{"text": "`cat`", "correct": false, "explanation": "`cat` can display readable text but does not safely edit, lock, or validate sudoers syntax."}, {"text": "`visudo`", "correct": true, "explanation": "`visudo` provides locking and syntax validation designed for sudoers policy changes."}, {"text": "`echo` with `>`", "correct": false, "explanation": "Shell redirection can truncate policy immediately and offers no sudoers syntax validation."}]}]}, {"lesson_id": "etc-passwd-file", "course_id": "user-management", "title": "/etc/passwd", "order_index": 3, "description": "Learn how to read local passwd records and distinguish them from the complete NSS account view.", "content": "`/etc/passwd` stores local account records in a colon-separated text format. It maps login names to numeric UIDs and records a primary GID, descriptive field, home path, and login program.\n\n## Local Records versus Resolved Accounts\n\nDisplay the local file with a read-only command:\n\n```bash\n$ cat /etc/passwd\n```\n\nThis is not necessarily every account known to the system. The Name Service Switch (NSS) can resolve accounts from files, directory services, system databases, or other configured sources. Use `getent` to query the resolved passwd database:\n\n```bash\n$ getent passwd\n$ getent passwd root\n```\n\nThe first command can disclose account names and metadata, so review output before sharing it publicly.\n\n## Reading the Seven Fields\n\nA local record commonly looks like this:\n\n```text\nroot:x:0:0:root:/root:/bin/bash\n```\n\nThe seven colon-separated fields are:\n\n1. **Login name**: The human-readable account name, such as `root`.\n2. **Password field**: Usually `x` on a shadow-password system, indicating protected password data is stored separately.\n3. **UID**: The numeric user identity. UID 0 has traditional superuser treatment.\n4. **Primary GID**: The numeric ID of the account's primary group.\n5. **GECOS/comment**: Descriptive account information, often internally comma-separated.\n6. **Home directory**: The path used as the account's home setting; it may be absent on disk.\n7. **Login shell/program**: The program requested for applicable login sessions, such as `/bin/bash` or a non-login program.\n\nThe kernel does not require UID values to be unique across malformed or deliberately duplicated records, but accounts that share a UID are indistinguishable for many ownership and permission decisions. Administrators should normally keep account UIDs unique.\n\n## Interpreting the Password Placeholder\n\nOn typical shadow-password systems, `x` in field 2 directs password-aware tools to protected data in `/etc/shadow`. Values such as `*` or `!` are not valid password hashes and generally prevent authentication with a Unix password through that entry.\n\nThat does not prove the account cannot authenticate by every method. SSH keys, certificates, tokens, or service-specific mechanisms may be independent. Likewise, an empty password field has security-sensitive behavior that depends on the authentication stack; do not create or \u201cfix\u201d it manually.\n\n## Recognizing Service Accounts\n\nMany records represent services rather than people. Separate service identities help confine files and processes to the authority required by one daemon. Their home paths can be nonstandard or nonexistent, and their login program may be `/usr/sbin/nologin`, `/bin/false`, or another restricted program.\n\nDo not infer account purpose from UID range alone without checking the distribution's policy. Allocation ranges vary, and centrally managed accounts may follow different conventions.\n\n## Modifying Account Records Safely\n\nPrefer account-management tools such as `useradd`, `usermod`, and `userdel` because they coordinate related records and apply system defaults. Their exact behavior is distribution-configurable, so review options before changing an account.\n\nIf a local passwd database truly requires manual repair, use `vipw` rather than an ordinary editor. It applies locking intended to avoid concurrent edits. Validate databases with tools such as `pwck` and maintain a recovery session before changing authentication files remotely.\n\nTo practice user and group records in a controlled environment, try these hands-on labs:\n\n1. **[Manage Linux User Accounts with useradd, usermod, and userdel](https://labex.io/labs/comptia-manage-linux-user-accounts-with-useradd-usermod-and-userdel-590837)** - Practice the complete lifecycle of user administration, from creating and securing new accounts to modifying and deleting them.\n2. **[Manage Linux Groups with groupadd, usermod, and groupdel](https://labex.io/labs/comptia-manage-linux-groups-with-groupadd-usermod-and-groupdel-590836)** - Gain hands-on experience with core command-line utilities for group administration, including creating new groups and modifying user memberships.\n## Summary\n\nYou can now interpret local passwd records without mistaking them for the complete identity database.\n\n1. Query NSS-resolved accounts with `getent passwd`.\n2. Read the seven colon-separated passwd fields.\n3. Locate the UID and primary GID fields.\n4. Interpret password placeholders without overclaiming login state.\n5. Use account tools or `vipw` instead of an ordinary editor.", "code_snippets": ["$ cat /etc/passwd", "$ getent passwd\n$ getent passwd root", "root:x:0:0:root:/root:/bin/bash"], "quizzes": [{"id": "passwd-query-resolved-database", "question": "Which command queries the NSS-resolved passwd database rather than reading only the local file?", "options": [{"text": "`cat /etc/passwd`", "correct": false, "explanation": "This displays only the local file and does not include accounts supplied solely by other NSS sources."}, {"text": "`cat /etc/shadow`", "correct": false, "explanation": "The shadow file contains protected local password-aging data and should not be displayed for this purpose."}, {"text": "`getent passwd`", "correct": true, "explanation": "`getent` consults the configured passwd database sources through NSS."}]}, {"id": "passwd-uid-field", "question": "In `root:x:0:0:root:/root:/bin/bash`, which field contains the UID?", "options": [{"text": "The second field, `x`", "correct": false, "explanation": "The second field is the password placeholder, not the numeric user identity."}, {"text": "The fourth field, the second `0`", "correct": false, "explanation": "Field 4 is the primary GID rather than the UID."}, {"text": "The third field, the first `0`", "correct": true, "explanation": "Field 3 is the UID, so the first zero identifies this record as UID 0."}]}, {"id": "passwd-primary-gid-field", "question": "Which field of a passwd record stores the account's primary GID?", "options": [{"text": "Field 5", "correct": false, "explanation": "The fifth field is the GECOS or comment field."}, {"text": "Field 4", "correct": true, "explanation": "The fourth colon-separated field identifies the primary group numerically."}, {"text": "Field 7", "correct": false, "explanation": "The seventh field specifies the login shell or program."}]}, {"id": "passwd-x-placeholder", "question": "What does `x` commonly mean in field 2 of a local `/etc/passwd` record?", "options": [{"text": "The account is guaranteed to have no authentication method.", "correct": false, "explanation": "The placeholder does not describe every possible authentication method and does not itself mean the account is unusable."}, {"text": "The account's home directory has been deleted.", "correct": false, "explanation": "Home-directory information is stored in field 6 and is unrelated to the `x` placeholder."}, {"text": "Protected password data is kept in the shadow database.", "correct": true, "explanation": "The public passwd record carries a placeholder while password hash and aging fields live in protected shadow data."}]}, {"id": "passwd-nologin-shell", "question": "What is a common purpose of a login program such as `/usr/sbin/nologin` in field 7?", "options": [{"text": "Delete the account's files whenever a service stops.", "correct": false, "explanation": "The login program does not automatically remove owned data or manage service shutdown files."}, {"text": "Prevent an ordinary interactive shell through login paths that honor the field.", "correct": true, "explanation": "A non-login program is commonly used for service accounts that should not receive an interactive shell through normal login."}, {"text": "Grant the account the same privileges as UID 0.", "correct": false, "explanation": "Restricting interactive login does not elevate the account or change its numeric UID."}]}]}, {"lesson_id": "etc-shadow-file", "course_id": "user-management", "title": "/etc/shadow", "order_index": 4, "description": "Learn how local shadow records represent password hashes and aging policy without exposing sensitive data.", "content": "`/etc/shadow` stores protected local password-hash and password-aging fields. Separating these values from the generally readable `/etc/passwd` database reduces exposure to offline password-guessing attacks.\n\n## Protecting Shadow Data\n\nPasswords are not stored reversibly \u201cencrypted\u201d for later display. A local password entry normally contains a one-way password hash encoded with an algorithm identifier, salt, and parameters. An attacker who obtains hashes can guess candidate passwords offline, so the database should remain restricted.\n\nExact ownership and permission details vary, but access is commonly limited to root and narrowly authorized system components. Do not print, copy, log, or share shadow contents merely to inspect account status.\n\n## Reading the Nine-Field Format\n\nA local shadow record contains nine colon-separated fields. A schematic record looks like this, with the hash deliberately omitted:\n\n```text\nalice:<password-field>:20000:0:90:7:14:20500:\n```\n\nThe fields are:\n\n1. **Login name**.\n2. **Password hash or special password marker**.\n3. **Last password change**, in days since 1970-01-01; `0` requests a change at the next password-authenticated login in typical tooling.\n4. **Minimum password age**, in days.\n5. **Maximum password age**, in days.\n6. **Warning period** before password expiration, in days.\n7. **Inactivity period** after password expiration, in days.\n8. **Account expiration date**, in days since 1970-01-01.\n9. **Reserved field**.\n\nEmpty fields and special numeric values have defined meanings that can vary by field and tooling. Use account-management commands instead of editing values by sight.\n\n## Interpreting the Password Field Carefully\n\nA valid hash in field 2 supports local Unix-password verification. A value beginning with `!` commonly locks that password hash, while `*` or another invalid hash marker prevents successful password verification through that field. An empty value is security-sensitive and can allow passwordless behavior depending on PAM policy.\n\nThese markers describe the local password path, not every possible authentication method. SSH public keys, certificates, tokens, and application-specific credentials can remain usable unless separately restricted. Account expiration in field 8 is also distinct from password locking.\n\n## Distinguishing Password and Account Dates\n\nFields 3 through 7 concern password aging: when the password last changed, when another change is allowed, when it expires, when warnings begin, and how long after expiration password login remains available. Field 8 expires the account on an absolute day regardless of the password's age.\n\nFor example, a 90-day maximum password age is not the same as an account expiration date. The former moves relative to the last password change; the latter is a fixed date until an administrator changes it.\n\n## Inspecting and Changing Policy through Tools\n\nAdministrators should query only the information required for the task:\n\n```bash\n$ sudo passwd -S alice\n$ sudo chage -l alice\n```\n\n`passwd -S` summarizes local password status, while `chage -l` lists aging information in a readable form. Output formats and authorization requirements can vary by distribution.\n\nUse `passwd`, `chage`, `usermod`, and related account tools for changes. If manual repair of the local shadow database is unavoidable, `vipw -s` provides locking; validate account databases with `pwck`. Maintain a recovery session before remote authentication changes.\n\nPAM and NSS can integrate authentication and identity sources beyond local shadow files. A system account may therefore have no local shadow record or may authenticate through additional services.\n\nTo practice account status and aging policy in a controlled environment, try these hands-on labs:\n\n1. **[Manage Linux User Accounts with useradd, usermod, and userdel](https://labex.io/labs/comptia-manage-linux-user-accounts-with-useradd-usermod-and-userdel-590837)** - Practice the complete lifecycle of user administration, from creating and securing new accounts with `useradd` and `passwd` to modifying and deleting them.\n2. **[Configure User Accounts and Sudo Privileges in Linux](https://labex.io/labs/comptia-configure-user-accounts-and-sudo-privileges-in-linux-590856)** - Learn essential techniques for managing user accounts and sudo privileges, including enforcing password policies and securing accounts.\n\n## Summary\n\nYou can now interpret shadow policy without exposing the complete password database.\n\n1. Treat password hashes as restricted authentication material.\n2. Read the nine shadow fields by purpose.\n3. Distinguish password locking from disabling every login method.\n4. Separate password aging from absolute account expiration.\n5. Inspect and change policy through focused account tools.", "code_snippets": ["alice:<password-field>:20000:0:90:7:14:20500:", "$ sudo passwd -S alice\n$ sudo chage -l alice"], "quizzes": [{"id": "shadow-restricted-reason", "question": "Why is local shadow data normally protected from general read access?", "options": [{"text": "The file contains every user's unencrypted current password.", "correct": false, "explanation": "Proper shadow entries store one-way password hashes or special markers, not retrievable plaintext passwords."}, {"text": "Password hashes can be attacked offline if they are disclosed.", "correct": true, "explanation": "An attacker can test password guesses against stolen hashes without interacting with the login service."}, {"text": "Reading it automatically changes all password-expiration dates.", "correct": false, "explanation": "A read does not inherently update policy fields; the concern is disclosure of sensitive authentication material."}]}, {"id": "shadow-account-expiration-field", "question": "Which shadow field stores the account expiration date as days since 1970-01-01?", "options": [{"text": "Field 3", "correct": false, "explanation": "Field 3 records the last password-change date rather than account expiration."}, {"text": "Field 8", "correct": true, "explanation": "The eighth field is the absolute account-expiration day count."}, {"text": "Field 5", "correct": false, "explanation": "Field 5 records the maximum password age."}]}, {"id": "shadow-password-lock-scope", "question": "What can you safely conclude from a shadow password field that begins with `!`?", "options": [{"text": "The stored Unix password hash has been made unusable for normal password verification.", "correct": true, "explanation": "Prefixing the hash with `!` prevents it from matching a supplied password through the shadow password path."}, {"text": "Every possible login method for the account is disabled.", "correct": false, "explanation": "Other authentication methods can be independent, so the password marker alone does not prove a complete account lockout."}, {"text": "The account has been deleted from all identity databases.", "correct": false, "explanation": "A shadow record still exists, and deletion is a separate account-management operation."}]}, {"id": "shadow-max-age-versus-expire", "question": "What is the difference between shadow fields 5 and 8?", "options": [{"text": "Field 5 stores the username; field 8 stores the login shell.", "correct": false, "explanation": "Username is field 1, and the login shell is recorded in `/etc/passwd`, not the shadow record."}, {"text": "Field 5 stores a password hash; field 8 stores its salt.", "correct": false, "explanation": "The password hash encoding belongs in field 2, and aging fields do not separately store its salt."}, {"text": "Field 5 is maximum password age; field 8 is an absolute account expiration date.", "correct": true, "explanation": "Password age is relative to the last change, while account expiration is stored as an absolute day count."}]}, {"id": "shadow-list-aging-policy", "question": "Which command is designed to list readable password-aging information for the local account `alice`?", "options": [{"text": "`cat /etc/shadow`", "correct": false, "explanation": "This exposes every local shadow record and more sensitive information than the task requires."}, {"text": "`passwd -d alice`", "correct": false, "explanation": "The `-d` operation removes the password hash and is a state-changing, security-sensitive action rather than a listing command."}, {"text": "`chage -l alice`", "correct": true, "explanation": "The lowercase `-l` option asks `chage` to display the account's password-aging fields in readable form."}]}]}, {"lesson_id": "etc-group-file", "course_id": "user-management", "title": "/etc/group", "order_index": 5, "description": "Learn how local group records map names to GIDs and list supplementary members.", "content": "`/etc/group` stores local group records. It maps group names to numeric GIDs and lists explicit members, supporting access control shared by several accounts.\n\n## Local Groups versus Resolved Groups\n\nThe file is only one possible group source. NSS can resolve groups from local files, directory services, or other configured databases. Display the local records with:\n\n```bash\n$ cat /etc/group\n```\n\nQuery the resolved group database with `getent`:\n\n```bash\n$ getent group\n$ getent group developers\n```\n\nGroup lists can disclose internal account and role names, so review output before sharing it.\n\n## Reading the Four Fields\n\nA local record has four colon-separated fields:\n\n```text\ndevelopers:x:1500:alice,bob\n```\n\n1. **Group name**: `developers`.\n2. **Password field**: Commonly `x`, `*`, or another placeholder; protected group-password data can be stored in `/etc/gshadow`.\n3. **GID**: The numeric group identity, `1500` here.\n4. **Member list**: Comma-separated explicit member names, `alice` and `bob` here.\n\nGroup passwords are a legacy feature used by tools such as `newgrp` in some configurations. They are not the normal mechanism for granting sudo authorization and should not be introduced through manual field edits.\n\n## Accounting for Primary Group Membership\n\nThe member list in `/etc/group` does not normally repeat users whose passwd record names that GID as their primary group. A user can therefore be a member even when their name is absent from field 4.\n\nFor example, if Alice's passwd record has primary GID 1500, she belongs to `developers` even if the local group record ends with an empty member field:\n\n```text\ndevelopers:x:1500:\n```\n\nThis is why parsing field 4 alone produces an incomplete membership view.\n\n Alice's passwd record uses GID 1500 as its primary GID, but her name is absent from group 1500's field 4. Is she a member of that group?\n\n## Inspecting a User's Groups\n\nUse `id USER` or `groups USER` for a resolved account view:\n\n```bash\n$ id alice\n$ groups alice\n```\n\nFor the current process, plain `id` reports the groups actually present in its credentials. A newly configured supplementary membership usually does not appear in an already running login session; start a new authenticated session or use a deliberately configured mechanism such as `newgrp` when appropriate.\n\n## Changing Local Groups Safely\n\nUse tools such as `groupadd`, `groupmod`, `groupdel`, `gpasswd`, and `usermod` rather than editing records with a general-purpose editor. Be especially careful with:\n\n- `usermod -aG GROUP USER`, which appends supplementary membership.\n- `usermod -G ...`, which replaces the supplementary group list when `-a` is omitted.\n\nIf manual local database repair is unavoidable, use `vigr` for locking and `grpck` for validation. Keep a recovery path before remote identity changes.\n\nTo practice local group management in a controlled environment, try these hands-on labs:\n\n1. **[Manage Linux User Accounts with useradd, usermod, and userdel](https://labex.io/labs/comptia-manage-linux-user-accounts-with-useradd-usermod-and-userdel-590837)** - Practice the complete lifecycle of user administration, from creating and securing new accounts to modifying and deleting them.\n2. **[Manage Linux Groups with groupadd, usermod, and groupdel](https://labex.io/labs/comptia-manage-linux-groups-with-groupadd-usermod-and-groupdel-590836)** - Gain hands-on experience with core command-line utilities for group administration, including `groupadd`, `usermod`, and `groupdel`.\n3. **[Add New User and Group](https://labex.io/labs/linux-add-new-user-and-group-17987)** - Simulate adding new team members to a server environment by creating new user accounts, setting up custom groups, and managing group memberships.\n\n## Summary\n\nYou can now interpret local group records and resolve complete membership more accurately.\n\n1. Query configured group sources with `getent group`.\n2. Read the four colon-separated group fields.\n3. Locate the numeric GID and explicit member list.\n4. Include primary membership from passwd records.\n5. Inspect active credentials before relying on a changed membership.", "code_snippets": ["$ cat /etc/group", "$ getent group\n$ getent group developers", "developers:x:1500:alice,bob", "developers:x:1500:", "$ id alice\n$ groups alice"], "quizzes": [{"id": "group-query-resolved-database", "question": "Which command queries the NSS-resolved group database?", "options": [{"text": "`getent group`", "correct": true, "explanation": "`getent` consults the configured NSS sources for group records."}, {"text": "`cat /etc/group`", "correct": false, "explanation": "This reads only the local group file and can omit groups supplied by other sources."}, {"text": "`groups /etc/group`", "correct": false, "explanation": "`groups` expects user names and reports memberships; it does not treat the local database pathname as an NSS query."}]}, {"id": "group-gid-field", "question": "In `developers:x:1500:alice,bob`, which field contains the GID?", "options": [{"text": "The second field, `x`", "correct": false, "explanation": "Field 2 is the group-password placeholder rather than the numeric identity."}, {"text": "The fourth field, `alice,bob`", "correct": false, "explanation": "Field 4 lists explicit member names rather than the GID."}, {"text": "The third field, `1500`", "correct": true, "explanation": "The third colon-separated field is the numeric group ID."}]}, {"id": "group-explicit-member-field", "question": "How are explicit member names represented in a local group record?", "options": [{"text": "As a comma-separated list in field 4.", "correct": true, "explanation": "The final field contains explicit supplementary member names separated by commas."}, {"text": "As a space-separated list in field 2.", "correct": false, "explanation": "Field 2 is reserved for password-related data or a placeholder, not the member list."}, {"text": "As numeric UIDs embedded in the group name.", "correct": false, "explanation": "The group name and member names are separate fields; ordinary member entries are login names, not embedded UID digits."}]}, {"id": "group-primary-membership-visibility", "question": "Alice's passwd record uses GID 1500 as its primary GID, but her name is absent from group 1500's field 4. Is she a member of that group?", "options": [{"text": "No, every membership must appear in `/etc/group` field 4.", "correct": false, "explanation": "This ignores primary GID membership and would undercount group members."}, {"text": "Yes, primary membership comes from the passwd record's GID field.", "correct": true, "explanation": "The group file's explicit list is mainly for supplementary memberships; primary membership is recorded with the account."}, {"text": "Only if the group password field contains her username.", "correct": false, "explanation": "The password field is unrelated to declaring primary membership."}]}, {"id": "group-current-process-credentials", "question": "Which command reports the UID, primary GID, and supplementary groups of the current process?", "options": [{"text": "`id`", "correct": true, "explanation": "With no user operand, `id` reports identity credentials for the current process."}, {"text": "`cat /etc/group`", "correct": false, "explanation": "The local file lists records but does not show which resolved groups are active in the current process."}, {"text": "`getent passwd`", "correct": false, "explanation": "This queries account records and does not specifically report the current process's supplementary group list."}]}]}, {"lesson_id": "user-management-tools", "course_id": "user-management", "title": "User Management Tools", "order_index": 6, "description": "Learn how to create, modify, secure, verify, and remove local accounts with explicit options.", "content": "Linux distributions commonly provide account tools from the shadow utilities suite, but defaults and higher-level wrappers vary. Before changing a local account, confirm that it is not centrally managed, review the command's local manual, and maintain a recovery path.\n\nThe commands in this lesson change authentication and ownership state. Practice only in an authorized disposable environment, not on a production host.\n\n## Reviewing Account-Creation Defaults\n\n`useradd` creates a local account using command options plus site defaults. Inspect compiled and configured defaults with:\n\n```bash\n$ useradd -D\n```\n\nFiles such as `/etc/default/useradd`, `/etc/login.defs`, and skeleton content can influence behavior, but their roles vary by distribution. A higher-level `adduser` command may exist, but its interface is not standardized across all Linux systems.\n\n## Creating a Local Account Explicitly\n\nIn a controlled environment, specify important properties rather than relying on unknown defaults:\n\n```bash\n$ sudo useradd -m -s /bin/bash -c \"Bob Example\" bob\n```\n\n- `-m` requests creation of the home directory.\n- `-s /bin/bash` chooses the login shell after confirming that path is permitted and installed.\n- `-c` supplies the GECOS/comment field.\n\nThe new account usually cannot authenticate with a usable local password until one is set, but exact initial password and lock state depend on local tooling and policy. Verify the records instead of assuming:\n\n```bash\n$ getent passwd bob\n$ sudo passwd -S bob\n$ id bob\n```\n\n## Setting or Changing a Password\n\nA regular user changes their own local password interactively with:\n\n```bash\n$ passwd\n```\n\nAn authorized administrator can set another local account's password with:\n\n```bash\n$ sudo passwd bob\n```\n\nEnter passwords only at the protected prompt, not in command arguments, shell history, lesson notes, or chat. PAM policy can reject weak or reused passwords. Directory-managed accounts can require a different tool.\n\n## Modifying Account Properties and Groups\n\n`usermod` changes local account fields. Examples include:\n\n```bash\n$ sudo usermod -s /bin/zsh bob\n$ sudo usermod -d /srv/home/bob -m bob\n$ sudo usermod -aG developers bob\n```\n\nFor the home move, verify the destination, ownership, available space, running processes, mounts, and services first. For supplementary groups, `-aG` means append to the current list. Using `-G` without `-a` replaces the entire supplementary group list and can remove access unexpectedly.\n\nGroup changes normally affect new login sessions rather than processes already running under the old credential set.\n\n## Locking a Local Password\n\nAn administrator can lock the local password hash with `passwd -l USER` and inspect status with `passwd -S USER`. Unlocking is performed with `passwd -u USER` only after reviewing why the lock exists and whether a valid hash remains.\n\nA password lock does not necessarily stop SSH keys, tokens, scheduled jobs, already running processes, or service-specific authentication. To disable an account comprehensively, define the threat and access paths, then apply a coordinated policy that can include account expiration, login shell, service access, keys, and session termination.\n\n## Removing a Local Account Deliberately\n\nPlain `userdel bob` removes the local account records but normally leaves the home directory. `userdel -r bob` also attempts to remove the home directory and mail spool, making it a destructive operation.\n\nBefore any removal:\n\n1. Confirm the exact account with `getent passwd bob` and `id bob`.\n2. Identify running processes, scheduled tasks, services, keys, and delegated access.\n3. Inventory files owned by the UID across the intended filesystems.\n4. Decide whether data must be transferred, archived, retained, or securely deleted.\n5. Confirm that the UID will not be reassigned while orphaned files remain.\n\n`userdel -r` does not guarantee removal of files outside the configured home and mail locations. Account deletion can also leave numeric ownership on files, database permissions, application identities, and remote directory records.\n\nTo practice the account lifecycle in an isolated environment, try these hands-on labs:\n\n1. **[Manage Linux User Accounts with useradd, usermod, and userdel](https://labex.io/labs/comptia-manage-linux-user-accounts-with-useradd-usermod-and-userdel-590837)** - Practice the complete lifecycle of user administration, from creating and securing new accounts to modifying and deleting them.\n2. **[Manage Linux Groups with groupadd, usermod, and groupdel](https://labex.io/labs/comptia-manage-linux-groups-with-groupadd-usermod-and-groupdel-590836)** - Gain hands-on experience with core command-line utilities for group administration, including adding, modifying, and deleting groups.\n3. **[Configure User Accounts and Sudo Privileges in Linux](https://labex.io/labs/comptia-configure-user-accounts-and-sudo-privileges-in-linux-590856)** - Learn essential techniques for managing user accounts and sudo privileges to enhance the security of a Linux system.\n\n## Summary\n\nYou can now manage local accounts with explicit scope and verification.\n\n1. Review `useradd` defaults before creation.\n2. Request home, shell, and metadata settings explicitly.\n3. Change passwords only through protected prompts.\n4. Append supplementary groups without replacing the existing list.\n5. Inventory identity dependencies before destructive removal.", "code_snippets": ["$ useradd -D", "$ sudo useradd -m -s /bin/bash -c \"Bob Example\" bob", "$ getent passwd bob\n$ sudo passwd -S bob\n$ id bob", "$ passwd", "$ sudo passwd bob", "$ sudo usermod -s /bin/zsh bob\n$ sudo usermod -d /srv/home/bob -m bob\n$ sudo usermod -aG developers bob"], "quizzes": [{"id": "user-tools-create-home", "question": "Which `useradd` option explicitly requests creation of the new account's home directory?", "options": [{"text": "`-M`", "correct": false, "explanation": "Uppercase `-M` explicitly tells common `useradd` implementations not to create the home directory."}, {"text": "`-s`", "correct": false, "explanation": "The `-s` option chooses a login shell and does not itself create a home directory."}, {"text": "`-m`", "correct": true, "explanation": "The lowercase `-m` option requests that `useradd` create and populate the home directory according to local defaults."}]}, {"id": "user-tools-change-own-password", "question": "Which command normally lets the current user change their own password through an interactive prompt?", "options": [{"text": "`useradd`", "correct": false, "explanation": "`useradd` creates an account record and is not the ordinary interactive password-change command."}, {"text": "`userdel`", "correct": false, "explanation": "`userdel` removes a local account and is unrelated to changing the caller's password."}, {"text": "`passwd`", "correct": true, "explanation": "With no username operand, `passwd` operates on the invoking user's local password under PAM policy."}]}, {"id": "user-tools-append-group", "question": "Which command adds `bob` to supplementary group `developers` without replacing his other supplementary memberships?", "options": [{"text": "`usermod -G developers bob`", "correct": false, "explanation": "Without `-a`, `-G` replaces the supplementary group list and can remove existing memberships."}, {"text": "`usermod -aG developers bob`", "correct": true, "explanation": "The `-a` option appends the group named by `-G`, preserving other supplementary memberships."}, {"text": "`groupdel developers bob`", "correct": false, "explanation": "`groupdel` removes a group definition and does not append a user membership."}]}, {"id": "user-tools-password-lock-scope", "question": "What does `passwd -l bob` primarily lock?", "options": [{"text": "Every possible authentication and execution path for the account.", "correct": false, "explanation": "Keys, tokens, jobs, services, and existing sessions can require separate controls."}, {"text": "All files currently owned by Bob's UID.", "correct": false, "explanation": "Password state does not change filesystem ownership or automatically make owned data inaccessible."}, {"text": "The local Unix password hash used by password authentication.", "correct": true, "explanation": "The command prefixes or otherwise disables the local password hash, preventing normal verification through that path."}]}, {"id": "user-tools-userdel-r-scope", "question": "What extra removal does common `userdel -r bob` request compared with plain `userdel bob`?", "options": [{"text": "Every file with Bob's UID on every mounted filesystem.", "correct": false, "explanation": "The tool does not universally discover and erase all UID-owned files across all storage."}, {"text": "Every remote account whose username is also `bob`.", "correct": false, "explanation": "`userdel` operates on the applicable local account databases and does not delete unrelated directory-service identities."}, {"text": "Bob's home directory and local mail spool, in addition to account records.", "correct": true, "explanation": "The recursive account-removal option targets the configured home and mail spool, but not every object Bob may own elsewhere."}]}]}]}, {"course_id": "permissions", "course_name": "Permissions", "icon": "\ud83d\udd12", "description": "File permissions, octal math, chmod, chown, umask, SUID, SGID, and Sticky Bit.", "lessons_count": 8, "quizzes_count": 32, "lessons": [{"lesson_id": "file-permissions", "course_id": "permissions", "title": "File Permissions", "order_index": 1, "description": "Learn how to read Linux file types and owner, group, and other permission bits.", "content": "Linux represents many resources through file-like interfaces, and each filesystem object has metadata that controls access. Reading that metadata is a foundation for working safely with files and directories.\n\n## Reading a Long Listing\n\nUse `ls -l` to display a long listing:\n\n```bash\n$ ls -ld Desktop/\ndrwxr-xr-x 2 pete penguins 4096 Dec 1 11:45 Desktop/\n```\n\nThe first field, `drwxr-xr-x`, combines one file-type character with nine permission characters. The listing also identifies `pete` as the owner and `penguins` as the group associated with the directory.\n\nThe initial character describes the object type. Common values include:\n\n- `-` for a regular file\n- `d` for a directory\n- `l` for a symbolic link\n\nOther special file types also exist. The remaining nine characters are the access permissions:\n\n```text\nd | rwx | r-x | r-x\n```\n\n## Understanding `r`, `w`, and `x`\n\nEach permission triplet uses these characters:\n\n- `r` grants read permission.\n- `w` grants write permission.\n- `x` grants execute permission.\n- `-` means that permission is absent.\n\nFor a regular file, read permits access to its contents, write permits modification of its contents, and execute permits the kernel to attempt to run it as a program. Execution can still fail if the file format, interpreter line, mount options, or another security control does not allow it.\n\nFor a directory, the meanings concern directory entries:\n\n- Read permits listing names in the directory.\n- Write permits creating or removing entries, normally in combination with execute permission.\n- Execute, also called search permission, permits traversing the directory and accessing entries by name.\n\nDeleting a file is governed primarily by permissions on its parent directory, not by the file's own write bit.\n\n## Owner, Group, and Other Classes\n\nThe nine mode characters form three triplets in a fixed order:\n\n1. **Owner**: permissions used when the process's effective user ID matches the file owner.\n2. **Group**: permissions used when an applicable process group ID matches the file's group.\n3. **Other**: permissions used when neither of the preceding classes matches.\n\nThe kernel selects one applicable class; it does not combine the three triplets to find the most permissive result. Additional mechanisms such as access control lists, mount options, capabilities, or mandatory access controls can further affect the final decision.\n\nIn the example, the owner triplet is `rwx`, while both group and other are `r-x`. The owner can read, write, and search the directory. The group and other classes can read and search it but cannot create or remove entries through the directory's ordinary mode bits.\n\n After the file-type character, in what order do the three permission triplets appear?\n\nTo reinforce these concepts in an isolated environment, try the [Linux User Group and File Permissions](https://labex.io/labs/linux-linux-user-group-and-file-permissions-18002) lab. It provides practice reading modes and changing ownership and permissions.\n\n## Summary\n\nYou can now interpret the basic permission field in a Linux long listing.\n\n1. Separate the file-type character from the nine permission bits.\n2. Read `r`, `w`, and `x` according to whether the object is a file or directory.\n3. Divide the mode into owner, group, and other triplets.\n4. Relate the triplets to the owner and group shown by `ls -l`.", "code_snippets": ["$ ls -ld Desktop/\ndrwxr-xr-x 2 pete penguins 4096 Dec 1 11:45 Desktop/", "d | rwx | r-x | r-x"], "quizzes": [{"id": "file-permissions-type-character", "question": "In `drwxr-xr-x`, what does the first `d` indicate?", "options": [{"text": "The object is a symbolic link.", "correct": false, "explanation": "A symbolic link is normally shown with `l` in the file-type position."}, {"text": "The object is a directory.", "correct": true, "explanation": "The first character is the file type, and `d` identifies a directory."}, {"text": "The owner has delete permission.", "correct": false, "explanation": "Linux mode strings do not use `d` as a delete permission; the first position describes the object type."}]}, {"id": "file-permissions-directory-execute", "question": "What does execute permission on a directory primarily allow?", "options": [{"text": "Running every regular file stored in the directory.", "correct": false, "explanation": "A directory's execute bit does not grant execute permission on each file inside it."}, {"text": "Changing the contents of every file in the directory.", "correct": false, "explanation": "Writing file contents depends on the files' permissions and other access controls."}, {"text": "Traversing the directory and accessing entries by name.", "correct": true, "explanation": "Directory execute, or search, permission allows pathname traversal through that directory."}]}, {"id": "file-permissions-triplet-order", "question": "After the file-type character, in what order do the three permission triplets appear?", "options": [{"text": "Group, owner, then other.", "correct": false, "explanation": "The group triplet is second, not first."}, {"text": "Other, group, then owner.", "correct": false, "explanation": "The other triplet is last, and the owner triplet is first."}, {"text": "Owner, group, then other.", "correct": true, "explanation": "The nine permission characters always present owner, group, and other triplets in that order."}]}, {"id": "file-permissions-example-group", "question": "What ordinary permissions does the group class have in `drwxr-xr-x`?", "options": [{"text": "Read and write.", "correct": false, "explanation": "The group triplet is `r-x`, so its write position contains `-`."}, {"text": "Write and execute.", "correct": false, "explanation": "The group triplet contains `r` rather than `w` in its first position."}, {"text": "Read and execute.", "correct": true, "explanation": "The middle triplet is `r-x`, which grants read and execute but not write."}]}]}, {"lesson_id": "modifying-permissions", "course_id": "permissions", "title": "Modifying Permissions", "order_index": 2, "description": "Learn how to change Linux permission bits with symbolic and octal `chmod` modes.", "content": "The `chmod` command changes the mode bits of files and directories. Normally, only the file owner or a process with the necessary privilege can make this change. Inspect the current mode with `ls -l` before and after running `chmod`.\n\n## Using Symbolic Mode\n\nA symbolic mode states which permission class to change, how to change it, and which permissions are involved.\n\n- `u` selects the owner class.\n- `g` selects the group class.\n- `o` selects the other class.\n- `a` selects all three classes.\n- `+` adds permissions, `-` removes them, and `=` sets the selected class exactly.\n\nFor example, add execute permission for the owner:\n\n```bash\n$ chmod u+x myfile\n```\n\nRemove write permission from the group:\n\n```bash\n$ chmod g-w myfile\n```\n\nAdd write permission for both the owner and group:\n\n```bash\n$ chmod ug+w myfile\n```\n\nMultiple clauses can be separated with commas. This command sets the owner to read and write, the group to read only, and other to no permissions:\n\n```bash\n$ chmod u=rw,g=r,o= myfile\n```\n\nIf the class is omitted, as in `chmod +x myfile`, the process umask affects which classes are changed. Naming the class explicitly makes the intended result easier to review.\n\n## Using Octal Mode\n\nAn octal mode sets each basic permission triplet with a digit. Add these values within each class:\n\n- `4` for read\n- `2` for write\n- `1` for execute\n- `0` for no permissions\n\nThe three rightmost digits represent owner, group, and other in that order. For example:\n\n```bash\n$ chmod 755 myfile\n```\n\nThe mode `755` expands as follows:\n\n- Owner `7` is `4 + 2 + 1`, or `rwx`.\n- Group `5` is `4 + 1`, or `r-x`.\n- Other `5` is `4 + 1`, or `r-x`.\n\nUnlike `+` or `-` symbolic operations, an octal mode supplies the complete ordinary permission set. A later lesson covers the optional leading digit used for special mode bits.\n\n## Applying Changes Safely\n\nGrant only the access that users and services require. Avoid using `chmod 777` as a troubleshooting shortcut because it grants read, write, and execute to every class, often creating more risk without addressing ownership, directory traversal, ACLs, or service policy.\n\nRecursive changes deserve extra care. Preview the target tree, account for symbolic links and mounted filesystems, and test on a small scope before using `chmod -R`. After a change, verify the resulting mode rather than assuming the command affected the intended objects.\n\nFor hands-on practice in an isolated environment, use the [Linux User Group and File Permissions](https://labex.io/labs/linux-linux-user-group-and-file-permissions-18002) lab and inspect each mode before and after changing it.\n\n## Summary\n\nYou can now change ordinary Linux mode bits with deliberate `chmod` expressions.\n\n1. Use symbolic mode for targeted additions, removals, or assignments.\n2. Build octal digits from read `4`, write `2`, and execute `1`.\n3. Read octal classes in owner, group, and other order.\n4. Verify changes and apply the least privilege needed.", "code_snippets": ["$ chmod u+x myfile", "$ chmod g-w myfile", "$ chmod ug+w myfile", "$ chmod u=rw,g=r,o= myfile", "$ chmod 755 myfile"], "quizzes": [{"id": "modifying-permissions-remove-group-write", "question": "Which symbolic mode removes group write permission without changing the other group bits?", "options": [{"text": "`chmod u-w myfile`", "correct": false, "explanation": "This removes write permission from the owner class rather than the group class."}, {"text": "`chmod g-w myfile`", "correct": true, "explanation": "The `g` selects the group class, `-` removes a bit, and `w` identifies write permission."}, {"text": "`chmod g=w myfile`", "correct": false, "explanation": "The `=` operator replaces the selected class with write-only permission instead of removing write."}]}, {"id": "modifying-permissions-octal-read-value", "question": "Which octal value represents read permission?", "options": [{"text": "`1`", "correct": false, "explanation": "The value `1` represents execute permission."}, {"text": "`2`", "correct": false, "explanation": "The value `2` represents write permission."}, {"text": "`4`", "correct": true, "explanation": "Read permission contributes the octal value `4` to a class digit."}]}, {"id": "modifying-permissions-mode-640", "question": "What ordinary permissions does `chmod 640 report` set?", "options": [{"text": "Owner read, group write, other execute.", "correct": false, "explanation": "Octal digits are sums for each class, not separate read, write, and execute columns."}, {"text": "Owner read/execute, group write, other none.", "correct": false, "explanation": "Owner value `6` is read plus write, while group value `4` is read."}, {"text": "Owner read/write, group read, other none.", "correct": true, "explanation": "The digits expand to owner `6` (`rw-`), group `4` (`r--`), and other `0` (`---`)."}]}, {"id": "modifying-permissions-least-privilege", "question": "Why is `chmod 777` usually a poor general fix for an access problem?", "options": [{"text": "It removes all permissions from the owner.", "correct": false, "explanation": "Each `7` grants read, write, and execute; it does not remove the owner's permissions."}, {"text": "It grants every basic permission to owner, group, and other.", "correct": true, "explanation": "All three classes receive `rwx`, which commonly exceeds the access actually required."}, {"text": "It changes only the file's group ownership.", "correct": false, "explanation": "`chmod` changes mode bits; group ownership is changed with a tool such as `chgrp` or `chown`."}]}]}, {"lesson_id": "ownership-permissions", "course_id": "permissions", "title": "Ownership Permissions", "order_index": 3, "description": "Learn how to inspect and change the user and group ownership of Linux filesystem objects.", "content": "Every Linux filesystem object records a user owner and a group owner. These identities determine which owner or group permission triplet applies, but they do not themselves grant a particular permission. Inspect both ownership and mode with `ls -l`.\n\n## Changing the User Owner\n\nUse `chown`, short for change owner, to assign a different user owner:\n\n```bash\n$ sudo chown patty myfile\n```\n\nThis changes the user owner of `myfile` to `patty` and leaves its group unchanged. Changing a file's user owner normally requires appropriate privilege, even if you currently own the file. This restriction prevents users from transferring files to evade quotas or other ownership-based controls.\n\n## Changing the Group Owner\n\nUse `chgrp` to assign a different group owner:\n\n```bash\n$ chgrp whales myfile\n```\n\nOn typical systems, an unprivileged owner can change a file's group only to a group of which that user is a member. Privileged processes can make broader changes. The equivalent `chown` form begins with a colon:\n\n```bash\n$ chown :whales myfile\n```\n\nAfterward, the group's mode bits apply when the kernel selects the group class; changing the group does not automatically add read, write, or execute bits.\n\n## Changing User and Group Together\n\nSupply `USER:GROUP` to `chown` to update both fields in one operation:\n\n```bash\n$ sudo chown patty:whales myfile\n```\n\nThe command assigns `patty` as the user owner and `whales` as the group owner. Verify the result rather than assuming it succeeded:\n\n```bash\n$ ls -l myfile\n```\n\n## Handling Recursive Changes Carefully\n\nThe `-R` option changes ownership recursively, but a broad recursive command can cross unexpected directory trees or affect service data. Confirm the exact target, understand symbolic-link behavior for your implementation, preview the tree, and verify a small sample before changing a large hierarchy. Avoid copying privileged ownership commands from examples onto real systems without reviewing their scope.\n\n After changing a file's group owner, what happens to its ordinary group permission bits?\n\nFor practice in an isolated environment, the [Linux User Group and File Permissions](https://labex.io/labs/linux-linux-user-group-and-file-permissions-18002) lab covers inspecting and modifying ownership alongside file modes.\n\n## Summary\n\nYou can now distinguish ownership metadata from permission bits and change it deliberately.\n\n1. Use `chown USER FILE` to change the user owner.\n2. Use `chgrp GROUP FILE` or `chown :GROUP FILE` to change the group owner.\n3. Use `chown USER:GROUP FILE` to set both fields.\n4. Verify results and scope recursive changes carefully.", "code_snippets": ["$ sudo chown patty myfile", "$ chgrp whales myfile", "$ chown :whales myfile", "$ sudo chown patty:whales myfile", "$ ls -l myfile"], "quizzes": [{"id": "ownership-permissions-change-user", "question": "Which command changes the user owner of `myfile` to `patty` while leaving its group unchanged?", "options": [{"text": "`chown patty myfile`", "correct": true, "explanation": "A username alone as the `chown` ownership operand changes the user owner and preserves the group."}, {"text": "`chgrp patty myfile`", "correct": false, "explanation": "`chgrp` changes the group owner rather than the user owner."}, {"text": "`chmod patty myfile`", "correct": false, "explanation": "`chmod` changes mode bits and does not accept a username as the new owner."}]}, {"id": "ownership-permissions-change-group", "question": "What does `chgrp whales myfile` change?", "options": [{"text": "The user owner recorded for `myfile`.", "correct": false, "explanation": "The user owner is changed with `chown`, not `chgrp`."}, {"text": "The members listed in the `whales` group.", "correct": false, "explanation": "The command changes file metadata; it does not edit the system's group membership database."}, {"text": "The group owner recorded for `myfile`.", "correct": true, "explanation": "`chgrp` assigns the named group as the filesystem object's group owner."}]}, {"id": "ownership-permissions-change-both", "question": "Which ownership specification assigns user `patty` and group `whales` in one `chown` command?", "options": [{"text": "`patty:whales`", "correct": true, "explanation": "A colon separates the user and group names in the combined ownership specification."}, {"text": "`patty/whales`", "correct": false, "explanation": "A slash is not the introduced separator for a `chown` user and group operand."}, {"text": "`patty+whales`", "correct": false, "explanation": "A plus sign is not used to combine the two ownership fields for `chown`."}]}, {"id": "ownership-permissions-mode-separate", "question": "After changing a file's group owner, what happens to its ordinary group permission bits?", "options": [{"text": "They always become read and write automatically.", "correct": false, "explanation": "`chgrp` does not automatically select a fixed group mode."}, {"text": "They are copied from the owner's permission triplet.", "correct": false, "explanation": "The owner and group triplets remain independent when ownership changes."}, {"text": "They remain as set unless a separate operation changes them.", "correct": true, "explanation": "Ownership fields and mode bits are separate metadata; changing the group does not inherently grant new group bits."}]}]}, {"lesson_id": "umask", "course_id": "permissions", "title": "Umask", "order_index": 4, "description": "Learn how a process umask limits the permission bits requested for newly created files and directories.", "content": "A process's file-creation mask, or umask, prevents selected permission bits from being set when that process creates a filesystem object. It is a mask, not a complete default mode: the application first requests a mode, and the kernel removes bits prohibited by the umask.\n\nConceptually:\n\n```text\nresulting mode = requested mode AND NOT umask\n```\n\nAccess control lists and application behavior can add further details, so inspect the result when exact permissions matter.\n\n## Viewing and Setting the Umask\n\nRun `umask` without an operand to display the current shell's mask, often in octal form:\n\n```bash\n$ umask\n0022\n```\n\nSet it for the current shell and the processes subsequently started by that shell:\n\n```bash\n$ umask 027\n```\n\nEach octal position corresponds to owner, group, and other. A mask bit removes the corresponding requested permission: `2` masks write, `4` masks read, and `1` masks execute.\n\n## Calculating New File and Directory Modes\n\nMany ordinary programs request `0666` for new regular files, because creating executable files by default would be unsafe. They commonly request `0777` for new directories, where execute permission is required for traversal.\n\nWith umask `0022`:\n\n```text\nregular file: 0666 masked by 0022 -> 0644 (rw-r--r--)\ndirectory:    0777 masked by 0022 -> 0755 (rwxr-xr-x)\n```\n\nThe umask only removes requested bits. It cannot add execute permission when an application did not request it. An application can also request a more restrictive starting mode, producing a more restrictive result.\n\n If a program requests mode `0666` for a regular file and the umask is `0022`, which mode results?\n\n If a program requests `0777` for a directory and the umask is `0027`, which mode results?\n\n## Scope and Persistence\n\nChanging the umask in one shell does not alter its parent process or unrelated sessions. The value applies to future creations by that shell and its descendants; existing files retain their modes.\n\nTo make a preferred value persistent, configure it in the appropriate login, shell, PAM, service-manager, or application configuration for your environment. The correct location varies, and services may set their own umask. Avoid assuming that editing one interactive shell file governs every process on the system.\n\nFor hands-on practice, create files and directories under different masks in an isolated environment, then compare their modes with `ls -ld`. The [Linux User Group and File Permissions](https://labex.io/labs/linux-linux-user-group-and-file-permissions-18002) lab offers a suitable permissions workspace.\n\n## Summary\n\nYou can now predict how a umask limits newly requested permissions.\n\n1. View or set the current shell's mask with `umask`.\n2. Remove masked bits from the mode requested by an application.\n3. Distinguish common file requests of `0666` from directory requests of `0777`.\n4. Treat umask scope and persistence as process- and environment-specific.", "code_snippets": ["resulting mode = requested mode AND NOT umask", "$ umask\n0022", "$ umask 027", "regular file: 0666 masked by 0022 -> 0644 (rw-r--r--)\ndirectory:    0777 masked by 0022 -> 0755 (rwxr-xr-x)"], "quizzes": [{"id": "umask-command-purpose", "question": "What does `umask 027` change in the current shell?", "options": [{"text": "The permissions of every file that already exists.", "correct": false, "explanation": "A umask affects creation requests; it does not retroactively run `chmod` on existing objects."}, {"text": "The mask inherited by commands subsequently started from that shell.", "correct": true, "explanation": "The shell sets its process umask, and child processes normally inherit that value."}, {"text": "The owner and group names stored on new files.", "correct": false, "explanation": "The mask filters permission bits and does not select ownership identities."}]}, {"id": "umask-file-mode-022", "question": "If a program requests mode `0666` for a regular file and the umask is `0022`, which mode results?", "options": [{"text": "`0666`", "correct": false, "explanation": "The group and other write bits requested by `0666` are removed by mask `0022`."}, {"text": "`0755`", "correct": false, "explanation": "Execute bits were not requested for the regular file, so the umask cannot add them."}, {"text": "`0644`", "correct": true, "explanation": "Removing group and other write from `0666` leaves owner read/write and read-only access for group and other."}]}, {"id": "umask-directory-mode-027", "question": "If a program requests `0777` for a directory and the umask is `0027`, which mode results?", "options": [{"text": "`0777`", "correct": false, "explanation": "The requested group-write and other permissions are filtered by the nonzero mask."}, {"text": "`0640`", "correct": false, "explanation": "That result also removes execute bits that mask `0027` does not remove from owner or group."}, {"text": "`0750`", "correct": true, "explanation": "The mask removes group write and all permissions for other, leaving `rwxr-x---`."}]}, {"id": "umask-existing-file-effect", "question": "What happens to an existing file when you set a new umask?", "options": [{"text": "Its current mode remains unchanged.", "correct": true, "explanation": "A new umask filters later creation requests and does not modify modes already stored on filesystem objects."}, {"text": "Its mode is recalculated from `0666`.", "correct": false, "explanation": "Existing objects are not recreated or automatically passed through the new mask."}, {"text": "Its owner loses the masked permissions immediately.", "correct": false, "explanation": "Changing a process umask is not an operation on existing file metadata."}]}]}, {"lesson_id": "setuid-set-user-id", "course_id": "permissions", "title": "Setuid", "order_index": 5, "description": "Learn how the set-user-ID mode bit affects executable programs and why it requires careful security review.", "content": "Some programs need narrowly controlled access that their callers do not ordinarily have. On an executable regular file, the set-user-ID bit can cause a new process to receive the file owner's user ID as its effective user ID. The program can then perform operations authorized for that identity while retaining information about the caller.\n\nSetuid is not a general instruction to \u201crun as root.\u201d Its effect depends on the executable's owner, the operating system, the filesystem and mount options, and the way the program manages its credentials.\n\n## Recognizing Setuid\n\nOn systems that use a setuid `passwd` executable, a long listing may resemble:\n\n```bash\n$ ls -l /usr/bin/passwd\n-rwsr-xr-x 1 root root 68248 Jan 10 09:30 /usr/bin/passwd\n```\n\nThe lowercase `s` in the owner's execute position means both setuid and owner execute are set. If setuid is present but owner execute is absent, `ls -l` displays an uppercase `S` in that position.\n\nDo not assume every distribution has the same mode or authentication design. Inspect the actual system rather than relying on the example.\n\n## Understanding the Credential Change\n\nWhen the kernel honors setuid during execution, the new process normally gets an effective user ID based on the executable's owner. For a root-owned program, that can provide root-authorized access, but only while the program runs and only through the operations its code performs.\n\nThis mechanism can allow a carefully written program to validate a request and make a restricted change to protected state. For example, a local password-changing utility may need controlled access to authentication data that ordinary users cannot edit directly. Modern implementations also rely on PAM, file locking, policy, and other safeguards; setuid alone does not explain the complete workflow.\n\n When a setuid executable is honored, which identity is primarily taken from the file owner?\n\n## Setting and Removing the Bit\n\nSet setuid symbolically with:\n\n```bash\n$ sudo chmod u+s myfile\n```\n\nIn octal notation, setuid contributes `4` in a leading special-bits digit:\n\n```bash\n$ sudo chmod 4755 myfile\n```\n\nHere, the leading `4` sets setuid and `755` sets the ordinary owner, group, and other bits. Remove setuid without otherwise changing the mode with `chmod u-s myfile`.\n\n## Treating Setuid as Security-Sensitive\n\nA flaw in a privileged setuid program can become a privilege-escalation path. Such programs must validate input, control the environment and file paths they trust, avoid unsafe subprocess behavior, minimize privileged code, and drop elevated credentials as soon as possible.\n\nLinux normally does not honor setuid on interpreted scripts because doing so safely has race and interpreter-related problems. Filesystems mounted with `nosuid` also suppress setuid and setgid effects. Prefer narrower mechanisms such as service-mediated operations, carefully scoped `sudo` policy, or capabilities when they fit the requirement.\n\nNever add setuid to an arbitrary shell, interpreter, or copied program as an experiment on a shared system. Audit existing setuid files and practice only in an isolated disposable environment.\n\n## Summary\n\nYou can now recognize setuid and explain its credential and security implications.\n\n1. Find `s` or `S` in the owner's execute position.\n2. Relate setuid execution to the executable owner's effective user identity.\n3. Set or remove the bit with symbolic or octal `chmod` modes.\n4. Treat every privileged executable as security-sensitive code.", "code_snippets": ["$ ls -l /usr/bin/passwd\n-rwsr-xr-x 1 root root 68248 Jan 10 09:30 /usr/bin/passwd", "$ sudo chmod u+s myfile", "$ sudo chmod 4755 myfile"], "quizzes": [{"id": "setuid-lowercase-s", "question": "What does lowercase `s` in the owner's execute position indicate?", "options": [{"text": "Setuid is set but owner execute is absent.", "correct": false, "explanation": "That combination is displayed as uppercase `S`, not lowercase `s`."}, {"text": "The file has a sticky bit and group execute.", "correct": false, "explanation": "The sticky bit appears in the other execute position, while setuid appears in the owner position."}, {"text": "Setuid is set and owner execute is also set.", "correct": true, "explanation": "Lowercase `s` represents the setuid bit together with the ordinary owner execute bit."}]}, {"id": "setuid-effective-identity", "question": "When a setuid executable is honored, which identity is primarily taken from the file owner?", "options": [{"text": "The login name stored in `/etc/passwd`.", "correct": false, "explanation": "Executing a file does not rewrite the caller's account record or login name."}, {"text": "The process's effective user ID.", "correct": true, "explanation": "The set-user-ID execution mechanism changes the effective user identity used for many authorization checks."}, {"text": "The group owner of every opened file.", "correct": false, "explanation": "Setuid affects process credentials, not ownership metadata on unrelated files."}]}, {"id": "setuid-octal-value", "question": "Which leading octal value represents the setuid special bit?", "options": [{"text": "`4`", "correct": true, "explanation": "Setuid contributes value `4` in the leading special-bits digit."}, {"text": "`1`", "correct": false, "explanation": "A leading `1` represents the sticky bit."}, {"text": "`2`", "correct": false, "explanation": "A leading `2` represents the setgid bit."}]}, {"id": "setuid-nosuid-mount", "question": "What is the purpose of mounting a filesystem with `nosuid`?", "options": [{"text": "Remove every execute bit stored on files in that filesystem.", "correct": false, "explanation": "The option does not rewrite ordinary execute bits in file metadata."}, {"text": "Suppress setuid and setgid execution effects on that filesystem.", "correct": true, "explanation": "The `nosuid` mount option prevents those special mode bits from granting their normal credential-changing execution behavior."}, {"text": "Make all files on the filesystem owned by root.", "correct": false, "explanation": "Mounting with `nosuid` does not change user or group ownership fields."}]}]}, {"lesson_id": "setgid-set-group-id", "course_id": "permissions", "title": "Setgid", "order_index": 6, "description": "Learn how set-group-ID affects executable credentials and group inheritance in shared directories.", "content": "The set-group-ID bit, commonly called setgid or SGID, has two important uses. On an executable regular file, it can change the effective group ID of the new process. On a directory, it makes newly created entries inherit the directory's group, which is especially useful for collaborative trees.\n\n## Setgid on Executable Files\n\nA long listing can show setgid in the group execute position:\n\n```bash\n$ ls -l /path/to/program\n-rwxr-sr-x 1 root operators 24576 Jan 10 09:30 /path/to/program\n```\n\nLowercase `s` means that both setgid and group execute are set. Uppercase `S` means setgid is set but group execute is absent.\n\nWhen the kernel honors this bit during execution, the process receives an effective group ID based on the executable's group owner. The behavior can be suppressed by controls such as a `nosuid` mount, and it must not be treated as a universal guarantee across every file type or environment.\n\n When setgid on an executable is honored, which credential comes from the executable's group owner?\n\n## Setgid on Directories\n\nSetgid on a directory has a different purpose. New files and subdirectories normally inherit the directory's group instead of the creator's default group. New subdirectories also inherit the setgid bit on Linux, helping a shared project tree keep a consistent group.\n\nSetgid does not itself grant group write access. The directory mode, process umask, requested creation mode, default ACLs, and other controls still determine access.\n\n```bash\n$ sudo chgrp developers /srv/project\n$ sudo chmod g+s /srv/project\n$ ls -ld /srv/project\ndrwxr-sr-x 2 root developers 4096 Jan 10 09:30 /srv/project\n```\n\n## Setting and Removing Setgid\n\nSet the bit symbolically with:\n\n```bash\n$ sudo chmod g+s myfile\n```\n\nSet it together with ordinary mode bits using a leading octal `2`:\n\n```bash\n$ sudo chmod 2755 myfile\n```\n\nRemove only the special bit with `chmod g-s myfile`.\n\n## Using Shared Directories Safely\n\nFor a collaborative directory, combine the intended group owner, setgid, and narrowly chosen access bits. Test creation as representative users and inspect results with `ls -ld`. Avoid making a tree world-writable merely to solve group-sharing problems; a dedicated group, appropriate umask or default ACL, and setgid directory usually provide clearer control.\n\n Does setting setgid alone give group members permission to create files in a directory?\n\n## Summary\n\nYou can now distinguish the executable and directory meanings of setgid.\n\n1. Recognize setgid in the group execute position.\n2. Relate executable setgid to the effective group ID.\n3. Use directory setgid to preserve group ownership in shared trees.\n4. Set or remove the bit without confusing it with ordinary write access.", "code_snippets": ["$ ls -l /path/to/program\n-rwxr-sr-x 1 root operators 24576 Jan 10 09:30 /path/to/program", "$ sudo chgrp developers /srv/project\n$ sudo chmod g+s /srv/project\n$ ls -ld /srv/project\ndrwxr-sr-x 2 root developers 4096 Jan 10 09:30 /srv/project", "$ sudo chmod g+s myfile", "$ sudo chmod 2755 myfile"], "quizzes": [{"id": "setgid-executable-effect", "question": "When setgid on an executable is honored, which credential comes from the executable's group owner?", "options": [{"text": "The process's effective group ID.", "correct": true, "explanation": "Set-group-ID execution establishes the executable owner's group as the process's effective group identity."}, {"text": "The process's real user ID.", "correct": false, "explanation": "The bit concerns the group credential, not the caller's real user identity."}, {"text": "The owner of every file the process opens.", "correct": false, "explanation": "Execution credentials do not rewrite ownership metadata on opened files."}]}, {"id": "setgid-directory-inheritance", "question": "What does setgid on `/srv/project` normally make a newly created file inherit?", "options": [{"text": "The directory's user owner.", "correct": false, "explanation": "Directory setgid affects group inheritance, not the new entry's user owner."}, {"text": "The directory's complete permission mode.", "correct": false, "explanation": "Creation permissions are still calculated from the requested mode, umask, and any ACLs."}, {"text": "The directory's group owner.", "correct": true, "explanation": "A new entry normally receives the setgid directory's group, supporting consistent shared ownership."}]}, {"id": "setgid-octal-value", "question": "Which value does setgid contribute to the leading special-bits octal digit?", "options": [{"text": "`4`", "correct": false, "explanation": "The value `4` represents setuid in the special-bits digit."}, {"text": "`1`", "correct": false, "explanation": "The value `1` represents the sticky bit."}, {"text": "`2`", "correct": true, "explanation": "Setgid contributes `2`, as in mode `2755`."}]}, {"id": "setgid-directory-write-access", "question": "Does setting setgid alone give group members permission to create files in a directory?", "options": [{"text": "Yes; setgid always adds group read, write, and execute.", "correct": false, "explanation": "The special bit does not automatically alter the three ordinary group permission bits."}, {"text": "Yes; setgid disables all checks for members of the group.", "correct": false, "explanation": "Normal discretionary and additional security checks still apply."}, {"text": "No; the applicable write and search permissions must also allow creation.", "correct": true, "explanation": "Setgid controls group inheritance, while ordinary permissions and other access controls govern directory writes."}]}]}, {"lesson_id": "process-permissions", "course_id": "permissions", "title": "Process Permissions", "order_index": 7, "description": "Learn how real, effective, and saved user IDs help Linux processes track callers and manage privilege.", "content": "Linux authorization checks act on process credentials rather than directly on a typed username. A process has several related user and group IDs, each serving a different role. Most ordinary programs start with matching identities, while privileged programs can use distinct values deliberately.\n\n## Real User ID\n\nThe real user ID identifies the account that started the process or its ancestor login session. Programs can consult it to distinguish the caller from an elevated effective identity.\n\nFor an ordinary command started by user Bob, the real user ID normally equals Bob's UID. Creating another process does not create a new account or change this identity by itself.\n\n## Effective User ID\n\nThe effective user ID is the user credential used for many filesystem and privilege checks. Ordinarily it matches the real UID. Executing an honored setuid program can instead initialize it from the executable's owner.\n\nFor example, a carefully designed password utility may run with an elevated effective UID so it can update protected authentication data. The program must still enforce policy based on the caller, requested account, PAM results, and other context. Possessing an effective UID does not automatically make every requested operation legitimate.\n\n## Saved Set-User-ID\n\nThe saved set-user-ID lets a program retain an identity it may later restore, subject to the system-call rules. A privileged program can temporarily switch its effective UID to a less privileged value, perform ordinary work with reduced authority, and restore the saved identity only for a narrowly scoped operation.\n\nThis is safer than retaining elevated authority throughout the entire program, but only when implemented correctly. Programs should permanently discard privilege when it is no longer needed and check every credential-changing call for failure.\n\n## User IDs Are Only Part of the Credential Set\n\nProcesses also have real, effective, saved, and supplementary group credentials. Filesystem IDs, capabilities, namespaces, security modules, ACLs, mount options, and service policies can further affect authorization. Therefore, \u201cthe UID allows it\u201d is often only part of a complete explanation.\n\nUse tools such as `ps` and `/proc/PROCESS/status` to inspect credentials on Linux. Field availability and display formats vary, so consult the local documentation and avoid changing credentials merely to experiment on a shared system.\n\n For most ordinary commands without a privilege transition, how do the real and effective UIDs compare?\n\n## Summary\n\nYou can now explain why a Linux process can carry several user identities.\n\n1. Use the real UID to identify the original caller.\n2. Relate the effective UID to active authorization checks.\n3. Use the saved identity to understand controlled privilege transitions.\n4. Consider group IDs and additional security mechanisms as part of the full decision.", "code_snippets": [], "quizzes": [{"id": "process-permissions-real-uid", "question": "What does a process's real user ID normally identify?", "options": [{"text": "The owner of the most recently opened file.", "correct": false, "explanation": "Opening a file does not replace the process's real UID with that file's owner."}, {"text": "The account associated with the process's original caller.", "correct": true, "explanation": "The real UID records the calling user identity inherited when the process is launched."}, {"text": "The group selected for every access check.", "correct": false, "explanation": "A UID is a user identity; group checks use separate group credentials."}]}, {"id": "process-permissions-effective-uid", "question": "Which user ID is used for many access-control decisions made on behalf of a process?", "options": [{"text": "The effective user ID.", "correct": true, "explanation": "The effective UID is the active user credential consulted for many authorization checks."}, {"text": "The saved user ID only.", "correct": false, "explanation": "The saved ID supports credential transitions but is not generally the active identity for access checks."}, {"text": "The UID stored on the current directory.", "correct": false, "explanation": "Filesystem ownership is object metadata, not the process's active user credential."}]}, {"id": "process-permissions-saved-uid", "question": "Why can a privileged program retain a saved set-user-ID?", "options": [{"text": "To switch its effective identity for controlled privileged and unprivileged phases.", "correct": true, "explanation": "The saved identity can support temporary privilege reduction and a permitted later restoration."}, {"text": "To assign that UID automatically to every file it reads.", "correct": false, "explanation": "Reading a file does not change its ownership to the process's saved UID."}, {"text": "To replace the system account database for the process.", "correct": false, "explanation": "Process credentials do not substitute for account records or name-service data."}]}, {"id": "process-permissions-ordinary-identities", "question": "For most ordinary commands without a privilege transition, how do the real and effective UIDs compare?", "options": [{"text": "The effective UID is always zero.", "correct": false, "explanation": "Ordinary commands do not automatically receive root's UID."}, {"text": "The real UID always equals the executable file owner.", "correct": false, "explanation": "The executable owner affects setuid behavior, not the ordinary real UID."}, {"text": "They normally match the invoking user's UID.", "correct": true, "explanation": "Without setuid or an explicit credential change, ordinary processes usually run with matching real and effective identities."}]}]}, {"lesson_id": "sticky-bit", "course_id": "permissions", "title": "The Sticky Bit", "order_index": 8, "description": "Learn how the sticky bit protects entries in shared writable directories such as `/tmp`.", "content": "A writable directory normally lets an authorized user remove or rename entries within it, even when that user does not own the files themselves. The sticky bit adds an ownership restriction that makes shared writable directories safer.\n\n## How the Sticky Bit Restricts Removal\n\nWhen a directory has the sticky bit set, Linux generally permits an entry to be removed or renamed only by a suitably privileged process, the directory owner, or the entry owner. Ordinary directory write and search permissions are still required.\n\nThe restriction concerns directory entries. It does not prevent a file owner from editing file contents when the file's permissions otherwise allow that operation, and it does not make the directory private.\n\n## Recognizing the Bit on `/tmp`\n\nThe system temporary directory is a common example:\n\n```bash\n$ ls -ld /tmp\ndrwxrwxrwt 17 root root 4096 Dec 15 11:45 /tmp\n```\n\nThe final lowercase `t` occupies the other execute position. It means that both the sticky bit and other execute permission are present. An uppercase `T` means the sticky bit is set while other execute permission is absent.\n\nBecause `/tmp` is commonly writable and searchable by everyone, multiple users can create entries there. The sticky bit prevents an ordinary user from removing another user's entries merely because the directory is world-writable. Applications must still create temporary objects securely because predictable names, unsafe links, and weak file modes create separate risks.\n\n## Setting and Removing the Sticky Bit\n\nSet the bit symbolically:\n\n```bash\n$ chmod +t shared-directory\n```\n\nIn a leading special-bits octal digit, sticky contributes `1`:\n\n```bash\n$ chmod 1777 shared-directory\n```\n\nThe leading `1` sets sticky, while `777` supplies the ordinary mode. This mode is appropriate only when the directory is intentionally shared by all local users. For a team directory, narrower group permissions may be preferable. Remove only the sticky bit with `chmod -t shared-directory`.\n\n## Verifying the Complete Directory Policy\n\nSticky does not grant write or search access; it only restricts removal and rename after ordinary permissions permit directory modification. Verify the directory's owner, group, ordinary mode, ACLs, and mount context together. Test with nonprivileged accounts in an isolated environment rather than altering `/tmp` on a working system.\n\n Does adding the sticky bit make a nonwritable directory writable to other users?\n\nFor practice, create a disposable shared directory, set an appropriate ordinary mode and sticky bit, then test entry removal as two nonprivileged users. The [Delete and Move Files](https://labex.io/labs/linux-delete-and-move-files-7777) lab can reinforce the underlying rename and deletion operations.\n\n## Summary\n\nYou can now explain and verify the sticky bit on shared directories.\n\n1. Relate sticky to ownership restrictions on removal and rename.\n2. Recognize lowercase `t` and uppercase `T` in a long listing.\n3. Set the bit symbolically or with leading octal value `1`.\n4. Evaluate sticky together with ordinary directory permissions.", "code_snippets": ["$ ls -ld /tmp\ndrwxrwxrwt 17 root root 4096 Dec 15 11:45 /tmp", "$ chmod +t shared-directory", "$ chmod 1777 shared-directory"], "quizzes": [{"id": "sticky-bit-removal-rule", "question": "In a sticky shared directory, which ordinary user can normally remove a particular entry?", "options": [{"text": "Any user who can list the directory.", "correct": false, "explanation": "Directory read permission can expose names but does not bypass the sticky ownership restriction."}, {"text": "The entry's owner, with required directory access.", "correct": true, "explanation": "The entry owner is one of the identities normally permitted by the sticky-directory rule."}, {"text": "Only a member of the entry's group.", "correct": false, "explanation": "Group membership alone is not the ownership exception defined by the sticky bit."}]}, {"id": "sticky-bit-lowercase-t", "question": "What does lowercase `t` at the end of a directory mode indicate?", "options": [{"text": "Sticky is set and other execute is set.", "correct": true, "explanation": "Lowercase `t` combines the sticky special bit with the ordinary other execute bit."}, {"text": "Sticky is set but other execute is absent.", "correct": false, "explanation": "That combination is displayed as uppercase `T`."}, {"text": "Setgid is set and group execute is set.", "correct": false, "explanation": "Setgid appears in the group execute position, not the final other position."}]}, {"id": "sticky-bit-octal-value", "question": "Which leading octal value represents the sticky bit?", "options": [{"text": "`2`", "correct": false, "explanation": "A leading `2` represents setgid."}, {"text": "`1`", "correct": true, "explanation": "The sticky bit contributes `1` to the leading special-bits digit."}, {"text": "`4`", "correct": false, "explanation": "A leading `4` represents setuid."}]}, {"id": "sticky-bit-access-scope", "question": "Does adding the sticky bit make a nonwritable directory writable to other users?", "options": [{"text": "Yes; sticky automatically adds write for every class.", "correct": false, "explanation": "The special bit does not rewrite the owner, group, or other write bits."}, {"text": "Yes; sticky disables the directory's other permission triplet.", "correct": false, "explanation": "The other triplet continues to participate in normal access checks."}, {"text": "No; ordinary write and search permissions still control access.", "correct": true, "explanation": "Sticky narrows certain removal and rename operations but does not add missing ordinary permissions."}]}]}]}, {"course_id": "processes", "course_name": "Processes", "icon": "\u2699\ufe0f", "description": "Process monitoring, ps, top, lifecycle, signals, killing, niceness, and job control.", "lessons_count": 11, "quizzes_count": 45, "lessons": [{"lesson_id": "monitor-processes-ps-command", "course_id": "processes", "title": "ps (Processes)", "order_index": 1, "description": "Learn how to take process snapshots with `ps` and monitor changing activity with `top`.", "content": "A process is a running instance of a program, together with its memory, credentials, open resources, and execution state. Linux identifies each live process with a numeric process ID, or PID. A PID is unique among processes that exist at the same time, but the kernel can reuse it after a process exits.\n\n## Taking a Basic Snapshot\n\nRun `ps` without options to see a snapshot selected by the implementation's defaults, commonly processes associated with your current terminal and user:\n\n```text\n$ ps\n    PID TTY          TIME CMD\n  41230 pts/4    00:00:00 bash\n  51224 pts/4    00:00:00 ps\n```\n\nTypical fields include:\n\n- `PID`: process ID\n- `TTY`: controlling terminal, or `?` when none is associated\n- `TIME`: accumulated CPU time, not elapsed wall-clock duration\n- `CMD`: command name or command line, depending on the selected format\n\nExact columns and selection defaults vary between `ps` implementations and environments.\n\n## Listing Processes with BSD-Style Options\n\nLinux `ps` accepts several option styles. BSD-style options are commonly written without a leading dash:\n\n```bash\n$ ps aux\n```\n\nIn this combination:\n\n- `a` expands selection to processes belonging to other users that have terminals.\n- `x` also includes processes without controlling terminals and broadens the selection when combined with `a`.\n- `u` selects a user-oriented output format with fields such as `USER`, `%CPU`, `%MEM`, `VSZ`, and `RSS`.\n\nBecause option meanings can interact, interpret the complete combination rather than treating every letter as an independent command.\n\n## Using Standard-Style Options\n\nThe widely used standard-style command `ps -ef` writes options with a leading dash:\n\n```bash\n$ ps -ef\n```\n\n- `-e` selects every process visible to the caller.\n- `-f` requests a full-format listing.\n\nThe output commonly includes `UID`, `PID`, `PPID`, start time, and command information. `PPID` is the parent process ID. This listing is not inherently hierarchical; use an option such as `--forest` where supported, or a dedicated tree viewer such as `pstree`, when parent-child layout matters.\n\n## Monitoring Activity over Time\n\n`ps` exits after producing one snapshot. Use `top` for an interactive view that refreshes periodically:\n\n```bash\n$ top\n```\n\n`top` helps identify changing CPU and memory consumers, but its values are samples and can fluctuate. Confirm a suspected problem across multiple observations and relate percentages to the machine's CPU count, memory accounting, and workload.\n\nFor hands-on practice, use [Manage and Monitor Linux Processes](https://labex.io/labs/comptia-manage-and-monitor-linux-processes-590864) to compare snapshots with an interactive monitor, or explore sorting and filtering in the [Linux `top` Command](https://labex.io/labs/linux-linux-top-command-real-time-system-monitoring-388500) lab.\n\n## Summary\n\nYou can now choose a process view and interpret its basic identifiers.\n\n1. Treat a PID as a reusable identifier for a currently live process.\n2. Use plain `ps` for a small default snapshot.\n3. Use `ps aux` or `ps -ef` for broader selections and richer columns.\n4. Use `top` when changes over time matter.", "code_snippets": ["$ ps\n    PID TTY          TIME CMD\n  41230 pts/4    00:00:00 bash\n  51224 pts/4    00:00:00 ps", "$ ps aux", "$ ps -ef", "$ top"], "quizzes": [{"id": "ps-command-pid-meaning", "question": "What does the `PID` column identify?", "options": [{"text": "The process's current directory number.", "correct": false, "explanation": "A current directory is a filesystem reference and is not represented by PID."}, {"text": "The accumulated CPU time in seconds.", "correct": false, "explanation": "CPU usage is shown in a separate field such as `TIME`."}, {"text": "The process ID assigned by the kernel.", "correct": true, "explanation": "PID is the numeric identifier used to refer to a live process."}]}, {"id": "ps-command-aux-user-format", "question": "In `ps aux`, which option requests the user-oriented output format?", "options": [{"text": "`u`", "correct": true, "explanation": "The BSD-style `u` option selects a user-oriented set of output columns."}, {"text": "`x`", "correct": false, "explanation": "The `x` option affects process selection, particularly processes without controlling terminals."}, {"text": "`a`", "correct": false, "explanation": "The `a` option expands selection beyond only the current user's terminal processes."}]}, {"id": "ps-command-ef-selection", "question": "What does `-e` request in `ps -ef`?", "options": [{"text": "An update every second until interrupted.", "correct": false, "explanation": "`ps` produces a snapshot; continuous refresh is a feature of tools such as `top`."}, {"text": "A selection containing every process visible to the caller.", "correct": true, "explanation": "The standard-style `-e` option broadens the snapshot to all selectable processes."}, {"text": "Only processes whose command ended with an error.", "correct": false, "explanation": "Process selection is not based on a command's eventual exit status."}]}, {"id": "ps-command-snapshot-versus-top", "question": "Which tool introduced here refreshes its process display periodically by default?", "options": [{"text": "`top`", "correct": true, "explanation": "`top` is an interactive monitor that updates its display at intervals."}, {"text": "`ps -ef`", "correct": false, "explanation": "This command prints a full-format process snapshot and then exits."}, {"text": "`ls -l`", "correct": false, "explanation": "`ls -l` displays filesystem entries rather than a live process monitor."}]}]}, {"lesson_id": "controlling-terminal", "course_id": "processes", "title": "Controlling Terminal", "order_index": 2, "description": "Learn how controlling terminals connect sessions to interactive input, signals, and shell job control.", "content": "An interactive login session can have a controlling terminal: a terminal device associated with the session and used by the kernel for terminal-generated signals and job control. The `TTY` field in process listings helps identify that association.\n\n## Terminal and Pseudo-Terminal Devices\n\nThe name TTY comes from historical teletypes. On modern Linux, terminal interfaces are device abstractions rather than necessarily physical equipment.\n\nA system virtual console can appear under a name such as `tty1`. Desktop shortcut assignments for switching consoles vary by distribution and should not be assumed. A terminal emulator, remote login, or multiplexer commonly uses a pseudo-terminal pair, with the interactive side shown under a name such as `pts/3`.\n\nDisplay the terminal connected to the current command's standard input with:\n\n```bash\n$ tty\n/dev/pts/3\n```\n\nThis result is related to, but not identical with, the broader controlling-terminal concept. A process can redirect standard input or output while remaining in a session that has a controlling terminal.\n\n## Sessions, Process Groups, and Job Control\n\nA controlling terminal belongs to a session, not merely to the command that happened to open a window. Within that session, the terminal tracks a foreground process group. The shell places a foreground pipeline into that group so it can read input and receive terminal-generated signals.\n\nFor example, pressing `Ctrl-C` normally makes the terminal driver send `SIGINT` to the foreground process group. A background group that tries to read from the terminal can receive `SIGTTIN`. These rules allow the shell to coordinate foreground and background jobs.\n\n To which processes does a terminal normally direct the signal generated by `Ctrl-C`?\n\n## Reading the `TTY` Column\n\nRequest selected process fields explicitly when you want a stable view:\n\n```bash\n$ ps -o pid,tty,stat,cmd\n```\n\nA terminal name such as `pts/3` identifies the controlling terminal recorded for that process. A question mark (`?`) normally means that the process has no controlling terminal.\n\nMany service processes have no controlling terminal because a service manager starts them independently of an interactive login session. However, a missing TTY does not by itself prove that a process is a daemon, and a background shell job can still have a controlling terminal.\n\n## Terminal Closure and Hangups\n\nWhen a terminal connection disappears, the kernel or terminal/session software can send `SIGHUP` to associated processes. A process may terminate, catch the signal, ignore it, or already have been arranged to survive it. Shell features such as `disown`, utilities such as `nohup`, multiplexers, and service managers all affect lifecycle behavior.\n\nTherefore, closing a terminal does not guarantee that every command started from it will exit. Inspect the process's session, signal handling, redirections, and supervisor when persistence matters.\n\nThe [Manage and Monitor Linux Processes](https://labex.io/labs/comptia-manage-and-monitor-linux-processes-590864) lab provides a safe environment for comparing foreground jobs, background jobs, and their `TTY` fields.\n\n## Summary\n\nYou can now relate a controlling terminal to interactive process management.\n\n1. Distinguish virtual terminals from pseudo-terminals.\n2. Connect terminal signals with the foreground process group.\n3. Interpret terminal names and `?` in `ps` output.\n4. Treat terminal closure as signaling, not guaranteed process termination.", "code_snippets": ["$ tty\n/dev/pts/3", "$ ps -o pid,tty,stat,cmd"], "quizzes": [{"id": "controlling-terminal-pts-meaning", "question": "What does a name such as `pts/3` usually identify?", "options": [{"text": "A process ID assigned to the third shell.", "correct": false, "explanation": "A PID is numeric process metadata and is not expressed as a `pts/N` device name."}, {"text": "A pseudo-terminal device used by an interactive session.", "correct": true, "explanation": "Entries under `/dev/pts` are pseudo-terminal slave devices commonly used by terminal emulators and remote sessions."}, {"text": "A filesystem partition containing terminal programs.", "correct": false, "explanation": "The name identifies a terminal device interface, not a storage partition."}]}, {"id": "controlling-terminal-ctrl-c-target", "question": "To which processes does a terminal normally direct the signal generated by `Ctrl-C`?", "options": [{"text": "Every process owned by the current user.", "correct": false, "explanation": "Terminal-generated signals are scoped to the foreground process group, not all of a user's processes."}, {"text": "Only the login shell regardless of the foreground job.", "correct": false, "explanation": "While another job is foregrounded, that job's group is the normal signal target."}, {"text": "The terminal's foreground process group.", "correct": true, "explanation": "The terminal driver sends `SIGINT` to the current foreground process group."}]}, {"id": "controlling-terminal-question-mark", "question": "What does `?` in the `ps` `TTY` column normally mean?", "options": [{"text": "The process has no controlling terminal.", "correct": true, "explanation": "A question mark is the conventional display when no controlling terminal is associated with the process."}, {"text": "The process's terminal could not be read because it is busy.", "correct": false, "explanation": "The marker represents the absence of a controlling terminal, not temporary device contention."}, {"text": "The process is always a kernel thread.", "correct": false, "explanation": "Kernel threads often lack terminals, but many user-space services do as well."}]}, {"id": "controlling-terminal-close-effect", "question": "Why is it inaccurate to say that closing a terminal always terminates every process started there?", "options": [{"text": "Linux terminals never generate any signals when they close.", "correct": false, "explanation": "Hangup signaling is a real terminal and session behavior, even though its outcome is not guaranteed termination."}, {"text": "Only processes with numeric PIDs can receive hangups.", "correct": false, "explanation": "All ordinary processes have numeric PIDs; that fact does not determine terminal survival."}, {"text": "Processes can handle or avoid the hangup and may be managed independently.", "correct": true, "explanation": "Signal disposition, shell behavior, multiplexers, and supervisors can allow a process to continue after terminal closure."}]}]}, {"lesson_id": "process-details", "course_id": "processes", "title": "Process Details", "order_index": 3, "description": "Learn what state and resources distinguish a running process from a program stored on disk.", "content": "A program is executable code and data stored in a file. A process is a live execution context: it includes mapped code, memory, credentials, open file descriptors, signal state, scheduling information, and one or more threads. The same program can have many independent process instances.\n\n## Program Instances and PIDs\n\nFor example, start `cat` without operands in two terminals. Each instance waits for input and has its own process ID:\n\n```bash\n$ pgrep -a cat\n18420 cat\n18457 cat\n```\n\nBoth processes execute the same program, but they can have different input streams, memory contents, credentials, working directories, and lifetimes. A PID identifies one live process at a time and can later be reused after that process exits.\n\n## State Tracked by the Kernel\n\nThe kernel maintains the information required to schedule and control each process, including:\n\n- process and parent identifiers\n- user and group credentials\n- virtual memory mappings\n- open file descriptors and current directory\n- signal dispositions and pending signals\n- scheduling policy, priority, and execution state\n- accounting data such as CPU time\n\nSome underlying resources can be shared. Related processes may share mapped memory, and threads in one process share an address space and many process-wide resources. A process therefore provides isolation boundaries without implying that every byte or kernel object is physically private.\n\n## CPU Scheduling and Memory\n\nRunnable threads compete for CPU time. The kernel scheduler chooses which thread runs on which CPU according to scheduling class, priority, CPU affinity, load, and policy. This is not a promise that every process receives an equal share.\n\nEach process normally sees a virtual address space. The kernel and hardware map virtual addresses to physical memory or other backing storage, enforce protections, and can share pages where appropriate. A memory figure in `ps` or `top` is therefore not automatically the amount of unique physical RAM attributable to that process.\n\n## Process Exit and Resource Cleanup\n\nWhen a process exits, the kernel releases most of its private resources, closes remaining descriptors, and records termination information for its parent. A small process-table record can remain as a zombie until the parent retrieves the exit status. This means \u201cthe process has finished executing\u201d and \u201cevery trace has disappeared from the process table\u201d are not always simultaneous.\n\nUse the [Manage and Monitor Linux Processes](https://labex.io/labs/comptia-manage-and-monitor-linux-processes-590864) lab to start multiple instances and compare their PIDs and states. The [Linux `top` Command](https://labex.io/labs/linux-linux-top-command-real-time-system-monitoring-388500) lab provides a changing view of scheduling and resource metrics.\n\n## Summary\n\nYou can now describe a process as more than a program file.\n\n1. Distinguish stored executable code from a live process instance.\n2. Identify the state and resources tracked by the kernel.\n3. Relate scheduling to runnable threads rather than equal shares.\n4. Recognize that exit status can remain until the parent collects it.", "code_snippets": ["$ pgrep -a cat\n18420 cat\n18457 cat"], "quizzes": [{"id": "process-details-program-versus-process", "question": "What distinguishes two running instances of the same program?", "options": [{"text": "The executable file must be copied once for each instance.", "correct": false, "explanation": "Multiple processes can map and share the same executable file's code pages without duplicating the file."}, {"text": "Only one instance can have memory or open files.", "correct": false, "explanation": "Every process can have its own memory mappings and file-descriptor table."}, {"text": "Each instance has its own process context and PID.", "correct": true, "explanation": "Separate executions receive distinct live process state even when their executable code originates from the same file."}]}, {"id": "process-details-kernel-state", "question": "Which component maintains scheduling and credential state for Linux processes?", "options": [{"text": "The kernel.", "correct": true, "explanation": "The kernel tracks process state and applies scheduling, memory, signal, and access-control rules."}, {"text": "The executable file's directory.", "correct": false, "explanation": "A directory stores a name-to-inode mapping and does not schedule running processes."}, {"text": "The user's terminal emulator alone.", "correct": false, "explanation": "A terminal can interact with processes, but process management remains a kernel responsibility."}]}, {"id": "process-details-scheduler-role", "question": "What does the Linux scheduler select?", "options": [{"text": "Which runnable thread executes on an available CPU.", "correct": true, "explanation": "Scheduling policy chooses among runnable execution contexts and assigns CPU time."}, {"text": "Which file owner is recorded when a disk is formatted.", "correct": false, "explanation": "Filesystem ownership is unrelated to CPU scheduling."}, {"text": "Which command line a user is allowed to type.", "correct": false, "explanation": "The scheduler manages execution time rather than interactive command syntax."}]}, {"id": "process-details-exit-status", "question": "Why can an exited process briefly remain as a zombie?", "options": [{"text": "It is still executing instructions with full memory allocated.", "correct": false, "explanation": "A zombie has completed execution and no longer retains a normal running address space."}, {"text": "Its parent has not yet collected the recorded termination status.", "correct": true, "explanation": "The kernel retains minimal exit information until the parent performs a wait operation."}, {"text": "Its executable file is permanently locked by the kernel.", "correct": false, "explanation": "Zombie state is about parent-child exit accounting, not a permanent executable lock."}]}]}, {"lesson_id": "process-creation", "course_id": "processes", "title": "Process Creation", "order_index": 4, "description": "Learn how fork, exec, PIDs, and parent relationships participate in Linux process creation.", "content": "Linux processes form parent-child relationships. A shell commonly starts an external command by creating a child process and arranging for that child to execute the requested program. The classic explanation separates this work into `fork` and `exec` operations.\n\n## Creating a Child with `fork`\n\nThe `fork()` system call creates a child process based on the calling process. Parent and child continue from the return point of `fork`, but receive different return values and have different PIDs.\n\nThe child gets logically separate process state. Linux can initially share physical memory pages using copy-on-write, copying a page only when one process modifies it. Open file descriptors are inherited and refer to the same underlying open file descriptions, so details such as file offsets can remain shared.\n\n## Replacing a Program with `execve`\n\nAn `execve()` call loads a new program into the calling process. On success, it replaces the process image and does not return to the old program. The PID remains the same because `execve()` does not create a new process.\n\nMany shell commands therefore follow a fork-exec pattern:\n\n1. The shell creates a child.\n2. The child prepares redirections and other execution state.\n3. The child executes the requested program.\n4. The shell waits or continues, depending on foreground or background execution.\n\nLibraries and applications can expose higher-level interfaces such as `posix_spawn()`, and Linux has additional primitives such as `clone()`. The familiar fork-exec model remains useful without being the only possible interface.\n\n## Inspecting Parent and Child IDs\n\n`PID` identifies the process, while `PPID` identifies its parent. Request those fields explicitly:\n\n```bash\n$ ps -o pid,ppid,stat,cmd\n```\n\nIf a shell starts `ps`, the shell's PID will normally appear as the `PPID` of that `ps` process. Timing matters: short-lived processes may exit before a separate observation captures them.\n\n## PID 1 and Reparenting\n\nThe kernel starts the first user-space process with PID 1. Depending on the system, it may be `systemd`, another init implementation, or a small init inside a container or PID namespace. PID 1 starts and supervises parts of the user-space environment and has special signal and orphan-reaping responsibilities.\n\nWhen a parent exits before its child, the child is reparented to an appropriate subreaper or the init process in its PID namespace. It does not need to terminate merely because its original parent ended.\n\nThe [Manage and Monitor Linux Processes](https://labex.io/labs/comptia-manage-and-monitor-linux-processes-590864) lab lets you observe parent and child IDs while running foreground and background commands.\n\n## Summary\n\nYou can now trace the classic Linux process-creation sequence.\n\n1. Use `fork()` to create a child with a distinct PID.\n2. Use `execve()` to replace a process image without changing its PID.\n3. Read PID and PPID to identify parent-child relationships.\n4. Recognize PID 1 and subreapers as destinations for reparented children.", "code_snippets": ["$ ps -o pid,ppid,stat,cmd"], "quizzes": [{"id": "process-creation-fork-result", "question": "What does a successful `fork()` create?", "options": [{"text": "A replacement program inside the same process only.", "correct": false, "explanation": "Replacing the current program image is the role of an `exec` operation."}, {"text": "A child process with a new PID.", "correct": true, "explanation": "`fork()` establishes a separate child process and parent-child relationship."}, {"text": "A permanent copy of every physical memory page immediately.", "correct": false, "explanation": "Linux commonly uses copy-on-write rather than eagerly duplicating all physical pages."}]}, {"id": "process-creation-exec-pid", "question": "What happens to a process's PID after a successful `execve()`?", "options": [{"text": "It becomes identical to the parent PID.", "correct": false, "explanation": "Parent and child retain separate process IDs."}, {"text": "It remains the same while the program image is replaced.", "correct": true, "explanation": "`execve()` transforms the calling process rather than creating another process."}, {"text": "It is removed before the new program starts.", "correct": false, "explanation": "The existing process continues under its PID with new code, data, stack, and related program state."}]}, {"id": "process-creation-ppid", "question": "What does `PPID` represent in a process listing?", "options": [{"text": "The previous PID formerly assigned to the process.", "correct": false, "explanation": "PIDs can be reused, but `PPID` does not record identifier history."}, {"text": "The process's scheduling priority identifier.", "correct": false, "explanation": "Scheduling priority is represented by other fields such as priority or nice value."}, {"text": "The process ID of the parent process.", "correct": true, "explanation": "PPID records the process's current parent relationship."}]}, {"id": "process-creation-pid-one", "question": "Which statement about PID 1 is accurate?", "options": [{"text": "It must always be a program whose executable name is exactly `init`.", "correct": false, "explanation": "The implementation can be `systemd`, another init, or a container-specific program."}, {"text": "It is the parent that directly created every process currently running.", "correct": false, "explanation": "Most processes are created through many generations of intermediate parents."}, {"text": "It is the first process in its PID namespace and has init-like responsibilities.", "correct": true, "explanation": "PID 1 anchors user-space process supervision and reaping within a PID namespace."}]}]}, {"lesson_id": "process-termination", "course_id": "processes", "title": "Process Termination", "order_index": 5, "description": "Learn how exit status, waiting, zombies, and reparenting complete the Linux process lifecycle.", "content": "A process can finish by returning from its main function, calling an exit interface, or being terminated by a signal. The kernel releases most of its resources, but parent-child accounting continues until the parent collects the termination information.\n\n## Exit Status\n\nA normally exiting program supplies an integer status. By convention, status `0` means success and a nonzero value reports some form of failure or alternate outcome. The exact meanings of nonzero values belong to the program's interface.\n\nIn a shell, inspect the most recent foreground pipeline's status with:\n\n```bash\n$ command\n$ printf '%s\\n' \"$?\"\n```\n\nShells expose a limited encoded status range and also represent signal termination, so this value is not a complete diagnostic record. Programs should document their own exit codes.\n\n By Unix convention, which normal exit status indicates success?\n\n## Waiting and Reaping\n\nThe kernel records how a child terminated and notifies its parent. The parent uses a member of the `wait()` system-call family to retrieve that information. Collecting the record is called reaping.\n\nWaiting can also coordinate execution: a shell waits for a foreground command before displaying another prompt, while it can defer waiting for a background job. A well-designed long-running parent must arrange to reap children without blocking unrelated work.\n\n## Zombie Processes\n\nAfter a child exits but before its termination record is reaped, it appears as a zombie, often with state `Z` in `ps`. It no longer executes and retains no ordinary address space, but a minimal process-table entry and accounting information remain.\n\nSending a signal to a zombie cannot make it exit again. Fix persistent zombie accumulation by diagnosing the parent that is failing to wait, restarting or correcting that parent through an appropriate operational procedure, or allowing reparenting to a process that will reap it. Large numbers can exhaust PID or process-table capacity.\n\n## Orphans and Reparenting\n\nIf a parent exits while its child remains, the kernel reparents that child to an eligible subreaper or to the init process in the relevant PID namespace. The child may be running, sleeping, stopped, or later become a zombie; \u201corphan\u201d describes the lost original parent relationship rather than one execution state.\n\nThe adopting process becomes responsible for collecting termination status. Modern service managers and container environments make it important not to assume that the new parent is always the host's PID 1.\n\nUse the [Manage and Monitor Linux Processes](https://labex.io/labs/comptia-manage-and-monitor-linux-processes-590864) lab to observe exit codes and process states without disrupting a production workload.\n\n## Summary\n\nYou can now distinguish execution ending from parent-side cleanup.\n\n1. Interpret zero as conventional success and nonzero statuses by program documentation.\n2. Use waiting to collect a child's termination information.\n3. Recognize a zombie as exited but unreaped.\n4. Recognize an orphan as a child reparented after its original parent exits.", "code_snippets": ["$ command\n$ printf '%s\\n' \"$?\""], "quizzes": [{"id": "process-termination-success-status", "question": "By Unix convention, which normal exit status indicates success?", "options": [{"text": "`1`", "correct": false, "explanation": "Many programs use `1` for a general failure, although meanings are command-specific."}, {"text": "`0`", "correct": true, "explanation": "A normal status of zero conventionally signals successful completion."}, {"text": "`255`", "correct": false, "explanation": "This is nonzero and does not conventionally represent success."}]}, {"id": "process-termination-wait-purpose", "question": "What does a successful wait operation let a parent retrieve?", "options": [{"text": "The child's termination information.", "correct": true, "explanation": "The wait family reports how a child stopped or terminated and reaps a completed child."}, {"text": "A copy of the child's former address space.", "correct": false, "explanation": "Most process memory has already been released and is not returned to the parent by `wait()`."}, {"text": "Ownership of every file the child opened.", "correct": false, "explanation": "Waiting does not transfer filesystem ownership metadata."}]}, {"id": "process-termination-zombie-definition", "question": "Which description matches a zombie process?", "options": [{"text": "A running child whose parent has already exited.", "correct": false, "explanation": "That describes an orphaned child, not a zombie state."}, {"text": "A completed child whose termination record has not been reaped.", "correct": true, "explanation": "The process has stopped executing, but the kernel retains minimal status for its parent."}, {"text": "A process consuming CPU in an uninterruptible loop.", "correct": false, "explanation": "A zombie does not execute instructions or consume CPU time."}]}, {"id": "process-termination-orphan-definition", "question": "What happens when a process outlives its original parent?", "options": [{"text": "It is reparented to an eligible subreaper or namespace init process.", "correct": true, "explanation": "The kernel preserves a valid parent relationship by assigning an adopting process."}, {"text": "It immediately becomes a zombie even if it has not exited.", "correct": false, "explanation": "Zombie state begins only after execution has ended and status awaits collection."}, {"text": "It permanently loses its PID and continues anonymously.", "correct": false, "explanation": "A live orphan retains its process identity while its parent relationship changes."}]}]}, {"lesson_id": "process-signals", "course_id": "processes", "title": "Signals", "order_index": 6, "description": "Learn how Linux generates, blocks, delivers, and handles signals for process control and event notification.", "content": "A signal is an asynchronous notification delivered to a process or a particular thread. Signals report events and request actions, but carry only limited information compared with data-oriented interprocess communication mechanisms.\n\n## Where Signals Come From\n\nSignals can originate in several places:\n\n- A terminal can generate `SIGINT` for `Ctrl-C` or `SIGTSTP` for `Ctrl-Z` and direct it to the foreground process group.\n- The kernel can generate a synchronous signal such as `SIGSEGV` when a thread makes an invalid memory reference.\n- A process can send an authorized signal to another process or process group.\n- Timers, child-state changes, and terminal hangups can generate other signals.\n\nThe sender must have appropriate permission, normally based on credentials or capabilities. Signals are therefore a kernel-mediated control interface, not unrestricted messages between arbitrary users.\n\n## Dispositions and Default Actions\n\nMost signals have a process-wide disposition that selects one of three responses:\n\n- perform the signal's defined default action\n- ignore the signal\n- invoke a user-installed handler\n\nDefault actions differ: a signal may terminate, terminate and create a core dump, stop, continue, or be ignored. Catching `SIGTERM` can let a program begin an orderly shutdown, but a handler must follow strict async-signal-safety rules and the program can still delay or decline to exit.\n\nSignal names are more portable and readable than numbers. Although common Linux architectures use `SIGTERM` as 15, do not assume all signal numbers except those guaranteed by the relevant standard are identical everywhere. Use `kill -l` to inspect the local mapping.\n\n## Blocking and Pending Signals\n\nThreads have signal masks that can temporarily block delivery of selected signals. A generated blocked signal remains pending until it can be delivered, subject to the rules for standard and real-time signals. Standard signals of the same type can coalesce rather than queue once per occurrence.\n\nIn a multithreaded process, a process-directed signal can be delivered to an eligible thread that does not block it; a thread-directed signal targets the specified thread. Correct signal design therefore requires more than checking whether \u201cthe process blocked it.\u201d\n\n## Signals That Cannot Be Handled\n\n`SIGKILL` terminates a process and `SIGSTOP` stops it. Neither signal can be caught, ignored, or blocked. This guarantees that the kernel retains ultimate control, but it also means `SIGKILL` provides no opportunity for application-level cleanup.\n\nEven `SIGKILL` may not make a task disappear instantly from an observer's perspective. A task can be waiting in an uninterruptible kernel operation, and after termination its parent still must reap its status.\n\n## Summary\n\nYou can now explain the major stages and constraints of Linux signal handling.\n\n1. Identify terminal, kernel, and process-generated signals.\n2. Distinguish default actions, ignored signals, and handlers.\n3. Relate blocking to pending delivery and thread masks.\n4. Remember that `SIGKILL` and `SIGSTOP` cannot be handled or blocked.", "code_snippets": [], "quizzes": [{"id": "process-signals-ctrl-c", "question": "Which signal does a terminal normally generate for `Ctrl-C`?", "options": [{"text": "`SIGTSTP`", "correct": false, "explanation": "`SIGTSTP` is normally associated with the terminal suspend character such as `Ctrl-Z`."}, {"text": "`SIGCONT`", "correct": false, "explanation": "`SIGCONT` resumes a stopped process rather than representing keyboard interruption."}, {"text": "`SIGINT`", "correct": true, "explanation": "The terminal interrupt character normally generates `SIGINT` for the foreground process group."}]}, {"id": "process-signals-term-behavior", "question": "Why can a process respond gracefully to `SIGTERM`?", "options": [{"text": "It can install a handler for that signal.", "correct": true, "explanation": "Unlike `SIGKILL`, `SIGTERM` can be caught so a program can initiate its own shutdown logic."}, {"text": "The kernel always saves every open document automatically.", "correct": false, "explanation": "Application cleanup depends on program code; the kernel does not understand and save arbitrary document state."}, {"text": "`SIGTERM` cannot cause termination by default.", "correct": false, "explanation": "Its default action is termination when the process has not changed the disposition."}]}, {"id": "process-signals-blocked-state", "question": "What normally happens when a blockable signal is generated while its target blocks it?", "options": [{"text": "It remains pending until delivery becomes possible.", "correct": true, "explanation": "Blocking postpones handling; the pending signal can be delivered after it is unblocked."}, {"text": "It is converted automatically into `SIGKILL`.", "correct": false, "explanation": "The kernel does not escalate an ordinary blocked signal into an uncatchable signal."}, {"text": "It changes the target process's user ID.", "correct": false, "explanation": "Signal masks affect delivery and do not alter process credentials."}]}, {"id": "process-signals-uncatchable-pair", "question": "Which pair cannot be caught, ignored, or blocked?", "options": [{"text": "`SIGKILL` and `SIGSTOP`", "correct": true, "explanation": "The kernel reserves these two signals so a process cannot override or postpone their fundamental actions."}, {"text": "`SIGINT` and `SIGTERM`", "correct": false, "explanation": "Both can have user-installed handlers and can be blocked."}, {"text": "`SIGHUP` and `SIGCONT`", "correct": false, "explanation": "These signals have special semantics but are not the uncatchable pair."}]}]}, {"lesson_id": "killing-processes", "course_id": "processes", "title": "kill (Terminate)", "order_index": 7, "description": "Learn how to identify a process and send an appropriate signal with `kill` using a safe escalation sequence.", "content": "The `kill` command sends a signal to a process or process group. Its name is historical: the requested signal might terminate, stop, continue, or prompt some application-defined action. Always confirm the exact target and understand the program's documented signal behavior before sending one.\n\n## Requesting an Orderly Termination\n\nWith only a PID, `kill` sends `SIGTERM` by default:\n\n```bash\n$ kill 12445\n```\n\nPrefer the symbolic name when specifying a signal explicitly:\n\n```bash\n$ kill -TERM 12445\n```\n\n`SIGTERM` has a default action of termination, but a program can catch or ignore it. A well-designed service can use a handler to stop accepting work, save appropriate state, and release application resources. That is a possibility, not a guarantee of immediate or successful cleanup.\n\n## Verifying the Target\n\nPIDs can be reused, so a stale PID can identify a different process later. Inspect the live target immediately before acting:\n\n```bash\n$ ps -p 12445 -o pid,ppid,user,lstart,stat,cmd\n```\n\nCheck its user, start time, command, parent, service ownership, and operational role. If a service manager owns the process, use that manager's stop or reload command when possible so it can maintain correct state and avoid immediately restarting the child.\n\nYou may signal processes you own, subject to credential rules. Signaling another user's process normally requires appropriate privilege. Do not use a broad name-based command until you have reviewed every match.\n\n## Checking Signal Permission with Signal Zero\n\nSignal number zero performs error checking without delivering a real signal:\n\n```bash\n$ kill -0 12445\n```\n\nA successful result means a process with that PID exists and the caller is permitted to signal it at that instant. Failure is ambiguous: the process might not exist, or the caller might lack permission. Examine the error and exit status rather than translating every failure into \u201cnot running.\u201d It is also only a momentary check and cannot eliminate a later PID-reuse race.\n\n## Escalating Only When Necessary\n\nIf an authorized target does not terminate after `SIGTERM`, allow a workload-appropriate timeout and investigate why. Then, when forced termination is justified, send:\n\n```bash\n$ kill -KILL 12445\n```\n\n`SIGKILL` cannot be caught, ignored, or blocked, so the program cannot perform application-level cleanup. It can leave incomplete transactions, temporary state, or recovery work for other components. Use it as an escalation, not a routine first step.\n\nOther signals are meaningful only according to the receiving program's contract. `SIGHUP` often requests configuration reload, but some programs retain its default termination behavior. `SIGSTOP` pauses without cleanup and `SIGCONT` resumes a stopped process.\n\nPractice signal selection only on processes you started in an isolated environment. The [Manage and Monitor Linux Processes](https://labex.io/labs/comptia-manage-and-monitor-linux-processes-590864) lab provides a controlled workflow for inspection and termination.\n\n## Summary\n\nYou can now send process signals with a deliberate, verifiable workflow.\n\n1. Confirm the live target and its supervisor before acting.\n2. Use `SIGTERM` as the normal termination request.\n3. Interpret signal zero as a momentary existence-and-permission check.\n4. Reserve `SIGKILL` for justified escalation after investigation.", "code_snippets": ["$ kill 12445", "$ kill -TERM 12445", "$ ps -p 12445 -o pid,ppid,user,lstart,stat,cmd", "$ kill -0 12445", "$ kill -KILL 12445"], "quizzes": [{"id": "killing-processes-default-signal", "question": "Which signal does `kill PID` request by default?", "options": [{"text": "`SIGKILL`", "correct": false, "explanation": "The forceful uncatchable signal must be selected explicitly."}, {"text": "`SIGTERM`", "correct": true, "explanation": "Without another signal operand, `kill` sends the standard termination request."}, {"text": "`SIGSTOP`", "correct": false, "explanation": "Stopping a process is not the default action requested by `kill`."}]}, {"id": "killing-processes-pid-reuse", "question": "Why should you inspect a PID immediately before signaling it?", "options": [{"text": "A PID changes every time the process reads a file.", "correct": false, "explanation": "A live process normally retains the same PID throughout its lifetime."}, {"text": "The kernel can reuse a PID after its earlier process exits.", "correct": true, "explanation": "A remembered numeric PID can later refer to a different live process."}, {"text": "`kill` accepts command names but not numeric identifiers.", "correct": false, "explanation": "A numeric PID is the ordinary target operand for `kill`."}]}, {"id": "killing-processes-signal-zero", "question": "What does successful `kill -0 PID` establish at that moment?", "options": [{"text": "The process has completed all cleanup and exited.", "correct": false, "explanation": "Success indicates a signalable live target, not completed termination."}, {"text": "The process will retain that PID permanently.", "correct": false, "explanation": "The check is instantaneous and PIDs can be reused after exit."}, {"text": "The process exists and the caller may signal it.", "correct": true, "explanation": "Signal zero checks target existence and authorization without delivering a normal signal."}]}, {"id": "killing-processes-kill-tradeoff", "question": "What is the main operational drawback of `SIGKILL`?", "options": [{"text": "It can be handled only by the process owner.", "correct": false, "explanation": "No target process can install a handler for `SIGKILL`."}, {"text": "It pauses the process but never terminates it.", "correct": false, "explanation": "`SIGSTOP` pauses; `SIGKILL` terminates."}, {"text": "It gives the program no opportunity for application-level cleanup.", "correct": true, "explanation": "The kernel enforces termination without invoking a user-space signal handler."}]}]}, {"lesson_id": "process-niceness", "course_id": "processes", "title": "Niceness", "order_index": 8, "description": "Learn how nice values influence CPU scheduling weight for ordinary Linux processes.", "content": "Linux can execute threads simultaneously on different CPU cores and time-share a core among more runnable threads than it can run at once. The scheduler makes those choices according to scheduling policy, priority, affinity, and workload. A nice value is one input for ordinary time-sharing policies.\n\n## Interpreting Nice Values\n\nThe conventional nice range is `-20` through `19`:\n\n- A lower value gives a task greater scheduling weight relative to comparable tasks.\n- A higher value makes it \u201cnicer\u201d by giving it less relative weight.\n- The default is commonly `0`.\n\nNiceness does not reserve a percentage of a CPU or guarantee immediate execution. Its effect is most visible when comparable runnable tasks contend for CPU time. Real-time policies, cgroups, CPU affinity, I/O waits, and other controls can dominate observed behavior.\n\n Under the same ordinary scheduling policy, which nice value gives greater relative CPU weight?\n\n## Viewing Niceness\n\nIn `top`, the `NI` column displays the nice value. You can also request it from `ps`:\n\n```bash\n$ ps -o pid,ni,pri,stat,cmd -p 3245\n```\n\n`NI` is the user-visible nice value. A `PRI` or similar column can be a derived scheduler priority and its scale varies by tool and scheduling class, so do not assume the two columns are interchangeable.\n\n## Starting a Command with `nice`\n\nUse `nice` to launch a new command with an adjusted value:\n\n```bash\n$ nice -n 5 long-computation\n```\n\nThe requested adjustment and accepted syntax can be checked in the local manual. A nonprivileged user can normally make a command nicer by increasing its value. Giving it a lower nice value, and therefore more favorable scheduling weight, requires appropriate privilege or configured resource limits.\n\n## Changing an Existing Process with `renice`\n\nUse `renice` for an already running process:\n\n```bash\n$ renice -n 10 -p 3245\n```\n\nThis requests nice value `10` for PID `3245`. Verify the target first because PIDs can be reused, then confirm the resulting value. Permissions depend on ownership, privilege, resource limits, and system policy. Increasing the nice value is usually allowed for a process you own; reversing that change may not be allowed without privilege.\n\nThe [Manage and Monitor Linux Processes](https://labex.io/labs/comptia-manage-and-monitor-linux-processes-590864) lab offers a controlled environment for viewing and changing nice values. Compare contending CPU-bound tasks rather than expecting a visible difference from an idle system.\n\n## Summary\n\nYou can now interpret and adjust niceness without treating it as a CPU guarantee.\n\n1. Read lower nice values as greater relative scheduling weight.\n2. Inspect `NI` separately from derived priority fields.\n3. Use `nice` when launching a command.\n4. Use `renice` for an existing, verified process.", "code_snippets": ["$ ps -o pid,ni,pri,stat,cmd -p 3245", "$ nice -n 5 long-computation", "$ renice -n 10 -p 3245"], "quizzes": [{"id": "process-niceness-lower-value", "question": "Under the same ordinary scheduling policy, which nice value gives greater relative CPU weight?", "options": [{"text": "`10`", "correct": false, "explanation": "A positive value is nicer and normally carries less weight than zero or a negative value."}, {"text": "`19`", "correct": false, "explanation": "This is the nicest end of the conventional range and has relatively low weight."}, {"text": "`-5`", "correct": true, "explanation": "Lower nice values correspond to greater relative weight among comparable ordinary tasks."}]}, {"id": "process-niceness-top-column", "question": "Which `top` column normally displays the nice value?", "options": [{"text": "`PID`", "correct": false, "explanation": "`PID` identifies a process rather than showing its scheduling adjustment."}, {"text": "`TTY`", "correct": false, "explanation": "`TTY` identifies a controlling terminal association."}, {"text": "`NI`", "correct": true, "explanation": "`NI` is the conventional abbreviation for the process or thread nice value."}]}, {"id": "process-niceness-nice-command", "question": "What does `nice -n 5 long-computation` do?", "options": [{"text": "Starts the command with nice value 5, if permitted.", "correct": true, "explanation": "`nice` launches a new command using the requested scheduling adjustment."}, {"text": "Changes PID 5 to the lowest possible nice value.", "correct": false, "explanation": "The operand after `-n` is a nice value, not a PID target."}, {"text": "Guarantees the command exactly five percent of one CPU.", "correct": false, "explanation": "Nice values express relative weight and do not reserve fixed CPU percentages."}]}, {"id": "process-niceness-renice-purpose", "question": "Which tool changes the nice value of an existing process?", "options": [{"text": "`nice`", "correct": false, "explanation": "`nice` primarily starts a new command with an adjusted value."}, {"text": "`kill`", "correct": false, "explanation": "`kill` sends signals and does not serve as the ordinary niceness editor."}, {"text": "`renice`", "correct": true, "explanation": "`renice` targets an existing PID, process group, or user according to its options."}]}]}, {"lesson_id": "process-states", "course_id": "processes", "title": "Process States", "order_index": 9, "description": "Learn how to interpret common Linux process-state codes in `ps` snapshots.", "content": "A Linux task moves between execution states as it runs, waits, stops, and exits. The `STAT` field from `ps` captures one moment, so repeated observations are more useful than a single letter when diagnosing behavior.\n\n```bash\n$ ps -o pid,ppid,stat,wchan:24,cmd\n```\n\nThe first character in `STAT` is the primary state. Additional characters are modifiers describing properties such as session leadership or foreground process-group membership. Consult the local `ps` manual for the complete set.\n\n## Running and Interruptible Sleep\n\n- `R` means running or runnable. The task is executing on a CPU or waiting in a run queue for CPU time.\n- `S` means interruptible sleep. The task is waiting for an event and can be awakened by an appropriate signal or event.\n\nSleeping is normal. Interactive programs and services spend much of their time waiting for input, timers, network traffic, locks, or other events rather than consuming CPU continuously.\n\n## Uninterruptible Sleep\n\n`D` means uninterruptible sleep, commonly while the task waits in a kernel operation such as some storage or network-filesystem I/O. The task does not act on ordinary signals until it leaves that wait; a signal can remain pending in the meantime.\n\nA brief `D` state can be normal. Persistent or numerous `D` tasks can indicate slow, unavailable, or faulty I/O, but the state alone does not identify the cause. Inspect the wait channel, kernel logs, storage and network health, and the relevant subsystem before drawing conclusions.\n\n## Stopped and Zombie States\n\n- `T` normally means stopped by job-control action, such as `SIGTSTP`, or by `SIGSTOP`. Some tools use lowercase `t` for a tracing stop.\n- `Z` means zombie: the process has exited, but its parent has not yet collected the termination record.\n\nResume a job-control stop with `SIGCONT` when appropriate. A zombie cannot be resumed or killed because it is no longer executing; its parent or an adopting reaper must collect it.\n\n## Reading States in Context\n\nState codes are observations, not diagnoses. Combine them with elapsed time, CPU use, wait channels, parent relationships, logs, and repeated samples. A task can switch states between the instant the kernel reports it and the instant you read the screen.\n\nThe [Manage and Monitor Linux Processes](https://labex.io/labs/comptia-manage-and-monitor-linux-processes-590864) lab provides a safe environment for observing foreground, sleeping, stopped, and terminated tasks.\n\n## Summary\n\nYou can now interpret the most common primary process states.\n\n1. Read `R` as running or runnable and `S` as interruptible sleep.\n2. Investigate persistent `D` as a wait symptom rather than a diagnosis.\n3. Distinguish stopped `T` from exited, unreaped `Z`.\n4. Use repeated observations and surrounding evidence.", "code_snippets": ["$ ps -o pid,ppid,stat,wchan:24,cmd"], "quizzes": [{"id": "process-states-runnable-code", "question": "What does primary state `R` mean?", "options": [{"text": "Running on a CPU or ready to run.", "correct": true, "explanation": "`R` combines currently executing and runnable tasks waiting for CPU service."}, {"text": "Reaped after its parent collected status.", "correct": false, "explanation": "A fully reaped process no longer appears as a normal process-table entry."}, {"text": "Waiting in uninterruptible sleep.", "correct": false, "explanation": "Uninterruptible sleep is represented by `D`."}]}, {"id": "process-states-interruptible-code", "question": "Which primary state represents interruptible sleep?", "options": [{"text": "`D`", "correct": false, "explanation": "`D` denotes uninterruptible sleep."}, {"text": "`Z`", "correct": false, "explanation": "`Z` denotes an exited child whose status has not been reaped."}, {"text": "`S`", "correct": true, "explanation": "`S` is the conventional `ps` code for interruptible waiting."}]}, {"id": "process-states-uninterruptible-code", "question": "Which primary state denotes uninterruptible sleep?", "options": [{"text": "`T`", "correct": false, "explanation": "`T` identifies a stopped task."}, {"text": "`D`", "correct": true, "explanation": "`D` is used for a task waiting in an uninterruptible kernel sleep."}, {"text": "`R`", "correct": false, "explanation": "`R` identifies an executing or runnable task."}]}, {"id": "process-states-zombie-code", "question": "What does primary state `Z` identify?", "options": [{"text": "An exited process whose termination record awaits reaping.", "correct": true, "explanation": "A zombie retains minimal parent-visible status after execution has ended."}, {"text": "A process paused by a terminal suspend signal.", "correct": false, "explanation": "A job-control stop is normally shown as `T`."}, {"text": "A process currently using an entire CPU core.", "correct": false, "explanation": "An actively running task is represented by `R`, while a zombie executes no instructions."}]}]}, {"lesson_id": "proc-filesystem", "course_id": "processes", "title": "/proc Filesystem", "order_index": 10, "description": "Learn how Linux exposes live process and kernel information through the virtual `/proc` filesystem.", "content": "Linux commonly mounts `procfs` at `/proc`. This virtual filesystem presents kernel-generated interfaces as files and directories; its contents are not ordinary persistent files stored on disk. It exposes process state as well as selected system-wide kernel information.\n\n## Finding Process Directories\n\nList the mount and top-level entries with:\n\n```bash\n$ findmnt /proc\n$ ls /proc\n```\n\nNumeric directory names correspond to process IDs visible in the caller's PID namespace. For example, `/proc/12345` represents PID 12345 at the instant it exists. `/proc/self` is a symbolic link that resolves to the observing process's own directory, and `/proc/thread-self` identifies the current thread.\n\nVisibility and access depend on credentials, namespaces, security policy, and procfs mount options such as `hidepid`. A process can exit between listing a directory and opening one of its files, so disappearance is a normal race that inspection tools must handle.\n\n## Reading Process Information\n\nInspect a process status file when permissions allow:\n\n```bash\n$ less /proc/12345/status\n```\n\nIt includes fields such as process name, state, IDs, credentials, memory counters, capabilities, and signal masks. Other useful entries include:\n\n- `/proc/12345/cmdline`: command-line arguments separated by null bytes\n- `/proc/12345/environ`: environment entries, access-controlled and potentially sensitive\n- `/proc/12345/fd/`: symbolic links representing open file descriptors\n- `/proc/12345/maps`: current memory mappings\n- `/proc/12345/cwd`: symbolic link to the current working directory\n\nTreat these as changing observations. Fields can differ by kernel version, a process can change state during a multi-file read, and some counters have subtleties not captured by their names alone.\n\n## Reading System-Wide Interfaces\n\nNot every `/proc` entry belongs to a process. Examples include:\n\n- `/proc/cpuinfo` for kernel-reported CPU information\n- `/proc/meminfo` for system memory counters\n- `/proc/mounts` for the current process's view of mounts\n- `/proc/loadavg` for load-average and runnable-task information\n- `/proc/sys/` for runtime kernel parameters\n\nSome files, especially under `/proc/sys`, are writable configuration interfaces. Do not write to them merely because they look like regular files. Understand the parameter, scope, persistence mechanism, and rollback before making an authorized system change.\n\n## Using `/proc` through Tools\n\nLinux implementations of tools such as `ps`, `top`, and `free` obtain much of their data from procfs and other kernel interfaces, then label, calculate, and format it. Prefer those tools for routine work when they provide the needed field; read `/proc` directly for specific details or scripting only after studying the interface documentation.\n\nDirect readers must parse formats correctly, tolerate missing processes, protect sensitive output, and avoid assuming one read is an atomic system snapshot.\n\n## Summary\n\nYou can now use procfs as a live, access-controlled kernel interface.\n\n1. Associate numeric `/proc` directories with visible PIDs.\n2. Read selected per-process files while accounting for races and sensitivity.\n3. Distinguish process directories from system-wide interfaces.\n4. Prefer documented tools and formats for reliable routine inspection.", "code_snippets": ["$ findmnt /proc\n$ ls /proc", "$ less /proc/12345/status"], "quizzes": [{"id": "proc-filesystem-numeric-directory", "question": "What does numeric directory `/proc/12345` normally represent?", "options": [{"text": "The disk block numbered 12345.", "correct": false, "explanation": "`/proc` is a virtual kernel interface, not a directory of raw disk blocks."}, {"text": "The process currently visible with PID 12345.", "correct": true, "explanation": "Per-process procfs data is grouped under a directory named for the visible PID."}, {"text": "The user account whose UID is 12345.", "correct": false, "explanation": "The numeric top-level process directories are keyed by PID rather than UID."}]}, {"id": "proc-filesystem-status-file", "question": "Which path contains a readable field-oriented summary for PID 12345?", "options": [{"text": "`/proc/status/12345`", "correct": false, "explanation": "Per-process files live inside the PID-named directory, not under a top-level `status` directory."}, {"text": "`/proc/12345/status`", "correct": true, "explanation": "The per-process `status` interface presents identifiers, state, memory, signal, and credential fields."}, {"text": "`/proc/cpuinfo/12345`", "correct": false, "explanation": "`/proc/cpuinfo` is a system-wide interface and not a directory of per-PID status files."}]}, {"id": "proc-filesystem-system-interface", "question": "Which entry provides system-wide memory counters rather than one process's status?", "options": [{"text": "`/proc/self/status`", "correct": false, "explanation": "This resolves to the observing process's own per-process status."}, {"text": "`/proc/meminfo`", "correct": true, "explanation": "`meminfo` contains kernel-reported system memory statistics."}, {"text": "`/proc/1/fd`", "correct": false, "explanation": "This directory represents file descriptors belonging to PID 1, subject to access controls."}]}, {"id": "proc-filesystem-live-data", "question": "Why can `/proc/PID` disappear between two inspection commands?", "options": [{"text": "Every procfs file is automatically renamed once per second.", "correct": false, "explanation": "There is no periodic renaming rule for all procfs entries."}, {"text": "Reading `status` deletes the process directory.", "correct": false, "explanation": "Status inspection is read-only and does not terminate or remove the process."}, {"text": "The process can exit while it is being observed.", "correct": true, "explanation": "Procfs reflects live state, so the kernel removes a per-process directory after that process is gone."}]}]}, {"lesson_id": "job-control", "course_id": "processes", "title": "Job Control", "order_index": 11, "description": "Learn how an interactive shell manages foreground, background, and stopped jobs.", "content": "Interactive shells use job control to coordinate pipelines within one terminal session. A job can contain one process or an entire pipeline, normally grouped into a process group so the terminal and shell can act on it as a unit.\n\n## Starting a Background Job\n\nAppend `&` to start a pipeline asynchronously:\n\n```bash\n$ sleep 1000 &\n[1] 18420\n```\n\nThe shell returns a prompt without waiting for the job to finish. Background status does not automatically redirect output, detach the controlling terminal, or make the job survive logout. Redirect input and output explicitly when required, and use a service manager, scheduler, or terminal multiplexer for work that must outlive the interactive shell.\n\nA background job that attempts to read from the controlling terminal is normally stopped with `SIGTTIN` because it is not the terminal's foreground process group.\n\n## Listing Shell Jobs\n\nThe `jobs` builtin lists jobs known to the current shell:\n\n```text\n$ jobs\n[1]    Running    sleep 1000 &\n[2]-   Running    sleep 1001 &\n[3]+   Stopped    sleep 1002\n```\n\nThe bracketed number is a shell job ID, not a PID. A `%` prefix forms a job specification such as `%1`. The `+` marker identifies the current job selected by many commands when no operand is given; `-` identifies the previous job.\n\nBecause the job table belongs to one shell, another terminal's shell normally cannot list or address these jobs through its own `jobs`, `fg`, or `bg` builtins.\n\n## Stopping and Continuing a Job\n\nWhile a job is in the foreground, pressing `Ctrl-Z` normally makes the terminal send `SIGTSTP` to its foreground process group. The shell regains control after the job stops:\n\n```text\n$ sleep 1002\n^Z\n[3]+  Stopped    sleep 1002\n```\n\nContinue the current stopped job in the background with:\n\n```bash\n$ bg\n```\n\n`bg` sends a continuation signal and leaves the job outside the terminal foreground. It is useful only for a stopped job; a command already running in the background does not need to be resumed.\n\n## Moving a Job to the Foreground\n\nUse `fg` with a job specification to make a job the terminal's foreground process group and wait for it:\n\n```bash\n$ fg %1\n```\n\nWithout an operand, `fg` normally selects the current job marked `+`. A stopped job is continued as it enters the foreground.\n\n## Signaling a Job\n\nShells let `kill` accept a job specification:\n\n```bash\n$ kill -TERM %1\n```\n\nThis normally signals the job's process group rather than just one pipeline member. Inspect the selected job first and use `SIGTERM` before considering forceful escalation. Job specifications are shell syntax; scripts and external tools more commonly work with verified PIDs or process-group IDs.\n\nPractice these operations with harmless commands such as `sleep` in the [Manage and Monitor Linux Processes](https://labex.io/labs/comptia-manage-and-monitor-linux-processes-590864) lab.\n\n## Summary\n\nYou can now move jobs deliberately between shell-controlled states.\n\n1. Use `&` to start a background job without automatic detachment.\n2. Use `jobs` to inspect the current shell's job table.\n3. Stop with `Ctrl-Z` and continue in the background with `bg`.\n4. Return a selected job to the terminal with `fg`.\n5. Address shell jobs with `%JOB_ID` when sending signals.", "code_snippets": ["$ sleep 1000 &\n[1] 18420", "$ jobs\n[1]    Running    sleep 1000 &\n[2]-   Running    sleep 1001 &\n[3]+   Stopped    sleep 1002", "$ sleep 1002\n^Z\n[3]+  Stopped    sleep 1002", "$ bg", "$ fg %1", "$ kill -TERM %1"], "quizzes": [{"id": "job-control-ampersand-effect", "question": "What does a trailing `&` ask an interactive shell to do?", "options": [{"text": "Guarantee that the job survives logout and system restart.", "correct": false, "explanation": "Backgrounding alone provides neither durable supervision nor restart persistence."}, {"text": "Run the pipeline as a background job without waiting before the next prompt.", "correct": true, "explanation": "The shell starts the job asynchronously and remains available for more commands."}, {"text": "Discard the job's standard output and errors.", "correct": false, "explanation": "Unless redirected, a background job can still write to the terminal."}]}, {"id": "job-control-jobs-scope", "question": "What does the `jobs` builtin list?", "options": [{"text": "Jobs tracked by the current shell session.", "correct": true, "explanation": "Job IDs and state are maintained by the interactive shell that started or adopted those jobs."}, {"text": "Every process currently visible on the system.", "correct": false, "explanation": "System-wide process inspection belongs to tools such as `ps`; the shell job table is narrower."}, {"text": "Only services started during system boot.", "correct": false, "explanation": "Boot services are normally supervised by a service manager, not the interactive shell job table."}]}, {"id": "job-control-bg-purpose", "question": "What does `bg %3` do to stopped job 3?", "options": [{"text": "Moves its files into a directory named `bg`.", "correct": false, "explanation": "`bg` is a shell job-control builtin and does not move filesystem objects."}, {"text": "Continues it as a background job.", "correct": true, "explanation": "The shell resumes the selected stopped job without assigning it the terminal foreground."}, {"text": "Terminates it with `SIGKILL`.", "correct": false, "explanation": "The builtin continues the job rather than terminating it."}]}, {"id": "job-control-fg-effect", "question": "What does `fg %1` do?", "options": [{"text": "Assigns job 1 to the terminal foreground and waits for it.", "correct": true, "explanation": "The shell foregrounds the selected job so it can interact with the terminal."}, {"text": "Changes job 1 into PID 1.", "correct": false, "explanation": "A shell job ID does not replace or rewrite process IDs."}, {"text": "Starts a second copy of job 1 in the background.", "correct": false, "explanation": "`fg` operates on the existing job rather than creating a duplicate."}]}, {"id": "job-control-job-specification", "question": "Which operand refers to shell job 1 rather than process ID 1?", "options": [{"text": "`1`", "correct": false, "explanation": "A plain numeric operand to `kill` is normally interpreted as a PID."}, {"text": "`#1`", "correct": false, "explanation": "A hash prefix is not the introduced syntax for a shell job ID."}, {"text": "`%1`", "correct": true, "explanation": "The percent prefix identifies a shell job specification."}]}]}]}, {"course_id": "packages", "course_name": "Packages", "icon": "\ud83d\udce6", "description": "Debian vs Red Hat package managers, repositories, compiling source code, and tar archives.", "lessons_count": 7, "quizzes_count": 31, "lessons": [{"lesson_id": "software-distribution", "course_id": "packages", "title": "Software Distribution", "order_index": 1, "description": "Learn how upstream projects, distribution maintainers, packages, and package formats form a Linux software supply chain.", "content": "Linux software is commonly delivered as packages managed by distribution-specific tools. A package groups installable files with metadata so the system can track versions, dependencies, ownership, checksums, and lifecycle actions.\n\n## What a Package Contains\n\nA binary package can contain executables, libraries, documentation, default configuration, service definitions, and other resources. It also carries metadata such as:\n\n- package name and version\n- target architecture and distribution context\n- declared dependencies and conflicts\n- file lists and integrity information\n- optional scripts or triggers used during lifecycle operations\n\nNot every package is an interactive application. A package can provide a library, kernel component, language data, fonts, debug symbols, or metadata that depends on a collection of other packages.\n\n## Upstream and Distribution Roles\n\nAn upstream project develops and releases the original source code. A Linux distribution's maintainers then adapt selected releases to the distribution. Their work can include reviewing licenses, applying integration or security patches, defining build instructions, splitting output into packages, declaring dependencies, running tests, and maintaining updates.\n\nDistribution build infrastructure produces packages for supported releases and architectures. Repository tooling publishes metadata and signatures that clients can verify. Exact responsibilities vary: some upstream projects publish their own packages, while distributions may build independently from source.\n\n## Common Native Package Formats\n\nTwo widely used native formats are:\n\n- `.deb`, used by Debian and distributions derived from it, including Ubuntu and Linux Mint\n- `.rpm`, used by Fedora, Red Hat Enterprise Linux, and many related distributions\n\nOther native and cross-distribution formats exist. A matching filename extension alone does not guarantee compatibility: package architecture, distribution release, library versions, policies, signatures, and dependencies also matter.\n\n## Why Managed Distribution Matters\n\nA package manager records installed state and coordinates changes across packages. Installing from trusted distribution repositories usually provides consistent dependency resolution, signature verification, security updates, and clean removal. A manually copied binary or source installation can be appropriate, but it does not automatically enter that managed lifecycle.\n\nTrust still depends on repository configuration and signing keys. A cryptographically valid package proves association with a trusted key, not that arbitrary third-party software is safe or suitable. Prefer the distribution's repositories when possible and assess any external source before granting it installation privileges.\n\nUse the [Managing Packages with RPM](https://labex.io/labs/rhel-managing-packages-with-rpm-in-linux-590868) lab to inspect package metadata and integrity, or the [Build Software from Source Code](https://labex.io/labs/comptia-build-software-from-source-code-in-linux-590853) lab to compare a source workflow with managed packages.\n\n## Summary\n\nYou can now identify the major parts of Linux software distribution.\n\n1. Separate package payload files from package metadata.\n2. Distinguish upstream development from distribution integration.\n3. Associate `.deb` and `.rpm` with their distribution families.\n4. Evaluate compatibility and trust beyond a filename extension.", "code_snippets": [], "quizzes": [{"id": "software-distribution-package-metadata", "question": "Which information is normally package metadata rather than an application executable?", "options": [{"text": "The CPU instructions that implement the application.", "correct": false, "explanation": "Compiled instructions are package payload content rather than dependency metadata."}, {"text": "Declared dependency relationships.", "correct": true, "explanation": "Packages describe required or conflicting packages so management tools can reason about installation."}, {"text": "The user's unsaved document currently open in memory.", "correct": false, "explanation": "Runtime user data is not part of the distributed package metadata."}]}, {"id": "software-distribution-maintainer-role", "question": "Which task commonly belongs to a distribution package maintainer?", "options": [{"text": "Adapting upstream source to distribution build and dependency rules.", "correct": true, "explanation": "Maintainers adapt software to distribution policies, builds, dependencies, and supported environments."}, {"text": "Choosing every user's local account password.", "correct": false, "explanation": "Local authentication data is unrelated to package maintenance."}, {"text": "Scheduling each installed process on a CPU.", "correct": false, "explanation": "The running kernel scheduler handles CPU execution after installation."}]}, {"id": "software-distribution-debian-format", "question": "Which native package format is used by Debian and Ubuntu?", "options": [{"text": "`.deb`", "correct": true, "explanation": "Debian-family package tools use the `.deb` archive format."}, {"text": "`.rpm`", "correct": false, "explanation": "RPM is native to Fedora, RHEL, and related distribution families."}, {"text": "`.tar`", "correct": false, "explanation": "A tar archive is a general container and does not by itself provide Debian package metadata and lifecycle semantics."}]}, {"id": "software-distribution-package-manager-benefit", "question": "What is one advantage of installing through a trusted package repository?", "options": [{"text": "The manager can track versions and resolve declared dependencies.", "correct": true, "explanation": "Repository metadata and installed-state records support coordinated installation, updates, and removal."}, {"text": "Every installed program becomes immune to security flaws.", "correct": false, "explanation": "Package management supports updates but cannot guarantee flaw-free software."}, {"text": "All packages from every distribution become interchangeable.", "correct": false, "explanation": "Native packages remain tied to formats, releases, architectures, and dependency environments."}]}]}, {"lesson_id": "package-repositories", "course_id": "packages", "title": "Package Repositories", "order_index": 2, "description": "Learn how repositories publish signed package indexes and how APT discovers configured Debian-family sources.", "content": "A package repository publishes packages together with indexes and release metadata. A package manager downloads those indexes, selects versions compatible with its configured distribution and architecture, verifies repository authentication, and retrieves the required package files.\n\n## Repository Metadata and Local Catalogs\n\nA repository is more than a directory of archives. Its metadata describes available package names, versions, architectures, checksums, dependencies, and repository sections. The client caches a local catalog so it can search and resolve packages without downloading every archive first.\n\nOn a Debian-family system, refresh configured metadata with:\n\n```bash\n$ sudo apt update\n```\n\nThis updates the local package indexes; it does not by itself install all available upgrades. Review the reported sources and authentication errors rather than ignoring failed entries.\n\n## APT Source Configuration\n\nAPT reads configured sources from both:\n\n- `/etc/apt/sources.list`\n- files ending in `.list` or `.sources` under `/etc/apt/sources.list.d/`\n\nThe `.list` extension uses the traditional one-line format. The `.sources` extension uses deb822-style stanzas, which current APT documentation recommends for new configurations. A distribution can place its default sources in either location, so `/etc/apt/sources.list` is not guaranteed to contain the complete or primary configuration.\n\nA deb822-style source can resemble:\n\n```text\nTypes: deb\nURIs: https://deb.example.invalid/repository\nSuites: stable\nComponents: main\nSigned-By: /etc/apt/keyrings/example.gpg\n```\n\nThis is syntax illustration only; the reserved `.invalid` domain is not a usable repository.\n\n## Repository Authentication\n\nAPT verifies signed repository release metadata, then checks downloaded package files against the authenticated checksums in that metadata. `Signed-By` can scope a source to a specific keyring instead of trusting every globally configured key for that repository.\n\nA valid signature establishes that the metadata came from a holder of an accepted signing key and was not modified undetected. It does not prove that the publisher's software is defect-free, nonmalicious, or appropriate for the system. Confirm the key fingerprint and source instructions through an independent trusted channel.\n\n## Adding Third-Party Sources Deliberately\n\nA repository can install packages and lifecycle scripts with system privileges, so adding one extends the system's software trust boundary. Before doing so:\n\n1. Prefer the distribution repository when it meets the requirement.\n2. Confirm the publisher, supported release, architecture, and signing-key fingerprint.\n3. Use a dedicated source file and scoped keyring.\n4. Inspect package names and dependency changes before installation.\n5. Document how to disable the source and migrate or remove its packages.\n\nDo not copy obsolete instructions that disable signature checks or pipe an unaudited remote script into a privileged shell.\n\nPractice repository-backed installation in [Software Installation on Linux](https://labex.io/labs/linux-software-installation-on-linux-18005) or compare a Red Hat-family workflow in [Query and Update Packages with YUM](https://labex.io/labs/rhel-query-and-update-packages-with-yum-in-linux-590869). For exact APT syntax, consult the local `sources.list(5)` manual.\n\n## Summary\n\nYou can now explain how a configured repository becomes trusted package metadata.\n\n1. Distinguish repository indexes from package archives.\n2. Use `apt update` to refresh the local catalog.\n3. Locate both one-line and deb822-style APT source definitions.\n4. Scope signing keys and review third-party trust deliberately.", "code_snippets": ["$ sudo apt update", "Types: deb\nURIs: https://deb.example.invalid/repository\nSuites: stable\nComponents: main\nSigned-By: /etc/apt/keyrings/example.gpg"], "quizzes": [{"id": "package-repositories-apt-update", "question": "What does `apt update` primarily refresh?", "options": [{"text": "Every installed package binary without confirmation.", "correct": false, "explanation": "Installing upgrades is a separate operation from refreshing metadata."}, {"text": "The passwords of users allowed to install packages.", "correct": false, "explanation": "Repository index refresh does not modify local authentication credentials."}, {"text": "The local indexes describing packages available from configured sources.", "correct": true, "explanation": "APT downloads current repository metadata so later searches and dependency resolution use an updated catalog."}]}, {"id": "package-repositories-apt-locations", "question": "Where can APT read active repository definitions?", "options": [{"text": "Only from `/etc/apt/sources.list`.", "correct": false, "explanation": "APT also reads supported source files from `/etc/apt/sources.list.d/`."}, {"text": "Only from files inside each user's home directory.", "correct": false, "explanation": "System APT source configuration normally resides under `/etc/apt`."}, {"text": "From `/etc/apt/sources.list` and supported files in `/etc/apt/sources.list.d/`.", "correct": true, "explanation": "APT combines the main file with `.list` and `.sources` definitions in the source-list directory."}]}, {"id": "package-repositories-signed-by", "question": "What is the security purpose of `Signed-By` in an APT source definition?", "options": [{"text": "Encrypt every installed package so root cannot read it.", "correct": false, "explanation": "Repository signing provides origin and integrity checks, not secrecy from the local administrator."}, {"text": "Limit that source to selected signing keys.", "correct": true, "explanation": "The field ties repository verification to selected keyring material rather than an unrestricted global key set."}, {"text": "Guarantee that the repository contains no vulnerable software.", "correct": false, "explanation": "Cryptographic authenticity does not evaluate software quality or security defects."}]}, {"id": "package-repositories-third-party-risk", "question": "Why does adding a third-party repository expand the system's trust boundary?", "options": [{"text": "Its authenticated packages and scripts may be installed with system privileges.", "correct": true, "explanation": "Trusting the signing source can authorize code and lifecycle actions that affect the operating system."}, {"text": "It makes the Linux kernel stop enforcing file permissions.", "correct": false, "explanation": "Repository configuration does not disable the kernel's normal access-control mechanisms."}, {"text": "It converts all native packages into source archives.", "correct": false, "explanation": "Adding a repository changes available package sources, not the fundamental format of existing packages."}]}]}, {"lesson_id": "compressed-archives-tar", "course_id": "packages", "title": "tar and gzip", "order_index": 3, "description": "Learn how to archive files with `tar`, compress streams with `gzip`, and inspect archives before safe extraction.", "content": "Archiving and compression solve different problems. An archive combines a directory tree and its metadata into one stream. Compression encodes a stream to reduce its size. A `.tar.gz` file is conventionally a tar archive whose stream has been compressed with gzip.\n\n## Compressing One Stream with `gzip`\n\nBy default, `gzip` compresses a file and replaces the original name with a `.gz` file:\n\n```bash\n$ gzip report.txt\n```\n\nThis normally removes `report.txt` after successfully creating `report.txt.gz`. Decompress it with:\n\n```bash\n$ gunzip report.txt.gz\n```\n\nUse `gzip -k report.txt` where supported to keep the input file, or use standard streams when you need explicit control. A filename extension is a convention, not proof of the actual format; tools such as `file` can inspect content.\n\n## Creating a Tar Archive\n\nCreate an uncompressed archive with:\n\n```bash\n$ tar -cvf project.tar file1 file2 directory1\n```\n\n- `-c` creates a new archive.\n- `-v` lists members while processing and is optional.\n- `-f project.tar` names the archive file; because `-f` consumes an argument, keep the filename beside it.\n\nPaths are stored as archive member names. Create archives from a deliberate working directory and avoid unintentionally capturing secrets, caches, sockets, or broad absolute paths.\n\n## Creating a Gzip-Compressed Tar Archive\n\nGNU tar and many other implementations can invoke gzip with `-z`:\n\n```bash\n$ tar -czvf project.tar.gz file1 file2 directory1\n```\n\nThe result is one gzip-compressed tar stream. Compression does not encrypt the archive or hide its contents from someone who can read and decompress it. If confidentiality is required, use an appropriate authenticated-encryption workflow and manage keys separately.\n\n## Listing Before Extracting\n\nTreat an archive from another party as untrusted input. List its member names first:\n\n```bash\n$ tar -tzf download.tar.gz\n```\n\nLook for unexpected absolute paths, `..` traversal components, surprising symbolic or hard links, device files, and names that would overwrite important files. Modern tar implementations apply protections, but behavior and options vary, and extracting still creates attacker-chosen names and content.\n\nExtract into a newly created, nonprivileged staging directory:\n\n```bash\n$ mkdir extraction-stage\n$ tar -xzf download.tar.gz -C extraction-stage\n```\n\nDo not extract an unreviewed archive as root. Verify what was created before moving selected files to their final locations.\n\n## Other Compression Formats\n\nTar implementations can work with compressors such as bzip2 and xz, commonly selected with `-j` and `-J` respectively in GNU tar. Format support and automatic detection differ, so consult `tar --help` or the local manual. ZIP is a separate archive format operated with tools such as `zip` and `unzip`.\n\n Does gzip compression make a tar archive confidential?\n\nPractice with disposable files in [File Packaging and Compression](https://labex.io/labs/linux-file-packaging-and-compression-385413), then apply inspection and staging in [Create and Restore a Backup with tar](https://labex.io/labs/comptia-create-and-restore-a-backup-with-tar-in-linux-590843).\n\n## Summary\n\nYou can now combine tar archiving with gzip compression safely.\n\n1. Distinguish a tar archive from gzip compression.\n2. Create archives with `-c` and gzip streams with `-z`.\n3. List members with `-t` before extracting with `-x`.\n4. Extract untrusted content into a nonprivileged staging directory.\n5. Treat compression as separate from encryption.", "code_snippets": ["$ gzip report.txt", "$ gunzip report.txt.gz", "$ tar -cvf project.tar file1 file2 directory1", "$ tar -czvf project.tar.gz file1 file2 directory1", "$ tar -tzf download.tar.gz", "$ mkdir extraction-stage\n$ tar -xzf download.tar.gz -C extraction-stage"], "quizzes": [{"id": "tar-gzip-gzip-role", "question": "What is the primary role of `gzip` in this lesson?", "options": [{"text": "Combining a directory tree into an archive with file metadata.", "correct": false, "explanation": "Tar performs that archiving role before gzip compression is applied."}, {"text": "Compressing a single input stream.", "correct": true, "explanation": "Gzip transforms one byte stream and does not itself encode a directory hierarchy."}, {"text": "Installing dependency metadata into a package database.", "correct": false, "explanation": "Compression is separate from native package installation and dependency tracking."}]}, {"id": "tar-gzip-create-option", "question": "Which `tar` option creates a new archive?", "options": [{"text": "`-x`", "correct": false, "explanation": "The `-x` operation extracts archive members."}, {"text": "`-c`", "correct": true, "explanation": "The create operation writes a new archive from the named inputs."}, {"text": "`-t`", "correct": false, "explanation": "The `-t` operation lists archive members without extracting them."}]}, {"id": "tar-gzip-z-option", "question": "What does `-z` request in the shown `tar` command?", "options": [{"text": "Encrypt the archive using a zero-knowledge key.", "correct": false, "explanation": "Neither tar nor gzip provides encryption through this option."}, {"text": "Discard every zero-length member.", "correct": false, "explanation": "The option selects gzip and does not filter archive members by size."}, {"text": "Process the archive stream through gzip.", "correct": true, "explanation": "The `z` option connects tar's archive operation with gzip compression or decompression."}]}, {"id": "tar-gzip-list-before-extract", "question": "Which operation lists archive members without extracting them?", "options": [{"text": "`tar -czf download.tar.gz .`", "correct": false, "explanation": "This creates or replaces an archive from the current directory."}, {"text": "`tar -xzf download.tar.gz`", "correct": false, "explanation": "The `-x` operation writes members into the target directory."}, {"text": "`tar -tzf download.tar.gz`", "correct": true, "explanation": "The `-t` operation reads and displays the member table while `-z` handles gzip."}]}, {"id": "tar-gzip-archive-confidentiality", "question": "Does gzip compression make a tar archive confidential?", "options": [{"text": "No; anyone who can read it can ordinarily decompress it.", "correct": true, "explanation": "Compression changes representation and size but does not provide access control or cryptographic secrecy."}, {"text": "Yes; gzip derives an encryption key from the filename.", "correct": false, "explanation": "Gzip does not implement such an encryption mechanism."}, {"text": "Yes; tar encrypts every member before gzip sees it.", "correct": false, "explanation": "Tar archives members but does not automatically encrypt their contents."}]}]}, {"lesson_id": "package-dependencies", "course_id": "packages", "title": "Package Dependencies", "order_index": 4, "description": "Learn how package metadata expresses required capabilities, versions, conflicts, and shared-library relationships.", "content": "A package dependency states that one package needs another package, capability, or compatible version for installation or operation. Repository-aware package managers use this metadata to calculate a consistent set of changes rather than treating each archive in isolation.\n\n## Dependency Relationships\n\nPackage metadata can express more than a simple required name. Depending on the distribution format, relationships can include:\n\n- required dependencies\n- minimum, maximum, or exact version constraints\n- alternatives, where any one of several providers satisfies a requirement\n- recommendations or suggestions with weaker semantics\n- conflicts, breaks, or replacements\n- virtual capabilities supplied by more than one package\n\nThese rules let a solver choose a set of package versions compatible with the configured repositories, architecture, and installed state. A solution can require upgrades, removals, or a choice between providers, so review the proposed transaction before approving it.\n\n## Shared Libraries as Dependencies\n\nA shared library contains compiled code that multiple programs can map at runtime. Sharing reduces duplicated implementations and lets distributions update a common library independently, but programs depend on a compatible application binary interface, or ABI.\n\nOn ELF-based Linux systems, an executable can record a needed library name such as a SONAME. The dynamic linker locates a matching installed library when the program starts. Package metadata usually represents this requirement as a dependency on the package or capability providing the compatible library.\n\n## Version and ABI Compatibility\n\nHaving a file with a similar library name is not sufficient. The required ABI, architecture, symbols, and sometimes minimum version must match. Replacing a distribution library manually can break every dependent program even if the filename appears correct.\n\nPackage maintainers encode library relationships and coordinate transitions when an ABI changes. Keep native libraries under package-manager control; use supported parallel-installation, container, environment, or build mechanisms for software that needs a conflicting version.\n\n## Broken Dependency States\n\nA dependency problem can arise from mixed repositories, interrupted operations, manually installed archives, held versions, removed files, or incompatible third-party software. Do not respond by deleting package-database files or forcing an install blindly.\n\nFirst read the package manager's diagnostics, refresh only trusted repository metadata, inspect held or pinned versions, and review the proposed repair. A low-level package installer can unpack an archive without fetching all dependencies; a higher-level repository tool is usually safer for ordinary installation because it resolves the complete transaction.\n\nUse [Manage Shared Libraries in Linux](https://labex.io/labs/comptia-manage-shared-libraries-in-linux-590867) to inspect runtime relationships, then compare them with package metadata in [Managing Packages with RPM](https://labex.io/labs/rhel-managing-packages-with-rpm-in-linux-590868).\n\n## Summary\n\nYou can now explain how package dependency resolution works.\n\n1. Recognize required, alternative, versioned, and conflicting relationships.\n2. Relate shared-library packages to runtime ABI requirements.\n3. Treat filenames as weaker evidence than architecture and interface compatibility.\n4. Review a complete package-manager transaction before applying repairs.", "code_snippets": [], "quizzes": [{"id": "package-dependencies-solver-role", "question": "What does a repository-aware dependency solver try to produce?", "options": [{"text": "A consistent set of package versions and required changes.", "correct": true, "explanation": "The solver evaluates declared relationships across installed and available packages."}, {"text": "A new user account for each installed application.", "correct": false, "explanation": "Account creation can be a package lifecycle action, but it is not the purpose of dependency resolution."}, {"text": "A compressed copy of every file in the repository.", "correct": false, "explanation": "The solver selects metadata and packages; it does not archive the entire repository."}]}, {"id": "package-dependencies-shared-library", "question": "What is a shared library?", "options": [{"text": "Compiled code that multiple programs can load and use.", "correct": true, "explanation": "A shared library provides reusable binary interfaces rather than embedding a separate implementation in every program."}, {"text": "A repository list shared between unrelated distributions.", "correct": false, "explanation": "Repository configuration and executable library code are different concepts."}, {"text": "A text file containing every user's shell history.", "correct": false, "explanation": "Shell history is user data and not a program library dependency."}]}, {"id": "package-dependencies-filename-insufficient", "question": "Why might a program still fail when a similarly named library file exists?", "options": [{"text": "Linux permits only one executable to use each library.", "correct": false, "explanation": "A defining purpose of shared libraries is use by multiple processes and programs."}, {"text": "Package dependencies apply only before the first system boot.", "correct": false, "explanation": "Dependencies remain relevant throughout installation, upgrades, and runtime."}, {"text": "The library's ABI or architecture may not satisfy the program.", "correct": true, "explanation": "Runtime linking depends on compatible binary interfaces and machine architecture, not only a filename."}]}, {"id": "package-dependencies-low-level-limit", "question": "What is a common limitation of installing one local package with a low-level archive tool?", "options": [{"text": "It may not fetch and solve all missing repository dependencies.", "correct": true, "explanation": "Low-level tools manage package archives and databases but may leave dependency retrieval to a higher-level manager."}, {"text": "It always recompiles the Linux kernel from source.", "correct": false, "explanation": "Installing a package archive does not inherently rebuild the kernel."}, {"text": "It prevents the package from containing any shared libraries.", "correct": false, "explanation": "A package archive can contain libraries regardless of which tool installs it."}]}]}, {"lesson_id": "package-install-tools", "course_id": "packages", "title": "rpm and dpkg", "order_index": 5, "description": "Learn how `dpkg` and `rpm` inspect and modify their native package databases and local archives.", "content": "`dpkg` is the low-level package tool on Debian-family systems, while `rpm` serves a similar role for RPM-family systems. They unpack native archives, run package lifecycle actions, and update installed-package databases. Repository-aware tools such as APT and DNF build on these lower-level mechanisms.\n\n## Inspecting an Archive Before Installation\n\nA package archive is not equivalent to one executable file. It can contain many payload files, metadata, configuration handling, and privileged lifecycle scripts. Inspect its origin, signature or authenticated download path, metadata, and contents before installation.\n\n```bash\nDebian: $ dpkg-deb --info ./some-package.deb\nDebian: $ dpkg-deb --contents ./some-package.deb\nRPM:    $ rpm -qip ./some-package.rpm\nRPM:    $ rpm -qlp ./some-package.rpm\n```\n\nThe `p` in the shown RPM query forms means \u201cquery a package file\u201d rather than the installed database. Query output helps review a package but cannot prove that its scripts or programs are safe.\n\n## Installing a Local Archive\n\nDirect low-level installation uses:\n\n```bash\nDebian: $ sudo dpkg -i ./some-package.deb\nRPM:    $ sudo rpm -U ./some-package.rpm\n```\n\n`dpkg -i` can unpack and configure the requested archive, but it does not fetch missing repository dependencies. Raw `rpm` similarly does not provide the normal repository solver workflow. A higher-level command is usually preferable for a local archive because it can resolve dependencies from configured sources:\n\n```bash\nDebian: $ sudo apt install ./some-package.deb\nRPM:    $ sudo dnf install ./some-package.rpm\n```\n\nReview the transaction before confirming. A leading `./` distinguishes a local Debian archive path from a repository package name in APT.\n\n## Removing an Installed Package\n\nRemoval targets an installed package name, not the archive filename used earlier:\n\n```bash\nDebian: $ sudo dpkg --remove package-name\nRPM:    $ sudo rpm --erase package-name\n```\n\nOn Debian, `--remove` normally retains configuration files classified as conffiles; `--purge` requests their removal as well, subject to package scripts and unmanaged data. Neither command guarantees deletion of user-created data. Higher-level `apt remove` or `dnf remove` is generally better because it can evaluate related packages and present a complete transaction.\n\n## Querying Installed State\n\nList installed or known package records with:\n\n```bash\nDebian: $ dpkg-query -l\nRPM:    $ rpm -qa\n```\n\nFor targeted inspection, prefer a specific package name and machine-readable format where scripting reliability matters. Package databases describe managed state; local administrators or applications can still modify files afterward, so use verification features when you need to compare installed files with recorded metadata.\n\nUse [Managing Packages with RPM](https://labex.io/labs/rhel-managing-packages-with-rpm-in-linux-590868) to practice archive queries and integrity checks in an isolated system.\n\n## Summary\n\nYou can now distinguish low-level package operations from repository transactions.\n\n1. Inspect local archive metadata and contents before installation.\n2. Use `dpkg` for `.deb` and `rpm` for `.rpm` low-level operations.\n3. Prefer APT or DNF when dependencies must be resolved.\n4. Remove by installed package name and verify managed state separately.", "code_snippets": ["Debian: $ dpkg-deb --info ./some-package.deb\nDebian: $ dpkg-deb --contents ./some-package.deb\nRPM:    $ rpm -qip ./some-package.rpm\nRPM:    $ rpm -qlp ./some-package.rpm", "Debian: $ sudo dpkg -i ./some-package.deb\nRPM:    $ sudo rpm -U ./some-package.rpm", "Debian: $ sudo apt install ./some-package.deb\nRPM:    $ sudo dnf install ./some-package.rpm", "Debian: $ sudo dpkg --remove package-name\nRPM:    $ sudo rpm --erase package-name", "Debian: $ dpkg-query -l\nRPM:    $ rpm -qa"], "quizzes": [{"id": "package-install-tools-native-format", "question": "Which low-level tool manages Debian `.deb` packages and their installed database?", "options": [{"text": "`rpm`", "correct": false, "explanation": "RPM manages its own native format and database on RPM-family systems."}, {"text": "`tar`", "correct": false, "explanation": "Tar can read archives but does not implement the Debian installed-package lifecycle."}, {"text": "`dpkg`", "correct": true, "explanation": "Debian-family systems use `dpkg` for low-level `.deb` archive and package-database operations."}]}, {"id": "package-install-tools-local-dependencies", "question": "Which shown command can install a local `.deb` while resolving available repository dependencies?", "options": [{"text": "`dpkg -l ./some-package.deb`", "correct": false, "explanation": "`dpkg -l` lists installed-package selections and is not the local dependency-resolving install workflow."}, {"text": "`rpm -qa ./some-package.deb`", "correct": false, "explanation": "RPM query syntax does not install a Debian archive."}, {"text": "`apt install ./some-package.deb`", "correct": true, "explanation": "APT recognizes the explicit local path and can use configured repositories to satisfy declared dependencies."}]}, {"id": "package-install-tools-remove-operand", "question": "What operand does `dpkg --remove` expect for an installed package?", "options": [{"text": "The URL of the repository index.", "correct": false, "explanation": "Repository location is not the package identity passed to low-level removal."}, {"text": "The installed package name.", "correct": true, "explanation": "Removal addresses the package record, such as `example`, rather than requiring its former `.deb` path."}, {"text": "The PID of a process started by the package.", "correct": false, "explanation": "Process IDs are unrelated to the installed package database key."}]}, {"id": "package-install-tools-rpm-list-installed", "question": "Which command queries all packages recorded as installed in the RPM database?", "options": [{"text": "`rpm -qa`", "correct": true, "explanation": "`-q` selects query mode and `-a` expands it to all installed package records."}, {"text": "`rpm -e`", "correct": false, "explanation": "`-e` requests package removal rather than a read-only listing."}, {"text": "`dpkg-deb --contents`", "correct": false, "explanation": "This inspects the payload of a Debian archive file, not the RPM installed database."}]}]}, {"lesson_id": "package-management-systems", "course_id": "packages", "title": "yum and apt", "order_index": 6, "description": "Learn the repository-aware APT and DNF workflows for inspecting, installing, removing, and upgrading packages.", "content": "Repository-aware package managers retrieve metadata, solve dependencies, verify authenticated content, and coordinate transactions. Debian-family systems commonly use APT. Current Fedora and Red Hat Enterprise Linux releases use DNF; on current RHEL, the `yum` command remains as a compatibility alias for DNF, while older systems used the original YUM implementation.\n\nAlways follow the documentation for the installed distribution and release rather than assuming one command set applies everywhere.\n\n## Refreshing and Inspecting Metadata\n\nAPT separates metadata refresh from package upgrades:\n\n```bash\nDebian family: $ sudo apt update\n```\n\nSearch and inspect before installation:\n\n```bash\nDebian family: $ apt search package-name\nDebian family: $ apt show package-name\nRPM family:    $ dnf search package-name\nRPM family:    $ dnf info package-name\n```\n\nRepository configuration determines what these commands can discover. Read source names, architectures, versions, and signing errors carefully.\n\n## Installing Packages\n\nInstall by repository package name with:\n\n```bash\nDebian family: $ sudo apt install package-name\nRPM family:    $ sudo dnf install package-name\n```\n\nThe manager proposes dependencies and any conflicts or replacements. Do not confirm automatically until you have reviewed package origin, version, architecture, download size, disk change, removals, and newly installed dependencies.\n\n## Removing Packages\n\nRequest removal with:\n\n```bash\nDebian family: $ sudo apt remove package-name\nRPM family:    $ sudo dnf remove package-name\n```\n\nRemoval can affect dependent packages or leave now-unused dependencies and configuration. Review the proposed transaction, distinguish remove from purge semantics on Debian-family systems, and preserve application data according to its own backup and retention procedure. Package removal does not promise to delete user-created data.\n\n## Applying Updates\n\nOn an APT system, refresh metadata and then review upgrades as separate successful steps:\n\n```bash\n$ sudo apt update\n$ apt list --upgradable\n$ sudo apt upgrade\n```\n\nOn a DNF system, inspect and apply available updates with the locally documented workflow:\n\n```bash\n$ dnf check-update\n$ sudo dnf upgrade\n```\n\nAn update command can change core libraries, services, kernels, and dependencies. Use backups, maintenance policy, release notes, and restart or reboot planning appropriate to the system. Check command exit semantics: for example, some \u201ccheck update\u201d operations use a nonzero status to report that updates are available rather than an execution failure.\n\n## Choosing `dnf` or `yum`\n\nUse `dnf` in current Fedora and RHEL documentation. A `yum` command on a recent RHEL system can invoke DNF compatibility behavior, but scripts should not infer the implementation from the executable name alone. On legacy hosts, verify the installed version and supported syntax before translating instructions.\n\nPractice APT in [Installing and Removing Packages](https://labex.io/labs/linux-installing-and-removing-packages-385380) and DNF/YUM-family concepts in [Query and Update Packages with YUM](https://labex.io/labs/rhel-query-and-update-packages-with-yum-in-linux-590869).\n\n## Summary\n\nYou can now choose and review common repository package operations.\n\n1. Use APT on Debian-family systems and DNF on current RPM-family systems.\n2. Inspect metadata and proposed dependency changes before installation.\n3. Treat removal as a dependency-aware transaction, not a single-file deletion.\n4. Separate metadata refresh from upgrade application where the tool does.\n5. Verify whether `yum` is legacy YUM or a DNF compatibility command.", "code_snippets": ["Debian family: $ sudo apt update", "Debian family: $ apt search package-name\nDebian family: $ apt show package-name\nRPM family:    $ dnf search package-name\nRPM family:    $ dnf info package-name", "Debian family: $ sudo apt install package-name\nRPM family:    $ sudo dnf install package-name", "Debian family: $ sudo apt remove package-name\nRPM family:    $ sudo dnf remove package-name", "$ sudo apt update\n$ apt list --upgradable\n$ sudo apt upgrade", "$ dnf check-update\n$ sudo dnf upgrade"], "quizzes": [{"id": "package-management-systems-apt-show", "question": "Which command displays APT package details for `package-name`?", "options": [{"text": "`apt remove package-name`", "correct": false, "explanation": "The `remove` subcommand proposes uninstalling the package."}, {"text": "`dnf search package-name`", "correct": false, "explanation": "This searches RPM-family repositories and is not the APT detail command."}, {"text": "`apt show package-name`", "correct": true, "explanation": "The `show` subcommand presents metadata for the named binary package."}]}, {"id": "package-management-systems-dnf-install", "question": "Which current command installs `package-name` from configured RPM-family repositories?", "options": [{"text": "`rpm -qa package-name`", "correct": false, "explanation": "This is an RPM installed-database query, not a repository install request."}, {"text": "`dnf install package-name`", "correct": true, "explanation": "DNF is the current repository-aware manager on Fedora and recent RHEL releases."}, {"text": "`apt update package-name`", "correct": false, "explanation": "APT update refreshes indexes and does not install a named RPM-family package."}]}, {"id": "package-management-systems-remove-review", "question": "Why should you review a removal transaction before confirming it?", "options": [{"text": "Removal always reformats the filesystem containing the package.", "correct": false, "explanation": "Package managers remove managed files and state; they do not ordinarily format a filesystem."}, {"text": "Package managers cannot display a proposed change set.", "correct": false, "explanation": "Interactive managers normally show the planned transaction precisely so it can be reviewed."}, {"text": "Other packages can depend on the selected package and may also be affected.", "correct": true, "explanation": "Dependency constraints can expand a request beyond the one package name originally entered."}]}, {"id": "package-management-systems-apt-update-upgrade", "question": "What is the relationship between `apt update` and `apt upgrade`?", "options": [{"text": "`update` removes packages; `upgrade` restores their configuration files.", "correct": false, "explanation": "Neither command has that remove-and-restore relationship."}, {"text": "`update` refreshes metadata; `upgrade` applies an approved package upgrade plan.", "correct": true, "explanation": "APT separates catalog refresh from installation of newer package versions."}, {"text": "They are identical names for one operation.", "correct": false, "explanation": "They perform distinct stages and should be checked separately."}]}, {"id": "package-management-systems-yum-current-rhel", "question": "What does `yum` commonly represent on a current RHEL system?", "options": [{"text": "A compatibility command backed by DNF.", "correct": true, "explanation": "Recent RHEL releases use DNF while preserving the yum command name for compatibility."}, {"text": "The Debian low-level `.deb` archive tool.", "correct": false, "explanation": "Debian systems use tools such as APT and dpkg rather than YUM for native package management."}, {"text": "A compressor for repository metadata only.", "correct": false, "explanation": "YUM and DNF are package-management interfaces, not standalone compression formats."}]}]}, {"lesson_id": "compile-source-code", "course_id": "packages", "title": "Compile Source Code", "order_index": 7, "description": "Learn how to verify, configure, build, test, stage, and track software compiled from source.", "content": "Building from source can provide a version or feature unavailable in configured repositories, but it moves integration, update, and trust work from the distribution to you. Prefer a supported distribution package when it meets the requirement.\n\n## Verify and Read Before Building\n\nObtain source from an authenticated upstream release channel. Verify its signature or checksum through a trusted path, then inspect the archive before extracting it into a nonprivileged staging directory. Read files such as `README`, `INSTALL`, `SECURITY`, and the project's build documentation.\n\nBuild instructions are executable code. A `configure` script, build definition, test, or compiler plugin can run arbitrary commands as your user. Do not build untrusted source, and do not run the build itself with `sudo`.\n\n## Install Build Requirements\n\nOn a Debian-family development system, a common starting point is:\n\n```bash\n$ sudo apt install build-essential\n```\n\nThis installs a baseline compiler and build tools, not every dependency required by every project. Projects can also need language runtimes, generators, build-system tools, development headers, or exact library versions. Install requirements from trusted repositories and separate build dependencies from runtime dependencies.\n\n## Configure and Build\n\nOne traditional Autoconf-style project uses:\n\n```bash\n$ ./configure --prefix=/usr/local\n$ make\n```\n\n`configure` checks the environment and generates build files according to selected options. `make` reads dependency and command rules, typically from a `Makefile`, and creates the requested targets.\n\nThis sequence is not universal. Projects can use CMake, Meson, Ninja, language-specific tools, or custom scripts. Follow the documentation for the exact release rather than running `./configure` merely because it is familiar. An out-of-tree build directory can keep generated files separate when the build system supports it.\n\n## Test Before Installation\n\nRun the project's documented test target, for example:\n\n```bash\n$ make check\n```\n\nThe actual target might be `test`, `check`, or a separate command. Investigate failures instead of installing untested output. Tests may require network access, services, special hardware, or isolation; review them before execution just as you review other build code.\n\n## Stage and Track Installation\n\n`sudo make install` can copy files directly into system prefixes without recording them in the native package database. Uninstallation targets are optional and can be incomplete, while later upgrades may overwrite or orphan files.\n\nPrefer one of these controlled approaches:\n\n- build an official native package using the distribution's packaging tools\n- install under a clearly separated prefix such as `/usr/local` when policy permits\n- stage files into a temporary packaging root with a supported mechanism such as `DESTDIR`\n- use a nonprivileged user prefix, isolated environment, or container when appropriate\n\n`checkinstall` can create a simple package for some `make install` workflows, but it is not universal and does not replace a reviewed distribution-quality package recipe. Never treat it as an \u201calways\u201d rule. Before any privileged copy, inspect the staged file list, ownership, permissions, paths, and uninstall or upgrade plan.\n\nUse [Build Software from Source Code in Linux](https://labex.io/labs/comptia-build-software-from-source-code-in-linux-590853) in a disposable environment to practice the workflow without mixing experimental files into a production system.\n\n## Summary\n\nYou can now approach source builds as a controlled software-supply workflow.\n\n1. Authenticate the source and review its instructions as executable code.\n2. Install explicit build requirements from trusted repositories.\n3. Configure, build, and test without unnecessary privilege.\n4. Stage and inspect outputs before system installation.\n5. Track installed files with native packaging or an intentional isolated prefix.", "code_snippets": ["$ sudo apt install build-essential", "$ ./configure --prefix=/usr/local\n$ make", "$ make check"], "quizzes": [{"id": "compile-source-code-build-privilege", "question": "Why should the compilation step normally run without `sudo`?", "options": [{"text": "Compilers refuse to produce machine code for the root user.", "correct": false, "explanation": "Compilers can run as root, but doing so unnecessarily increases risk."}, {"text": "`sudo` automatically deletes every generated object file.", "correct": false, "explanation": "Privilege elevation does not inherently remove build outputs."}, {"text": "Build logic can execute arbitrary commands and usually needs no system privilege.", "correct": true, "explanation": "Keeping the build unprivileged limits damage from mistakes or malicious build instructions."}]}, {"id": "compile-source-code-build-essential-scope", "question": "What does `build-essential` provide on a Debian-family system?", "options": [{"text": "A baseline set of common compilation and build tools.", "correct": true, "explanation": "It supplies foundational tooling but cannot anticipate all project-specific libraries or generators."}, {"text": "Every dependency for every source project.", "correct": false, "explanation": "Individual projects declare additional and sometimes version-specific requirements."}, {"text": "A guarantee that downloaded source is trustworthy.", "correct": false, "explanation": "Tool installation does not authenticate a separate source release."}]}, {"id": "compile-source-code-make-role", "question": "In the traditional workflow, what does `make` do?", "options": [{"text": "Registers every output in the distribution package database.", "correct": false, "explanation": "Compilation alone does not create native package ownership records."}, {"text": "Downloads an authenticated source release automatically.", "correct": false, "explanation": "Source acquisition and verification occur before the local build unless a project explicitly defines otherwise."}, {"text": "Executes applicable rules from the build description.", "correct": true, "explanation": "Make evaluates dependencies and runs the commands needed to bring selected targets up to date."}]}, {"id": "compile-source-code-test-failure", "question": "What should you do when the documented test suite fails?", "options": [{"text": "Run the same installation immediately as root.", "correct": false, "explanation": "Privilege does not resolve an unknown correctness failure and increases the consequences."}, {"text": "Delete the package manager database to avoid conflicts.", "correct": false, "explanation": "The native database is unrelated to resolving a source test failure and must not be discarded."}, {"text": "Investigate the failure before installing the build.", "correct": true, "explanation": "A failed test can reveal incompatible dependencies, build defects, or environmental assumptions."}]}, {"id": "compile-source-code-destdir-purpose", "question": "What is the purpose of a supported `DESTDIR` staging installation?", "options": [{"text": "Place intended install files under a temporary root for inspection or packaging.", "correct": true, "explanation": "Staging separates file collection from immediate writes into the live system prefix."}, {"text": "Change the compiler into a remote package repository.", "correct": false, "explanation": "The variable redirects installation paths and does not publish repository metadata."}, {"text": "Skip compilation and download unknown binaries instead.", "correct": false, "explanation": "Staging applies after a build and does not substitute an external binary download."}]}]}]}];